<!-- SPDX-FileCopyrightText: 2026 Jori Huisman -->
<!-- SPDX-License-Identifier: LicenseRef-BrowserAI-FSL-1.1-MIT-5yr -->

# Re-verification index

Everything in [`kb/`](README.md) marked `[FLOATS]` depends on a version this
project re-resolves to latest at every build. This table is the list of those
facts, what would break each one, how to check it, and whether anything checks
it automatically. It is a **build gate**, not an article: it is read by
`ReVerificationIndexTests` on every run, and it is the half of an
[upstream review](../UPSTREAM-REVIEW.md) that a golden snapshot cannot do -- a
snapshot says a byte moved, this says what a moved byte would invalidate.

It lives in its own file because it has a different audience and a different
lifecycle from [the article index](README.md): the index is read once, by
somebody finding out what is here; this is worked through line by line every
time an upstream version moves.

## What it covers, and what it does not

Every `[FLOATS]` fact is *meant* to be re-checked at upstream review, and this
table is how that happens. **It does not yet cover all of them**: **247**
`[FLOATS]` markers stand across the articles against the **137** numbered rows
below (146 lines, counting 2a, 2b, 4a, 4b, 4c, 16a, 26a, 38a and 63a),
because one row often stands for a cluster of related entries and because rows
have simply been missed. Read a missing row as a gap in this table, never as
permission to skip the fact.

> **These three numbers are checked on every build**, by
> `ReVerificationIndexTests` -- it re-runs the count, counts the rows, and fails
> if the sentence above disagrees with either. A tally maintained by hand is a
> tally that is wrong; this one had already drifted twice before it became a
> test. **The test reads the sentence above as its anchor**, so rewording it
> fails the suite instead of quietly unhooking the check.
>
> ⚠️ **The count is of the token, so it cannot tell a marker from a mention --
> and the scope is what keeps that from mattering.** The counter reads the
> Markdown under `kb/` and nothing else, excluding this file and
> [the article index](README.md), which are the two files whose job is to
> *discuss* the convention and not to stamp facts with it.
>
> That scope was wrong until 2026-08-17, and the symptom had been recorded here
> without the cause being fixed: the counter swept every tracked `.md` in the
> repository, so three sentences in [`../CLAUDE.md`](../CLAUDE.md), one in
> [`../TODO.md`](../TODO.md) and one on build-order step 4 that name the marker
> in prose were all counted as facts -- the recorded number was **195** for
> **190** stamped facts. The workaround this note used to prescribe -- *an
> article that wants to discuss the convention must say "floats" in words* -- was
> applied to `playwright/provisioning-and-timings.md` and never to the files
> outside `kb/` that were inside the counter's reach, which is what a rule
> enforced only where it is remembered looks like. Narrowing the scope makes the
> sentence true as written, and a real marker added anywhere in an article is
> still red. `Corrected 2026-08-17 (previously "195 ... stand across the
> articles")`.
>
> `Corrected 2026-08-17 (previously "193 ... stand across the articles")` -- the
> documentation restructure retired `history.md`, whose five markers left with
> it, and gave the Firefox cost ratios a home of their own in
> [`playwright/provisioning-and-timings.md`](playwright/provisioning-and-timings.md#firefox-against-chromium-the-standing-cost-ratios),
> which is one marker back. **Nothing was re-measured and no number moved**: this
> is a re-count of the same facts after a file boundary changed, which is the
> only kind of edit this sentence ever takes without a measurement behind it.
>
> `Corrected 2026-08-20 (previously "against the **109** numbered rows below (118
> lines")` -- **two new rows and no new marker**, which is a shape this table had
> not carried before. Both facts are about Windows and the BCL, not about
> a floating upstream, so they are stamped `[MACHINE]` and `[STABLE]` and the
> marker count does not move: row 110 is what a `Global\` mutex's DACL actually
> contains and what a token with no ACE on one gets back, row 111 is what the
> pre-gate probe costs per `browserai_list` entry. **Row 110 exists because a
> decision rests on it and has not been taken** -- whether to refuse a shared
> install root at startup -- and a measurement taken for a pending decision is the
> one most likely to be quoted from memory a month later. Both are *manual*, and
> row 110 says in place why it can never be otherwise here: producing it needs a
> second user account or a second logon session, and neither could be created on
> this machine.

> `Corrected 2026-08-19 (previously "**216** ... against the **108** numbered rows
> below (117 lines")` -- **one new marker and one new row**, from the research the
> maintainer asked for before deciding anything: whether the user agent and
> `navigator.webdriver` are reachable from the generated child config (row 109).
> **Nothing was implemented on the strength of it**, which is the point of it
> having a row -- a measurement taken for a decision that has not been made yet is
> the one most likely to be quoted from memory a month later.

> `Corrected 2026-08-19 (previously "**213** ... against the **105** numbered rows
> below (114 lines")` -- **three new markers and three new rows**, all from the
> maintainer's corrections of the same day: `--storage-state` beside
> `--user-data-dir` being a silent no-op (row 106), `--caps` accepting any word at
> all (row 107), and what two browsers on one profile directory cost -- a clean
> 5,036 ms refusal from Chromium against a 180,402 ms hang from Firefox whose
> error never names the profile (row 108). **All three are facts about upstream
> that BrowserAI does not depend on today**, which is why they are recorded and not
> fixed; a row each is what stops them being re-discovered.

> `Corrected 2026-08-19 (previously "**212** ... against the **104** numbered rows
> below (113 lines")` -- **one new marker and one new row**, from the day the
> 45-minute provisioning cap became a stall cap: what actually grows on disk
> while an install runs, and when (row 105). The marker is one entry carrying
> both `[FLOATS]` and `[MACHINE]`, and the per-article table below moved with it
> by being re-derived.

> `Corrected 2026-08-19 (previously "**207** ... against the **99** numbered rows
> below (108 lines")` -- **one new marker and one new row**, from the day
> `browserai_reinstall_browser` gained its `shared` value: one
> `install-browser ffmpeg` is measured to rebuild both shared components, which
> is the grouping that target is built on (row 100). The per-article table below
> moved with it, and moved by being re-derived, not incremented.

> `Corrected 2026-08-19 (previously "**206** ... against the **98** numbered rows
> below (107 lines")` -- **one new marker and one new row**, both from the day
> `browserai_init` began accepting `browser: "firefox"`: the advertised tool
> surface is measured not to depend on the browser family (row 99). The Firefox
> provisioning figures moved on the same day and added **no** marker, because
> they replaced an entry that already carried one -- see row 21:
> the replacement was a *re-measurement of a different predicate*, not an
> adjustment: the old pair counted the Firefox archive and directory alone where
> Chromium's counted a whole provisioning run.

> `Corrected 2026-08-19 (previously "**203** ... against the **96** numbered rows
> below (105 lines")` -- **three new markers and two new rows, all measurements
> taken that day**: what a mapped network drive costs and what the two
> object-manager calls that see it cost (row 97), and which alias forms
> `Path.GetFullPath` resolves on .NET 10 (row 98). The third marker rides on the
> same article section and is covered by row 97. **Row 98 is the first row here
> whose failure direction is a TEST, not the product** -- if `GetFullPath`
> stopped expanding, `CanonicalPath` would resolve the short spelling through the
> filesystem and
> the product would stay correct while the test asserting the equality became a
> lie. Say so when re-checking it: a green suite is not evidence either way.

> `Corrected 2026-08-18 (previously "193 ... against the **93** numbered rows below
> (102 lines")` -- **six new markers and two new rows, all of them measurements
> taken that day**: `browser_annotate`'s window and its unbounded wait (four
> markers, row 94) and the DPAPI/App-Bound-Encryption question under a
> provisioned Chromium (two markers, row 95). Both were the *reason* behind a
> decision that had already been taken, which is the worst place for an
> unmeasured claim to sit -- see rows 94 and 95 for what each one holds up.

> **There was an earlier row 54, added and withdrawn on 2026-08-16, and the
> number has since been reused.** Read this note as being about the withdrawal
> and not about whatever row 54 says today. It asserted that `dotnet test`
> runs zero tests on this toolchain; it does not, and a fresh worktree of the
> very commit the row cited as its proof returns 30 passed / exit 0. One
> transient observation had been written up as a standing property, so the row
> was **deleted, not marked**, because this index lists facts that must
> be re-checked and there was no fact left to re-check. The retraction is kept
> in [the article](toolchain.md#dotnet-test-and-the-test-host), so a reader who
> met the original meets the correction. **The row was caught by this table's own
> test** --
> `EveryRowIsEitherManualOrNamesSomethingThatExists` refused `-- *withdrawn*` in
> the `Automated by` column, which is the mechanism working on the person
> maintaining it and not only on upstream.

## Where the holes are

The paragraph above admits the table does not cover every marker. Naming *which*
articles are thin is the difference between an admission and a usable one.

**Two predicates, quoted before their numbers.** **Markers** is occurrences of
the token `[FLOATS]` in that article file -- the same count, the same corpus and
the same token-not-meaning caveat as the anchor sentence above, so the column
sums to the **242** that sentence publishes -- *corrected 2026-09-21 (previously
"**241**"), when the 0.0.82 review's WebMCP measurement added a marker to
`playwright/tools-and-artifacts.md` and row 133 with it; re-counted through the
same scan the test sums and not incremented. Corrected 2026-09-17 a second
time (previously "**240**"), when the SDK's pending-request reading added a
marker to `mcp/sdk.md` and row 132 with it; re-counted through the same scan the
test sums and not incremented. Corrected 2026-09-17 before that (previously
"**236**"), and that number was **four behind the anchor it claims to equal**
when it was corrected: the anchor already read 239, so this sentence had drifted
past the point where it could be read as a second reading of the same count.
Re-counted, not incremented, from the same scan the test sums. Corrected
2026-09-16 before that (previously
"**231**"), re-counted, not incremented: the new marker is the
`[STAThread]`-under-NativeAOT measurement in `toolchain.md`, which has row 129.
Corrected again 2026-09-15 before that
(previously "**230**" earlier the same day), re-counted, not incremented:
the new marker is the installer-starts-the-app-in-a-console measurement in
`packaging/velopack.md`, which has row 124. The three before it were the
zero-byte WebP measurement in `playwright/tools-and-artifacts.md` on row 122 and
the one-uninstall-key-per-app-id measurement in `packaging/velopack.md` on row
123, against a previous **228**. Corrected 2026-08-29 before that (previously
"**225**"), re-counted, not incremented, and **it was already two short
before this change**: the anchor read 227 while this sentence read 225, which is
the third time in five days that this sentence has drifted against a figure a
test was holding correct the whole time. The twentieth marker in
`playwright/configuration.md` arrived with the network-service sandbox entry, on
row 121. Re-measured 2026-08-26 by running
`ReVerificationIndexTests` and reading the number back out of its failure
(previously "**222**", which was **already wrong by one before this change**: the
column summed to 223 and the anchor said 223, and only this sentence said 222 --
which is precisely the drift its own warning below predicts, in a sentence
nothing reads). The two new markers are the vendored SQLite compile's readings of
the ILC build integration, both in `toolchain.md`, both on row 118* -- *corrected 2026-08-24 a second time
(previously "**221**"), re-counted, not incremented: the twelfth marker in
`windows/processes.md` arrived with the write-gate entry. Corrected 2026-08-24
(previously "**219**"), re-counted, not incremented: the column had been left behind
on 2026-08-23 when the console-window marker arrived, so it was already one short
before this change added another. Corrected 2026-08-20 before that (previously
"**208**"), which had been stale against the anchor's own 218 and was never in
the sentence the test reads.* ⚠️ **Nothing asserts this sentence** -- the test
sums the column against the live corpus and reads the anchor sentence above, not
this one, which is how it drifted twice while every number it describes stayed
green. **Rows** is numbered rows of the
table below whose cells carry a relative `kb` link to that article; a row citing
two articles counts once for each, so this column is not a tally and does not sum
to the row count in the anchor sentence above. *Corrected 2026-08-26 (previously
"does not sum to 109"). The point was right and the number was stale -- the row
count had moved to 117 while that 109 stood, and it is asserted by nothing, so
the sentence now names the figure that is asserted instead of carrying a second
copy of it that can drift.*

⚠️ **Re-counted 2026-08-19 and asserted from that day** -- *previously "Counted
2026-08-17"*, and **19 of its 28 numbers had drifted** by then: 9 of the 14
markers and 10 of the 14 rows, the widest being `tools-and-artifacts.md` at 30
against 39 and `windows/processes.md` at 17 against 7 -- one stale in each
direction, which is what makes a hand-maintained count worse than none. A
fifteenth article, [`not-established.md`](not-established.md), was missing from
the table altogether. Nothing here was adjusted: every figure below is what
`RecordedCountTests.TheHolesTableIsWhatTheArticlesAndTheRowsHold` re-derives, and
that test also requires the table to name **every** article in the corpus, so a
new one cannot escape the map by being forgotten.

| Article | Markers | Rows | Read this as |
|---|--:|--:|---|
| [`playwright/tools-and-artifacts.md`](playwright/tools-and-artifacts.md) | 45 | 16 | **Thin, and the thinnest here.** **The forty-third marker arrived 2026-09-21** with the WebMCP measurement taken on the `@playwright/mcp` 0.0.82 review, and it has **row 133** of its own instead of joining the tool-count cluster: what floats there is not a COUNT but whether `tools/list` is a static surface at all, and the row exists because two of its three halves are covered by nothing and say so. The tool-count and artifact rows each carry a large cluster; the credential-reach and registry-leak entries are one row each. **The forty-second marker arrived 2026-09-17** with the pointer measurement that closed upstream ask #1 -- every artifact pointer a tool result carries is absolute -- and it has **no row of its own**, deliberately: what floats there is upstream's `Response._printablePath`, which is the same subject as the artifact cluster row 19 already covers, and the fact is held on every build by `ConfigRoundTripTests` and `FileAccessRootTests` and not by a re-measurement. **The forty-first arrived 2026-09-15** with the zero-byte WebP measurement, and it has row 122 of its own -- a fact already known to be about to change, which is the shape row 102 was for |
| [`playwright/provisioning-and-timings.md`](playwright/provisioning-and-timings.md) | 29 | 14 | **Thin**, and partly on purpose: row 21 is one row over every size and timing, because a suite that re-measured them would provision on every run |
| [`mcp/sdk.md`](mcp/sdk.md) | 25 | 18 | Covered. Two entries carry **no row deliberately** -- see the rule below. **The twenty-fifth marker arrived 2026-09-17** with the pending-request reading, and it has row 132 of its own: what floats there is how `McpSessionHandler` treats a request registered *after* the transport's channel completed, and a change in either direction is silent -- the product refuses at the door before the SDK is reached, so nothing in the suite would go red if the SDK started faulting later requests too |
| [`windows/detection.md`](windows/detection.md) | 26 | 19 | Partly. The lock-file and Restart Manager entries have rows; the enumeration hazards and the canonicalisation table ride inside row 4's cluster. **The twenty-fifth and twenty-sixth arrived 2026-08-26** with the one path function, and each has a row of its own: 119 for `GetDriveTypeW` blocking through a `subst`, 120 for the three name shapes `Path.GetFullPath` rewrites and does not reject. **Rows 110 and 111 arrived 2026-08-20 carrying no `[FLOATS]` marker at all**, which is why the marker column did not move with them: both are Windows and BCL properties, stamped `[MACHINE]` and `[STABLE]`. **Row 115 did the same on 2026-08-24** for a third reason: the fact it re-establishes lives in `windows/processes.md` and the *condition* it can only be re-established under lives here, so the row cites two articles and stamps a marker in one of them |
| [`playwright/configuration.md`](playwright/configuration.md) | 20 | 22 | Covered, and then some: several rows split one entry into two separate checks -- row 102 was the shape this map is for, a row added for a fact already known to be about to change. **The twentieth marker arrived 2026-08-29** with the network-service sandbox entry, and it has row 121 of its own: what floats there is an upstream *flag list*, and the row exists to catch the day it stops carrying one element |
| [`packaging/velopack.md`](packaging/velopack.md) | 21 | 13 | Partly. The nine landmines are one row between them. **The fifteenth and sixteenth markers both arrived 2026-09-15**, each with a row of its own: the one-uninstall-key-per-app-id measurement (row 123), a property of Velopack's identity model, not one of the nine, and the installer-starts-the-app-in-a-console measurement (row 124), which is a property of how `Setup.exe` launches what it installed. Both were observed, not read. **The seventeenth arrived later the same day** on row 126, when the published v1.0.0 was installed for real and did **not** take the `VELOPACK_FIRSTRUN` branch row 124 assumed it would: what floats there is whether the variable reaches the app at all, and the stub's second entry point through `Update.exe start` never carries it. **The eighteenth arrived 2026-09-15 with the two-binary pack** and has row 127: the sizes, the compression ratio and the app's startup-to-window time, all of which move with `vpk`, with ILC and with the payload. **The nineteenth arrived 2026-09-16 with the second cut of `1.0.0`** and has row 130: a non-silent `Setup.exe` stops and asks before installing over a non-empty directory, and the affirmative button it offers a user is labelled from the version comparison -- `Repair` when the two are equal, which is what a re-ship produces. **The most recent arrived 2026-09-23** and has row 137: which of the three files `vpk pack` writes beside the packages is actually read from a release, which is the question an upload set turns on |
| [`toolchain.md`](toolchain.md) | 20 | 13 | Covered. **The fifteenth marker arrived 2026-08-24** with the tree-node filter grammar, and it has row 117 of its own **The sixteenth and seventeenth arrived 2026-08-26 together**, with the vendored SQLite compile, and they share row 118 instead of taking one each: both are readings of the ILC build integration's own files, both move on the same trigger -- an SDK bump -- and one re-establish procedure covers them. What makes them one row and not a cluster nobody re-checks is that the row names a **behavioural** control that fails on either of them, which is the shape [the rule below](#a-floats-entry-with-no-row-the-one-rule) asks for **The eighteenth arrived 2026-09-15** with the release body, and it has row 128 of its own -- the one marker in this article that is a NUMBER THIS PROJECT ACTS ON AND NOBODY HERE HAS ESTABLISHED: GitHub's release-body limit is carried from the 2026-09-15 cut and their own documentation states none, so the row's *how to check it* names an experiment that was deliberately not run **The twentieth arrived 2026-09-16** with the `?plain=1#L<n>-L<m>` line-range anchors a release body now links with, and it **shares row 128** instead of taking one of its own: it is the same subject -- this project's readings of GitHub's release-body surface -- it moves on the same trigger, and one procedure re-establishes it, which is the shape [the rule below](#a-floats-entry-with-no-row-the-one-rule) asks for **The nineteenth arrived 2026-09-16** with `[STAThread]` under NativeAOT, and it has row 129 of its own -- the one marker here that was an ASSUMPTION for as long as the configuration app has existed, and is now read off the published binary on every gate |
| [`windows/job-objects.md`](windows/job-objects.md) | 11 | 5 | Covered -- rows 2, 2a, 2b and 5 carry the containment cluster |
| [`chromium/resurrection.md`](chromium/resurrection.md) | 10 | 2 | **Thin.** Rows 1 and 9 carry the whole article |
| [`mcp/protocol.md`](mcp/protocol.md) | 9 | 8 | Covered |
| [`chromium/fingerprinting.md`](chromium/fingerprinting.md) | 8 | 2 | **Thin.** Row 6 is the call-site inventory; the 486-field differ result, the baseline-exposure list and the renderer-propagation finding have nothing |
| [`windows/processes.md`](windows/processes.md) | 13 | 12 | Covered for the BCL entries. Most of this article is `[STABLE]` Win32 and out of this table's scope by construction. **The ninth marker arrived 2026-08-19** with the Firefox rename measurement -- Playwright's Firefox build brings no Remote Agent up, so its liveness signal is the content-process tree, not DevTools -- and it is re-established by row 103 and not by a row of its own, because it is a property of *how* that measurement is taken **The tenth arrived 2026-08-23** with the console-window measurement, and it has row 114 of its own -- what floats there is the *class name* a console allocation produces, which is a different question from the flag's behaviour and is the half a sweep is written against **The eleventh arrived 2026-08-24** with the show-window measurement beside it, on row 115, and it is the GUI half of the same launch: what floats there is the *browser build*, because a Chromium revision is free to call `ShowWindow` itself and defeat a flag that governs only the first one **The twelfth arrived 2026-08-24** with the process log's cross-process write gate, and it is row 116 -- two *toolchain* refusals, not a Windows property, which is why it is the only marker in this article whose subject is neither the BCL nor Win32: the source generator will not marshal `NativeOverlapped` and CsWin32 will not generate `OVERLAPPED`, and each is the reason for a deviation from a rule this repository states elsewhere **The thirteenth arrived 2026-09-15** with the read nothing wakes, and it is row 125 -- a BCL property, and the one in this article whose *breaks if* is not the behaviour itself but its corollary: a runtime in which the parked read became a foreground thread would turn abandoning it into a process that never exits |
| [`chromium/profiles.md`](chromium/profiles.md) | 5 | 2 | Thin |
| [`packaging/dependencies.md`](packaging/dependencies.md) | 5 | 2 | Covered |
| [`not-established.md`](not-established.md) | 0 | 1 | **Zero markers is correct and is not a hole.** This page is the register of what was *not* measured; it stamps no fact, so it has nothing to re-verify. It is listed because a table that silently omits an article cannot be read as a map of the corpus. ⚠️ **The cited column moved 0 → 1 on 2026-09-18** -- *re-counted from the scan's own failure message, never adjusted* -- because row 34 links here since the idle-CPU axis was retired into this page. **A pointer in that direction is what a row GIVING UP a fact looks like**, and it is the first one: every other citation in this table runs from a row to the article that holds its measurement |

Those per-article numbers are a map of the holes and not a second tally to
maintain -- but they are no longer maintained by hand, which is what changed on
2026-08-19. A number nobody re-derives is indistinguishable from a measured one,
and this table had been stamped *Counted* while more than two thirds of it was
wrong.

## A `[FLOATS]` entry with no row: the one rule

Two rules used to contradict each other here. This index said *an entry with no
row is an entry nobody will re-check*; [`mcp/sdk.md`](mcp/sdk.md) says of two of
its own entries that they carry *no re-verification row on purpose*. Both
arguments are good, and both are about different facts. The reconciliation is
the distinction they were each reaching for:

**A `[FLOATS]` entry may omit its row only when a change to the fact cannot be
silent** -- an `internal` member becoming public, a `private protected` one
becoming reachable, a workaround becoming redundant. Those make the build red or
make a piece of our code visibly pointless; either way somebody is told without
this table's help. **Every other floating fact needs a row**, because the
failure this index exists for is the one that reports nothing.

The exemption carries an obligation: the entry must **say so in place, and say
why**, as `sdk.md`'s two do. An entry that merely lacks a row is
indistinguishable from one that was forgotten, which is the whole complaint.

## The `Automated by` column

**The `Automated by` column is what makes this a gate, not a checklist.** A row naming a test is answered by the suite and needs nobody. A row marked *manual* **must be answered by name in the [`upstream-review.json`](../upstream-review.json) entry**, with an outcome -- whether an upstream change touches one of our abstractions is judgement, and judgement cannot be asserted mechanically. **A row that is neither automated nor answered fails [the gate](../TESTING.md#the-upstream-review-gate).** Automating a manual row is always an improvement; naming a test here that does not exist is worse than leaving it manual, because it reads as covered. In
priority order -- the first three would each silently invalidate a design
decision:

> **A build script is a third answer, and it is named as one.** Rows 46-49 cite
> `build/Build-Payload.ps1` and not a test. That is deliberate and it is not
> the suite: the script throws while assembling the payload, so `dotnet test`
> passing says nothing about those rows. It is nonetheless a stronger gate than
> *manual* for exactly these facts, because **there is no route to a bumped
> upstream that skips it** -- the new version only exists in the tree because the
> script put it there. Read a script row as *automated, by the build and not
> by the suite*, and never as covered by a test run.

| # | Fact | Breaks if | Check | Automated by |
|---|---|---|---|---|
| 1 | Playwright's restart command line overshoots 1023 by 531+, so **Chromium ends up unregistered** -- and **Firefox registers anyway**, because its own pref does it independently of any length. **Turned off since Firefox support landed**: an upstream-default Firefox leaves 1 process registered and BrowserAI's own config leaves 0, measured on both sides the same day ([kb](chromium/resurrection.md#the-firefox-half-measured-on-both-sides----2026-08-16), [kb](windows/job-objects.md)) | Playwright trims its arg list; or Mozilla changes `toolkit.winRegisterApplicationRestart`; or upstream moves where `firefoxUserPrefs` is delivered -- it goes over juggler on the classic path and into `user.js` only on the BiDi one, so a test reading the file would be green-when-broken | `GetApplicationRestartSettings` on the **live** processes, never on a command line's length. Three arms: every Chromium process must answer `0x80070490`; a Firefox launched from a hand-written config with no preference must leave **exactly one** registered; a Firefox launched from the product's generator must leave **none**, with `browser_get_config` echoing the preference back. A change in any direction is red, not quiet | `BrowserContainmentTests` · `FirefoxTests.AFirefoxWeLaunchedIsAttributedToItsSessionAndIsNotRegisteredForRestart` |
| 2 | Job containment holds end to end for **process trees**, measured against the product's own job and launcher -- 10 processes in the probe arm, 4 under the bundled `node.exe`, 0 escapees and 0 survivors in both ([kb](windows/job-objects.md)) | Node, libuv or the SDK changes how a child is created; a flag is added to the job | Run the suite. Each arm asserts `IsProcessInJob` for every walked pid, cross-checks against the job's own pid list **in both directions**, then hard-kills the launcher and requires zero survivors | `JobContainmentTests` · `JobObjectTests` |
| 2a | The same guarantee **against real browsers**, re-established 2026-08-16 against the product's own job, launcher and delete routine: Chromium **11 then 10** processes, Firefox **10 then 10**, **0 escapees and 0 survivors** in all four runs, every profile deleted cleanly ([kb](windows/job-objects.md)) | Chromium or Firefox starts requesting breakaway on a browser path, or Playwright changes `detached`. The 2026-08-15 figure this replaces -- 16 runs, 106 processes -- came from a spike harness that no longer exists | Run the suite. Each arm drives a real browser through `cli.js` inside a job the product created, asserts `IsProcessInJob` for every walked pid, cross-checks against the job's own pid list **in both directions**, hard-kills the launcher from outside and requires zero survivors -- then deletes the profile with §E's own routine, because a directory still holding a lock proves an escaped browser | `BrowserContainmentTests` |
| 2b | The same guarantee **from the published NativeAOT binary, against a real browser** -- `[LibraryImport]` and `PROC_THREAD_ATTRIBUTE_JOB_LIST` after ILC and not under the test host ([kb](windows/job-objects.md)) | Any SDK, ILC or runtime bump changes how the attribute list or the handles survive native compilation. Step 6 proved the flags under the test host and said so; this is the arm that closes it | Drive the published binary until a browser is up, record every job member with its creation time, `TerminateProcess` the binary from outside, and require zero survivors | `VerticalSliceTests.KillingThePublishedBinaryLeavesNoNodeAndNoBrowser` |
| 3 | `chromiumSandbox` config key **honoured**, upstream having deleted the CLI-stage normaliser that always defined it -- *corrected 2026-09-14 @ `playwright-core` 1.63.0-alpha-2026-08-31, previously "still discarded, and the CLI stage always defining `chromiumSandbox` is why"; the trigger in the next column fired exactly as written and the arm that watched for it is what reported it* ([kb](playwright/configuration.md#silent-config-failures)) | **It happened.** Kept pointing the other way now: upstream reverting, or giving commander's `sandbox` option a `false` default again. BrowserAI is sandboxed on both sides of the change because it passes `--sandbox` on the command line, so this row tracks a fact, not a defect | Read the **resolved browser command line** of a live Chromium, never the config: with `--sandbox`, `--no-sandbox` must be absent from the browser and every child; with the config key alone and no flag, it must **now be absent too** -- measured 0 processes carrying it on 2026-09-14 against 6 on the previous pin, with the browser's own process count asserted non-zero so the absence is not vacuous | `SandboxFlagTests` |
| 4 | `Chrome_MessageWindow` title format, and the four canonicalisation rules -- backslashes hit, case is ignored, a trailing separator misses, forward slashes miss ([kb](windows/detection.md#the-sweep-measured-through-the-products-own-code-paths)) | Chromium changes `ProcessSingleton` | Exact-title lookup against a window whose title the test chose, with all four spellings and a `NULL` class. **The title format itself** -- that a real Chromium publishes its `userDataDir` there at all -- is the row-4c arm against a real browser | `MessageWindowTests.TheExactTitleProbeMatchesOnlyTheSpellingWindowsItselfMatches` |
| 4a | **Cross-process `GetWindowTextW` bypasses `WM_GETTEXT`** -- undocumented behaviour of a documented function, and the sweep rests on it ([kb](windows/detection.md#cross-process-title-reads----settled-by-two-independent-agents)) | A Windows change routes the read through the message queue | Child process with a WndProc that suppresses `WM_GETTEXT`, named with a GUID; assert the parent still reads the kernel name, and that the child's **own** same-process reads come back empty -- without that half, a window answering normally would pass. **No browser needed -- runs in milliseconds on every build.** The documented fallback is compared against `GetWindowTextW` on every window the walk finds, so a divergence is red, not silent | `MessageWindowTests.ACrossProcessReadGetsTheRealNameFromAWindowThatSuppressesWmGetText` · `MessageWindowTests.TheTwoTitleApisAgreeOnEveryMessageWindowOnThisMachine` |
| 4b | A headless launch resolves to full `chrome.exe`, not `chrome-headless-shell` -- **because a chromium-alias channel is in force**, and `chromiumAliases` is exactly `["chrome-for-testing"]`. The selector is **three functions, not one**: `resolveBrowserParam` substitutes the alias for a bare `--browser chromium`, `configFromCLIOptions` copies it into `launchOptions`, and only then does `getExecutableName` test `isChromiumAlias` ahead of its `headless ? ...` fall-through -- reading the last one alone is what produced a two-entry disagreement that turned out not to exist (settled 2026-08-17). An empty browsers root therefore **fails** instead of falling back to system Chrome ([selector](playwright/configuration.md#defaults-that-are-not-what-they-look-like)) | Upstream changes binary selection, renames the one alias, or our channel stops reaching the child. Not silent -- the shell is never provisioned, so the launch **fails loudly** -- but the failure would be baffling without this note | Launch through the real path and read the browser process's own image path: it must be `<browsers-root>\chromium-<rev>\chrome-win64\chrome.exe` with `--headless` on its command line. Then repeat against an **empty** browsers root and require a refusal naming `chrome-for-testing` | `HeadlessBinaryTests` |
| 4c | The other half of 4b: the **resolved channel as the child reports it**, and the window walk that finds a titled window owned by that pid ([selector](playwright/configuration.md#defaults-that-are-not-what-they-look-like), and the [0.0.79 observation it was once thought to contradict](windows/detection.md#enumeration-works----and-it-moves-the-safety-boundary)) | The same changes as 4b, but observed from inside the child and not from the process table | `browser_get_config` round trip for the channel; the walk needs the sweep. **The disagreement this cell used to say it closed was closed by a source read instead, 2026-08-17** -- `resolveBrowserParam` substitutes the alias channel before `getExecutableName` is reached, so the two entries never disagreed. The round trip still earns its place: it is what proves the channel *we generate* survives to the child | `ConfigRoundTripTests.TheChildResolvesOurChannelAndOurProfileRatherThanADefault` for the channel half, landed at the session tools. The window walk landed at the stray sweep: `StraySweepTests.TheSweeperFindsARealBrowserItLaunchedItselfInTheInteractiveSession` launches a real headless Chromium out of the provisioned tree, finds it by image path, and matches its message window's title to the profile it was given -- **543 ms and 547 ms**, two runs. Split out of 4b on 2026-08-16 |
| 5 | Chromium/Firefox request no breakaway on browser paths ([kb](windows/job-objects.md)) | Either adds one | Source search for `CREATE_BREAKAWAY_FROM_JOB` | -- *manual* |
| 6 | `--browser-test` call-site inventory (11 files) ([kb](chromium/fingerprinting.md)) | Chromium adds a web-facing site | Source search for `switches::kBrowserTest` | -- *manual* |
| 7 | `browserName`/`channel`/binary-selection defaults ([kb](playwright/configuration.md#defaults-that-are-not-what-they-look-like)) | `validateBrowserConfig` or `getExecutableName` changes | Config round-trip via `browser_get_config`: every leaf of the generated file must come back from the running child with the same value, and the named list of keys a session depends on must still be in the generated file at all | `ConfigRoundTripTests` |
| 8 | `outputMaxSize` has no default ([kb](playwright/configuration.md#defaults-that-are-not-what-they-look-like)) | `defaultConfig` gains one | Assert unset in the resolved config | -- *manual*; owed as `OutputMaxSizeTests` at the session tools |
| 9 | Firefox honours `toolkit.winRegisterApplicationRestart`, **observed on a live browser and not read out of source** ([kb](chromium/resurrection.md#the-firefox-half-measured-on-both-sides----2026-08-16)) | Mozilla removes the pref, renames it, or stops observing it at runtime -- any of which turns BrowserAI's only resurrection lever into a no-op that nothing else would notice | Launch a real Firefox from the product's generated config and ask `GetApplicationRestartSettings` of every process running our binary: all must answer `0x80070490`, against a control launch with no preference that leaves one answering `S_OK`. **A source check would no longer be enough** -- the delivery route (juggler, not `user.js`) is upstream's and can move without Mozilla changing anything | `FirefoxTests.AFirefoxWeLaunchedIsAttributedToItsSessionAndIsNotRegisteredForRestart` |
| 10 | `winldd` no-op for Chromium (*re-read 2026-09-21 @ `playwright-core` 1.64.0-alpha-1789764292000 and **UNCHANGED**: the bundle passes `["chrome-win"]` to `_validateHostRequirements` for Chromium while Chromium extracts to `chrome-win64`, so the check is still a permanent no-op and the 0.0.82 roll brought no cold-start latency with it. Measured by literal count: `"chrome-win"` appears twice in the bundle and `"chrome-win64"` once, which is the same shape as before. Re-read 2026-09-17 @ `playwright-core` 1.64.0-alpha-2026-09-17 and **UNCHANGED**: the bundle still passes `["chrome-win"]` while Chromium extracts to `chrome-win64`, so the check is still a permanent no-op and the `--file-paths` pull-forward brought no cold-start latency with it*) ([kb](playwright/configuration.md#browser-provisioning)) | Upstream fixes `chrome-win` → `chrome-win64` | Cold-start latency; source check | -- *manual* |
| 11 | Tool counts -- 83 internal, 72 exposed, 25 default, and **which eleven are `skillOnly`** (*re-measured 2026-09-21 @ `@playwright/mcp` 0.0.82 / `playwright-core` 1.64.0-alpha-1789764292000, previously "83 internal, 74 exposed, 27 default" and **which nine**. **The registry did not move and the wire did**: nothing was added and nothing was removed, and upstream marked `browser_webmcp_list` and `browser_webmcp_call` `skillOnly: true` in `packages/playwright-core/src/tools/backend/webmcp.ts`. Both keep capability `core`, so they left the DEFAULT surface as well as the exposed maximum, and every one of the three moving numbers moved by exactly two in the direction that follows. The `skillOnly` set is the previous nine plus those two. Counted off the regenerated snapshot, never incremented. Re-measured 2026-09-17 @ `playwright-core` 1.64.0-alpha-2026-09-17, previously "82 internal, 73 exposed, 26 default"; the pull-forward added **one** `core` tool, `browser_emulate_media`, reclassified none and removed none, and the nine `skillOnly` names are unchanged again. Counted off the regenerated snapshot, never incremented. Re-measured 2026-09-15 @ `playwright-core` 1.64.0-alpha-2026-09-14, previously "80 internal, 71 exposed, 24 default"; the roll inside `@playwright/mcp` 0.0.81 added two **`core`** tools, reclassified none and removed none, and the nine `skillOnly` names are unchanged. **The default moved this time because the arrivals were `core`, which is unconditional** -- the 0.0.80 pair were `devtools` and left the default at 24, so a reader who took "the default does not move" from that bump took it from a coincidence. Previously "78 internal, 69 exposed" before that*) ([kb](playwright/tools-and-artifacts.md#the-tool-surface-and-the-package-shape)) | Upstream adds, removes or reclassifies a tool | Golden `tools/list` snapshot; count `skillOnly` in the resolved bundle | `build/Update-UpstreamSnapshots.ps1` · `UpstreamSnapshotTests` |
| 12 | `storage` is 17 tools, and **the per-capability breakdown**: 45 tools for `config`+`vision`+`devtools`, 62 with `storage` added (*re-measured 2026-09-21 @ `@playwright/mcp` 0.0.82 / `playwright-core` 1.64.0-alpha-1789764292000, previously "47 tools for `config`+`vision`+`devtools`, 64 with `storage` added" -- the two tools 0.0.82 made `skillOnly` were **`core`** and so were in every set, which is why both figures moved by two while no capability's own membership changed except `core`'s exposed count 24 → 22; `storage` is still 17, for the sixth review running. Re-measured 2026-09-17 @ `playwright-core` 1.64.0-alpha-2026-09-17, previously "46 tools for `config`+`vision`+`devtools`, 63 with `storage` added" -- the one `core` tool the pull-forward added is in **every** set, `core` being unconditional, so both figures moved by one while no capability's own membership changed except `core`'s 23 → 24; `storage` is still 17. Re-measured 2026-09-15 @ `playwright-core` 1.64.0-alpha-2026-09-14, previously "44 tools for `config`+`vision`+`devtools`, 61 with `storage` added" -- the two `core` tools `@playwright/mcp` 0.0.81 added are in **every** set, `core` being unconditional, so both figures moved by two while no capability's own membership changed except `core`'s 21 → 23; `storage` is still 17. Previously "42 tools in `headless`/`interactive`, 59 in `persistent`" before that, and the mode names went when session modes did*) ([kb](playwright/tools-and-artifacts.md#the-per-capability-breakdown-counted)) | Any capability's membership changes | Count every capability's tools from the resolved bundle. Never from memory | `build/Update-UpstreamSnapshots.ps1` · `UpstreamSnapshotTests` |
| 13 | `browser_run_code_unsafe` reaches `httpOnly` cookies from the **default** surface ([kb](playwright/tools-and-artifacts.md#tools-that-reach-credentials)) | Upstream sandboxes it or moves it out of `core` | Run the probe: default caps, `page.context().cookies()` | -- *manual* |
| 14 | `browser_storage_state` omits IndexedDB ([kb](playwright/tools-and-artifacts.md#tools-that-reach-credentials)) | Upstream passes `{indexedDB:true}` | Source check at the `storageState()` call site | -- *manual* |
| 15 | The child's protocol ceiling is `2025-11-25`, and it never rejects -- it caps or echoes ([kb](mcp/protocol.md#the-protocol-split)) | Upstream adopts a newer revision | Assert the negotiated version at startup. The product logs `requested=... negotiated=...` and throws when they differ; the test asserts that pair against the ceiling the snapshot recorded, so the pin and the measurement cannot drift apart | `build/Update-UpstreamSnapshots.ps1` · `ProtocolSplitTests` |
| 16 | `DiscoverProbeTimeout` is 5 s, and **the client pin is what skips the probe** ([kb](mcp/protocol.md#the-protocol-split)) | The SDK changes the default, the probe, or which revisions trigger it | Read the default off `McpClientOptions` and not from a document; then, against a double that records every method it is asked for, assert three things in one run -- a pinned client sends **no** `server/discover`, an unpinned one **does**, and an unpinned one against a double that drops the method pays the **whole** timeout. The third arm is what makes the first evidence and not a tautology | `FakeChildHarnessTests.TheClientPinIsWhatSkipsTheDiscoverProbe` |
| 16a | The same cost **against a real spawn**: the ~300 ms baseline a probe stall would be measured against ([kb](mcp/protocol.md#the-protocol-split)) | The same changes as 16, but observed as wall-clock and not as a method the double did or did not hear | Time a real `node` child spawn with the pin in place and again without it. The in-process arm cannot answer this: it pins the probe timeout **short** deliberately, so its numbers are not comparable to production's | -- *manual*; split out of 16 on 2026-08-16, when the mechanism half became a test and marking the whole row automated would have read as covering the timing too |
| 17 | `PLAYWRIGHT_MCP_*` count is **47**, **three** of them outside the config mapping (*re-measured 2026-09-21 @ `playwright-core` 1.64.0-alpha-1789764292000 with the previous bundle as the positive control, fetched with `npm pack playwright-core@1.64.0-alpha-2026-09-17`, which returned 43 + 3 = 46 exactly as this row recorded. 0.0.82 added `PLAYWRIGHT_MCP_WEBMCP`, read **inside** `configFromEnv`, so the mapping went 43 → 44 and the **outside** set is unchanged at three -- still `PING_TIMEOUT_MS`, `EXTENSION_TOKEN` and `PROFILE_DIR_NAME`. Nothing left. Previously "**46**, **three** of them outside the config mapping". Re-measured 2026-09-17 @ `playwright-core` 1.64.0-alpha-2026-09-17 with the previous bundle as the positive control, which returned 42 + 3 = 45 exactly as this row recorded. The pull-forward added `PLAYWRIGHT_MCP_FILE_PATHS`, read **inside** `configFromEnv` as `options.filePaths = enumParser("--file-paths", ["relative", "absolute"], e.PLAYWRIGHT_MCP_FILE_PATHS)`, so the mapping went 42 → 43 and the **outside** set is unchanged at three -- still `PING_TIMEOUT_MS`, `EXTENSION_TOKEN` and `PROFILE_DIR_NAME`. Previously "**45**, **three** of them outside the config mapping". Re-measured 2026-09-15 @ `playwright-core` 1.64.0-alpha-2026-09-14 with the previous bundle as the positive control, which returned 41 + 2 = 43 exactly as recorded. `@playwright/mcp` 0.0.81 added `PLAYWRIGHT_MCP_IDLE_TIMEOUT`, which joins the `configFromEnv` mapping (41 → 42), and `PLAYWRIGHT_MCP_PROFILE_DIR_NAME`, which is read straight off `process.env` in the `--extension` channel resolver and so joins the **outside** set (2 → 3). ⚠️ **This row said 42 and was stale** -- previously "`PLAYWRIGHT_MCP_*` count is **42**, two of them outside the config mapping", which was the 0.0.79 figure; the 0.0.80 review moved the kb article to 43 and did not move this row, so the two disagreed for a day*) ([kb](playwright/configuration.md#environment-merge-order-and-startup-output)) | Upstream adds a variable | Derive the count from the resolved bundle; the allowlist test must not carry a literal. **Use the previous bundle as a positive control** -- a predicate that silently stops matching reads as "upstream added nothing" | -- *manual* |
| 18 | `--caps` and `PLAYWRIGHT_MCP_CAPS` replace instead of merging ([kb](playwright/configuration.md#environment-merge-order-and-startup-output)) | `mergeConfig` changes | Config round-trip via `browser_get_config`: the generated `capabilities` list must come back whole. The flag is never built and the variable is refused by name, so the consequence is what is asserted and not the mechanism | `ConfigRoundTripTests.NothingCanReplaceTheCapabilityListOnTheWayIn` · `ChildEnvironmentTests` |
| 19 | **Eleven** artifact generator prefixes plus one empty one, and the eleven `filename` arguments that reach them (*re-measured 2026-09-21 @ `@playwright/mcp` 0.0.82 / `playwright-core` 1.64.0-alpha-1789764292000 off the regenerated snapshot and **UNCHANGED**: the same eleven -- `annotations`, `console`, `download`, `element`, `network`, `page`, `request`, `response`, `result`, `storage-state`, `video` -- plus the empty traces one, from the same thirteen template expressions including the ternary and the template literal*) ([kb](playwright/tools-and-artifacts.md#artifacts-and-output-directory-behaviour)) | Upstream adds an artifact type, or gives an existing tool a `filename` | `build/upstream-snapshots.mjs` derives the prefix set from the resolved bundle into `tools-list.json`, so a change is a snapshot diff first; the tests then compare it against the folders `ArtifactRouting` declares **in both directions**, because a rename presents as one new prefix beside one dead folder. A prefix with no folder, a folder with no prefix, and a `filename` on a tool nobody has classified are each a red build. **Was nine and manual until 2026-08-16**, when deriving the set for the first time found `element` and `annotations` -- both hidden from a hand scan, one behind a ternary and one behind a template literal | ⚠️ **manual since 2026-08-26, and the row stays because the FACT is still true** *(previously "`ArtifactRoutingTests.TheDeclaredFoldersAreExactlyThePrefixesTheResolvedChildCarries` · `ArtifactRoutingTests.ATenthPrefixArrivingUpstreamFailsTheSort` · `ArtifactRoutingTests.EveryToolCarryingAFilenameHasBeenJudged`")*. BrowserAI no longer routes anything by prefix, so there are no folders to compare the set against and no `filename` for a tool to be judged on -- the three arms went with `ArtifactRouting` and `ArtifactTools`. **The prefix set is still derived into `tools-list.json` on every build and still diffed**, so an upstream change is still a snapshot diff; what is gone is the second, both-directions comparison. Re-establish by hand from the snapshot's `artifactPrefixes` |
| 20 | Killed children leak `browser@<guid>` descriptors ([kb](playwright/tools-and-artifacts.md#artifacts-and-output-directory-behaviour)) | `BrowserServer.stop()` learns to clean up with a `userDataDir` set | Kill a child, list the browsers-registry root | -- *manual* |
| 21 | Payload sizes, the **207.3 MB** first-run download, **437.24 MiB** on disk and **11.28 s / 11.31 s** provisioning; **Firefox is 130.9 MB down, 348.64 MiB on disk, 7.25 s / 6.80 s**. ✅ **RE-ESTABLISHED 2026-09-21 at chromium 1246 and firefox 1549**, in the same batch as the `@playwright/mcp` 0.0.81 → 0.0.82 roll and not left owed, **because the product quotes these figures**: `BrowserProvisioner.FirstRunDownloadBytes` is read by every caller refused while provisioning runs, and `ProvisioningTests.TheQuotedFirstRunDownloadSizeIsTheFigureTheKnowledgeBasePublishes` goes red the moment the kb moves without it. Two clean runs per family through the rig, byte-identical within each pair, plus a `HEAD` on each of the four archives. **Chromium did not move at all, for the third roll running** -- 1246 carries the same `browserVersion` 154.0.8037.0 as 1245 and 1244, `cftUrl()` is keyed on the version, so 207,274,189 B on the wire and 458,475,923 B across 316 files on disk are identical to the byte and the file. **Firefox is the first roll here that is a new BROWSER, not a rebuild**, 155.0 → **156.0**: `+1,431,551` B on the wire to 130,934,199 B, `+3,458,122` B on disk to 365,579,913 B, and 61 → 63 files inside its tree. `ffmpeg` 1011 and `winldd` 1007 are byte-identical, which is the control. Evidence [`docs/evidence/2026-09-21-provisioning-1246`](../docs/evidence/2026-09-21-provisioning-1246/README.md). *Previously* ✅ **RE-ESTABLISHED 2026-09-17 at chromium 1245 and firefox 1548**, clearing the `[STALE]` this row carried for a day; two clean runs per family through the rig, byte-identical within each pair, plus a `HEAD` on each of the four archives. **Chromium did not move at all and that is a finding, not an absence of one:** `playwright-core` builds its URL with `cftUrl()`, keyed on `browserVersion`, and 1245 carries the same **154.0.8037.0** as 1244 -- so the archive is the same archive, `chromium-1245` holds **454,699,952 B across 308 files** exactly as `chromium-1244` did, and the wire total is **207,274,189 B** unchanged. **Firefox moved by almost nothing:** `firefox-win64.zip` 127,961,896 → **127,962,223** B (`+327`) and `firefox-1548` 358,344,918 → **358,345,820** B (`+902`) across the same 61 files, so the family totals are **129,502,648 B** down and **362,121,791 B** on disk. `ffmpeg` **1011** and `winldd` **1007** are byte-identical, which is the control for the two revisions that moved. The timings moved in both directions on a roll that moved almost no bytes, which is what `[MACHINE]` on them is for. `BrowserProvisioner.FirstRunDownloadBytes` follows: Firefox to 129,502,648, Chromium re-measured and unchanged. **The run is [`docs/evidence/2026-09-17-provisioning-1245`](../docs/evidence/2026-09-17-provisioning-1245/README.md).** ⚠️ *Previously, and kept because it is what the debt looked like:* "**`[STALE]` AND OWED AGAIN SINCE 2026-09-17**, one day after the re-establishment below: the `playwright-core` pull-forward to 1.64.0-alpha-2026-09-17 moved **chromium 1244 → 1245 and firefox 1544 → 1548**, which is this row's own trigger, so every browser figure above is one revision stale. **Nothing was adjusted and no number was carried forward as though it still held** -- re-running them is a measurement session rather than a gate, and the debt is recorded here rather than paid in the batch that created it." The **payload** half of the row WAS re-measured by that build and one of its two figures moved: `node.exe` is unchanged at 93,580,104 bytes and the `mcp` tree is 18,619,618 → **18,659,660**. ✅ **RE-ESTABLISHED 2026-09-16 at chromium 1244 / 154.0.8037.0 and firefox 1544 / 155.0** under `playwright-core` 1.64.0-alpha-2026-09-14 and `@playwright/mcp` 0.0.81, clearing the debt this row carried from 2026-09-14: **all four browser figures moved and both payload figures are unchanged**. Wire totals from `content-length`, disk totals from two clean runs per family that came back byte-identical. The three revision-keyed archives resolve under `builds/{firefox,ffmpeg,winldd}/<revision>/`; **Chromium does not** -- it is `builds/cft/<browserVersion>/win64/chrome-win64.zip` off the bare CDN host, which is a correction to this row's own procedure ([kb](playwright/provisioning-and-timings.md#firefox-measured-the-same-way----2026-08-19)) | Any browser or Node revision bump | Re-measure at each bump; [`docs/probes/2026-09-16-provisioning`](../docs/probes/2026-09-16-provisioning/README.md) is the rig. **The Firefox and Chromium figures are quoted at a caller** by `BrowserProvisioner.FirstRunDownloadBytes`, so a stale one is a measured-looking number in a refusal and not a note nobody reads -- and since 2026-09-17 that is held against this article by `ProvisioningTests.TheQuotedFirstRunDownloadSizeIsTheFigureTheKnowledgeBasePublishes`, planted red against the 1237/1539 constants and watched, so the two can no longer drift apart. `FirefoxSessionTests.EveryFamilyThisBuildProvisionsHasAMeasuredDownloadSize` proves a figure *exists* per family and can say nothing about whether it is still true. **Two things this row no longer claims:** the per-phase boundaries, which a piped stdout cannot produce because Node buffers it, and Firefox's incremental *and nothing else*, whose third run was not repeated. The browser download and the timings stay manual **and must**: a suite that measured them would provision on every run. `build/Build-Payload.ps1` still re-measures the two *payload* rows on every payload build. The **compressed** payload size has still never been measured | `build/Build-Payload.ps1` (payload rows only), plus `ProvisioningTests.TheQuotedFirstRunDownloadSizeIsTheFigureTheKnowledgeBasePublishes` for the quoted totals |
| 22 | Full Chromium refuses a second instance; `chrome-headless-shell` does not notice one ([kb](windows/detection.md#process-image-path----the-fully-documented-detection-path)) | Upstream changes the singleton, or the shell is ever shipped | Launch twice against one profile directory, on both binaries | -- *manual* |
| 23 | SDK behaviours -- `cmd.exe /c` prefix, `ListToolsAsync` filtering, `ContentBlock` drop-and-throw ([kb](mcp/sdk.md#sdk-behaviours-a-proxy-must-work-around)) | Any `ModelContextProtocol` bump | The `cmd.exe` half is row 31. **The other two no longer reach the product**, which is a change of kind, not of coverage: since lossless passthrough nothing calls `ListToolsAsync` and no `ContentBlock` is ever constructed, and `LosslessPassthroughTests.TheProductNeverCallsTheOverloadThatDropsToolsSilently` fails the build if that stops being true. What stays owed is whether the SDK **still** behaves this way, because that is the justification for the deviations -- and nothing in the suite travels those paths any more | -- *manual* |
| 24 | Velopack landmines -- feed-URL composition, `SetAutoApplyOnStartup`, the stub, `force_stop_package` ([kb](packaging/velopack.md#the-nine-landmines-claim-and-verdict)) | Any Velopack bump | The update lane: real feed URL, real N→N+1, and a delta produced ON DEMAND -- *re-scoped 2026-09-22 (previously "real delta")*, because no RELEASE carries one since the full-packages-only decision; what is checked is that `vpk` still can, which is what makes that decision a choice | -- *manual* |
| 25 | Claude Code truncates `instructions` and tool descriptions at 2 KB, defers schemas, and **now handles** `tools/list_changed` ([kb](mcp/protocol.md#the-client-claude-code)) | Any client release | Measure both strings at build time; re-stamp the client version the claim was checked at | -- *manual* |
| 26 | Payload licensing as shipped -- `winldd` has no license file, full Chromium has no OSS license. ✅ **RE-READ 2026-09-22 at `@playwright/mcp` 0.0.82, `playwright-core` and `playwright` 1.64.0-alpha-1789764292000, Node v24.21.0, chromium 1246 (154.0.8037.0), firefox 1549 (156.0), ffmpeg 1011 and winldd 1007, CLEARING THE `[STALE]` BELOW -- and the debt was owed for the right reason: THE FIREFOX TERMS MOVED.** *Corrected 2026-09-22 (previously "`[STALE]` ⚠️ **OWED SINCE 2026-09-21 and deliberately not taken in that batch** ... Chromium is the cheap half and is very likely unmoved ... but *very likely* is not a reading").* **Both `license.html` files inside `omni.ja` grew by exactly +375 B**: `chrome/toolkit/content/global/license.html` 320,171 → **320,546 B** (SHA-256 `5E415C91...`) and `chrome/browser/content/browser/license.html` 320,613 → **320,988 B** (SHA-256 `06B04CA1...`). **The 375 bytes were diffed, not merely noted**, because a size is not a licensing reading: three hunks, seven added lines, nothing removed, each adding a component to an existing licence's own file list -- `gfx/graphite2` under **LGPL-2.1**, `third_party/dav1d` under **BSD 2-Clause**, and `Microsoft.WindowsAppRuntime.dll` + `Microsoft.WindowsAppRuntime.Insights.Resource.dll` under **MIT**, the last two being **exactly the two new files that took the shipped tree from 61 to 63**. So the text and the tree moved together. **No licence was added, removed or changed**; the MPL-2.0 headline is unmoved and the mention counts are re-counted and not carried, still **6** `Apache License` and **13** `BSD`. **`THIRD-PARTY-NOTICES.txt` IS STILL TRUE AND FOR A REASON**: its Firefox entry names the two *paths* and no byte counts, so a size change cannot falsify it -- a property of how it was written, not luck, and the reason nothing shipped had to be re-cut. **`README.md`'s table quoted the SIZES, was marked `[STALE]` on 2026-09-21 saying so, and now carries the re-measured pair.** **EVERY OTHER CELL IS UNMOVED TO THE BYTE**, each re-read and not carried: `@playwright/mcp` `LICENSE` 11,552 B and still no `NOTICE`; `playwright-core` and `playwright` six files apiece at the same twelve sizes; `payload\node` exactly two files, `LICENSE` 160,555 B beside a `node.exe` reporting 24.21.0; `ffmpeg-1011` 4 files with `COPYING.LGPLv2.1` 26,526 B; `winldd-1007` 3 files and no licence. **CHROMIUM 1246 IS 1245 IS 1244** -- same **308 files at the same 308 sizes** (`Compare-Object` over name+size: **0 differences**), `chrome.exe` SHA-256 `E3390AB4...` and `ABOUT` SHA-256 `122B32F3...` in all three -- **and the sweep was run anyway**, which is this row's procedure: byte-identical is what the run produced, not what an unchanged `browserVersion` entitled anyone to assume. ⚠️ **A PREDICATE RECONCILED IN PASSING, because a re-count that measures a different predicate is this repository's named trap**: this row counts **files only** and row 21 of this table counts a family's whole cost on disk. `chromium-1246` is 308 files / 454,699,952 B and `firefox-1549` is 63 files / 361,803,942 B here; row 21 records 316 and 71, and the gap is the same **8 shared files** in both -- `ffmpeg-1011` (4), `winldd-1007` (3) and the root's `reinstall.lock` -- which lands both counts exactly. The byte totals then sit **69 B** below row 21's in *both* families, identically, which is `reinstall.lock` holding 69 bytes then and 0 now. **An identical offset in two independent families is what makes that a reading, not a guess.** *Previously* ✅ **RE-READ 2026-09-17 at chromium 1245, firefox 1548, `@playwright/mcp` 0.0.81, `playwright-core` 1.64.0-alpha-2026-09-17 and Node v24.21.0**, clearing the debt below. **Five claims held and four did not, and the four are the reason a row like this is re-read and not reasoned about.** **Held:** `winldd` 1007 ships **no licence file** (3 files, listed exhaustively and not searched); `ffmpeg` 1011 still carries `COPYING.LGPLv2.1`, 26,526 B of a 4-file tree; Node still carries its own `LICENSE`, 160,555 B, now beside a `node.exe` reporting 24.21.0; `@playwright/mcp` still carries `LICENSE` and **no `NOTICE`**; and full Chromium is still Google-branded with **no OSS licence file among its 308 files** -- `ABOUT` 257 B, `chrome.exe` CompanyName **Google LLC**. That last one *could not* have moved and the evidence says why and not the conclusion asserting it: `chromium-1245` and `chromium-1244` hold the **same 308 files at the same 308 sizes** and `chrome.exe` is SHA-256 `e3390ab4...` in both, which is row 21 of this table's `cftUrl()` finding on the licensing axis. **It was still run** -- sweep, `ABOUT` digest and version resource were all re-read at 1245. **Did not hold:** (1) *"**No `NOTICE` file is published upstream**, so §4(d) has nothing to propagate"* is **false** -- `playwright-core` and `playwright` each ship one, 254 B and byte-identical to each other, plus three per-bundle `*.LICENSE` sidecars apiece; the artifact's own `THIRD-PARTY-NOTICES.txt` has named that path since `f18b657`, so the defect was in this row and never in what ships. (2) The `chromium-headless-shell` row describes **a tree nothing provisions**: `--no-shell` has been passed since `a45749b` on 2026-08-16, **two days after the row was written**, and the browsers root holds no `chromium_headless_shell-*` directory at any revision. (3) **`playwright` is a third Playwright package that ships and was never listed** -- in the payload lock since the first payload build -- while `THIRD-PARTY-NOTICES.txt` says *"the two Playwright packages"*. Its terms and NOTICE text are byte-identical to `playwright-core`'s, so what is missing is a **name**, not a licence. ⚠️ **Reported and NOT fixed: what ships beside the binary is the maintainer's call**, and this row records the gap instead of closing it. ✅ **FIXED 2026-09-18 (Q214 = a)**: `THIRD-PARTY-NOTICES.txt` names `playwright` with its three paths, says *"3 Playwright packages ship in the payload"*, and states the resolved `playwright` and `playwright-core` versions because the override separated them. **The naming is derived and not typed from 2026-09-18**: `ThirdPartyNoticeTests.TheNoticesNameEveryPackageThatShipsAndEveryFamilyThatIsProvisioned` enumerates `build/payload/package-lock.json`, so a fourth package in the payload is a red build here. Watched red on *"the payload ships 'playwright' and THIRD-PARTY-NOTICES.txt does not name it"*. (4) **Firefox was never listed either**, having been a provisioned family since 2026-08-19; it is listed now -- **no standalone licence file in 61 files**, the terms inside `omni.ja` as `license.html` (320,171 B and 320,613 B), **byte-identical across 1544 and 1548**. ✅ **AND THE SHIPPED NOTICES LIST IT TOO, 2026-09-18 (Q214 = a)**, which is the half this row could not close by listing it here: a *Browsers provisioned on first run* block in `THIRD-PARTY-NOTICES.txt` names each family, what it is, where it is fetched to and where its terms live, and the block is checked against `ProvisionedBrowsers.Families` so a third family is a red build. Watched red on *"'firefox' is a provisioned family and THIRD-PARTY-NOTICES.txt has no entry for it"*, and on the same sentence for `chromium`. ✅ **AND SO DOES `README.md`, 2026-09-18** -- the same two omissions stood one document across, in the table that tells a *reader* what the machine downloads and not a recipient what it owes: it read *"full `chromium` 1237"* against a payload resolving 1245, carried a `chromium-headless-shell` row, and had no Firefox row. The Chromium row now states the revision that ships, a Firefox row sits beside it, the headless-shell row is struck with the reason and its 1237 deliberately left unrolled, and `ThirdPartyNoticeTests.TheReadmeTablesNameEveryPackageThatShipsAndEveryFamilyThatIsProvisioned` holds the two tables to `build/payload/package-lock.json`, `ProvisionedBrowsers.Families` and the committed `upstream-snapshots/browsers.json`. Watched red on four offences at once, among them *"README.md's 'What the user's machine downloads' table says 'chromium 1237' and the committed browsers.json snapshot says 1245"*. ⚠️ *Previously, and kept because it is what the debt looked like:* "**owed since 2026-09-17**: chromium moved 1244 → 1245 and firefox 1544 → 1548 with the `playwright-core` pull-forward, which is this row's 'revision bump', and the shipped trees were not re-read in that batch. `winldd` is unmoved at 1007, so the half of the claim that is about `winldd` cannot have changed; the Chromium half is what is owed" -- **that last sentence scoped the debt too narrowly**: the Chromium half was the half that could not have moved, and every one of the four defects was somewhere else. ([kb](packaging/dependencies.md#third-party-payload-as-shipped)) | Upstream adds one, or the payload composition changes | Re-read the shipped trees at each revision bump. **Two things make this row re-runnable, not re-assertable.** A full-depth case-insensitive sweep for licence-shaped names needs a **positive control**, and it has two standing ones: the same run that finds nothing under `firefox-1548` and `winldd-1007` returns `ffmpeg-1011/COPYING.LGPLv2.1` and `chromium-1245/chrome-win64/ABOUT`. And a tree small enough to list -- 3 files, 4 files, 61 files -- is **enumerated instead**, because an exhaustive listing outranks a search that matched nothing. **The payload half reads a real publish**, never the source tree: what is licensed is what a recipient gets | -- *manual* |
| 26a | **The compiled-in packages' licence text** -- Velopack MIT, the MCP SDK's three-licence Apache-2.0 file, and 17 MIT `Microsoft.Extensions.*` under two copyright lines ([kb](packaging/dependencies.md#third-party-payload-as-shipped)) | Any bump of Velopack, `ModelContextProtocol` or anything that moves the `Microsoft.Extensions.*` closure. **A licence text is a measurement**: a bump can change the terms, the years or the holder, and text copied against an older version is a confident wrong answer, not a gap | **The version stamp is mechanised and the text is not.** `ThirdPartyNoticeTests` fails the build when the version in `THIRD-PARTY-NOTICES.txt` stops matching `packages.lock.json`, and when a new `Microsoft.Extensions.*` package appears in the closure unnamed -- so the bump cannot pass unnoticed. What stays manual is re-fetching the licence **from the new package's own `.nuspec` `repository commit`** and confirming the text is unchanged. Added 2026-08-16 at the plan's final audit | `ThirdPartyNoticeTests.EveryReproducedLicenceIsStampedWithTheVersionTheBuildResolved` and `ThirdPartyNoticeTests.EveryMicrosoftExtensionsPackageLinkedIntoTheProductIsNamedInTheNotices` |
| 27 | **NativeAOT publishes clean and the proxy runs** ([kb](mcp/sdk.md#driving-the-whole-sdk-aot-passthrough-filters-and-cancellation)) | Any SDK bump | `PublishAot`, zero warnings, run the published binary against a real child -- **and grep the publish output for `will always throw`**. Amended 2026-08-16: ILC reports an always-throwing method as [neither a warning nor an error](mcp/sdk.md#ilc-the-native-toolchain-and-source-generated-json), so exit 0 plus zero warnings does not cover it, and the check as originally written would have passed a binary with a dead code path in it | -- *manual* |
| 28 | The SDK still never relays `notifications/cancelled`, **and a `ct.Register` scoped to the call cannot substitute for one** -- LIFO disposal unregisters it first ([kb](mcp/sdk.md#lossless-passthrough-cancellation-notifications-and-error-frames)) | The SDK fixes it -- our hand-rolled path would then double-send | Hold a call open at the double, cancel it from the caller, and assert the double sees **exactly one** `notifications/cancelled` naming the id BrowserAI sent. The count is the half that catches a fix; the id is the half that catches a relay aimed at the wrong request | `LosslessPassthroughTests.CancellingACallIsObservedAtTheFakeChild` |
| 29 | `Filters.Message.IncomingFilters` still exposes `Result` as raw `JsonNode?`, and the per-request CTS is still registered **before** the filter runs ([kb](mcp/sdk.md#lossless-passthrough-cancellation-notifications-and-error-frames)) | Any SDK bump; this is the whole proxy hook, and the ordering is what keeps a short-circuited request cancellable | Short-circuit a `tools/call` and compare the **byte span** of `result` at both ends, by `Utf8JsonReader` token offset and never by re-serialising | `LosslessPassthroughTests.AResultArrivesAtTheCallerAsTheExactBytesTheChildWrote` |
| 30 | `ListToolsAsync(RequestOptions?, ct)` still drops silently ([kb](mcp/sdk.md#sdk-behaviours-a-proxy-must-work-around)) | SDK fixes or changes it | Fake child with an invalid `x-mcp-header`; compare both overloads. **Deliberately still manual after lossless passthrough:** the product calls neither overload now, so its immunity is a source scan, not a measurement of the SDK, and constructing an annotation that fails SEP-2243 validation has never been done here | -- *manual* |
| 31 | `StdioClientTransport` still wraps in `cmd.exe` ([kb](mcp/sdk.md#writing-replacement-transports-against-the-public-surface)) | SDK fixes it -- the custom transport stays correct either way, but the rationale changes | Start a child through the SDK's own transport and read its **real** parent, via `NtQueryInformationProcess` and not `process.ppid`: the answer has to come from outside the child, or a child that cannot report is indistinguishable from one with no shell above it | `SdkStdioClientTransportTests.TheSdkTransportStillPutsCmdExeBetweenUsAndTheChild` |
| 32 | An unusable `--user-data-dir` still falls back invisibly, a deny-all DACL still exits 21, and the "failed to create data directory" dialog still blocks startup ([kb](chromium/profiles.md)) | Chromium changes `RecursiveDirectoryCreate`, `ProcessSingleton` or its error dialogs | Launch against a file-occupied path and against a DACL'd directory; assert fallback, exit 21 and the dialog behave as recorded. **That article is the whole basis for [validate every path before launch](../DECISIONS.md#processes-browsers-and-session-modes)** and had no row here until 2026-08-15 | -- *manual* |
| 33 | A healthy start still prints `Session: <path>` to stderr -- **and only when `saveSession` is on**, which is a condition this row did not carry until 2026-08-16. With it off, a healthy start writes nothing at all; with it on, exactly that one line. Both re-measured twice ([kb](playwright/configuration.md#environment-merge-order-and-startup-output)) | Upstream changes startup output -- in either direction: a new benign line trips the classifier, a removed one turns it into dead code. Also if the missing-browser diagnostic returns to stderr, which it has left at 0.0.79 | Classify a **real** start's stderr, never a fixture. [the observability design](../ARCHITECTURE.md#process-containment-and-observability) ports the two regexes **verbatim**, so this is behaviour our code copies and not merely observes; the test takes the line off the captured log record instead of retyping it, so it cannot pass against a string no child produced | `StandardErrorClassifierTests.ARealHealthyStartPrintsTheBenignSessionLineAndIsNotWarnedAbout` |
| 34 | Firefox's cost ratios against Chromium -- **1.19× RAM, 4.65× first navigate and 2.76× profile disk, three axes and no longer four**, six rounds per family. ✅ **RE-ESTABLISHED 2026-09-22 at chromium 1246 and firefox 1549 (`browserVersion` 156.0), IN ONE SITTING, CLEARING THE `[STALE]` OWED SINCE 2026-09-21.** *Corrected 2026-09-22 (previously "**1.19× RAM, 4.37× first navigate and 2.76× profile disk** ... `[STALE]` ⚠️ **OWED SINCE 2026-09-21 and deliberately not taken in that batch** ... faking it from the previous numbers is what `[STALE]` exists to prevent").* **TWO OF THE THREE CAME BACK IDENTICAL AND ONE MOVED BACK UP**: RAM **1.19×** and profile disk **2.76×** to three figures, first navigate **4.37× → 4.65×**. Chromium 6 rounds; Firefox **8 rounds that produced a browser out of 9 run**. ⭐ **THE MOVEMENT IS MOSTLY THE MACHINE, AND THE CONTROL IS STRONGER THAN IT WAS: 1246 IS 1245 IS 1244**, the same 308 files at the same sizes with `chrome.exe` identical to the byte, so Chromium's own column between the two sittings is ENTIRELY the instrument -- and it drifted **+15.3%** on first navigate and **+18.3%** on second, against Firefox's **+22.7%** and **+25.7%**. Read against that, almost nothing of the ratio's movement is Firefox 156.0, and what is established is the band this axis has occupied across three sittings: **4.37×, 4.62×, 4.65×**. The two axes that did not drift are the two worth three figures: profile disk **2.76× on every one of eight rounds** (Firefox's own profile grew 0.07% and the ratio did not move at the third figure) and RAM **1.19×** against a control drift of −0.9%. **Processes moved 0.78× → 0.88× and it is CHROMIUM'S median that moved**, 9 → 8 on one round of six; Firefox was 7 on all eight, as on all six before. **Idle CPU stays RETIRED and the reading is a third confirmation, not a number**: the two columns overlap completely again, Chromium 405-767 ms against Firefox 329-874 ms. ⚠️ **ONE FIREFOX ROUND IN NINE PRODUCED NO BROWSER AT ALL, named here and not dropped.** `browserai_init` answered in 459 ms and then BOTH navigations returned only after **180,031 ms** and **180,679 ms** -- three minutes each, which is neither BrowserAI's own timeout nor anything it writes. Zero processes under the browsers root, resident set 0, profile stalled at **1,159,208 B / 25 files** against ~36.47 MB / 66 on every healthy round. **The product's own stray sweep is EXCLUDED from its own announcements and not by argument**: all fifteen sweeps in the sitting, including the failing round's own, reported `candidates=0` and terminated nothing. **What it was is NOT established**, and the reason is the rig, not the machine: `ratios-probe.js` destroys its session on the way out, taking the session log with it, so the one record that would have said why was deleted by the measurement. Three further rounds were clean, so the observed rate is 1 in 9. The medians are over the eight rounds that produced a browser, and that predicate is stated because including a 180-second navigate and a zero resident set would be a different claim, not a more honest one ([kb](playwright/provisioning-and-timings.md#firefox-against-chromium-the-standing-cost-ratios)) | Any Firefox or Chromium revision bump | ✅ **RE-ESTABLISHED 2026-09-17 at chromium 1245 and firefox 1548, at SIX rounds per family**, clearing a `[STALE]` this row carried for six hours. **Three of the six axes came back identical, one moved inside its own spread, one moved, and one is withdrawn as unmeasurable at this sample size.** Unchanged: **RAM 1.19×**, **profile disk 2.76×** (on every one of six rounds), **processes 0.78×**. Moved inside its band: **first navigate 4.62× → 4.37×**. Moved: **second navigate 0.90× → 0.68×**, which also retires this article's sentence calling that row the one where the families are *genuinely close*. ⚠️ **Withdrawn: idle CPU.** It read 0.77× in the morning and **1.31×** in the evening -- **a second sign reversal inside one day** -- and the conclusion is about the instrument, not the browsers: Chromium's six rounds span 360-955 ms (**2.65×, on a binary that did not change**) against Firefox's 266-781 ms, and the **two distributions overlap completely**. Across ~24×, 0.77× and 1.31× exactly one thing is established: **Firefox does not burn an order of magnitude more idle CPU than Chromium.** Which burns more is not established, and more rounds of `TotalProcessorTime` differenced over one 30 s window will not establish it -- ⚠️ **what to do about the axis is left open and not decided.** ✅ **DECIDED 2026-09-18 (Q215 = a): the axis is RETIRED as not established**, and it is now a row in [what this project has not established](not-established.md) and not a number this row holds. *Corrected 2026-09-18 (previously "**1.19× RAM, 4.37× first navigate, 2.76× profile disk, and NO SIGN on idle CPU**" in this row's own claim cell)* -- **this row holds three axes now and the other three are unchanged.** **The evidence is four things, and the third is what makes it a fact about the instrument and not about either browser.** (1) Three readings: **~24×** in 2026-08, **0.77×** on 2026-09-16 and **1.31×** on 2026-09-17 -- two sign reversals, the second inside a single day. (2) The distributions **overlap completely**: Chromium 360-955 ms against Firefox 266-781 ms, Firefox's lowest below Chromium's lowest and Chromium's highest above Firefox's highest. (3) The 2026-09-17 control: Chromium is **byte-identical across 1244 and 1245**, so its own column between the two runs measures the rig -- and on this axis it moved **−41%**, against **0.9%** on resident set and **0.0006%** on profile disk. An axis whose control moves 41% cannot resolve a 31% difference. (4) What survives, and it is all three readings together support: **Firefox does not burn an order of magnitude more idle CPU than Chromium.** **Nothing consumes the axis**: the four ratios stopped being the ground under the default family on 2026-09-17 (Q206), and the two sentences that still argued from the sign reversal -- [the charter's browser-families row](../DECISIONS.md) and `SessionManager`'s doc comment -- are corrected by addition. **Re-opening it needs a different instrument, not more rounds.** ⭐ **This run had a control the previous one could not have, and it is why the four verdicts above can be told apart: Chromium did not change.** 1245 and 1244 hold the same 308 files at the same sizes with `chrome.exe` identical to the byte, so Chromium's own column between the two runs measures the rig and not the browser -- profile disk repeatable to **0.0006%**, resident set to **0.9%**, first navigate to 20%, second navigate to 27% and idle CPU to **−41%**. Both families also ran 15-20% slower in the evening while **the ratio moved 5%**, which is the evidence behind this row's own claim that the ratio is the transferable half. The rig is unchanged: [`docs/probes/2026-09-17-cost-ratios`](../docs/probes/2026-09-17-cost-ratios/README.md). ⚠️ *Previously, and kept because it is what the debt looked like:* "**`[STALE]` AND OWED AGAIN SINCE 2026-09-17**, hours after the re-establishment below and against BOTH families at once: the `playwright-core` pull-forward moved chromium 1244 → 1245 and firefox 1544 → 1548. Three rounds per family is the stated minimum, so re-taking these is a measurement session, and it was not taken in the adopting batch." ✅ **RE-ESTABLISHED 2026-09-17 at chromium 1244 and firefox 1544**, clearing a debt this row had carried since 2026-08-14 with `[UNVERIFIED]` on the method. **All four moved and idle CPU REVERSED SIGN** -- the recorded ~24× said Firefox burns two dozen times Chromium's idle CPU and it burns less, 0.77×; RAM went 2× → 1.19×, first navigate 10× → 4.62×, profile disk 20× → 2.76×. The rig is preserved this time: [`docs/probes/2026-09-17-cost-ratios`](../docs/probes/2026-09-17-cost-ratios/README.md). **Three rounds per family is the floor, six is what this row was last taken at, and both families must run in ONE sitting** -- *corrected 2026-09-17 (previously "**Three rounds per family minimum** -- the per-round first-navigate ratio spans 1.47× to 7.16× around its median, so one pair supports any answer in that band")*, which is still true and is no longer the whole of it. That spread was a property of the morning: the evening's six rounds have no outlier in either family and span 4.00× to 4.68×. And the two sittings were six hours apart on a machine that moved 15-20% in between, so a ratio assembled across sittings measures that gap as much as the browsers'. ⚠️ **These four were the whole of the evidence behind Chromium being the default family.** *Corrected 2026-09-17 (previously "; that decision has not been revisited and this row does not revisit it")* -- it was revisited the same day and Chromium stays the default on a ground that is not a measurement at all: *"the reason for the default is that chrome is the most widely used"*, the maintainer's words, recorded in [the charter](../DECISIONS.md). **This row's job is unchanged**: it holds four numbers, and it no longer holds a decision up | -- *manual* |
| 35 | Floating NuGet still needs **two** restores -- `--force-evaluate` to resolve, locked-mode to verify ([kb](toolchain.md#nuget-floating-versions-and-central-package-management)) | NuGet changes lock-file semantics, or the SDK's default. A one-step `--locked-mode` build looks green with the float silently dead | Resolve, then `git diff --exit-code -- "**/packages.lock.json"`. The whole floating-dependency policy rests on this being two commands | -- *manual* |
| 36 | `setupExitWatchdog` still hooks stdin close / `SIGINT` / `SIGTERM` → `gracefullyCloseAll()`, hard-exiting after **15 s** ([kb](playwright/configuration.md#shutdown)) | Upstream changes its shutdown path or that ceiling | Close stdin on a real child; assert graceful close, and that nothing survives the ceiling. Teardown is built on it -- [no killing is involved in the normal path](../ARCHITECTURE.md#sessions) | -- *manual* |
| 37 | `--console-level` still defaults to `info`, silently dropping `debug` ([kb](playwright/configuration.md#defaults-that-are-not-what-they-look-like)) | Upstream changes the default | Config round-trip via `browser_get_config`. The default is [exposed on `init`](../DECISIONS.md#processes-browsers-and-session-modes), so an upstream change silently changes what a caller is choosing between | -- *manual* |
| 38 | A cross-process resume (**Path A**) costs **494 and 456 ms on Chromium, 483 and 492 ms on Firefox** and loses only `sessionStorage`; an in-process relaunch (**Path B**) costs **406 and 401 ms on Chromium, 396 and 406 ms on Firefox** and **loses MORE than `sessionStorage`**. ✅ **RE-ESTABLISHED 2026-09-22 at chromium 1246 and firefox 1549 (`browserVersion` 156.0), TWICE PER PATH PER FAMILY -- eight runs, and the first Firefox reading Path B has ever had -- CLEARING THE `[STALE]` OWED SINCE 2026-09-21.** *Corrected 2026-09-22 (previously "costs **375 ms and 379 ms on Chromium, 389 ms on Firefox** and loses only `sessionStorage`; an in-process relaunch costs **346 ms and 331 ms**. `[STALE]` ⚠️ **OWED SINCE 2026-09-21** ... The **loses only `sessionStorage`** half is a claim about behaviour rather than about milliseconds and is owed in the same sense").* ⭐ **THE DURABILITY CLAIM IS TRUE OF PATH A AND FALSE OF PATH B, AND NOBODY HAD EVER MEASURED PATH B'S -- it did not exist when the claim was written.** Path A: 4 runs, both families, cookie / `localStorage` / IndexedDB / CacheStorage / one service-worker registration all survived and `sessionStorage` alone did not, exactly as recorded. **Path B: 4 runs, and every one lost at least one PERSISTENT store beyond `sessionStorage`** -- the cookie on both Chromium runs, `localStorage` on one Chromium and both Firefox runs, with IndexedDB, CacheStorage and the service worker surviving throughout. **The cookie is `max-age=3600` and was read back after navigating to the same origin**, which was checked before this was written down: a session cookie would have explained it away and it is not one. **It reproduced across two independent sittings** -- an earlier one was discarded as invalid (its driver handed all eight runs one literal session path because a heredoc ate a level of backslash escaping) and its Path B durability column is identical store for store, so the finding rests on 8 runs and not 4. **The mechanism is labelled a READING**: Path A's server exits and the browser flushes; Path B kills the child and the lazily-flushed stores are the two that go. Nothing measured a flush. ⚠️ **AND `SessionManager.ChildWasRelaunched` SAYS THE OPPOSITE ON THIS EXACT PATH** -- *"so cookies and stored state are still there"* -- which is now [an open hazard row](../HAZARDS.md#hazard-index) and not a sentence quietly edited, because the wording of a model-facing string is the maintainer's call. ⭐ **FIREFOX RESUMES WITHIN 3% OF CHROMIUM ON PATH A AND 2% ON PATH B** although it is 4.65× slower to first navigate -- the second reading of a resume being about the DIRECTORY and not the browser, and Path B, which relaunches a real browser, says it too. ⭐ **PATH B IS CHEAPER THAN PATH A ON BOTH FAMILIES**, by ~90 ms on Chromium and ~88 ms on Firefox: Path A pays for a second process meeting a directory it does not own. **The cost movement is mostly the machine** -- Chromium is byte-identical across 1244/1245/1246 and its own first navigate drifted **+15.3%** between the two sittings (row 34), against Path A's +26% and Path B's +19%; two readings cannot separate the residual and none is attempted. ⚠️ **STILL NOT RE-MEASURED, said here and not in a footnote:** a forward made WITHOUT a resume, and therefore `SessionErrors.BrowserServerHasGone` -- the probes resume before they navigate ([kb](playwright/provisioning-and-timings.md#timings-spawn-resume-idle-close-proxy-overhead)) | Any browser or Playwright bump that changes profile durability or launch cost, **and any change to what `browserai_resume` checks before it answers** | ✅ **RE-ESTABLISHED 2026-09-17 at chromium 1245 and firefox 1548**, clearing a `[STALE]` this row carried for six hours. **The durability claim -- the load-bearing half -- held exactly, and now on two families:** cookie, `localStorage`, IndexedDB, CacheStorage and one service-worker registration all survived, `sessionStorage` alone did not, identically on Chromium 1245 and on **Firefox 1548, which is this row's first Firefox reading**. The cost moved **336 and 367 → 375 and 379 ms**, about 9%. ⭐ **Chromium cannot be the reason it moved**: 1245 and 1244 are the same 308 files at the same sizes with `chrome.exe` identical to the byte, so what changed under the number is the **server** (`5d0d04f` landed between the readings) or the machine, which drifted 15-20% the same day on an unchanged binary -- two readings cannot tell those apart and none is attempted. ⭐ **Firefox resumes within 4% of Chromium** (389 vs 375/379 ms) although it is 4.37× slower to first navigate, which is the first evidence and not argument that a resume is about the DIRECTORY and not about the browser. ⚠️ **What "resume" MEANS changed on the day this was re-taken, and the row now carries both paths.** Until `5d0d04f` the tool asked one question, *do I already own this directory*; it asks a second now and relaunches a child that has gone. **Path A** -- a different process meets the directory -- is the headline figure and the case the feature exists for. **Path B** -- the same process repairs a session whose child died -- did not exist when this row was last measured and was timed for the first time here: **345.77 ms and 330.92 ms**, against the **7.68 ms no-op** the unfixed product answered, with the next `browser_navigate` returning a real page in **444 ms and 426 ms** where it had **never returned in 900,000 ms**. That confirms the prediction row 131 wrote down and nobody had yet run. ⚠️ **Not re-measured, and said here and not in a footnote:** a forward made WITHOUT a resume, and therefore `SessionErrors.BrowserServerHasGone`, the door-refusal `5d0d04f` added for that case. ⚠️ *Previously, and kept because it is what the debt looked like:* "**`[STALE]` AND OWED AGAIN SINCE 2026-09-17**: the `playwright-core` pull-forward to 1.64.0-alpha-2026-09-17 is a Playwright bump AND a chromium revision move, 1244 → 1245, which is both halves of this row's trigger. The COST is what a revision most plausibly moves; the DURABILITY claim is the half [reclaim is forever](../ARCHITECTURE.md#sessions) rests on and is the one to re-take first. Neither was re-measured in the adopting batch." ✅ **RE-ESTABLISHED 2026-09-16 at chromium 1244** under `playwright-core` 1.64.0-alpha-2026-09-14: the cost moved 515 → 336 and 367 ms, and **the durability claim held exactly** -- cookie, `localStorage`, IndexedDB, CacheStorage and one service-worker registration all survived, `sessionStorage` alone did not. ⚠️ **This row's procedure was wrong and is corrected with it.** *Kill the node child, resume against the directory* does not produce a resume: with the child killed under a **live** BrowserAI the same server answers *already open in this BrowserAI* in 7.8 ms and the next browser call had not returned after 3 min 8 s. The measurement needs **two server processes** -- one creates and fills the session and exits, a second meets the directory -- and **since 2026-09-17 it also needs the one-server shape run separately**, because that shape stopped being a no-op and became the second resume path and not the broken version of the first. `resume-probe2.js` times Path A; `wedge-probe.js` times Path B and is no longer a probe for a wedge. [`docs/probes/2026-09-16-resume`](../docs/probes/2026-09-16-resume/README.md) carries both shapes. **[Reclaim is forever](../ARCHITECTURE.md#sessions) rests on this**: if a resume stopped preserving the profile the no-expiry decision would be wrong, and nothing else in the design would say so | -- *manual* |
| 38a | **`browser_close` takes the whole browser tree down and leaves the node child; the next tool call brings the browser back implicitly**, ~496 MB → ~118 MB and ~0.41 s, re-measured 2026-08-16 ([kb](playwright/provisioning-and-timings.md#timings-spawn-resume-idle-close-proxy-overhead)) | Upstream renames or re-scopes `browser_close`, or stops creating the browser lazily on first use | **The behaviour is the load-bearing half and the suite owns it**: the named test drives a real browser, waits past a shortened idle period, and asserts no browser process, a live node child, and a following navigation that succeeds with no *"browser is closed"* wording. If the relaunch ever stopped being implicit, [the whole timer](../ARCHITECTURE.md#sessions) would become a way of breaking a session and not of reclaiming memory. The **numbers** are not automated -- re-run the two-navigation probe described in the article, counting processes by full image path under the browsers root | `BrowserIdleTimerTests` |
| 39 | A console logging sink's **type initializer** calls `SetConsoleMode` on `STD_OUTPUT_HANDLE` before any log line, and no-ops silently when stdout is a pipe ([kb](windows/processes.md#stdio-exit-codes-and-process-startup)) | We adopt `Serilog.Sinks.Console` or any sink shaped like it -- or the referenced version moves. Upstream `main` has **already dropped** the `#if PINVOKE` guard that leaves 3.1.2 inert on `netstandard2.0`, so a version bump alone can arm it | Read `ConsoleSink.cs` and `Platform/WindowsConsole.cs` **at the version actually referenced**, not at whichever copy is on disk. **Conditional row:** nothing today depends on Serilog, so this is owed only once a console sink reaches the dependency list -- but it must be answered *then*, because the write is invisible under MCP and shows up only in interactive diagnostics | -- *manual* |
| 40 | A legitimately **empty** channel 404s exactly like a misconfigured feed URL ([kb](packaging/velopack.md#1-the-channel-must-not-go-in-the-feed-url)) | Velopack returns an empty release list instead, or changes the status code | Request a channel with nothing published, assert the status, and confirm the health check does **not** alarm. Row 24's update lane only ever exercises a populated channel, so this is not covered there | -- *manual* |
| 41 | `UpdateExe.Start`'s first positional parameter is the **locator**, not `waitPid`; `ApplyUpdatesAndRestart` already restarts on its own ([kb](packaging/velopack.md#the-restart-handover-race-and-why-updateexe-is-the-answer)) | Any Velopack bump changes either signature | Compile a call against the shipped assembly, and assert exactly **one** relaunch on the update path. The double-restart failure is silent -- two working launches look like one slow one | -- *manual* |
| 42 | **Diagnostic-severity precedence**: `NoWarn` beats both `WarningsAsErrors` and an `.editorconfig` `dotnet_diagnostic` severity; bulk `dotnet_analyzer_diagnostic.category-*` entries are ignored once `AnalysisMode` is an MSBuild property; IDE0005 needs `GenerateDocumentationFile` ([kb](toolchain.md#analyzers-and-diagnostic-severity)) | Any SDK or Roslyn bump. **The SDK floats** under `rollForward: latestMajor`, so this moves without anyone choosing it | Plant the failure and rebuild with `--no-incremental`, one variable at a time. `BuildConfigurationTests.NoBuildFileSuppressesWarnings` guards the *consequence* on every build; the precedence itself needs re-measuring | -- *manual* |
| 43 | Central package management refuses `Version="*"` without `CentralPackageFloatingVersionsEnabled` -- NU1011 ([kb](toolchain.md#nuget-floating-versions-and-central-package-management)) | Any SDK or NuGet bump. If it ever became the default, the property is harmless; if the property were ever renamed, restore fails loudly | Delete the property and restore. Every ordinary restore already proves the positive half, so only the negative half is owed | -- *manual* |
| 44 | NativeAOT embeds `ApplicationManifest` into the published binary ([kb](toolchain.md#analyzers-and-diagnostic-severity)) | Any SDK or ILC bump changes Win32 resource handling | Read the published exe's bytes for `longPathAware`, `asInvoker` and the supportedOS GUID. Cheap, and the guarantee is otherwise unfalsifiable until a caller picks a path over MAX_PATH | -- *manual* |
| 45 | `FileMode.Append` loses records across processes; `FILE_APPEND_DATA` without `FILE_WRITE_DATA` does not ([kb](windows/processes.md#files-durable-writes-and-deletes)) | .NET changes its append implementation -- in which case the Win32 path we took stays correct and the note's first half stops being a live hazard | `ProcessLogTests.ConcurrentProcessesDoNotLoseEachOthersRecords`: eight processes, 25 records each, assert all 200 present. It is a real regression guard, not a re-measurement, because it fails the same way the original defect presented -- silently missing lines | `ProcessLogTests` |
| 46 | **Node's `LICENSE` is not published beside `node.exe`** -- `dist/<version>/win-x64/` carries only `node.exe`, `node.lib` and two `node_pdb` archives, so the licence comes out of `node-<version>-win-x64.zip` ([kb](playwright/provisioning-and-timings.md#component-sizes)) | Node publishes a standalone `LICENSE`, or renames or restructures the archive. **The renaming half is the dangerous one**: the extraction is keyed on `node-<version>-win-x64/LICENSE`, and a build that quietly shipped no licence would look identical to one that did | Extract `node.exe` **and** `LICENSE` from the SHA-256-verified archive and fail if either entry is absent. Never fetch the bare `node.exe` -- that route is one `GET` shorter, 53 MB heavier, and ships no licence | `build/Build-Payload.ps1` |
| 47 | **`.links/` is written into the browsers root and never into `node_modules`** -- `path.join(registryDirectory, '.links')`, three call sites in `playwright-core/lib/coreBundle.js` ([kb](playwright/provisioning-and-timings.md#first-run-provisioning)) | Upstream moves it into the package tree, at which point the strip that [the runtime design](../ARCHITECTURE.md#the-runtime-it-ships) once demanded becomes real again and build paths would ship | Assert the assembled payload contains no `.links` directory. Also the reason the stale-browser GC must stay off: a registry directory with no `.links` entry is prunable | `build/Build-Payload.ps1` |
| 48 | **The payload tree's shape** -- `@playwright/mcp` pins `playwright-core` **exactly**, and no non-optional package declares an install script (*re-read 2026-09-21 @ `@playwright/mcp` 0.0.82 / `playwright-core` 1.64.0-alpha-1789764292000 and **BOTH HALVES HOLD**: 0.0.82 declares `playwright-core` and `playwright` at the exact string `1.64.0-alpha-1789764292000`, with no range character in either, and the resolved lock's four packages carry no non-optional `hasInstallScript`. **This row is why the dated override was available at all and it is why the override could END cleanly**: an exact pin is what made an `overrides` entry move the tree by one build, and an exact pin is what the retired exit check compared against. The half that now carries the invariant is `PayloadTests.TheLockRecordsUpstreamsOwnExactPinOfPlaywrightCore`, which asserts the resolved version EQUALS the declared one again -- planted red 2026-09-21 against a doctored lock. Re-read 2026-09-17 @ `@playwright/mcp` 0.0.81 / `playwright-core` 1.64.0-alpha-2026-09-17 and **BOTH HALVES HOLD**: the declared pin is still the exact string `1.64.0-alpha-2026-09-14`, and the resolved tree declares no `hasInstallScript` outside optional entries. ⚠️ **What the pin no longer equals is the RESOLVED version**, because an npm `overrides` entry now sits above it as a dated exception -- so "compare the declared dependency against the resolved version" became the wrong check on 2026-09-17, and the right one is in the next column. The exactness is what the override RESTS on: an exact pin can be moved by exactly one build, a range could not be*) ([kb](playwright/provisioning-and-timings.md#component-sizes)) | Upstream loosens the pin to a range, or adds a `postinstall`. Either is silent: the tree still installs, and the payload starts floating on a second axis or starts executing upstream code on the build machine | Require the declared dependency to be an exact version, and the resolved version to equal the override when `build/payload/package.json` carries one and the declared pin otherwise; scan the lock for `hasInstallScript` outside `optional` entries. The install runs **without** `--ignore-scripts` on purpose, so a new script is caught and not suppressed. **The override carries its own exit**, which is a second reading of the same two numbers: the day the declared pin reaches or passes the override, both instruments go red naming the key to delete | `build/Build-Payload.ps1` · `PayloadTests` |
| 49 | **npm keys a lock's root package on the empty string, and `npm ci` does not rewrite the lock** ([kb](toolchain.md#npm-for-a-vendored-payload)) | npm changes `lockfileVersion`, or PowerShell changes `ConvertFrom-Json`. The first fails loudly; the second is why `-AsHashtable` is not stylistic | Parse the resolved lock, and compare it byte for byte either side of `npm ci`. **Not covered:** whether `npm install` re-resolves a dist tag with a lock already present -- the build deletes the lock first, so that state never arises | `build/Build-Payload.ps1` |
| 50 | **`PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD` gates the npm-postinstall and server auto-install paths only, not `registry.install()`** -- and installing `ffmpeg` on Windows pulls `winldd` with it ([kb](playwright/provisioning-and-timings.md#first-run-provisioning)) | Upstream extends the flag to the explicit installer, which would stop first-run provisioning's provisioning dead while the variable stays [mandated in the child environment](../DECISIONS.md#the-five-rules-that-make-floating-safe) for the reason it was always mandated | `install-browser ffmpeg --no-progress` against an empty browsers root with the flag set; assert it still downloads. The payload build's own `chrome.exe` assertion catches the regression too, one step later and just as loudly | `build/Build-Payload.ps1` |

| 51 | **The snapshot gate is byte-exact across line endings.** All four snapshots are **LF** today; the `-text` exemption in `.gitattributes` is what keeps a committed byte copy from being normalised out from under the comparison if upstream ever ships CRLF ([kb](toolchain.md#git-line-ending-normalisation)) | Upstream changes its line endings **and** the exemption has been lost. Either alone is harmless; together the committed copy is LF, the regenerated one is CRLF, and every build is red on a difference that is not one | Count CR **bytes**, by piping `tr -cd '\r'` into `wc -c`, on the four snapshots and on the payload originals. Never `grep -c $'\r'`, which reported every line of an all-LF file as a match | `build/Update-UpstreamSnapshots.ps1` |
| 52 | **Three traps in the gate's own plumbing**: `$(IntermediateOutputPath)` is empty in a `.targets` imported from the project body (the stamp escapes `obj\`); PowerShell 7 colours redirected output; `Get-Command git` returns two executables on this machine ([kb](toolchain.md)) | Any SDK bump for the first, any PowerShell bump for the second. **The SDK floats**, so the first moves without anyone choosing it | Re-measure the first by pointing a `Touch` at `$(IntermediateOutputPath)` from an imported `.targets`. Its *consequence* is caught on every step by `git status --porcelain`, which is how it was found | -- *manual* |
| 53 | **`StreamServerTransport` still re-escapes every result**, because the escaping comes from `Utf8JsonWriter`'s own encoder and it sets none -- 127 bytes through our transport against 190 through the SDK's, for one message ([kb](mcp/sdk.md#writing-replacement-transports-against-the-public-surface)) | Upstream sets an `Encoder`, or gains an options seam. Deviation 5's transport stays correct either way, but [the reason recorded for owning it](../STACK.md#nine-places-where-the-sdk-must-be-deviated-from) would be stale, and a stale reason is how a component nobody needs survives a rewrite | Serialise one message through both transports and compare bytes, never decoded strings -- the escaping is semantically lossless and invisible to anything that round-trips | `DirectStdioServerTransportTests.TheSdkServerTransportStillEscapesTheSameResult` |
| 54 | **A double hyphen in an XML comment in a shared props file presents as `NETSDK1207: Ahead-of-time compilation is not supported for the target framework`**, and only `dotnet msbuild -getProperty` names the real cause ([kb](toolchain.md#msbuild-property-evaluation)) | Any SDK bump changes the diagnostic or the order of the checks. **The SDK floats** under `rollForward: latestMajor`, so this moves without anyone choosing it | Put `--` inside a comment in `Directory.Build.props`, then run `dotnet build` and `dotnet msbuild <project> -getProperty:TargetFramework` and compare what each says. Revert | -- *manual* |
| 55 | **`BannedApiAnalyzers` merges every additional file named `BannedSymbols.txt`**, which is what lets the repository-wide never-by-image-name list and the product-only Console list be separate files ([kb](toolchain.md#analyzers-and-diagnostic-severity)) | Any analyzer bump changes the file discovery. It would fail **open**: one of the two lists would stop applying, with a green build and no diagnostic anywhere | Plant one banned call from each file in the product project and require both RS0030s on the same build. Revert | -- *manual* |
| 56 | **`server/discover` exists on our side of the proxy and not on the child's** -- BrowserAI answers `-32602` (per-request metadata missing), the child answers `-32601` ([kb](mcp/protocol.md#the-protocol-split)) | The SDK stops implementing `2026-07-28`, or upstream adopts it. Either would make the two halves of the protocol split the same revision, which is the state the split exists to avoid | Send `server/discover` as the first frame to each end of the running proxy and compare the two error codes. A version string cannot show this, because a version can be echoed | `ProtocolSplitTests.TheServerReachesARevisionTheChildDoesNotImplement` |
| 57 | **`JsonArray.Add(x)` without the `(JsonNode)` cast fails `dotnet build` as well as `dotnet publish`**, as `IL2026` and `IL3050` at the same call site ([kb](mcp/sdk.md#the-published-nativeaot-binary-with-all-of-it-in-one-exe)) | Any SDK or `System.Text.Json` bump removes either attribute, or `EnableAotAnalyzer`/`EnableTrimAnalyzer` stop being set unconditionally. It fails **open**: the trap would then only appear at publish, or not at all | Plant the call, build, publish, revert. Lossless passthrough rewrites `tools/list` on `JsonNode`, which is where this shape is most likely to be written | -- *manual* |
| 58 | **`Capabilities.Tools` is what makes `initialize` advertise tools**, independently of whether tool handlers are registered ([kb](mcp/sdk.md#writing-replacement-transports-against-the-public-surface)) | Any SDK bump changes how capabilities are derived. Silent in the worst way: `tools/list` keeps working for anything that asks, and a caller that respects capabilities stops asking | Assert `capabilities.tools` on the `initialize` result the raw client receives, not on the options object we built | `VerticalSliceTests.ToolsListReturnsTheChildsToolsWithUpstreamsOwnNames` |
| 59 | **An exception escaping a `CallToolHandler` becomes a JSON-RPC *success* carrying `isError: true`, with a body that names no cause** -- the same frame for an unknown content type and for a child that died mid-call ([kb](mcp/sdk.md#error-shape-and-teardown-seen-from-an-in-process-harness)) | Any SDK bump changes the handler's exception policy. **The blast radius shrank at lossless passthrough**: there are no typed tool handlers left, so the SDK's policy can no longer reach a caller of BrowserAI at all, and the fact is kept because it is the shape the old design produced and the reason step 9 exists | Drive a child that dies mid-call through the proxy and assert on the **caller's** frame. It is now a JSON-RPC error naming the cause, not a success carrying `isError`, so the test guards the *replacement* -- re-establishing the SDK's own behaviour needs a typed handler this product no longer has | -- *manual*; the consequence is automated by `LosslessPassthroughTests.AChildThatDiesMidCallProducesANamedErrorRatherThanASuccess` |
| 60 | **A child's JSON-RPC error keeps its `code` and its `data` verbatim, and the SDK client still prefixes the message** with `Request failed (remote): ` ([kb](mcp/sdk.md#lossless-passthrough-cancellation-notifications-and-error-frames)) | Any SDK bump changes `CreateRemoteProtocolExceptionFromError` or how the server re-serialises an `McpException`. **Since lossless passthrough the prefix is never met** -- the caller is answered from the child's own error frame -- so a change would not break the product, it would strand the deviation's justification | Two tests, one per side. Program the double to answer with a code, a message and a nested `data`, and assert all three at the caller by **exact equality**; then drive a plain `McpClient` at the same double and assert the exception still carries the prefix, the code and a non-empty `Data` | `FakeChildHarnessTests.TheFakeChildInjectsAJsonRpcError` · `SdkErrorShapeTests.TheSdkStillPrefixesARemoteErrorMessageAndStillKeepsItsData` |
| 61 | **The in-process teardown order: cancellation and completing both pipe writers each end `McpServer.RunAsync` independently, and only completion closes the hop** ([kb](mcp/sdk.md#error-shape-and-teardown-seen-from-an-in-process-harness)) | Any SDK bump changes what `RunAsync` returns on. The consequence is guarded on every build by the rig's own liveness assertion; what is owed here is the *mechanism*, which that assertion cannot distinguish | Comment out step 1 or step 2 of `McpTestHarness.DisposeAsync` and run the suite, one at a time and then both. Ten tests turn red instead of hanging, because the wait is bounded and the server-task state is read **before** anything is disposed | -- *manual*; the consequence is automated by `McpTestHarness` on every run |
| 62 | **A response's `result` reaches the caller as the exact bytes the child wrote** -- escaping, numeric form, key order, unknown members and unknown content *types* all unchanged, because the payload is sliced by token offset and written with `WriteRawValue` and not re-serialised ([kb](mcp/sdk.md#lossless-passthrough-cancellation-notifications-and-error-frames)) | `System.Text.Json` changes `TokenStartIndex` / `BytesConsumed` semantics or `WriteRawValue`; or somebody re-introduces a `JsonNode` round trip on the answer path, which would pass every semantic check and quietly change the bytes | Compare the **span** of `result` in the double's sent frame against the span in the caller's received frame, sliced by a reader written for the tests and not by the product's own. Include an escape the child chose (`é`), a base64 payload containing `+` and `/`, an unknown content type, an unknown property and a 2 MiB body | `LosslessPassthroughTests.AnEscapeTheChildChoseStaysAnEscape` · `LosslessPassthroughTests.AnImagePayloadArrivesByteIdentical` · `LosslessPassthroughTests.AnOversizedPayloadArrivesByteIdentical` |
| 63 | **A named child-to-caller notification is relayed through the public `McpSession.RegisterNotificationHandler`, and the caller's own `progressToken` survives it** ([kb](mcp/sdk.md#lossless-passthrough-cancellation-notifications-and-error-frames)) | The SDK removes or narrows `RegisterNotificationHandler`, or starts consuming `notifications/progress` before user handlers see it. Silent either way: the call still succeeds and the caller simply never hears progress | Call a tool with a `_meta.progressToken` of the caller's choosing, have the double echo it back on every progress notification, and assert the token that reaches the caller is the caller's and not one the proxy minted | `LosslessPassthroughTests.AChildProgressNotificationReachesTheCallerUnderTheCallersToken` |
| 63a | **The relay does not preserve notification *order*** -- inbound messages are dispatched fire-and-forget, and two progress notifications written in order were observed arriving 2 then 1 ([kb](mcp/sdk.md#lossless-passthrough-cancellation-notifications-and-error-frames)) | The SDK starts awaiting message handling in order, which would make this stop being true in the harmless direction. It cannot be fixed from a notification handler, because the reordering happens before the handler runs | Run row 63's test with logging at `Trace` and read the order of the two `sending message` lines. Split out of 63 on 2026-08-16 instead of folded into it: that test asserts the token and the count, and says nothing about sequence | -- *manual*; deliberately unasserted, and carried in [`TODO.md`](../TODO.md) and not claimed |
| 64 | **`Utf8JsonWriter`'s default encoder escapes `+`**, so an ISO 8601 timestamp with a positive UTC offset is written with its sign escaped unless `UnsafeRelaxedJsonEscaping` is set ([kb](windows/processes.md#files-durable-writes-and-deletes)) | Any `System.Text.Json` bump changes the default encoder's safe list -- in either direction. It is silent both ways: the file round-trips perfectly and only a human reading it notices | Assert on the **literal bytes** of a written timestamp, never on the parsed value. Every parse-side assertion passed while this was wrong, which is why the check has to be on the file | `SessionRecordTests.EveryTimestampRoundTripsUnderADeliberatelyNonInvariantCulture` |
| 65 | **A rename over an open file surfaces as `UnauthorizedAccessException`, which is not an `IOException`** -- `File.Move(overwrite: true)` is refused `ERROR_ACCESS_DENIED` under every share mode, including `FILE_SHARE_DELETE` ([kb](windows/processes.md#files-durable-writes-and-deletes)) | .NET changes how `File.Move` maps the Win32 error, or stops routing through `MoveFileEx`. The durable `browserai.json` write retries on both types **because of this measurement**; a version that caught only `IOException` would stop retrying the case that happens, and would fail exactly under load | Hold the destination open under all three share modes and assert the exception type and HRESULT on each. The Windows half of the fact is stable; the .NET mapping is what floats | `SessionLockTests.ARenameCannotReplaceALockFileWhoseOwnHandleIsStillOpen` |
| 66 | **An unset `browser.userDataDir` puts every run's profile in `%LOCALAPPDATA%\ms-playwright-mcp\mcp-<channel>-<sha256(clientInfo.cwd)[..7]>`**, created eagerly at launch and never cleaned up -- 159 directories / 877 MB had accumulated here before the key was set ([kb](playwright/configuration.md#defaults-that-are-not-what-they-look-like)) | Upstream moves the fallback, changes the key it reads, or renames `userDataDir` -- which `loadConfig` would discard in silence, so the only signal is the directory reappearing | Delete `%LOCALAPPDATA%\ms-playwright-mcp\`, run the browser-touching tests, and require it to stay absent. The config round trip covers the key reaching the child; only the deletion covers the fallback not firing | `ConfigRoundTripTests` for the key; the deletion check is *manual* and belongs in the release gate. **Answered 2026-08-16 at first-run provisioning:** the directory was confirmed absent before the step and again after a full suite run that opened real sessions, drove two browser families and provisioned an empty root -- so the fallback did not fire once. **Re-run 2026-09-16 and held again:** the directory had accumulated **1,881 files in 624 profile directories, 173,612,736 bytes (165.6 MiB)**, was deleted whole in the machine sweep, and was **still absent** after two full suite runs -- one forcing `C:\` and one forcing `c:\`, 741/0/0 each, both `FULL RUN` and both browser-touching. Evidence: [`docs/evidence/2026-09-16-garbage/`](../docs/evidence/2026-09-16-garbage/README.md) |
| 67 | **There is no trace option at 0.0.79** -- not on the CLI, not in `config.d.ts`; `tracesDir` is computed as `<outputDir>/traces` and `saveSession` is the nearest surviving feature ([kb](playwright/configuration.md#defaults-that-are-not-what-they-look-like)) | Upstream restores a trace option, at which point [the session modes](../ARCHITECTURE.md#sessions)'s `tracing` modifier should stop meaning `saveSession` | Grep the resolved bundle for `saveTrace` and read the committed `cli-help.txt` and `config-schema.d.ts` snapshots | -- *manual*; a restored option would show up as a snapshot diff first, which is the loud half |
| 68 | **`browser_get_config` answers Markdown with the config JSON inside it**, because the response builder prefixes every text section with `### <title>` ([kb](playwright/configuration.md#defaults-that-are-not-what-they-look-like)) | The response builder changes its wrapping, or the tool stops emitting the whole resolved config | Call the tool and require a parseable config object inside the answer | `ConfigRoundTripTests` |
| 69 | **BrowserAI advertises 71 of the 72 tools a fully-capable child exposes, and a session permits all 71** -- the other one, `browser_annotate`, is withheld from `tools/list` and refused wherever it is named, on **liveness** grounds and not as a permission (*re-measured 2026-09-21 @ `@playwright/mcp` 0.0.82, previously "**advertises 72 of the 74** ... the other two, `browser_annotate` and `browser_webmcp_call`". **The withheld set went back to one and nobody decided that**: upstream made `browser_webmcp_call` `skillOnly`, so it left the exposed surface entirely and its verdict row was deleted as a judgement about nothing -- UPSTREAM-REVIEW.md's own instruction for a tool upstream removed. The deny's REASONING is preserved in `tool-verdicts.json`'s `_rows_removed_because_upstream_withdrew_the_tool` and in `upstream-review.json`, because it is what a future judgement would need if the tool comes back. Re-counted off the regenerated snapshot, never decremented*) ([kb](playwright/tools-and-artifacts.md#what-a-browserai-session-permits-after-its-own-filtering)). ⚠️ **Corrected 2026-09-17 (previously "71 of the 73 tools a fully-capable child exposes, and a session permits all 71")** -- the `playwright-core` pull-forward added `browser_emulate_media`, **`core`** and so unconditional, judged `allow`, so both figures moved by one and the withheld set is unchanged at two. Re-counted off the regenerated snapshot and not incremented. ⚠️ **Corrected 2026-09-15 a second time the same day (previously "70 of the 71 tools a fully-capable child exposes, and a session permits all 70 -- the 71st, `browser_annotate`, is withheld")** -- `@playwright/mcp` 0.0.81 added `browser_webmcp_list` and `browser_webmcp_call`, both **`core`** and so both unconditional, and **the pair was judged in opposite directions**: the list `allow`, the call `deny`. So the denominator moved by two and the numerator by one, and **the withheld set is two for the first time** -- which is a decision taken here, unlike every previous move of this row. ⚠️ **Corrected 2026-09-15 earlier the same day before that (previously "68 of the 69 tools a fully-capable child exposes, and a session permits all 68")** -- `@playwright/mcp` 0.0.80 added `browser_start_recording` and `browser_stop_recording` to `devtools` and both were judged `allow`, so both figures moved by two and the withheld one stayed one; **this is upstream growing, not a decision reversed here**. ⚠️ **Corrected 2026-08-20 before that (previously "58 of the 59 tools its union child exposes, and all three modes permit all 58")** -- session modes were deleted and every capability is granted to every session, so `network`, `pdf` and `testing` reached a child for the first time and brought ten tools with them. ⚠️ **Corrected 2026-08-18 before that, twice on the day (previously "58 / 59 / 59 of the 59 it advertises -- one refusal ... on a mode that promised no window", and "41 / 41 / 58 ... and the classification behind them is 69 names in five classes")** -- the `(tool, mode)` permission matrix was removed because it was never a boundary against the caller, and then the one refusal it had left stopped being per-mode | Upstream renames `browser_annotate`, or bounds its wait, or stops launching its dashboard headed and outside the session -- at which point the withholding is either aimed at nothing or no longer earned, and row 94 is the measurement that would have to be re-run. **The same trigger now applies to `browser_webmcp_call`**: upstream wrapping its call path in the `withTimeout` the list path already has would take the liveness ground out from under that denial too, and it would arrive with **no snapshot change at all**, because a timeout is not in a schema. A tool upstream **adds** no longer touches this row at all: change detection moved to the golden `tools/list` snapshot, which diffs each tool's `inputSchema` as well as its name | Recompute the union from the committed snapshot, apply the product's own withholding predicate, and ask its decision function about every name that survives -- then assert both denominators and the named hole individually, off the wire as well as in process. A count alone is satisfied by the wrong tool as easily as by the right one | `SessionPolicyTests.ASessionPermitsEveryToolItAdvertisesAndTheOneThatWouldHangIsNotAdvertised` · `SessionPolicyTests.TheAnnotationToolIsAbsentFromTheSurfaceAndRefusedIfNamedAnyway` · `ModelSurfaceTests.EverySessionGetsEveryCapabilityAndTheNewlyGrantedTenAreInTheSurface` |
| 70 | **The server `instructions` string is 1,276 bytes of a 2,048-byte budget**, leaving 772, and its three mode lines cost 106 / 121 / 92 bytes ([kb](mcp/protocol.md#the-client-claude-code)). ⚠️ **Corrected 2026-08-18 (previously "1,628 bytes of a 2,048-byte budget, and a fourth mode costs 223 of the remaining 420")** -- the mode lines carried what each mode *refuses*, rendered from the removed `(tool, mode)` policy, and lost 352 bytes with it | Our own wording changes, or a mode or authored tool is added. The client truncates at 2 KB in silence, so the failure is a tail nobody has ever read | Measure `ServerInstructions.ByteCount` in **bytes** and fail over the cap. The string carries `·` and `-`, so a character count under-reports exactly the string that uses them. The per-line costs come out of the emitted string itself, off a real `initialize` against the published binary | `ModelSurfaceTests.TheInstructionsStringFitsTheClientsSilentTruncationBudget` |
| 71 | **`config.secrets` is a real key** -- `--secrets <path>` on the CLI, `secrets?: Record<string, string>` in `config.d.ts` -- and `browser_get_config` answers with the key's **names** and `<secret>NAME</secret>` in place of every value, because the whole response is serialised through `redactSecrets` ([kb](playwright/tools-and-artifacts.md#tools-that-reach-credentials)). ⚠️ **Corrected 2026-08-20 (previously "and `browser_get_config` serialises it with no redaction")** -- measured against the bundled child with the key set; the replacement is a substring match on the value, so it is a convenience, not a boundary | Upstream **populates** the key by default, or makes it settable from something BrowserAI does pass. ⚠️ **Corrected 2026-08-18 (previously "Upstream renames the key, at which point the guard on the answer is looking for a word that no longer appears ... the guard is the only thing between a config that came from outside this process and a caller's context")** -- the guard was removed. It defended against upstream starting to populate a key BrowserAI never writes and never passes `--secrets` for, and a config a caller cannot influence is not a caller's disclosure | Read the committed `config-schema.d.ts` and `cli-help.txt` snapshots, which the build regenerates from the resolved payload and diffs. A change to the key -- its name, its type, or a default that fills it in -- arrives as a snapshot diff a human has to adjudicate before a release can be cut, which is strictly more than a substring match on the answer ever saw | `UpstreamSnapshotTests` · `UpstreamReviewTests` |
| 72 | **Chrome for Testing has exactly one mirror** -- `cftUrl` returns `mirrors: ["https://cdn.playwright.dev"]` and a `builds/cft/<version>/win64/` path with no `/dbazure/download/playwright` prefix, unlike every other component ([kb](playwright/provisioning-and-timings.md#first-run-provisioning)) | Upstream adds a mirror, or moves CFT onto the shared list. It fails **quiet**: the retry rotation reads as insurance and provides none for the 202 MB half of the download, so one unreachable host is five failed attempts | Grep `cftUrl` in the resolved bundle and compare its `mirrors` against `ffmpeg`'s. The consequence -- that stripping `PLAYWRIGHT_DOWNLOAD_HOST` protects only the three small components -- is what the note beside the retry rule now says | -- *manual* |
| 73 | **`browser_get_config` requires the browser executable to exist**, though it launches nothing ([kb](playwright/configuration.md#browser-provisioning)) | Upstream resolves the browser lazily, at which point the tool could answer during provisioning after all and BrowserAI's blanket refusal would be costing a caller an answer it could have had | Drive `cli.js` directly with `PLAYWRIGHT_BROWSERS_PATH` at an empty directory and call the tool. **This is the row that corrected [the runtime design](../ARCHITECTURE.md#the-runtime-it-ships)**, which said the tool keeps working while the download runs | `ProvisioningTests.EveryBrowserToolIsRefusedWithRowSixIncludingTheConfigOne` · `FirstRunProvisioningTests` |
| 74 | **`throwIfExecutableMissing`'s message shape** -- `Run \`<command>\` to install`, with the command chosen by a ternary on `skillMode` and the target being the resolved **channel** and not the browser family ([kb](playwright/configuration.md#browser-provisioning)) | Upstream rewords it. **Silent in the worst direction**: BrowserAI's replacement stops matching, the `npx` instruction reaches a model, and the model runs it -- fetching a different revision into a directory BrowserAI never launches from | Reproduce the sentence from upstream's own builder and require the strip to fire on both branches of the ternary, then assert at the caller that the imperative is gone and the diagnosis survived | `ProvisioningRemediationTests` |
| 75 | **The per-socket stall timeout is upstream's `NET_DEFAULT_TIMEOUT = 3e4`**, overridable by `PLAYWRIGHT_DOWNLOAD_CONNECTION_TIMEOUT`, which BrowserAI never sets ([kb](playwright/configuration.md#browser-provisioning)) | Upstream changes the default. BrowserAI's own caps -- 45 min absolute, 10 min extraction, 60 min tripwire -- are all reasoned against this figure, so a much larger one would make them the first thing to fire on a stalled socket and not the last | Grep `NET_DEFAULT_TIMEOUT` in the resolved bundle, and assert the variable is absent from the installer's environment -- which it is by construction, because the environment is an allowlist | `ProvisioningTests.TheInstallerInheritsNoDownloadHostAndNoStallTimeoutOverride` for the absence; the default itself is *manual* |

| 76 | **`EnumWindows` and the message-only walk have zero overlap** -- 673 top-level windows on this machine, **0** of class `Chrome_MessageWindow`, against **64** the class-qualified `FindWindowExW(HWND_MESSAGE, ...)` walk finds at the same instant ([kb](windows/detection.md#the-sweep-measured-through-the-products-own-code-paths)) | Windows changes how message-only windows are parented, or somebody "simplifies" the walk into an `EnumWindows` loop. **Silent in the worst possible way**: the loop does not throw, does not warn, and reports a clean machine forever | Enumerate top-level windows and count the class; walk the message-only set at the same moment; require zero and non-zero respectively, with the top-level count asserted above a floor so the question cannot be vacuous | `MessageWindowTests.EnumWindowsFindsNoMessageWindowsAtAllWhileTheWalkFindsThem` |
| 77 | **A UNC window title stalls `File.Exists` for 21 seconds, and 1 of the 13 named message windows on this machine is not a path at all** (`DeviceMonitorMessageWindow`) ([kb](windows/detection.md#the-sweep-measured-through-the-products-own-code-paths)) | .NET or SMB changes the timeout -- in either direction. The guard is a string check that costs microseconds, so a shorter stall would not make it wrong, only less urgent. What would break it is somebody moving the check after `Path.GetFullPath` or after the first `File.Exists` | Publish a window titled `\\10.255.255.1\share\profile` and require the pass to reject it **and finish**. `Corrected 2026-08-18 (previously "with the elapsed time asserted -- the failure this prevents is a stall, not a wrong answer")` -- the stopwatch was a promptness assertion a starved machine could reach, and it was deleted for the assertion that is *decisive* and not merely correlated: the title appears in `RejectedTitles`, which only the string check populates, so a pass that formed a path could not put it there at any speed. The live `DeviceMonitorMessageWindow` example is read out of the probe's `stray-sweep` report | `StraySweepTests.AUncTitleIsRefusedByAStringCheckAndTheSweepStaysFast` |
| 78 | **A full sweep pass costs 24.9-28.6 ms from the published binary** (37.5-39.3 ms from a framework-dependent build), over 666-668 pids of which 496-504 open, 64 message windows of which 13 are titled ([kb](windows/detection.md#the-sweep-measured-through-the-products-own-code-paths)) | Any Windows, .NET or machine change. The design rests on this being cheap enough that one process paying it per startup is free; nothing breaks at 10× and the sweep mutex means only one process pays at all | `BrowserAI.Server.exe --sweep` under a scratch `BROWSERAI_ROOT` **inside your own profile** *(corrected 2026-09-16 from `BrowserAI.exe --sweep`, which is the configuration app since 2026-09-15 and opens a window instead of sweeping)* -- since 2026-08-20 a root outside it is refused before the sweep runs, so `%LocalAppData%\BrowserAI-measure` and not anywhere in the repository -- then read the census line out of the process log. **Manual and must be**: a suite that asserted a wall-clock figure would be red on a busy machine, and the census is a report, not a budget | -- *manual* |
| 79 | **Detection matches the two provisioned binaries and nothing else on the machine** -- five `chrome-headless-shell.exe` processes from the retired `npx` setup, same vendor and same Chromium revision, running since 2026-08-15, produce **zero** candidates ([kb](windows/detection.md#the-sweep-measured-through-the-products-own-code-paths)) | The match loosens from a full image path to anything else -- a prefix on the wrong root, an image *name*, a `--user-data-dir` heuristic. **This is the property that stops BrowserAI closing a developer's browser** | Scan the whole machine and require every candidate's image path to be one of exactly two strings; plant a process at the same *shape* of path as the legacy tree and require it to be missed, so the check means something on a machine that never had one; and require the real legacy tree, matched by directory prefix, to be disjoint from the candidates | `StraySweepTests.DetectionMatchesOnlyTheBinariesBrowserAiProvisionedAndMissesTheLegacyTree` |
| 80 | **Registering a logon scheduled task non-elevated is denied on this machine** -- `schtasks /Create /XML` and the `Schedule.Service` COM API both answer `0x80070005`, for our definition and for a minimal one alike ([kb](windows/detection.md#the-logon-sweep-task)) | Nothing upstream: this is machine policy, and it is recorded because the stray sweep claimed the opposite. **Whether elevation fixes it is unverified** -- a UAC prompt cannot be answered from a non-interactive session | ✅ **Closed 2026-08-16 at the update lane: the task is dropped**, so this fact no longer gates anything and needs no re-check. The row is kept because it is the evidence for the decision. To re-establish anyway: register a minimal definition under a throwaway name and delete it | -- *manual, and no longer owed* |
| 81 | **Task Scheduler permits `LogonType` only beside a `UserId`** -- with a `GroupId`, `schtasks` refuses the file as *"an unexpected node"* ([kb](windows/detection.md#the-logon-sweep-task)) | The schema changes. It fails **loudly**, which is the good direction. **Corrected 2026-08-16 (previously "the setting it forces is the one whose absence makes a sweeper in session 0 report success forever, so it is asserted rather than reviewed")** -- with the task dropped there is no definition to assert on, and the session-0 property is now carried by `StraySweepTests`' R5 arm against BrowserAI's own process | Hand a definition carrying `<GroupId>` and `<LogonType>` to `schtasks /Create /XML` and require the refusal | -- *manual*; **previously `LogonSweepTaskTests.TheTaskRunsInTheUsersOwnSessionAndNotAsAService`, which was deleted with the feature** |
| 82 | **What MinVer 7.0.0 derives at three heights** -- `0.1.0` on the tag, `0.1.1-alpha.0.5` five commits later, `0.0.0-alpha.0.71` with no reachable tag ([kb](packaging/velopack.md#deriving-the-version-from-git-tags-with-minver)) | MinVer changes its default pre-release identifiers or its height rule; or the SDK changes what `Version` feeds. **The tagless row is the one that matters**: it is what a shallow clone produces, and the build refuses it -- a MinVer that stopped emitting `0.0.0` there would silently disarm the refusal and ship a build that does not know what it is | Re-measure all three. The tagged row needs only `dotnet msbuild -t:MinVer -getProperty:MinVerVersion`; the untagged row needs five `--allow-empty` commits on a throwaway branch; the tagless row needs `-p:MinVerTagPrefix=zzz`, which is the only arm that stays cheap once a tag exists | -- *manual*; the **consequence** is on every build, because `BuildVersionTests.TheVersionIsDerivedAndCarriesNoBuildMetadata` fails on a shape that is not three parts plus an optional suffix, and `RefuseAVersionDerivedFromNoTag` fails the build itself on `0.0.0` |
| 83 | **MinVer sets `AssemblyVersion` to `{Major}.0.0.0` by design**, so the number a caller reaches for first is `0.0.0.0` for the whole 0.x line ([kb](packaging/velopack.md#deriving-the-version-from-git-tags-with-minver)) | MinVer starts deriving all four parts, which would make the fact stop being true in the harmless direction -- the rule *nothing reads the assembly version* stays correct either way. **It was live in this product**: `SessionLock` stamped every `browserai.json` from `GetName().Version` until versions from git tags | Read `Assembly.GetName().Version` off the built artifact and compare it against the informational version, then scan `src/` for any read of the former. The scan is the half that matters: the measurement says the trap exists, the scan says nothing has walked into it | `BuildVersionTests.TheAssemblyVersionIsMajorOnlyWhichIsWhyNothingReadsIt` · `BuildVersionTests.NothingInTheProductReadsTheAssemblyVersion` · `BuildVersionTests.EverySessionRecordsTheVersionTheBuildWasDerivedAs` |
| 84 | **The SDK's `SourceRevisionId` decoration is gated on `SourceControlInformationFeatureSupported` as well as `IncludeSourceRevisionInInformationalVersion`**, and only `GetAssemblyAttributes` runs it ([kb](packaging/velopack.md#deriving-the-version-from-git-tags-with-minver)) | Any SDK bump changes either gate or the target's place in the graph. **It fails armed and not open**: adding SourceLink is enough to switch the feature on, and the repository would then depend entirely on the property that is set here -- which is why the property is set now, while nothing can provoke it | Force the feature on, supply a `SourceRevisionId`, and read `InformationalVersion` after **`GetAssemblyAttributes`** with the property false and then true. Asking after `MinVer` or `GetAssemblyVersion` returns an undecorated string in every arm and reads as proof of something it did not test | -- *manual*; the consequence is on every build, by `BuildVersionTests.TheVersionIsDerivedAndCarriesNoBuildMetadata` for the shipped attribute and `BuildVersionTests.TheSdkIsForbiddenFromDecoratingTheVersion` for the property |
| 85 | **The update lane's own numbers** -- full package **54,981,749 b**, N→N+1 delta **138,943 b**, shipped bytes **143,715,555**, compression **0.3826**, `Setup.exe` **59,490,421 b**. ✅ **RE-ESTABLISHED 2026-09-22** by two real packs at `1.0.1-reverify.3` and `1.0.1-reverify.4` into a scratch feed under the gitignored `Releases/`, both exit 0, no tag and nothing uploaded -- **clearing the `[STALE]` renewed on 2026-09-21, and the trigger was the one this row names.** *Corrected 2026-09-22 (previously "full package **54,926,688 b**, N→N+1 delta **138,515 b**, shipped bytes **143,503,406**, compression **0.3828**, `Setup.exe` **59,435,360 b**. `[STALE]` ⚠️ **OWED SINCE 2026-09-21 and deliberately not taken in that batch** ... every one of the five figures is derived from something that is no longer what was weighed").* **ONE INPUT MOVED FOR A REASON ANYBODY CHOSE**: `payload\mcp` 18,619,618 → **18,697,570** (`+77,952`) across 194 → 196 files, with `@playwright/mcp` 0.0.81 → 0.0.82. ⭐ **NODE DID NOT MOVE AT ALL** -- `payload\node` is 93,740,659 in both tables to the byte, because v24.21.0 is unchanged, and Node is the input this row's trigger sentence names first. Everything else is a rebuild: server exe `+63,488`, app exe `+1,024`, the three `.xml` `+61,826`, notices `+3,630`, `payload` other `+4,229`. ⭐ **THREE ARTIFACTS MOVED BY EXACTLY +55,061 B** -- the full `.nupkg`, `Setup.exe` and the portable zip -- which is one compression showing up three times and is recorded so a reader can check it instead of wondering. ⚠️ **THE DELTA IS A FLOOR AND NOT A COST, and this run makes that visible for the first time**: `.3` and `.4` pack the SAME source tree, nothing was edited between them, and their full packages still differ by **6 bytes** -- so 138,943 b is what Velopack charges for two packs that differ only by a NativeAOT rebuild of both binaries, not the cost of a change. The previous 138,515 was the same quantity, and this row's predicate note about *both* binaries is exactly that and stands unaltered. ⚠️ **THE ONE CLAUSE THAT IS STILL `[STALE]` AND STAYS SO**: *what Velopack costs the AOT binary*, 11,874,816 → 17,853,952 b -- a publish taken either side of adding the package, with Velopack now wired into three projects, so the before half needs a tree with the update lane removed, which is a different product. *Previously* ✅ **RE-ESTABLISHED 2026-09-17** by two real packs at `1.0.1-reverify.1` and `1.0.1-reverify.2` into a scratch feed, clearing a `[STALE]` debt owed since 2026-08-27 and renewed twice without being run. **Four inputs moved**: the two-binary split took 17,853,952 b of AOT binary to 29,614,080 across two, node grew, `payload\\mcp` shrank, and the XML trebled. ⚠️ **The delta's PREDICATE changed with it** -- 138,515 b is for a release in which **both** binaries changed, against 97,216 b for one. ⚠️ **One clause is still `[STALE]` and stays so**: *what Velopack costs the AOT binary*, 11,874,816 → 17,853,952 b, is a publish taken either side of adding the package, and Velopack is now wired into three projects -- the before half needs a tree with the update lane removed, which is a different product ([kb](packaging/velopack.md#the-update-lane-end-to-end-against-a-real-feed), [kb](packaging/velopack.md#nativeaot-hooks-and-vpk-output)) | Node moves (it is 93,740,659 b of 143,503,406), `@playwright/mcp` moves, or either AOT binary grows. **The delta WAS the row that matters and is now the row that records a road not taken** -- *narrowed 2026-09-22 (previously "**The delta is the row that matters**: it is the whole reason Velopack was chosen over a whole-package updater, and it degrades *silently* -- a release in which something large happens to change ships a full download and nothing says so")*. **Every release ships a full download by decision now** ([DECISIONS](../DECISIONS.md#locking-logging-versioning-and-registration)), so there is nothing left to degrade silently: `build/New-Release.ps1` passes `--delta None` and `ReleaseScriptTests` holds it. The number stays measured because it is what the decision costs -- 139 KB against 55 MB -- and because it is the one figure that would have to be re-read if the decision were ever revisited. The three download timers are deliberately **not** derived from these numbers, so a bump cannot invalidate them | `pwsh -File build/New-Release.ps1 -PackVersion <v> -OutputDir <feed> -AllowPreRelease` twice at two versions, then read the artifact sizes out of the feed. ⚠️ **A non-prerelease version is refused** at the release-body step unless `CHANGELOG.md` already carries its section, so the two exercise versions must carry a suffix. ⚠️ **Do NOT capture the script's returned object through a child `pwsh`** -- that gives you its stdout as text, not the object; run it in-process or measure the feed. **Manual and must be**: the suite may not run a publish, and it may certainly not run an installer -- `Setup.exe` renames a non-empty root aside and deletes it, and an install under the real pack id rewrites the real Add/Remove key | -- *manual*; the **decisions** are on every build, by `ReleaseScriptTests` |
| 86 | **`Directory.Delete(path, recursive: true)` makes partial progress** -- it deletes what it can and throws **one** exception naming **one** node, so it leaves the same nodes behind that a hand-rolled post-order walk does ([kb](windows/processes.md#files-durable-writes-and-deletes)) | A BCL change in either direction. It fails **quietly**: if the framework ever started aborting whole, `browserai_destroy` and the instance sweep would still be correct, because they do not use it -- what would go stale is the *justification* recorded in [`TreeDelete`](../src/BrowserAI.Core/Runtime/TreeDelete.cs), which is the only reason anyone would reopen the decision | Build a tree of a top-level file, a subdirectory holding a held-open file and a sibling, and a second subdirectory sorting after it; delete it recursively; list what survived. **The second subdirectory is the fixture** -- without it partial progress and none look identical | -- *manual*; the consequence is guarded on every run by `InstanceDirectoryTests.ADirectoryWithAFileSomethingHoldsIsEmptiedAroundItAndTheSurvivorIsNamed`, which asserts the per-node report the framework primitive cannot produce |

| 87 | **The MCP client's registration surface** -- `claude mcp add --scope user` writes `mcpServers.<name>` into `$CLAUDE_CONFIG_DIR\.claude.json`, needs **no elevation**, takes **613-645 ms**, and **is not idempotent**: a duplicate `add` and an absent `remove` both exit **1**, distinguishable only by the words *"already exists"* and *"No MCP server named"* ([kb](mcp/protocol.md#registering-browserai-with-the-client)) | Claude Code changes its wording, its exit codes or its config layout. **The wording is the fragile half and it fails loudly**: an unrecognised message reports a registration as *failed* in the log and in `mcp-registration.json`, never as done. The elevation and timing halves fail quietly, and a slow `add` eats a fast-exit hook's budget | `RegistrationTests.TheClientStillSaysWhatTheExitCodesCannot` and `RegistrationTests.TheRealClientRegistersBrowserAiAtUserScopeAndNothingElseIsTouched` run the real client against a **scratch `CLAUDE_CONFIG_DIR`** every time this suite has one. For the timings, run `add`/`remove` three times each and take the range | `RegistrationTests` -- 11 arms over a double and 3 against the real client, behind the suite's ClientCommandLine capability, so a run without a client **skips** and a release run **fails** |
| 88 | **`LoggerFactory.Dispose()` does not dispose a provider instance handed to `AddProvider`** -- 0 disposals, so `ProcessLog` had to close its writer explicitly ([kb](windows/processes.md#files-durable-writes-and-deletes)) | `Microsoft.Extensions.Logging` changes its ownership rule, which would make the explicit call redundant and not wrong. It failed **quietly** for as long as it existed: `Main` exits immediately afterwards, so nothing noticed until a short-lived caller opened a log and read it back | Plant a provider that counts its own disposals, create a factory over it, dispose the factory, read the count | `ProcessLogTests.DisposingTheProcessLogReleasesTheFileHandle`, which fails at the exclusive open if the explicit disposal is removed |
| 89 | **A real browser reaches MCP-ready in 0.9-1.2 s (Chromium) and 3.1-4.0 s (Firefox)** through the product's own job and launcher, four runs each. `Corrected 2026-08-18 (previously "against a **180 s** patience that the slowest run used 2.2% of")` -- **the times were not re-measured and have not moved**; the harness bound they sit under is now `TestDefaults.BrowserHang`, thirty minutes, so the slowest run uses 0.22% of it ([kb](playwright/provisioning-and-timings.md#timings-spawn-resume-idle-close-proxy-overhead)) | A browser revision, a Playwright bump, or a change to how the launcher waits. The ratio is the half that would move meaningfully; the absolute times are this machine's | Run the suite. `readyMilliseconds` is recorded beside the containment counts on **every** run including the passing ones, so the distribution stays reconstructable and not only the failures | `BrowserContainmentTests` |

| 90 | **A headless Chromium creates `Chrome_WidgetWin_1` -- the class a visible browser window uses -- without `WS_VISIBLE`, and never shows it**; across a full suite run headless Chromium and Firefox created 308 top-level windows and put **zero** on screen ([kb](windows/detection.md#what-a-suite-run-puts-on-the-screen)) | A Chromium revision or a Playwright bump starts realising a window before deciding it is headless, or `--headless=new` stops being window-free. It fails **on the developer**, not in the suite: a window appears over their work and steals their foreground, and nothing in a green run says so. The suite's own protection is the launch argument, not the browser -- every real launch here is headless, which is what this row exists to keep true. *(Corrected 2026-08-20, previously "the mode": session modes were deleted and headedness became a per-run argument.)* | Watch a full `dotnet test` with a `SetWinEventHook` on `EVENT_OBJECT_CREATE`/`EVENT_OBJECT_SHOW`/`EVENT_SYSTEM_FOREGROUND` plus an `EnumWindows` poll, both filtered to whole top-level windows, and require zero visible windows and zero foreground events from any image under the browsers root. **Key the detector on (handle, event), never on the handle alone** -- the first version deduplicated by handle and reported 308 creates and 0 shows | `FakeChildHarnessTests.NoRigOpensABrowserWindow` covers the half this project controls -- that no rig opens a window, the one starting a real browser included. *(Renamed and widened 2026-08-20 from `NoRigThatStartsARealBrowserOpensItWithAWindow`, which also asserted that the double-backed rig stayed HEADED, because `persistent` was the only mode permitting every tool. Every session now gets every capability, so that half had nothing left to protect and the window was pure cost.)* The browser's own behaviour is *manual*: a suite cannot assert on a window the developer's own machine may also be showing |

| 91 | **`McpClientOptions.InitializationTimeout` defaults to 60 s at 2.2.0** -- read by reflection off the shipped `ModelContextProtocol.Core.dll`, beside `DiscoverProbeTimeout` at 5 s ([kb](mcp/sdk.md#driving-the-whole-sdk-aot-passthrough-filters-and-cancellation)) | The SDK moves it in either direction. **It cannot break BrowserAI any more, and that is the row**: both sides now set it explicitly, so the recorded fact is about the *default a caller inherits by saying nothing* and not about anything shipped here. What a re-check protects is the reasoning -- ten minutes was chosen against a 60 s default and a ~300 ms warm handshake, and a default that moved to ten minutes would make the explicit set redundant while a default that moved to five seconds would make it load-bearing for a different reason. Left unset it failed **quietly and at scale**: 46 of the failures in one twenty-run session at unbounded parallelism, reported as `Initialization timed out` with no elapsed time, no child and no stderr | In PowerShell, `LoadFrom` the `ModelContextProtocol.Core.dll` under `~/.nuget/packages/modelcontextprotocol.core/<v>/lib/net10.0/`, `GetType('ModelContextProtocol.Client.McpClientOptions')`, `[Activator]::CreateInstance` it, and read `InitializationTimeout` off the instance. **Off the shipped assembly, not out of the source** -- what a caller inherits is what the binary initialises, and the two have disagreed before | `FakeChildHarnessTests.TheClientPinIsWhatSkipsTheDiscoverProbe` asserts the sibling `DiscoverProbeTimeout` default by exact equality; the initialization default itself is *manual*, because a test that waited for it to expire would take a minute of the suite's wall clock to assert a number |

| 92 | **The client's silent truncation is PER STRING, at 2,048 UTF-16 characters, cut at `> 2048`, with `... [truncated]` appended** -- no per-tool bucket, no total budget over `tools/list` (202 tools / 348,314 B through intact), and `inputSchema.properties[*].description` not truncated at all (20,000 characters through intact). Measured @ Claude Code 2.1.234 ([kb](mcp/protocol.md#what-2kb-each-means----measured-2026-08-18--claude-code-21234)) | A client release changes the unit, the number, or which surfaces it applies to. **Every half fails silently and none of it is observable from inside a server**: the cut happens after the JSON-RPC response has left, so a truncated `browserai_init` would look exactly like a healthy one from here. The dangerous direction is a new **per-tool** bucket, which today's surface would fail on `browserai_init` alone -- 3,360 B as the client sends it | Re-run the capture probe: point `ANTHROPIC_BASE_URL` at a local recorder, register a probe MCP server against a **scratch `CLAUDE_CONFIG_DIR`**, and diff the `tools` array in the captured `POST /v1/messages` against what the probe published. Full recipe in the kb entry | `ModelSurfaceTests.EveryModelFacingStringFitsTheClientsSilentTruncationBudget` gates every model-facing string against the measured predicate and **reports each tool's whole entry total unasserted**, which is the figure a per-tool bucket would be judged against. Nothing detects a client-side change -- only a re-run does |

| 93 | **`Marshal.GetLastPInvokeError()` is destroyed only by another capturing P/Invoke, not by managed work** -- the captured value survived a 5,000-append `StringBuilder`, a `GC.Collect()`, a `MemoryStream` round trip and a `Console.Out.Flush()`, and was zeroed by a single `File.Exists`; and the **11 of 43** declarations that omit `SetLastError = true` return a confident `0` instead of failing ([kb](windows/processes.md#the-win32-interop-surface)) | A BCL change to which framework calls P/Invoke with capture on the paths this product runs between a failing call and its error read. **It fails silently in the worst direction**: a clobbered error reports the wrong Win32 cause in a message a model then acts on, and a never-captured one reports `0`, which reads as success. The `SetLastError` half cannot move upstream -- it is ours -- but the audit only stays true while the surrounding code does | Call a deliberately failing import with `SetLastError = true` (`OpenProcess` on pid 4 returns `ERROR_ACCESS_DENIED`), then read `Marshal.GetLastPInvokeError()` after each candidate intervening statement in turn, and separately with the attribute omitted. **The control is the no-capture arm** -- without it a run that reads back `0` looks like clobbering and not like nothing having been captured | -- *manual*; nothing asserts the ordering rule at the call sites, which [`src/BrowserAI/Interop/CLAUDE.md`](../src/BrowserAI/Interop/CLAUDE.md) says in place |

| 94 | **`browser_annotate` realises a VISIBLE, foreground window on a `headless` session and then never returns of its own accord** -- three runs 2026-08-18: the window inside 1.2 s every time, and the call silent for the whole 90 s in the control arm. The window belongs to a **second, non-headless Chromium** under a detached, per-user-singleton dashboard daemon ([kb](playwright/tools-and-artifacts.md#what-browser_annotate-actually-does----measured-2026-08-18)) | Upstream stops launching the dashboard headed -- the headedness is `headless: !!process.env.PWTEST_DASHBOARD_APP_BIND_TITLE`, a test variable, so a single line moves it -- or gives the annotate wait a timeout, or drops the tool. **This row is the entire justification for the only refusal left in the product**, so a bump that fixed it would leave BrowserAI refusing a tool for a reason that had stopped being true; and the failure it protects against is the worst kind here, an unattended overnight run hanging with a window in front of nobody | Drive the resolved `cli.js` on the generated `headless` config, `browser_navigate` to a `data:` URL, then `browser_annotate` **under a hard timeout**, with the `SetWinEventHook` + `EnumWindows` watcher from row 90 running. Check `\\.\pipe\pw-*-dashboard-app` is absent first; put the tree in a kill-on-close job object; snapshot the process tree **while blocked**, because after the probe exits a `ParentProcessId` walk reaches none of it | -- *manual*, and it must be: the assertion is that a call **does not return**, so a suite arm would have to spend its budget waiting and would put a focus-stealing window on the developer's screen to do it. `SessionPolicyTests` covers the half this project controls -- that the tool is absent from `tools/list` in every mode and refused wherever it is named. *Corrected 2026-08-18 (previously "that the refusal exists and keys on `Headed`")* |

| 95 | **A session's cookie store decrypts with DPAPI alone**, from a second process as the same user: `os_crypt.app_bound_encrypted_key` **absent**, cookie scheme tag **`v10`** not `v20`, `CryptUnprotectData` returning a 32-byte key with no entropy, AES-256-GCM recovering the value ([kb](chromium/profiles.md#chromiums-cookie-store-and-what-it-takes-to-read-one----measured-2026-08-18)) | A Chromium revision enables App-Bound Encryption for a zip-provisioned Chrome for Testing under a custom `--user-data-dir`, or Playwright starts provisioning a branded build. **This row is the argument that deleted BrowserAI's whole `(tool, mode)` permission layer** -- *the profile is inside the directory the agent chose and DPAPI decrypts for it, so the matrix was never a boundary against the caller*. If it stopped being true the removal would rest on an overstatement, and nothing anywhere would notice: every test in the suite would stay green | Create a `headless` session, set a persistent cookie from a **loopback** server, `browser_close`, then from a separate process read `os_crypt` out of `Local State`, `CryptUnprotectData` the `DPAPI`-prefixed key, and AES-GCM one row of `Default\Network\Cookies` read **against a copy**. Check `app_bound_encrypted_key` and the scheme tag **first** -- without them a failed decrypt reads as a broken script and not as ABE. **Only ever against a session under `.work/`** | -- *manual*, and deliberately: automating it would mean the suite carrying a working cookie-decryption routine, which is a capability this repository should not ship to make a point about one it already has |

| 96 | **A screenshot the caller did not name comes back as a file AND as an `image` block, and the block costs `⌈w/28⌉ × ⌈h/28⌉` visual tokens and not anything proportional to its bytes** -- 1,196 for a 1280×720 viewport, whether the PNG weighs 4,417 B or 52,648 B. Upstream's guard is `if (!params.filename)` and its only `registerImageResult` call site is `browser_take_screenshot` ([kb](playwright/tools-and-artifacts.md#the-inline-screenshot-and-what-it-costs----measured-2026-08-18)) | Upstream moves the guard, registers an image for a second tool, changes the media type it derives, or starts honouring a config key BrowserAI does not write; or the client's per-patch billing changes. **It failed silently for the life of artifact routing** -- the guard was never true here because this build always supplies a `filename`, so no screenshot came back inline in any mode while bare `@playwright/mcp` returned one, and nothing anywhere reported the difference | Grep the resolved bundle for `registerImageResult` and read its one call site. Then, off the wire against the published binary, take a screenshot with **no `filename`** and compare the block's decoded base64 against the file at the path in BrowserAI's own note -- byte for byte, not by length | `VerticalSliceTests.AScreenshotComesBackInlineAsWellAsAsAFileWithALegibleName` against the real child ⚠️ **2026-08-26: the second arm is gone** *(previously "`ArtifactRoutingTests.AScreenshotTheCallerDidNotNameCarriesTheImageUpstreamWouldHaveSent`, for the guard's other side and the tools that get no image")*, and so is the defect it watched: BrowserAI stopped supplying a filename argument, so upstream's own guard is the whole mechanism again and there is no restoration to assert. The real-child arm is what remains and is the one that was always measuring the wire |

| 97 | **A mapped network drive letter costs the full network stall -- 22,210 ms for one `File.Exists` through `T:` against a dead hostname -- while `GetDriveTypeW` answers `DRIVE_REMOTE` for it in 0.9 ms and `QueryDosDeviceW` in 0.0103-0.0212 ms, neither touching the network** ([kb](windows/detection.md#a-mapped-drive-letter-is-a-network-path-and-costs-the-same-22-seconds)) | .NET or SMB changes the timeout, in either direction -- or, far worse, `GetDriveTypeW` starts round-tripping to the redirector. **The ordering of `CanonicalPath` rests entirely on the second half**: the network question is answered before any filesystem call precisely because these two calls read the object manager, and a `GetDriveTypeW` that blocked would put the 22 seconds *inside the guard that exists to prevent it*, on the startup path, for every caller. It would fail **silently** -- the refusal would still be correct, just twenty-two seconds late | Define a raw DOS device against `\Device\LanmanRedirector\;X:0000000000012345\<a hostname that does not resolve>\share`, then time `File.Exists` through the letter and `GetDriveTypeW`/`QueryDosDeviceW` on it. **Use a hostname, never an unroutable address** -- the address case fails at whatever layer answers first and measured 21,037 ms on 2026-08-14 and 12.8 ms on 2026-08-19 on the same machine. **And measure once**: negative DNS caching brought the second lookup back in 17.7 ms | `CanonicalPathTests.TheNetworkRefusalDoesNotComeFromTheCallThatOpensThings` builds a real redirector alias and proves the refusal came from the object-manager branch, because the opening call returns null there. **The ORDERING is not automated and cannot honestly be**: elapsed time is the only witness to it, and no hang detector with headroom a 419-test run cannot reach is also tight enough to catch one 22-second stall. The 22-second figure itself is *manual* and this row is how it gets re-asked |
| 98 | **`Path.GetFullPath` expands 8.3 short names on .NET 10** -- in full for an existing path, and prefix-only-with-the-tail-preserved for one that does not exist yet -- while leaving `\\?\`, `\\.\`, junctions, `subst` and mapped drives untouched ([kb](windows/detection.md#a-mapped-drive-letter-is-a-network-path-and-costs-the-same-22-seconds)) | A BCL change to `PathHelper.Normalize`'s `~` expansion. **It fails in the safe direction and still needs re-checking**: if expansion stopped, an 8.3 spelling would reach `CanonicalPath`, whose final-name test resolves it anyway -- so the product stays correct and one *test* branch becomes a lie, because the arm that asserts the equality is asserting `Path.GetFullPath`'s work and not the product's. This row also **corrects [review A4](../docs/reviews/2026-08-18-adversarial-locking.md)**, which lists 8.3 among the aliases `GetFullPath` does not resolve | `GetShortPathNameW` on a directory whose name contains a space, then `Path.GetFullPath` on the result -- and again with `\not\created\yet` appended, which is the half that matters for `init` | `CanonicalPathTests.An83SpellingNeverReachesASecondIdentityOnEitherKindOfVolume`. **It has two branches, because 8.3 generation is per-volume** -- measured 2026-08-19: this machine's system volume shortens, its other three do not, and neither did the volume the GitHub Windows runner checked out onto. On a volume with short names it asserts the equality above; on one without, it asserts the long path came back unchanged and then asserts the backstop that would catch a short name if one ever arrived unexpanded. Neither branch is a skip, and which one ran is printed. **So a run only re-verifies this row on the branch its volume selects** -- the equality half needs 8.3 generation on, the backstop half needs it off. *Corrected 2026-08-20 (previously "So a green CI run does not re-verify this row -- only a run on a volume with 8.3 generation on does"): CI was removed that day, so the sentence named an environment that no longer exists while leaving the converse gap unsaid.* Routine runs now happen on this machine's system volume, which shortens, so it is the **backstop** branch that nothing exercises unless somebody runs the suite from one of the other three |
| 99 | **The advertised tool surface does not depend on `browserName`** -- `chromium` and `firefox` children of the same payload answered identical `tools/list` responses at both of BrowserAI's capability sets: 42 and 59 tools, same names, same order, byte-identical schemas ([kb](playwright/tools-and-artifacts.md#does-the-surface-differ-by-browser-family----measured-2026-08-19)) | Upstream gives `filteredTools` a browser predicate, or gates a tool on the family the way it already gates on capability. **It would fail silently and in the direction nothing here can see:** BrowserAI builds its one static tool list from a single Chromium-configured surface child, and the MCP spec forbids the set varying per connection -- so a family-gated tool would be advertised to Firefox sessions whose child does not have it, and the failure would surface as a `tools/call` error a model reads as its own mistake. Every tool-surface count in this repository is a claim about both families and would quietly become a claim about one | Give [`build/upstream-snapshots.mjs`](../build/upstream-snapshots.mjs)'s `session()` helper a config with `browser.browserName: "firefox"` and `firefoxUserPrefs` in place of `channel` -- upstream's `validateBrowserConfig` drops a channel for a non-chromium family -- and diff its `tools/list` against the one that helper already takes. No browser is launched to answer `tools/list`, so this needs the payload and not a provisioned Firefox | -- *manual*. The golden snapshot is generated from one browser-less configuration and cannot see this axis at all; adding a second family to it would double every snapshot for a difference that is measured to be zero, which is a cost to pay when something moves and not now |
| 100 | **One `install-browser ffmpeg` downloads BOTH shared components and re-downloads whichever is missing** -- measured twice on 2026-08-19: into an empty root it produced `ffmpeg-1011` and `winldd-1007`, each with its own `INSTALLATION_COMPLETE`; re-run with only `winldd-1007` deleted it fetched `winldd` alone and left the complete `ffmpeg` untouched ([kb](playwright/provisioning-and-timings.md#one-install-browser-ffmpeg-rebuilds-both-shared-components----2026-08-19)) | Upstream regroups the two -- gives `winldd` its own top-level install, makes it conditional on something, or stops pulling it in with `ffmpeg`. This is the grouping `browserai_reinstall_browser`'s `shared` target is built on: it passes ONE name to rebuild TWO trees | Run `install-browser ffmpeg --no-shell --no-progress` into an empty `PLAYWRIGHT_BROWSERS_PATH`, then delete `winldd-<rev>` and run it again. Both runs must leave both markers | `ReinstallBrowserTests.TheSharedTargetRemovesAndRebuildsEveryComponentBothFamiliesUse` · **and the product itself, which is the stronger half**: the rebuild checks each component's own marker after the run and not the installer's exit code, so a component that stopped arriving is a reported failure and not a tree marked complete. The suite arm runs against a double and proves the wiring; the grouping itself is what this row re-asks |

| 101 | **Every `registry.install()` serialises on one `proper-lockfile` directory lock at `<PLAYWRIGHT_BROWSERS_PATH>\__dirlock`, taken before any executable and held for the whole install** -- chromium and firefox started 8 ms apart into one empty root produced four complete trees; a held lock stopped an installer dead for 30 s and it gave up at **470 s** with `ELOCKED` having written nothing; an abandoned lock was reclaimed as stale in ~3 s ([kb](playwright/provisioning-and-timings.md#two-installers-cannot-extract-into-one-root----upstreams-__dirlock----2026-08-19)) | Upstream drops the lockfile, moves it inside the per-executable loop, or scopes it to something narrower than the registry root. **This row is the whole reason `ReinstallSharedAsync`'s remarks no longer call a two-family race reachable** -- BrowserAI's provisioning mutex is keyed on the *family*, so it allows the pair and relies on upstream to serialise it. If upstream stopped, two extractions would meet in the shared `ffmpeg` and `winldd` trees and **both would write `INSTALLATION_COMPLETE` over the result**, which is the one failure the marker cannot report. The second half -- the 470 s budget -- moves with the retry constants and decides whether a concurrent install *waits* or *fails* | `mkdir <root>\__dirlock`, keep touching it faster than `proper-lockfile`'s 10 s staleness window, and run `install-browser ffmpeg --no-shell --no-progress` against that root: the root must stay empty. **The refresh is the control** -- stop refreshing and the installer proceeds in ~10 s, which is the abandoned-lock arm. For the budget, hold it and wait for the exit | `PayloadTests.UpstreamStillSerialisesEveryInstallOnOneLockOverTheWholeBrowsersRoot` reads the four anchors out of the **assembled** bundle -- `async install(executablesToInstall`, `"__dirlock"`, `await lock(registryDirectory` and `executable._install(` -- and asserts their order, so a lock that moved inside the loop is a red build. **The 470 s budget is *manual*** and must be: an arm asserting it would spend eight minutes of the suite's wall clock proving a number nothing here is sized against |

| 102 | **Upstream's `--sandbox` stops working by omission and starts setting `chromiumSandbox` explicitly** -- [microsoft/playwright#42288](https://github.com/microsoft/playwright/pull/42288) deletes `decorateMCPCommand`'s normaliser (`options.sandbox = options.sandbox === true ? undefined : false`), merged 2026-08-17, closing [playwright-mcp#1716](https://github.com/microsoft/playwright-mcp/issues/1716) as `COMPLETED`. **ARRIVED 2026-09-14** -- *corrected, previously "**Not in a resolved version yet**: `@playwright/mcp` 0.0.79 pins `playwright-core` 1.63.0-alpha-2026-08-05, both older than the merge, re-confirmed 2026-08-19"*: `@playwright/mcp` 0.0.80 pins `playwright-core` 1.63.0-alpha-2026-08-31, the normaliser is gone from the shipped bundle, and the predicted outcome held in both halves -- the config key started working and BrowserAI's own posture did not move, because it passes `--sandbox`. Adjudicated on the 0.0.79 -> 0.0.80 review; row 3 carries the measurement ([kb](playwright/configuration.md#silent-config-failures)) | The bump that carries it. **This is the row for a fact that is already known to be about to change, which is the case a golden snapshot is blindest to:** the tool surface and the config schema both hold still while the behaviour behind one key inverts, so `UpstreamSnapshotTests` cannot see it and nothing else would ask. BrowserAI is sandboxed on both sides -- it passes `--sandbox` on the command line -- so what re-verification is for here is confirming that the *mechanism* changed the way the PR says and that `--sandbox` still reaches the browser, instead of discovering a regression | Resolve `@playwright/mcp` and read `playwright-core/lib/coreBundle.js` for the four lines of the normaliser: their absence is the fix landed. Then re-run the three arms recorded in the kb entry from a live browser's resolved command line -- nothing set, the config key alone, and `--sandbox` -- and check that the second one has become *effective* while the third is unchanged | `SandboxFlagTests`, which asserts the sandbox flag reaches the browser and every node child, so the arm BrowserAI depends on is red if it stops. **The config-file arm is not covered by a product test and cannot be** -- nothing in this product sets that key, and a test that set it would be testing upstream and not BrowserAI |

| 103 | **A live Chromium AND a live Firefox each refuse BOTH renames of their own tree, and the same renames succeed the moment the browser is gone** -- `chrome-win64` with a sharing violation, `chromium-1237` with `ERROR_ACCESS_DENIED`, measured 2026-08-19 against Chromium 152.0.7977.8 running as ten processes with its own cwd elsewhere, and the identical pair against Firefox 153.0 from `firefox-1539` (`firefox` and `firefox-1539`, same two errors, two runs). **The browsers root itself is refused under both families and the two shared trees, `ffmpeg-1011` and `winldd-1007`, rename freely under both** ([kb](windows/processes.md#the-same-measurement-for-firefox-and-for-what-both-families-share----2026-08-19)) | A Chromium revision that opens its tree differently, or a Playwright layout change that moves `chrome.exe` up or down a level. **This row is what stops the 2026-08-18 rename measurement being over-applied**: that one says Windows permits renaming a running image's parent and grandparent, which is true and is not true of this browser -- so a future reader reaching for *download alongside and swap* has both halves in front of them and not the general rule alone | Start the provisioned browser headless with a scratch profile and its working directory outside the browsers root, attempt `Directory.Move` on the directory holding the executable and then on the revision directory above it, then kill it and attempt both again. **The control arm is the row**: two refusals with no control cannot tell the browser's hold from an ambient one. **Firefox needs a different liveness check** -- Playwright's build brings no Remote Agent up, so there is no `/json/version` to ask and the signal is the content-process tree instead. The asymmetric layouts: `chromium-<rev>\chrome-win64\` against `firefox-<rev>\firefox\` | **Manual.** Nothing in the suite renames a provisioned browser tree, and nothing should: a test that renamed the shared browsers root under a parallel run would break every other test on the machine instead of failing its own assertion |

| 104 | **`@playwright/mcp` emits no progress notifications at all** -- all four occurrences of `notifications/progress` in the shipped payload are the MCP SDK's own schema and capability arms, `sendNotification` appears once as a capability handed to a tool handler and is never called, and every `progressToken` use is client-side plumbing. Measured 2026-08-19 at `@playwright/mcp` 0.0.79 / `playwright-core` 1.63.0-alpha-2026-08-05, with a positive control ([kb](mcp/sdk.md#lossless-passthrough-cancellation-notifications-and-error-frames)) | Any upstream tool that starts reporting progress -- a download, a long navigation, a trace export. **It arrives in one bump with no schema change and nothing the golden snapshot can see**, and it is the event that makes a known SDK defect reachable: inbound notifications are dispatched fire-and-forget, so a relayed pair was observed arriving 2 then 1. While this row holds, the `IClientTransport` decorator that would fix that would be a component built for a notification nobody sends | Search the resolved payload tree for `notifications/progress` and for `sendNotification` call sites outside the SDK's own schema block. **The control is half the check**: confirm the same search finds `sendNotification` and `tools/list`, or a zero cannot be told from a failed search | **Manual.** The suite's own double can emit progress and does, which is what proves the relay is lossless -- so no product test can tell you whether the real child ever does |

| 105 | **Nothing under the browsers root grows while a browser is downloading, and everything does while it extracts** -- the archive goes to `os.tmpdir()\playwright-download-XXXXXX\`, the revision directory appears only at the unzip, and every one of 41 chromium and 27 firefox samples taken 250 ms apart differed from the one before it ([kb](playwright/provisioning-and-timings.md#what-grows-on-disk-while-an-install-runs-and-when----2026-08-19)) | Upstream downloads straight into the registry directory, or stops honouring `TEMP` for its temp directory, or `extractZip` starts streaming instead of writing whole files. **This row is the entire basis of `ProvisioningTimers.StallCap`**, which replaced a 45-minute total cap: the stall detector weighs the browsers root once a second and needs the download to be inside it, which is true only because BrowserAI sets `TEMP` and `TMP` for the installer child. If upstream stopped reading `TEMP`, the archive would land in the user's own temp again and **every install on a slow link would be killed as a stall** -- silently, because the sentence would still read as a cap firing | Sample `PLAYWRIGHT_BROWSERS_PATH` and the temp directory recursively every 250 ms across `install-browser <family> --no-shell --no-progress` into an empty root, once with `TEMP` redirected and once without. **The control is that pair** -- one run cannot tell "the redirect works" from "it landed here anyway". Check the archive figure against the CDN `content-length` already recorded, and expect the root total to differ by the length of `.links`, which is path-dependent | `ProvisioningTests.ASlowInstallThatKeepsWritingIsNotStoppedHoweverLongItTakes` holds the half this product controls -- that a run which keeps writing outlives a cap many times shorter than its own total -- and `.TheStallCapStopsADownloadThatNeverProgresses` holds the other. **Where the bytes come from is *manual***: both arms drive a double, so no test in the suite can tell you that upstream still puts them where this row says |

| 106 | **`--storage-state` together with `--user-data-dir` is a silent no-op** -- exit 0, empty stderr, no state applied. `storageState` is the **only** one of `BrowserNewContextParams`' 32 keys absent from `BrowserTypeLaunchPersistentContextParams`' 49; `tObject` iterates the *declared* schema and drops undeclared keys without error; `createPersistentBrowser` spreads `...contextOptions` straight through, so it looks accepted at every visible layer ([kb](playwright/configuration.md#silent-config-failures)) | Upstream adds `storageState` to the persistent schema, or starts rejecting undeclared keys, or begins seeding a persistent profile from a state file. **BrowserAI does not depend on this and that is exactly why the row exists**: nothing here would go red either way, and the next person reaching for `--storage-state` to seed a signed-in session will watch it appear to work. A change in the *fixing* direction is the one that would go unnoticed longest, because the symptom of the bug is silence and so is the symptom of the fix | Count the members of the two `tObject({...})` blocks in `playwright-core/lib/coreBundle.js` and diff them both ways, then read `tObject`'s own body for whether it walks the argument's keys. **The control is the reverse difference**: `storageState` alone one way and 18 launch keys the other means the blocks were found; empty both ways means they were not | -- *manual*, and it must be: nothing in this product writes `storageState` or would notice if it started working, so a test asserting the no-op would be a test of upstream with no bearing on anything BrowserAI does |

| 107 | **`--caps` accepts any word at all** -- `--caps bogus` exits 0 with no diagnostic, because the option is parsed by `commaSeparatedList` (`value.split(",").map(v => v.trim())`, and nothing else) where `--codegen`, `--console-level` and `--image-responses` all use `enumParser`. It is also why `--caps storage` works although the help documents only `vision, pdf, devtools` ([kb](playwright/configuration.md#silent-config-failures)) | Upstream gives `--caps` an enum parser, which would turn a typo from silence into an error -- good, and it would also make any capability name this repository has written down load-bearing at the CLI. **BrowserAI passes no `--caps` at all** (it would *replace* the config's capability list instead of merging with it), so nothing here breaks either way; what the row protects is the reasoning in [`upstream-snapshots/cli-help.txt`](../upstream-snapshots/cli-help.txt), whose documented value list is measured **not** to be the accepted set | Read the `--caps` declaration in the bundle's `decorateMCPCommand` chain and compare its third argument with `--codegen`'s, then read `commaSeparatedList`'s body. **The control is `--codegen bogus`**, which must fail -- without it, a `--caps bogus` that exits 0 cannot be told from a probe that never reached the parser | `UpstreamSnapshotTests` diffs `cli-help.txt` against the resolved payload, so the *help string* moving is a red build. **That the accepted set is wider than the help is *manual***: no snapshot can see a validation that does not happen |

| 108 | **Two browsers on one profile directory: Chromium refuses in 5,036 ms naming the cause, Firefox hangs for 180,402 ms with an error that never mentions the profile.** Upstream's `isProfileLocked` probes `<userDataDir>\lockfile` -- **Chromium's** lock file name -- while Firefox uses `parent.lock`, so the guard never fires and Firefox's own lock blocks the juggler handshake until the launch timeout. The 5,036 ms is `isProfileLocked5Times`' own five 1-second retries succeeding ([kb](windows/detection.md#process-image-path----the-fully-documented-detection-path)) | Upstream teaching `isProfileLocked` Firefox's name, or changing either lock's semantics. **Newly reachable because this product now offers Firefox** -- [row 22](#) covers the Chromium half alone. **BrowserAI's own path is already covered**, by `Runtime/ChildLaunch.Create`'s `parent.lock` preflight, which runs before the config is written and before anything spawns; so what a fix upstream would change here is nothing, and what this row protects is the *knowledge* that a three-minute silence is a held profile, not a dead network | Launch the provisioned browser twice against one profile directory, one family at a time, under a hard timeout, and time both. **The control is the Chromium arm** -- a Firefox launch that takes three minutes cannot be told from a slow machine without it. Read `isProfileLocked` out of the resolved `coreBundle.js` for the file name it probes | `FirefoxTests` drives the product's own preflight against a held `parent.lock` and asserts it refuses naming the holder. **Upstream's behaviour is *manual*, and must be**: an arm asserting that a launch does *not* return would spend three minutes of the suite's wall clock to prove a number nothing here depends on |

| 109 | **`browser.contextOptions.userAgent` sets the user agent on both families through the config alone; `navigator.webdriver` is `false` on Chromium and `true` on Firefox, and no config-reachable pref turns it off** -- `dom.webdriver.enabled: false` left it `true` while `general.useragent.override` through the same `firefoxUserPrefs` object took effect, which is the control ([kb](chromium/fingerprinting.md#the-user-agent-and-navigatorwebdriver-through-the-config-alone----measured-2026-08-19)) | Upstream dropping `userAgent` from the persistent-context schema -- which is exactly what already happened to `storageState` (row 106) -- or a Firefox build in which the pref starts working, or a Chromium revision that stops distinguishing the headless UA. **Nothing in this product depends on any of it yet**, and that is why it needs a row and not a test: this is research taken for a decision the maintainer has not made, so nothing would go red and the measurement would simply be remembered wrongly | Drive the payload's `cli.js` with a config per arm and evaluate `JSON.stringify({ ua: navigator.userAgent, webdriver: navigator.webdriver })`. **Keep the `general.useragent.override` arm** -- without a pref known to work, a `dom.webdriver.enabled` that changes nothing cannot be told from a prefs channel that was never wired up | -- *manual*, and it must stay manual until something is decided: a test would have to assert a value this product does not set, which is a test of upstream wearing the suite's colours |
| 110 | **A `Global\` object is creatable by a non-elevated interactive token with no `SeCreateGlobalPrivilege`, and the DACL the kernel gives it names LOCAL SYSTEM, the creating logon session and the creating user -- no group ACE at all**; a token holding no ACE is refused at `new Mutex(false, name, out created)` with `UnauthorizedAccessException`, which is `MachineMutex.Create`'s own documented arm ([kb](windows/detection.md#two-users-and-one-install-root----what-spans-users-and-what-does-not----measured-2026-08-20)) | A Windows release changing the token default DACL, or a domain policy that grants or removes `SeCreateGlobalPrivilege`. **It decides a question the maintainer has not answered yet** -- whether BrowserAI should refuse a shared install root at startup -- so what a wrong memory of it would produce is a design decision taken on a false premise, which is why it has a row while nothing in the product depends on it | Create a `Global\` mutex and dump its SDDL off the handle; then create a second one whose DACL grants only `SY` and try to open that name again. **Keep the `Everyone`-ACE control** -- an SDDL dump showing no group ACE is indistinguishable from a dump that cannot show one, and the control is what separates them. Re-read `whoami /priv` in the same session, because the absence of the privilege is half the finding | -- *manual*, and it must stay manual: a test would need a second user account or a second logon session, and [the article says plainly](windows/detection.md#what-could-not-be-measured-and-why) that neither could be produced on this machine |
| 111 | **The pre-gate probe costs 0.035 ms free and 0.049 ms held, against 0.6-2.3 ms for the `SizeOnDisk` walk `browserai_list` already performs per entry** -- so the liveness report added to that listing on 2026-08-20 is about a seventeenth of a cost the loop was already paying, and a drive-root listing cannot become pathological on its account ([kb](windows/detection.md#the-pre-gate-probe-as-a-liveness-report----measured-2026-08-20)) | A .NET release changing `FileStream`'s open path or the cost of raising `IOException`, since the held arm is a managed exception, not a return. **What it protects is a sizing argument, not a behaviour**: if the probe ever became comparable to the walk, `browserai_list` would need a cap it does not have today | Time the documented open in a loop against a real `browserai.json`, with and without a second handle holding it `FileShare.Read`, and **assert the held arm really is a sharing violation** (`HResult & 0xFFFF` is 32 or 33) before believing its number -- an open that quietly succeeded would otherwise be timed under the wrong name | -- *manual*. `SessionListTests.TheProbeReadsOnlyASharingViolationAsHeldAndNeverAnAbsenceAsFree` holds the probe's **answers** on every run; the **cost** is deliberately not asserted, because a wall-clock budget in the suite is the shape [TESTING.md](../TESTING.md#every-duration-is-a-hang-detector-or-it-is-a-defect) refuses |

| 112 | **`contextOptions.permissions: ["clipboard-read"]` is fatal to Firefox and fine on Chromium** -- a Firefox context created with it fails at `async initializeServer` with `Unknown permission: clipboard-read` and the browser exits, so a config that names it for both families makes **every** Firefox session unusable on the first browser call ([kb](playwright/configuration.md#silent-config-failures)) | Firefox learning the permission, or Chromium forgetting it, or upstream filtering the list per family on the way through -- the last of which would make this row silently stop mattering while the code that avoids it stayed correct. **BrowserAI writes the key for Chromium only**, the same shape `channel` has, so a change in either direction reaches this product as a working session and not a broken one; what the row protects is the reason the key is family-scoped, which is invisible in the config generator and would read as caution | Generate a session config with `permissions` set, once per family, and drive one `browser_navigate` through the published binary. **The control is the Chromium arm** -- a Firefox launch that fails cannot be told from a Firefox broken for some other reason without an identical Chromium launch that succeeds | `ConfigRoundTripTests` reads the key back out of a live Chromium and `RequiredSessionOpinions` requires it for Chromium only, so its *presence* is a red build; `FirefoxSessionTests` drives a real Firefox front to back, so its *absence* is one too. **That Firefox still rejects it is manual**, and must be: an arm asserting it would be a test whose success is a browser failing to start |
| 113 | **`redactSecrets` is a substring `replaceAll` on the secret VALUE, over the whole serialised response, so it both over- and under-fires** -- measured 2026-08-20 against the bundled child with `secrets` set, a secret whose value was `chromium` turned `"browserName": "chromium"` into `"browserName": "<secret>COMMON</secret>"` and `chromiumSandbox` into `"<secret>COMMON</secret>Sandbox"` ([kb](playwright/tools-and-artifacts.md#tools-that-reach-credentials)) | Upstream replacing the match with something structural -- per-field, boundary-aware, or applied before serialisation and not after. **It is the whole reason `secrets` is not recommended here**, and the row exists because the opposite claim survived a week: this repository recorded that `browser_get_config` *does not redact*, which was a reading of the handler, not a measurement of the response. A future reader meeting only "it redacts" would be one short step from treating it as a boundary | Start `cli.js` with a config carrying `secrets: {"MY_TOKEN": "sk-live-...", "COMMON": "chromium"}` and call `browser_get_config`. Require `<secret>MY_TOKEN</secret>` in place of the value, the literal value absent from the whole frame, **and** the `chromium` collateral present -- the third is the control, and without it a redaction that had quietly become a no-op for common values would read as a pass | -- *manual*. It cannot be a suite arm without BrowserAI writing the key it exists not to write, and a double asserting our own `replaceAll` would prove nothing about upstream's |
| 114 | **`CreateNoWindow` is the only thing that suppresses a child's console window; redirecting the streams does not, and the omission is invisible from a parent that has a console of its own** -- measured 2026-08-23, Windows 11 Pro 26200 / .NET 10: from a windowless parent the same launch put **2** visible top-level windows on screen without the flag and **0** with it, the visible one being `CASCADIA_HOSTING_WINDOW_CLASS` and not `ConsoleWindowClass` ([kb](windows/processes.md#redirecting-a-childs-streams-does-not-suppress-its-console-window----measured-2026-08-23)) | Windows changing its default console host again. The class name is the half that floats: the figures rest on Windows Terminal being what a console allocation produces, and the previous host produced a different class, so a sweep written against either one answers zero on the other. The **flag's** behaviour is documented and stable; what a window looks like from outside is not | From a parent that has called `FreeConsole`, start a console-subsystem child twice -- flag set, flag unset -- with both output streams redirected on each arm, and **diff `EnumWindows` over visible top-level windows** around each launch. Read the class names back instead of filtering for one; a filter is what makes this measurement answer zero on both arms and look like a clean result | `HouseRuleTests.EveryProcessLaunchInTheTreeSuppressesTheConsoleWindow` holds the **rule** on every run -- every launch site in the tree names the flag -- and deliberately not the **measurement**: reproducing it needs a real window station and a parent detached from its console, which is not a shape the suite can take without leaving windows on a developer's screen to prove that it does |
| 116 | **Two toolchain refusals stand between `LockFileEx` and a declaration for it**, and each looks like a defect in the code and not a rule of the toolchain: `LibraryImport` will not marshal `System.Threading.NativeOverlapped` without `DisableRuntimeMarshallingAttribute` on the whole assembly (`SYSLIB1051`), and CsWin32 will not generate `OVERLAPPED` at all (`PInvoke003: This API will not be generated. Use System.Threading.NativeOverlapped instead`) ([kb](windows/processes.md#locking-a-log-file-without-locking-its-readers-out----2026-08-24)) | An SDK that teaches the source generator to marshal the framework struct, or a CsWin32 that stops refusing the name. **Neither would break anything** -- the hand-written `NativeFile.Overlapped` stays correct either way -- which is exactly why this is a row and not a build failure: the *reason* the struct is hand-written and the *reason* its oracle is the framework and not the metadata would both quietly stop being true, and the next reader would find two unexplained deviations from this directory's own rules. | Add `OVERLAPPED` to [`NativeMethods.txt`](../tests/BrowserAI.Tests/NativeMethods.txt) and build the test project; then change `NativeFile.LockFileEx`'s parameter to `ref NativeOverlapped` and build the product. Two build errors is the recorded state; **either one disappearing is the fact moving**, and the control is that the other still fires | -- *manual*. `InteropLayoutTests` holds what depends on it -- that the hand-written struct is 32 bytes with `Offset` at 16 -- on every run, and deliberately not the refusals themselves: a test asserting a compiler diagnostic would have to compile code that does not compile. |
| 115 | **`STARTF_USESHOWWINDOW` with `SW_SHOWNOACTIVATE` is what keeps a headed Chromium off the foreground, and Firefox does not take it with the flag or without** -- measured 2026-08-24, one discriminating trial for Chromium (`chromium-1237`: no steal with the flag, steal without) and both arms for Firefox (`firefox-1539`: no steal either way, with a visible top-level window of its own as the positive control) ([kb](windows/processes.md#sw_shownoactivate-keeps-a-headed-chromium-off-the-foreground-and-firefox-never-takes-it----measured-2026-08-24)) | A browser revision, or a Playwright bump that moves the build. `wShowWindow` governs only the **first** `ShowWindow(SW_SHOWDEFAULT)`, so a Chromium that called `ShowWindow` itself afterwards would defeat the flag exactly the way Unity did on the StationeersPlus rig -- which is the failure mode this row exists for, and it would arrive as a browser bump and not as a change here. It fails **silently and only on somebody else's screen**: a headed session that stole the foreground would be invisible to every test in this suite | Read `SPI_GETFOREGROUNDLOCKTIMEOUT` first and record it, then put a window owned by an **ancestor of the launching process** in the foreground and confirm it with `GetForegroundWindow` + `GetWindowThreadProcessId`; launch the browser twice through `CreateProcessW` with the flag pair set and then unset, polling the foreground every 250 ms, and keep a visible-window control on **every** arm. ⚠️ **The condition is the whole measurement, not a detail of it**: [this machine's foreground lock is effectively infinite](windows/detection.md#this-machines-foreground-lock-is-effectively-infinite-so-it-cannot-see-a-focus-steal----measured-2026-08-24), so a run with an unrelated window in the foreground answers *no steal* on both arms and reads as a clean negative | -- *manual*, and it must stay manual: reproducing it needs a **headed** browser on a real desktop with the foreground arranged, and a suite arm that did that would put a browser window over whoever is at the machine -- which is the thing [the windowless rig](windows/detection.md#what-a-suite-run-puts-on-the-screen) was written to stop |
| 117 | **`--treenode-filter` treats the alternation character as an OR *inside one path segment* and never between path patterns**, so six class patterns joined with it select the whole assembly in one arrangement and the first class only in another -- both reporting a clean pass ([kb](toolchain.md#a-filtered-run-covers-less-than-it-asked-for-and-reports-success-anyway----measured-2026-08-24)) | Any `Microsoft.Testing.Platform` bump. **It floats**, and a grammar that started ORing whole paths would widen every filtered run silently, which is the safe direction; one that started rejecting the malformed forms would be the loud one | `--list-tests` with the three arrangements side by side, against classes whose sizes were established one at a time first. A fifth of a second, and no run needed; the procedure is in the article. **Two further halves ride on the same trigger**: that `dotnet test` forwards the filter to the test host as an argument (re-run the article's `dotnet test` procedure and read the printed command line), and that `ICommandLineOptions` is unreachable from a test (`ilspycmd -p` over `TUnit.Engine.dll` and read `TUnitServiceProvider`). **The grammar half is not automatable** -- a filtered run is a *correct* run and every number it prints is true of what it ran, so what would have to be caught is the sentence a reader writes underneath it | *manual* for the grammar. **The premise is now mechanised**: `SuiteCoverageTests.TheRunReportsWhetherItWasFiltered` and `SuiteCoverageTests.AFilteredChildRunReadsAsFilteredAndIsRefusedAsARelease`, the second of which starts a genuinely filtered child so that the row's `FULL RUN` is a reading and not a reading that could only ever be empty |
| 118 | **The ILC build integration's own contract, which the vendored SQLite compile depends on in two places**: `findvcvarsall.bat` echoes `link.exe`'s directory and `%LIB%` and **not** `%INCLUDE%`, which is why the compile calls `vcvarsall.bat` itself instead of reusing ILC's discovery; and `DirectPInvoke` is read by `IlcCompile` while `NativeLibrary` is read by `SetupOSSpecificProps`, both of which run **before** `LinkNative` ([kb](toolchain.md#compiling-a-vendored-c-library-into-the-publish)) | Any `Microsoft.DotNet.ILCompiler` bump -- it arrives with the SDK, so it moves without anybody choosing it. **The second half fails silently and is the dangerous one**: a version that read those items later, or a `build/Sqlite.targets` edited to declare them inside the compile target, publishes successfully and binds `e_sqlite3` **lazily at run time** instead of statically, and the symptom is then a `DllNotFoundException` at whatever moment a session first touches the record. The first half fails loudly -- `cl` cannot find `stdio.h` -- and would only mislead a reader of the comment | Read `findvcvarsall.bat` in the restored `microsoft.dotnet.ilcompiler` package and check it still echoes two fields and that neither is `INCLUDE`; then read `Microsoft.NETCore.Native.targets` for `LinkNativeDependsOn` and `Microsoft.NETCore.Native.Windows.targets` for the `LinkerArg`/`NativeLibrary` construction in `SetupOSSpecificProps`. **The behavioural control is cheaper than either and is the one to trust**: publish, then confirm the binary reports a SQLite version -- a lazily-bound build cannot, because there is no `e_sqlite3.dll` beside it | `SqliteTests.ThePublishedBinaryReportsTheStaticallyLinkedSqliteVersion` is the behavioural half and runs on every gate: it fails if the static link did not happen, whatever the reason. The two *readings* of the ILC package are *manual*, because a test asserting the contents of somebody else's build files would assert the file and not the behaviour |

| 119 | **`GetDriveTypeW` through a `subst`ed letter resolves the substitution and classifies what is at the end of it** -- measured 2026-08-26, `subst W: V:\dir` over a `V:` mapped to an unroutable address took **21,035 ms** and then answered `DRIVE_NO_ROOT_DIR`, while the mapping itself answered `DRIVE_REMOTE` in **1 ms** ([kb](windows/detection.md#getdrivetypew-through-a-subst-onto-a-mapped-drive-costs-the-full-21-seconds----measured-2026-08-26)) | A Windows release that makes `GetDriveTypeW` answer from the object manager alone, or an SMB timeout change. **It fails silently and in the expensive direction**: `Interop/VolumeIdentity.Of` asks `QueryDosDeviceW` first *because* of this figure, and a reader who found the old ordering more natural would restore it and put 21 seconds back inside the per-directory gate, where the answer would still be correct. **`[MACHINE]` for the figure, `[FLOATS]` for the timeout** | Define the two DOS devices unelevated and time `GetDriveTypeW` on each, **first call only** -- the second is cached and answers in 0 ms. The control is the mapped letter's own 1 ms: without it, 21 s through the substitution cannot be told from a machine on which every `GetDriveTypeW` is slow | -- *manual*, and it must be: what is automated is the ORDERING, which `CanonicalPathTests.ASubstOntoAMappedDriveIsRefusedAsNetworkRatherThanSentBackForASecondTurn` and `StraySweepTests.ATitleOnALetterSubstitutedOntoAMappedDriveIsRefusedToo` hold by asserting *which answer came back* and not how long it took -- and a clock [cannot be honest at this bound](../TESTING.md#every-duration-is-a-hang-detector-or-it-is-a-defect) |
| 121 | **The browser's network service runs unsandboxed because `--disable-field-trial-config` is the first element of upstream's `chromiumSwitches`**, so the field-trial config that enables `NetworkServiceSandbox` in an unbranded build is switched off and the sandbox is never attempted -- measured 2026-08-29, six arms, with a `Program Files` Chrome under a forced feature as the AppContainer positive control ([kb](playwright/configuration.md#the-network-service-runs-unsandboxed-and-the-cause-is---disable-field-trial-config-not-anything-about-our-tree----measured-2026-08-29)) | **Two moves, and they arrive by different routes.** Upstream dropping the switch from `chromiumSwitches` -- a `playwright-core` change, visible in the payload bundle. Or Chromium enabling the network-service sandbox by default, which arrives with a browser revision and makes the switch irrelevant. **Either one arms the latent half**: the provisioned tree under `%LOCALAPPDATA%` grants neither `ALL APPLICATION PACKAGES` nor `ALL RESTRICTED APPLICATION PACKAGES`, so an attempted sandbox fails its `AccessCheck` and the service is relaunched unsandboxed for the session. `[FLOATS]` | Read `chromiumSwitches` in `playwright-core/lib/coreBundle.js` and check whether the switch is still there and still unconditional. Then launch the provisioned `chrome.exe` directly with `--headless --enable-logging=stderr --v=1`, once plain and once with the switch, and grep each stderr for `sandbox_win.cc`. **Present-then-absent is the check**; the plain arm is the positive control, without which the absent one cannot be told from a capture that missed it | **MANUAL, and it must be.** The switch's presence could be asserted off the payload, and that would be the weaker half of the question -- what matters is whether the sandbox is *attempted*, which needs a real browser launched two ways and its stderr compared, and a suite that did that would provision and launch on every run. [`TODO.md`](../TODO.md#upstream-asks) carries what fires when either trigger lands |
| 120 | **`Path.GetFullPath` rewrites three name shapes instead of rejecting them** -- measured 2026-08-26 on .NET 10.0.11: a trailing dot and a trailing space are stripped, and `C:\work\NUL` comes back as `\\.\NUL`, a device path ([kb](windows/detection.md#pathgetfullpath-rewrites-three-name-shapes-instead-of-rejecting-them----measured-2026-08-26)) | A BCL or Win32 normalisation change. **It decides an ORDER, not a behaviour**: `Sessions/CanonicalPath` asks its segment questions *before* `GetFullPath` precisely because `GetFullPath` does not reject these -- a runtime that started rejecting them would make the ordering redundant and not wrong, and one that started rewriting something else would leave a new shape unrefused. `[FLOATS]` | `[System.IO.Path]::GetFullPath` on each row of the table, printed verbatim, from a shell whose working directory is known. **Keep the `NUL.png` and `a:b` rows** -- they are what separate *this shape is rewritten* from *everything is rewritten* | `CanonicalPathTests.ANameWindowsWillNotKeepVerbatimIsRefusedBeforeAnythingCanSilentlyRewriteIt` asserts the three rewrites **as its own positive controls** and then asserts the refusals, so a runtime that stopped rewriting them is a red build, not a stale comment |
| 122 | **A `webp` screenshot of a document taller than 16,383 px comes back as a ZERO-BYTE image and a zero-byte file with `isError: false`** -- the bracket measured one pixel either side on 2026-09-14 (16,383 px gives a valid 12,284-byte VP8X at 1280x16383; 16,384 px gives 0), with `png` and `jpeg` fine at the same height, on the raw child and reproduced on both binaries the child will drive ([kb](playwright/tools-and-artifacts.md#a-webp-screenshot-past-16383-px-comes-back-as-zero-bytes-with-iserror-false----measured-2026-09-14)) | Upstream checking the dimension, or erroring, or falling back to `png` -- any of which turns the silent success into something a caller can act on. [microsoft/playwright#42717](https://github.com/microsoft/playwright/issues/42717) is open and [PR #42721](https://github.com/microsoft/playwright/pull/42721) is open against it, so this is a fact **already expected to change**, which is the case a golden snapshot is blind to: the tool's schema does not move when the bytes behind it start being checked. `[FLOATS]` | Serve one document exactly 16,383 px tall and one exactly 16,384 px tall, drive `node.exe` against `@playwright/mcp/cli.js` over stdio, and call `browser_take_screenshot` with `fullPage: true` at both heights for each of `webp`, `png` and `jpeg`. Read the inline length from `content[].data` and the file's from disk. **The 16,383 arm is the positive control and is not optional** -- a zero at one height alone cannot tell a format limit from a broken rig | **Manual.** Nothing in the suite takes a screenshot of a 16,384 px document, and nothing should: the arm would render a 25-megapixel page on every run to assert a property of upstream's encoder |
| 123 | **Velopack writes ONE Add/Remove Programs key per app id per user, `HKCU\...\Uninstall\BrowserAI`, so a second install of the same id at any other root rewrites it and either uninstall deletes it for both** -- observed 2026-09-14 @ Velopack 1.2.0 when a probe's uninstall removed the release candidate's own entry, restored byte-identical from an export taken first ([kb](packaging/velopack.md#two-installs-of-one-app-id-share-one-uninstall-key----measured-2026-09-14)) | Velopack keying the entry on the install location as well as the id, or refusing a second install of a taken id. Also any change to this product's app id, which is the other half of the same fact. `[FLOATS]` | Export the key. Install a second copy with `--installto <scratch>` and re-read it -- `InstallLocation` names the scratch root. Uninstall the second copy and ask for the key again: it is gone. Restore from the export and hash both to prove the restore. **Take the export first**: without it the measurement removes a real install's uninstall entry, and the measurement is the damage | **Manual, and it must be.** A suite arm would install and uninstall a second copy of this product on the developer's machine, against the one registry key the real install depends on |

| 124 | **A non-silent `Setup.exe` starts the installed app itself with `show_window = true`, so a console-subsystem binary gets a real console window and a stdin that never reports EOF -- and `Setup.exe` has exited by the time the app looks for its launcher** -- measured 2026-09-14 @ Velopack 1.2.0: creation flags **1024** for the app start against **134,218,752** for a hook, `Setup.exe` gone **44 ms** later, and the server then served nobody for 254 s until it was killed ([kb](packaging/velopack.md#a-non-silent-install-starts-the-app-in-a-console-window-and-nobody-is-on-the-other-end-of-it----measured-2026-09-14)) | `process_win.rs` gaining a no-window path for `start_package`, `Setup.exe` gaining a flag that skips the start, or the app becoming a Windows-subsystem binary. Any of the three would make BrowserAI's `VELOPACK_FIRSTRUN` exit unnecessary and not wrong; the variable's own name and value are the other half of the fact. `[FLOATS]` | Launch `Setup.exe` from a **windowless** parent without `--silent`, against a scratch `--installto`, and watch the process tree and the top-level windows -- [`docs/evidence/2026-09-14-firstrun/`](../docs/evidence/2026-09-14-firstrun/README.md) holds the scripts that did it. ⚠️ **Sandbox `CLAUDE_CONFIG_DIR` first and export the Add/Remove key for the pack id**: the install hook registers with the real client, and the uninstall deletes that key (row 123) | `InstallerHandoffTests`. ⚠️ **Corrected 2026-09-15 (previously "Manual, and it must be. The suite may not allocate a console -- every launch site in this tree is required to set `CreateNoWindow` -- so the half of the rule that needs one cannot be run here").** Both premises were true and the conclusion was wrong, and **the gap cost a shipped release**: `CreateNoWindow` suppresses the console's WINDOW, not the console, so a windowless `cmd.exe` hands a child a real console handle with nothing on screen. `OrphanedConsoleStart` is that rig and `InstallerHandoffTests.ThePublishedBinaryExitsWhenItsLauncherIsGoneAndStdinIsAConsole` drives the published binary through it, with and without the variable. **What is still manual is the installer half** -- that `Setup.exe` is what produces the shape -- because the suite composes the shape itself instead of installing to get it |
| 125 | **A read parked on this process's standard input is completed by neither cancelling its token nor disposing the stream -- for a console stdin and a redirected pipe alike** -- measured 2026-09-15 on .NET 10 / Windows 11 Pro 26200 behind a `PipeReader`: 3 s each, both arms still `WaitingForActivation`, `System.ConsolePal+WindowsConsoleStream` in both cases; and a `Main` that returns with one parked **still exits**, because the read is on a thread-pool thread ([kb](windows/processes.md#a-read-parked-on-standard-input-is-woken-by-neither-cancelling-it-nor-disposing-the-stream----measured-2026-09-15)) | `Stream.ReadAsync` gaining a cancellable path for console handles, or `WindowsConsoleStream` starting to close the standard handle it was given. Either would make the abandon-rather-than-await in `JsonLinesTransport.DisposeAsync` unnecessary and not wrong -- and the *third* thing that would break it is the one to watch: a runtime in which the parked read became a **foreground** thread, which would make abandoning it a process that never exits. `[FLOATS]` | Park a `PipeReader.ReadAsync` on `Console.OpenStandardInput()`, wait for the read to be genuinely in flight, then cancel and dispose in turn, reporting the task status after each with a bound. Run it twice -- stdin inherited from a `CreateNoWindow` `cmd.exe`, and stdin redirected to a pipe nothing writes to -- or a console-specific behaviour is indistinguishable from a general one. [`docs/evidence/2026-09-15-fix/`](../docs/evidence/2026-09-15-fix/README.md) holds the probe | `DirectStdioServerTransportTests.DisposingDoesNotWaitForAReadTheCallerWillNeverEnd` holds the consequence in process, and `InstallerHandoffTests.ThePublishedBinaryExitsWhenItsLauncherIsGoneAndStdinIsAConsole` holds it over the published binary. **Neither asserts the framework behaviour itself**, which is what this row is for |
| 126 | **`VELOPACK_FIRSTRUN` is not a reliable signal that the installer started this process, and the app's stub is a second console-bearing entry point that never carries it** -- measured 2026-09-15 @ Velopack 1.2.0: the published v1.0.0, started by its own `Setup.exe`, took the *general* no-client branch and not the installer branch thirty lines earlier, while the same binary takes the installer branch in 0.313 s when the variable is set on an uninstalled start; and `Update.exe start` -- what the stub `BrowserAI.exe` in the install root runs (`start_windows_impl.rs:122-131`) -- sets no such variable at all ([kb](packaging/velopack.md#re-measured-2026-09-15-against-the-published-v100-and-the-paragraph-above-was-half-wrong)) | Velopack clearing or stopping clearing the variable inside `VelopackApp.Run()`, or the stub gaining it. ⚠️ **Why the published build did not see it is UNRESOLVED and is recorded as unresolved** -- `VelopackApp.Run()` is the only code between the launch and the read, and that it clears the variable is inferred and not measured. Nothing in the product depends on the answer: the exit keys on *launcher gone or unopenable AND stdin is a console*, which covers both entry points. `[FLOATS]` | Install non-silently from a real `Setup.exe` and read the product's own log for `Startup[8]` against `Startup[9]`; then start the same binary uninstalled with the variable set and read the same two. The second half needs no installer and is 0.3 s | `InstallerHandoffTests.ThePublishedBinaryExitsWhenItsLauncherIsGoneAndStdinIsAConsole` runs **both** arms -- variable set and variable absent -- and requires each to exit, which is the property that survives whatever the answer turns out to be. **Which branch the real installer's start takes is manual**: it needs an install |
| 127 | **A two-binary Velopack pack: a second executable is an ordinary payload file, and what it costs is measured.** Sizes, the 0.3825 compression ratio, the 4,693,726-byte installer delta and the configuration app opening its window in 155-243 ms ([kb](packaging/velopack.md#two-binaries-in-one-pack-measured-end-to-end----2026-09-15)) | Any `vpk`, Velopack, SDK or ILC bump, and any change to the payload -- the ratio is a property of all four at once | Run `build/New-Release.ps1` into a scratch output directory and read the object it prints: it reports `ShippedBytes`, `FullPackageBytes` and `CompressionRatio` itself. For the window time, launch the published app with no window and poll `EnumWindows` for a visible `#32770` owned by it from the moment it starts -- **poll; do not wait a fixed interval**, which is what made two by-hand probes of the same binary disagree | -- *manual*. **The SHAPE is mechanised and the numbers are not**: `ReleaseScriptTests` holds that both binaries publish behind their own ILC gate and that neither may be missing, and `TaskDialogLayoutTests` holds the subsystem of each. No test asserts a size, because `Releases/` is gitignored and an artifact does not exist on a clean clone |

| 128 | **GitHub's release-body limit is a carried number, not a measured one, and the body's rendering is checkable without publishing** -- 125,000 characters is what this project cuts at and GitHub's REST documentation for *Create a release* states no maximum at all (read 2026-09-15); `gh api -X POST markdown -f mode=gfm` returns the HTML GitHub would render, and a generated body's `<details>` lands inside its `<li>` with the headline as a `<strong>` ([kb](toolchain.md#gh-for-a-release-body-the-size-limit-is-carried-not-measured-and-the-rendering-is-checkable----2026-09-15)) | GitHub moving the field's limit in either direction, or changing how GFM renders an HTML block inside a list item. `[FLOATS]` | For the rendering: run the `gh api` line above over a generated body and read the HTML -- 30 s, no publishing, and it is [a step of the release checklist](../RELEASING.md#the-release-body-is-generated-and-its-rendering-is-checked-before-it-is-published). For the limit: create a release in a SCRATCH repository with a body over it and read the refusal -- **not done here**, because the only repository this project may publish to is the real one | -- *manual*. `ChangelogTests` holds the generator's two shapes and the size guard's threshold as a parameter, so what a test can see is that the fallback happens at the number it is given; whether that number is GitHub's is what nothing here can check |
| 129 | **`[STAThread]` is honoured under NativeAOT** -- measured 2026-09-16 @ .NET 10 / ILC 10.0.12: the published `BrowserAI.exe --report` writes `"apartment": "STA"`, read from `Thread.CurrentThread.GetApartmentState()` ([kb](toolchain.md#stathread-is-honoured-under-nativeaot----measured-2026-09-16)) | Any ILC or SDK bump. It had been **assumed** from the day the configuration app was written, and two things depend on it with no diagnostic if it is ever false: `BIF_NEWDIALOGSTYLE` falls back to the pre-Vista folder picker on a thread that is not in an STA, silently, and the version 6 common controls a task dialog is made of expect one. `[FLOATS]` | Publish the app and run `BrowserAI.exe --report out.json`, then read `apartment` -- seconds, and it needs no window | `TaskDialogLayoutTests.ThePublishedConfigurationAppRunsInASingleThreadedApartment` runs the **published** binary and requires the apartment to be single-threaded, so this is re-established on every gate and not on a day somebody looked. The suite's own host cannot answer it at all, being an ordinary CoreCLR process |

| 130 | **A non-silent `Setup.exe` STOPS and asks before installing over a non-empty target directory, at any version, and on a same-version re-ship the affirmative button says `Repair`** -- measured 2026-09-16 @ Velopack 1.2.0 against the published `v1.0.0` installer: a `#32770` titled `BrowserAI Setup`, 572x201, reading *"BrowserAI is already installed"*, waiting indefinitely; the trigger is `!is_dir_empty(&root_path)` and only the button LABEL varies with the version comparison ([kb](packaging/velopack.md#setup-will-not-install-over-an-existing-install-without-being-told-to-and-on-a-same-version-re-ship-the-button-says-repair----measured-2026-09-16)) | Velopack changing the trigger, the wording, or the labels -- and any of the three changes what an existing user is asked at the next release and not anything this product does. **The dangerous direction is the quiet one**: a Cancel returns exit **0** and installs nothing, so a future change that made this fire more often would look like a successful install to anything reading only the exit code. `[FLOATS]` | Run a non-silent `Setup.exe` against a root that already holds an install, from a windowless parent, and enumerate the top-level windows of its pid. **Read the body through UI Automation, not `GetWindowTextW`** -- a task dialog's text lives in a `DirectUIHWND` and comes back empty otherwise. [`docs/probes/2026-09-16-release/Read-Dialog.ps1`](../docs/probes/2026-09-16-release/README.md) is the enumerator | **Manual, and it must be.** The arm would install this product over the developer's own install and wait on a modal dialog with nobody to answer it; the suite installs with `--silent`, which is exactly the path that skips this |
| 131 | **A browser call against a session whose `node` child was killed under a live BrowserAI never returns, and no product timer bounds it** -- measured 2026-09-17: `browserai_resume` answers the no-op in **7.68 ms**, the next `browser_navigate` had not returned after **900,000 ms**, and a second client's `browserai_resume` is refused in **15.3 ms** by the ordinary in-use refusal ([kb](playwright/provisioning-and-timings.md#the-resume-wedge-measured----2026-09-17)) | Any change to `ChildConnection.AskAsync`, to the child transport, or to what `browserai_resume` checks before answering; and any per-call timeout arriving anywhere on the forward path | Re-run [`docs/probes/2026-09-16-resume/wedge-probe.js`](../docs/probes/2026-09-16-resume/README.md) against the published slice with a bound in milliseconds, never beside a suite run. The bound is the largest product timeout plus margin and not a number chosen at the assertion: 10 minutes is `ChildConnection.ChildInitializationHang` and `BrowserIdleTimer.DefaultIdlePeriod`, and 15 was used. **A return inside the bound is the finding moving**, and it would mean something on that path acquired a clock. ✅ **AMENDED 2026-09-17, LATER THE SAME DAY: THE PRODUCT MOVED, NOT THE MEASUREMENT.** Added by addition; the numbers above are what the unfixed product did and are not retracted. `BrowserProxy` now asks `ChildConnection.ChildHasGone` at the door and refuses with `SessionErrors.BrowserServerHasGone`, and `browserai_resume` relaunches the child instead of answering the no-op -- so re-running the rig against a **current** slice produces a refusal in milliseconds and a working session after the resume, and that is the pass and not the finding moving. **What the rig still establishes** is the half no suite arm can: that a REAL node child killed under a real server produces the same shape the in-process rig produces, and that the refusal is what a real client receives. ⭐ **AND THE RE-RUN THIS ROW ASKED FOR HAPPENED ON 2026-09-17, in the row 38 measurement session: the prediction above is now measured, not reasoned.** `wedge-probe.js`, twice against the current published slice, chromium 1245: the resume took **345.77 ms and 330.92 ms** and carried `SessionManager.ChildWasRelaunched` verbatim instead of the 7.68 ms no-op, and the next `browser_navigate` returned a real page in **444 ms and 426 ms** where it had never returned in 900,000 ms. Browsers went 8 → 0 on the kill → 8 under the replacement child; teardown left 0. **The refusal half was NOT exercised**: the rig resumes before it forwards, so nothing in that run met `SessionErrors.BrowserServerHasGone`, and a forward made without a resume is still unmeasured on a current slice. **The forward path still has no clock**, which is the property to check has not changed: a timeout arriving anywhere on it is still the thing this row is watching for | `DeadChildTests` covers the in-process half in both directions, with two controls; the real-child half is still **manual** |
| 132 | **The MCP SDK faults a pending request when the transport closes, and faults only the requests pending AT THAT MOMENT** -- read 2026-09-17 from `ModelContextProtocol.Core` **2.2.0**'s shipped IL: `McpSessionHandler.ProcessMessagesCoreAsync`'s `finally` walks `_pendingRequests` once, when `MessageReader.ReadAllAsync` ends, and `TrySetException`s each entry with the channel's completion exception or `new IOException("The server shut down unexpectedly.")`. `SendRequestAsync` registers its own TCS and then awaits `tcs.Task.WaitAsync(cancellationToken)` under **the caller's token and nothing else**, so a request registered after that walk is faulted by nothing ([kb](mcp/sdk.md#a-pending-request-is-faulted-once-at-the-close-and-never-again----measured-2026-09-17)) | Any `ModelContextProtocol` bump. It moves in the **good** direction if `SendRequestAsync` ever consults transport state or `McpClient.Completion`, and in the bad direction if the walk stops running | `ilspycmd -t ModelContextProtocol.McpSessionHandler` over the resolved `lib/net10.0/ModelContextProtocol.Core.dll`, and read `SendRequestAsync` beside `ProcessMessagesCoreAsync` | `LosslessPassthroughTests.AChildThatDiesMidCallProducesANamedErrorRatherThanASuccess` covers the **first** half -- a request pending at the close does come back as an `IOException` -- and nothing covers the second, **deliberately**: BrowserAI refuses at its own door before a later request reaches the SDK at all, so an arm asserting the hang would have to bypass the product's own fix to observe it. **Manual** |
| 133 | **A page can add tools to the child's `tools/list` and put its own text in front of a model** -- measured 2026-09-21 @ `@playwright/mcp` **0.0.82**: against a page registering two WebMCP tools the child's `tools/list` goes **72 → 74**, it sends `notifications/tools/list_changed`, and every snapshot-bearing tool result carries the page's own tool names, annotations, descriptions and `inputSchema`s. `webmcp: false` removes all of it. **Through BrowserAI the tool list does not move (78 → 78) and a page-supplied name is refused at the door, and the snapshot text arrives anyway** ([kb](playwright/tools-and-artifacts.md#a-page-can-add-tools-to-the-childs-toolslist-and-its-own-text-reaches-a-caller----measured-2026-09-21)) | Any `playwright-core` bump. The two halves move independently: the list half turns on `tools/list` still being answered from a pageless child, and the text half on the snapshot renderer. Upstream putting `browser_webmcp_list` or `browser_webmcp_call` back on the wire is a **third** change and a verdict question | Run [`docs/probes/2026-09-21-webmcp`](../docs/probes/2026-09-21-webmcp/README.md) twice against the child, once per value of `webmcp`, and diff; then `through-browserai.mjs` against a published slice, which is the only thing that can tell the two halves apart | **The door half is covered**, by the deny-by-default arms over `tool-verdicts.json`, and it is covered against names upstream might add and not names a page invents. **Nothing covers the list half or the text half**, and that is stated and not implied: a test would need a page serving WebMCP tools inside the suite, which is a rig, not an assertion -- *manual* |
| 134 | **A page tool's wire name is `webmcp_` plus the page's own tool NAME sanitised, and `annotations.title` is the page's `title` where it set one** -- measured 2026-09-21 @ `@playwright/mcp` **0.0.82**: `webmcp_` + `name.replace(/[^a-zA-Z0-9_-]/g, "_").slice(0, 64)`, de-duplicated `_2`, `_3` ..., while `annotations.title` falls back from the page's own `title` to its `name` and the snapshot block prints the `name` either way ([kb](playwright/tools-and-artifacts.md#a-page-tools-wire-name-is-built-from-the-pages-tool-name-and-annotationstitle-is-not-that-name----measured-2026-09-21)) | Any `playwright-core` bump. `browserai_page_tool` reproduces this function to turn the name a model read into the name the child answers to, so a change to either half makes every page tool uncallable -- the prefix, the character class, the cut, the collision suffix and the title-then-name fallback are five independent things to move | Drive the payload's own `cli.js` against a page registering `Do The Thing!`, a tool with a separate `title`, and `twin` twice; read `tools/list` and a `browser_snapshot` result side by side ([`docs/probes/2026-09-21-webmcp`](../docs/probes/2026-09-21-webmcp/README.md)) | **Partly, and the part that is covered is the one that fails loudly.** `PageToolTests` drives all three shapes through the published binary against real pages, so a changed rule turns those arms red instead of turning a capability off quietly. **What nothing covers is the 64-character cut and the `_3` and beyond**, which no page in the suite reaches -- *manual* |
| 135 | **A pending page-tool call does not block the child, and navigating away or closing the tab releases it** -- measured 2026-09-21 @ `@playwright/mcp` **0.0.82**: with one pending, `browser_snapshot` answered in **4-7 ms** and a second page tool in **~520 ms**; the pending call never completed on its own (**61 s** observed) and was released in **11-13 ms** by a navigation and **7 ms** by closing the tab ([kb](playwright/tools-and-artifacts.md#a-hung-page-tool-does-not-block-the-child-and-is-released-by-navigating-away----measured-2026-09-21)) | Any `playwright-core` bump, and any change to how upstream dispatches a page tool. **This is the fact `SessionToolSurface.PageToolBudget` rests on**: if a pending call ever DID block the child, the timeout would stop being a recovery and become a session that has to be torn down | Serve a page whose `invokeTool` returns a promise nothing settles, call it, and keep asking the same child for snapshots while it pends; then navigate away and time the release ([`docs/probes/2026-09-21-webmcp`](../docs/probes/2026-09-21-webmcp/README.md)) | **The abandonment is covered and the no-block claim is not.** `PageToolTests` holds that the call is abandoned on BrowserAI's own budget and that the session answers a snapshot AFTERWARDS; nothing asks a snapshot while the call is still pending, because that needs two calls outstanding at once against one session and the suite's client is one exchange at a time -- *manual* |
| 136 | **Claude Code reads `hasCompletedOnboarding` in `$CLAUDE_CONFIG_DIR\.claude.json` as "onboarding is finished", and the global config file is `.config.json` in that directory if it exists and otherwise `.claude<suffix>.json`, the suffix empty on the `prod` channel** -- established 2026-09-22 @ Claude Code **2.1.278** (`BUILD_TIME` 2026-09-19T01:12:36Z, `GIT_SHA` 809c9806) by reading the installed single-file bundle, with the three resolving functions and the deciding read quoted verbatim on `OnboardedClientConfig`. **There is no non-interactive signal to set instead**: `claude mcp add --help` carries no such flag, `CLAUDE_CODE_POWERUP_ONBOARDING` is a force-**on**, and a grep for `CLAUDE_CODE_NONINTERACTIVE` returns zero against `CLAUDE_CONFIG_DIR` at 74 hits and the marker at 19 from the same grep. **The published documentation does not document the marker at all**, so the bundle is the only source there is. `[FLOATS]` | The client renames the key, moves the file, or starts reading it on the `mcp` subcommand path -- where today it reads it nowhere, every occurrence sitting on the REPL, login or nudge paths. **The dangerous direction is silent**: a seeded marker the client no longer honours guards nothing, and the suite would go on passing while a sign-in window opened on the maintainer's screen. It is somebody else's binary, resolved from `PATH` and self-updating | Re-read the bundle: `where claude`, then grep it for `hasCompletedOnboarding` and for `CLAUDE_CONFIG_DIR`, and read the context around each hit. ⚠️ **Seed a scratch config directory and point `CLAUDE_CONFIG_DIR` at it before running the CLI at all** -- the whole point of the row is that an unseeded one may run onboarding. **Do NOT attempt to drive the sign-in flow to prove the guard works**: that is the event being guarded against, and it opens a browser window on a real desktop | `HouseRuleTests.EveryScratchClientConfigurationIsSeededAsOnboardedBeforeTheClientRuns` and `RegistrationTests.TheScratchConfigurationIsSeededWithWhatTheClientReadsAsOnboarded` hold that the marker is seeded at every site and that the file carries it -- **neither can see whether the client still honours it**, which is exactly what this row is for, and the [hazard row](../HAZARDS.md#hazard-index) says so in as many words |
| 137 | **Only `releases.{channel}.json` is read from a release -- no Velopack client, no `Update.exe`, no `Setup.exe` and no `vpk` command reads `RELEASES` or `assets.{channel}.json` from a published release** -- measured 2026-09-23 @ `vpk` **1.2.158** / Velopack **1.2.158**, against the files `v1.1.0` carries ([kb](packaging/velopack.md#nothing-anywhere-reads-releases-or-assetschanneljson-from-a-release----measured-2026-09-23)). **This is the row that justifies an upload set of three files**, so if it stops holding, a release stops carrying something an updater needs. ⚠️ **`assets.{channel}.json` is still needed ON DISK**: `vpk upload` reads the LOCAL copy to learn what to upload, so deleting it from `Releases/` is a different change from not publishing it. ⚠️ **The obvious control does not work on a local feed** -- `SimpleFileSource` falls back to scanning `*.nupkg` when the JSON is absent, so the packages have to go with it | Any Velopack or `vpk` bump. A new `IUpdateSource`, a change to the fallback, or `vpk upload github` learning to publish the legacy file again | Four scratch feeds and `vpk download local --path <dir> -o <out> -c win` against each; the decisive one holds both legacy files and neither the JSON nor any `.nupkg`, and must come back *No full / applicable release was found to download*. Then the suite half: `BROWSERAI_RELEASE_FEED` at a copy of `Releases/` with the two files deleted at both levels, `RealInstallerTests` green; restore them, delete `test-pack/releases.win.json` and run again under `BROWSERAI_RELEASE_RUN=1` for the control. **The clearance conditions apply** -- those arms run a real `Setup.exe` | -- *manual*; the SET this row justifies is on every build, by `ReleaseScriptTests.TheUploadSetIsDeclaredOnceAndIsTheInstallerTheFullPackageAndTheFeed` and `ReleaseScriptTests.NothingElseInTheReleaseDirectoryIsPublished` -- neither can see whether Velopack still reads only the one file, which is exactly what this row is for |

Add a row whenever a new `[FLOATS]` entry lands, unless the entry qualifies for
[the one exemption](#a-floats-entry-with-no-row-the-one-rule) and says in place
that it does. Otherwise an entry with no row is an entry nobody will re-check.
