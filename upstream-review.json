{
  "_what_this_is": "Records the version of each upstream dependency that a human has actually REVIEWED. A test asserts each of these equals the version the build RESOLVED. Every dependency floats to latest at build time, so when upstream publishes, that test goes red and the release gate cannot pass until this file is updated.",
  "_stop_before_you_edit_this_file": "Read UPSTREAM-REVIEW.md and follow it. Bumping a version here without doing the review defeats the only mechanism that catches upstream behaviour changes, renamed config keys and changed defaults. None of those are visible to the golden tools/list snapshot, and loadConfig is a bare JSON.parse with no schema validation, so a renamed key is discarded in silence.",
  "_notes_are_not_optional": "Every bump records what changed, what was adopted, and what was explicitly declined and why. A decline with a reason is worth as much as an adoption: it stops the same question being re-litigated at the next bump. An empty note is a review that did not happen.",
  "_snapshots_are_not_optional": "Each entry adjudicates every golden snapshot by name: `unchanged`, or `changed` and what the adjudication was. Four snapshots, four lines, on every entry, every time -- an entry that answers three of four is a review that looked at three. A snapshot the entry says is NOT IN PLAY is still answered, because 'this upstream cannot move that file' is a claim worth reading and worth being wrong about in public. UpstreamReviewTests asserts the block's names against the upstream-snapshots/ directory in BOTH directions, so a fifth snapshot forces every entry to answer for it and a deleted one cannot linger. What no test can assert is whether an adjudication is TRUE; that is what the procedure and the notes are for.",
  "_procedure": "UPSTREAM-REVIEW.md",
  "_this_file_is_gated": "Not by a prompt. Editing this file is unrestricted; what is gated is the RELEASE. The build regenerates four snapshots from the resolved payload -- tools/list, --help, config.d.ts, browsers.json -- and diffs them, runs the full suite, and asserts that this file adjudicates whatever actually moved and answers every manual re-verification row by name. An entry that ignores what the build observed fails the build. See TESTING.md, 'The upstream-review gate'.",
  "_correction_2026_08_15": "An earlier version of the note above said a PreToolUse hook asks a human to approve every edit, and that a sub-agent attempting one is killed silently. Both halves were wrong, and measurement disproved them: under permission_mode 'bypassPermissions' a hook's 'ask' returned to a sub-agent is silently downgraded to ALLOW -- the edit lands unprompted, and the gate was inert against exactly the caller most likely to need it. A sub-agent did die during that work, but not from the hook; the cause is unknown and its transcript was zero bytes. Recorded because a confident wrong explanation is worse than an admitted gap, which is the same rule this file applies to versions.",
  "_license": "LicenseRef-BrowserAI-FSL-1.1-MIT-5yr, Copyright 2026 Jori Huisman",
  "upstreams": {
    "@playwright/mcp": {
      "reviewed": "0.0.82",
      "date": "2026-09-21",
      "source": "https://github.com/microsoft/playwright-mcp",
      "why_it_matters": "Ships in the payload. Its CLI surface and config schema are what BrowserAI generates against, and both change without notice.",
      "snapshots": {
        "tools-list.json": "changed. The `tools` array lost exactly two entries, in order -- browser_webmcp_list and browser_webmcp_call -- with every surviving entry byte-identical in position. NO TOOL WAS ADDED AND NONE WAS RENAMED: upstream marked both skillOnly:true while leaving capability `core`, so the internal registry is unmoved at 83 while exposedMaximum went 74 -> 72, defaultSurface 27 -> 25 and skillOnly 9 -> 11, all three by the same two tools. ADOPTED, and the two verdict rows are DELETED, which is UPSTREAM-REVIEW.md's own instruction for a tool upstream removed.",
        "cli-help.txt": "changed. The two withdrawn tools are CLI commands now, webmcp-list and webmcp-call, under a new `webmcp` command category, and there is a new --no-webmcp flag. ADOPTED WITH NOTHING TO DO: BrowserAI configures the child through a generated config file and builds no flags, so a new flag is a fact about upstream's CLI and not a surface this product exposes.",
        "config-schema.d.ts": "changed, three diffs. (1) NEW KEY webmcp?: boolean -- ADJUDICATED 2026-09-21 BY THE MAINTAINER (Q219): written, `true`, explicitly. (2) filePaths?: 'relative' | 'absolute' is DECLARED now; ADOPTED WITH NOTHING TO DO, and it closes a gap rather than opening one -- BrowserAI has written that key since 2026-09-17 while the wrapper's own typings did not declare it, and the two have caught up. (3) The typings' own import of the playwright package moved from a relative path to the bare package name; DECLINED AS IRRELEVANT, because nothing this project compiles reads config.d.ts as TypeScript.",
        "browsers.json": "changed, confirmed from the REBUILT payload's own browsers.json rather than from the registry tarball the drift check read: chromium and chromium-headless-shell 1245 -> 1246 at the same browserVersion 154.0.8037.0, firefox 1548 -> 1549 with browserVersion 155.0 -> 156.0, webkit 2361 -> 2365 at the same 26.6 with its mac14 revisionOverrides unmoved at 2251, ffmpeg 1011, winldd 1007 and android 1001 unmoved. ADOPTED. Only chromium and firefox are provisioned by this product; webkit's move is recorded rather than costed."
      },
      "notes": "Reviewed 0.0.81 -> 0.0.82 on 2026-09-21, end to end per UPSTREAM-REVIEW.md, against the payload the build re-resolved. 0.0.82 was published 2026-09-18T23:38:28.161Z. ALL FOUR GOLDEN SNAPSHOTS MOVED and every difference is adjudicated below.\n\nTHE ROLL ENDED THE DATED playwright-core OVERRIDE, WHICH IS THE POINT OF THIS REVIEW. 0.0.82 declares playwright-core AND playwright at 1.64.0-alpha-1789764292000, whose bundle carries the --file-paths fix the override was taken for on 2026-09-17. The `overrides` block and its `//overrides` note are deleted from build/payload/package.json, the wrapper's own pin floats again, and the two instruments that existed only to announce the exit are RETIRED rather than left standing: PayloadTests.TheDatedPlaywrightCoreOverrideIsStillNeeded, PayloadTests.TheExpiryComparisonFiresInBothDirections, and the Compare-PlaywrightVersion / Split-Version ordering in build/Build-Payload.ps1. THE ORDERING WAS NOT WIDENED AND WILL NOT BE. It parsed <major>.<minor>.<patch>[-alpha-YYYY-MM-DD] and THREW on anything else; the version the wrapper actually rolled to is a 13-digit epoch-milliseconds alpha it refused by design, and that refusal was correct -- silently calling an unorderable shape 'lower' is what would have kept an override alive past its own exit. Nothing in this tree ranks a playwright-core version now; every comparison left is for IDENTITY against another recorded string, so a version is an opaque string and its shape is upstream's business. The invariant that survives is the one that was always the real one and it came back to where it started: PayloadTests.TheLockRecordsUpstreamsOwnExactPinOfPlaywrightCore now asserts resolved == declared again, planted red 2026-09-21 against a doctored lock, and build/Build-Payload.ps1 makes the same check against the live resolution.\n\nWHAT THE tools-list.json DIFF SAYS, AND IT IS NOT WHAT THE HEADLINE NUMBERS LOOK LIKE. NO TOOL WAS ADDED AND NONE WAS RENAMED. The internal registry is unmoved at 83 and the `tools` array lost exactly two entries, in order, with every surviving entry byte-identical in position: browser_webmcp_list and browser_webmcp_call. They were not deleted -- upstream marked BOTH `skillOnly: true` in packages/playwright-core/src/tools/backend/webmcp.ts while leaving capability `core` -- so exposedMaximum went 74 -> 72, defaultSurface 27 -> 25 and skillOnly 9 -> 11, all three by the same two tools and all three in the direction that follows. They are CLI commands now, `webmcp-list` and `webmcp-call`, under a new `webmcp` command category.\n\nADOPTED, WITH THE ROWS THAT MOVED WITH IT. The two verdict rows are DELETED, which is UPSTREAM-REVIEW.md's own instruction for a tool upstream removed and is the direction ToolVerdictTests refuses on purpose: a row naming a tool the snapshot does not carry is a judgement about nothing. THE DENY'S REASONING IS PRESERVED RATHER THAN LOST, in tool-verdicts.json's `_rows_removed_because_upstream_withdrew_the_tool` and here, because it is what a future judgement would need. browser_webmcp_call was denied 2026-09-15 on LIVENESS, not security: it runs a tool the PAGE supplies and waits for it with nothing bounding the wait, one measured call stood silent for 45 s, and any page can arrange that. RE-READ IN THE 0.0.82 BUNDLE AND STILL TRUE: the new 5,000 ms kFrameTimeout bounds the LISTING evaluate per frame and callWebMCPTool still awaits tab.waitForCompletion around a page-supplied handler with nothing bounding it. So if upstream ever puts these two back on the wire, that deny stands until a human re-judges it; it is not re-litigated by their removal. DECISIONS.md's tool-surface numbers moved with the surface and were RE-COUNTED off the regenerated snapshot rather than decremented: 74 -> 72 exposed and 72 -> 71 advertised, and the withheld set is back to one, browser_annotate. RecordedCountTests' `withheld` control moved 2 -> 1 with a stamp saying nobody decided that.\n\nTHE CHANGE THAT NO SNAPSHOT COULD HAVE SHOWN, AND IT IS THE LARGEST THING IN THIS ROLL. `tools/list` IS NO LONGER A STATIC SURFACE. createServer now declares capabilities.tools.listChanged TRUE and its ListTools handler ends with a push of backend.dynamicTools(); dynamicTools returns the CURRENT TAB's WebMCP tools, named `webmcp_` + the page's own tool name sanitised and cut at 64 characters. So a WEB PAGE can add tools to the child's advertised list, with its own descriptions and its own inputSchemas, and the server sends notifications/tools/list_changed when the page's set changes. In the same release, every snapshot-bearing tool result began carrying the page's tool listing INSIDE the snapshot -- a `- webmcp tools (page-provided, untrusted):` block, then per tool the page's name, its [readOnly]/[consequential] annotations, its full description and its inputSchema as JSON -- and the tab header carries `- N webmcp tools available on the page`. The collection runs wherever _includeSnapshot is not 'none', which is most tools rather than only browser_snapshot, by evaluating collectToolsInPage in EVERY FRAME of the current tab, each bounded at 5,000 ms.\n\nTHAT WAS MEASURED RATHER THAN READ, THREE WAYS, and the rig and the transcripts are kept: docs/probes/2026-09-21-webmcp and docs/evidence/2026-09-21-webmcp, with the finding in kb/playwright/tools-and-artifacts.md and re-verification row 133. Against a page registering two WebMCP tools, the CHILD's tools/list went 72 -> 74 and sent three list_changed notifications; with `webmcp: false` written, 72 -> 72, no notification, no header line and no snapshot block. THROUGH THE PUBLISHED BrowserAI.Server.exe, BOTH HALVES OF THE EXPOSURE ARE CLOSED AND ONE IS NOT. BrowserAI's tools/list was 78 BEFORE the page and 78 AFTER -- nothing was added -- because BrowserAI answers tools/list from THE RUN'S OWN CHILD, which never navigates and so has no page to collect from. And a tools/call naming webmcp_probe_tool_alpha was refused at the door with the unjudged-tool refusal, nothing forwarded and nothing started, which is deny-by-default meeting a name A WEB PAGE INVENTED. Neither mechanism was built for this and both hold. BrowserAI's own initialize still advertises an empty tools capability object, so the child's listChanged is not forwarded and no list_changed reaches a caller, which is correct because BrowserAI's list genuinely does not change.\n\nWHAT IS NOT CLOSED IS THE TEXT, AND IT IS REPORTED RATHER THAN DECIDED. Tool results are forwarded verbatim by design, so the page's own tool names, descriptions and schemas now arrive in every snapshot-bearing result. Upstream's [UNTRUSTED: ...] prefix is on the dynamic tool DESCRIPTIONS rather than on the snapshot block, which is labelled only `(page-provided, untrusted)`. `webmcp: false` removes all of it and BrowserAI writes no `webmcp` key today, so upstream's default is in force. THIS IS A PRODUCT STANCE UNDER THE WRITTEN-RATHER-THAN-OMITTED DOCTRINE, the same class as `timeouts.idle` and `imageResponses` on 2026-09-15, and it belongs to the maintainer. It is raised with alternatives rather than taken here.\n\nNEW CONFIG KEY: `webmcp?: boolean`, \"Whether to collect and expose the tools that a page registers through the experimental WebMCP API. Enabled by default.\" ADJUDICATED 2026-09-21 BY THE MAINTAINER (Q219): WRITTEN, `true`, EXPLICITLY. The stance recorded is today's behaviour rather than a change to it -- upstream's default is on -- and what settled it is that the capability arrived in the same pass: `browserai_page_tool` calls a tool the page registered, and the snapshot block this key switches on is the only catalogue a model has for those tools. `false` would have removed the capability with the text. The exposure is unchanged and stays an open hazard row; the decision, the framing and the direction not taken are in DECISIONS.md under \"A web page's own tools are reached through one tool of ours\". NEW CLI FLAG: --no-webmcp, \"do not collect or expose the tools that a page registers through the WebMCP API.\" Never built; BrowserAI configures through a generated config file and builds no flags.\n\nCONFIG SCHEMA, THE OTHER TWO DIFFS. (1) `filePaths?: 'relative' | 'absolute'` is DECLARED now. ADOPTED WITH NOTHING TO DO, and it closes a gap rather than opening one: BrowserAI has written that key since 2026-09-17 while @playwright/mcp's own typings did not declare it, because the implementation was in the overridden playwright-core and config.d.ts ships with the wrapper. The two have caught up, config-schema.d.ts is no longer the one golden snapshot that did not move on adoption, and the kb sentence saying otherwise is corrected by addition. (2) The typings' own import of the playwright package changed from a relative path to the bare package name. A packaging change in how the typings resolve their own dependency; nothing this project compiles reads config.d.ts as TypeScript, so it is DECLINED AS IRRELEVANT rather than adopted.\n\nTOOL SCHEMAS THAT MOVED, BOTH ADDITIVE AND BOTH ON TOOLS ALREADY `allow`. browser_start_video gained `fps` (number, \"Video frame rate in frames per second, defaults to 25\") and `cursor` (boolean, an animated cursor that paces actions by 800 ms). browser_video_show_actions gained a `style` object -- `point`, `highlight`, `title`, each a CSS declaration string -- and its description changed to say the callout marks the action point \"when styled\". NO VERDICT QUESTION: both tools are upstream's, both descriptions and schemas pass through byte for byte, and no existing parameter changed meaning or became required. ADOPTED WITH NOTHING TO DO. Worth knowing that `cursor: true` paces every action by 800 ms, which is a latency a caller chooses rather than one that arrives.\n\nBROWSER REVISIONS, confirmed from the REBUILT payload's own browsers.json rather than from the registry tarball the drift check read: chromium 1245 -> 1246 at the same browserVersion 154.0.8037.0, chromium-headless-shell the same, firefox 1548 -> 1549 with browserVersion 155.0 -> 156.0, webkit 2361 -> 2365 at the same 26.6 with its mac14 revisionOverrides unmoved at 2251, ffmpeg 1011 and winldd 1007 unmoved, android 1001 unmoved. ADOPTED. Only chromium and firefox are provisioned by this product; webkit is never installed and its move is recorded rather than costed.\n\nRE-VERIFICATION ROWS ANSWERED BY NAME. Re-verified and UNCHANGED: 10 (the winldd no-op for Chromium -- still [\"chrome-win\"] against an extracted chrome-win64, so still a permanent no-op and still no cold-start latency), 19 (the same eleven artifact prefixes plus the empty one, off the regenerated snapshot), 48 (both halves: the pin is exact and no non-optional package declares an install script). Re-verified and MOVED, re-counted off the snapshot rather than decremented: 11 (83 internal, 72 exposed, 25 default, eleven skillOnly), 12 (45 for config+vision+devtools, 62 with storage, and storage is still 17 for the sixth review running), 17 (47 PLAYWRIGHT_MCP_* variables, 44 inside configFromEnv and the same three outside; PLAYWRIGHT_MCP_WEBMCP is the arrival, and the POSITIVE CONTROL was the previous bundle fetched with npm pack playwright-core@1.64.0-alpha-2026-09-17, which returned 43 + 3 = 46 exactly as the row recorded), 69 (71 of 72 advertised, withheld set back to one). RE-MEASURED IN THIS BATCH rather than left owed, because the PRODUCT quotes its figures: 21. Two clean runs per family through docs/probes/2026-09-16-provisioning, byte-identical within each pair, plus a HEAD on each of the four archives. Chromium did not move at all for the third roll running -- 207,274,189 B on the wire and 458,475,923 B across 316 files on disk, identical to 1245 and 1244, because cftUrl() is keyed on browserVersion. Firefox is the first roll here that is a NEW BROWSER rather than a rebuild: 130,934,199 B on the wire (+1,431,551) and 365,579,913 B across 71 files on disk (+3,458,122), with its own tree going 61 -> 63 files. BrowserProvisioner.FirstRunDownloadBytes moved with it. Evidence in docs/evidence/2026-09-21-provisioning-1246. A NEW ROW, 133, was added for the WebMCP finding.\n\nWHAT IS OWED AND IS SAID OUT LOUD RATHER THAN FAKED. Rows 26, 34, 38 and 85 are marked [STALE] in place with the reason on each. 26 is payload licensing as shipped, and firefox 156.0 is exactly the case where the terms inside omni.ja's license.html can change. 34 is the Firefox-against-Chromium cost ratios, where the Chromium side is the same binary and the Firefox side is a new browser -- the shape that moves a ratio. 38 is resume and relaunch timings under both families. 85 is the update lane's own numbers, every one of which is derived from a payload that moved 18,659,660 -> 18,697,570 bytes. Each needs a measurement session rather than a read, and none of them was adjusted from the previous figures.\n\nPREVIOUS ENTRY, 2026-09-17 @ 0.0.81:\nRe-reviewed at 0.0.81 on 2026-09-17, WITHOUT THE VERSION MOVING. A SUCCESSOR ENTRY, not a correction: everything below was true of 0.0.81 on 2026-09-15 and is still true of 0.0.81 today. What changed is what 0.0.81 RUNS. playwright-core is overridden to 1.64.0-alpha-2026-09-17 underneath it (see that entry), so this wrapper's cli.js, its tool surface and its config parsing all moved while the package itself did not -- which is a state this file has never recorded before and the reason this entry exists at all.\n\nWHAT THAT MEANS FOR THIS PACKAGE'S OWN ARTEFACTS. cli.js is a thin re-export, so `--file-paths` appears in its --help. config.d.ts is NOT: it is this package's file, it is unmoved, and it therefore DOES NOT DECLARE the `filePaths` key the child honours. That asymmetry is the one thing an override buys that an ordinary roll does not, and it is adopted with the asymmetry stated: config-schema.d.ts is the only one of the four snapshots that did not move, and its not moving is a fact about the wrapper rather than evidence that no config key arrived. The key was measured to round-trip through a running child instead.\n\nTHE DECLARED PIN IS THE EXIT. @playwright/mcp 0.0.81 still declares playwright-core 1.64.0-alpha-2026-09-14 in its own dependencies, read 2026-09-17, and that number is what the override's exit is measured against: the day this package publishes a version pinning 1.64.0-alpha-2026-09-17 or later, the override is deleted. Nothing about this package is being held back -- `latest` is still resolved from the dist-tag on every payload build, and 0.0.81 is what it returns.\n\nbrowser_emulate_media is the only surface change and is judged in tool-verdicts.json; nothing was declined, because nothing else moved.\n\nPREVIOUS NOTE, kept in full: Reviewed 0.0.80 -> 0.0.81 on 2026-09-15. Published 2026-09-14T21:25:54Z on dist-tag latest -- about two hours AFTER the review that adopted 0.0.80 was written, which is why two reviews of this package carry consecutive dates. 4 commits between the tags. WHAT UPSTREAM CHANGED IN ITS OWN TREE: 8 files, and UNLIKE 0.0.80 THE TWO DIFFS UPSTREAM-REVIEW.md CALLS THE HIGHEST-VALUE READS BOTH CAME BACK NON-EMPTY. tests/capabilities.spec.ts gains exactly two lines -- browser_webmcp_call and browser_webmcp_list in the core snapshot tool list -- and config.d.ts gains 13 lines and loses 3. The rest is packaging: .azure-pipelines/publish.yml, .github/workflows/publish.yml, README.md (generated by running the server, which is why it carries the new tools), package-lock.json, package.json, server.json. The product commits are 059ff380e 'chore: roll Playwright to 1.64.0-alpha-2026-09-14' (#1750) and the config.d.ts half that rides with it; 8a13ef8e9 'devops: restore npm publishing from GitHub Actions' (#1734), f559b9643 'chore: mark v0.0.81' (#1751) and e73d72e01 'chore(deps): bump the github-actions group' (#1747) are not. ALL FOUR SNAPSHOTS CHANGED, which has not happened before -- 0.0.80 moved two of the four. config-schema.d.ts ADJUDICATED, three changes, and this is THE SILENT CLASS so each is named: (1) outputDir's doc now says it governs only AUTOMATICALLY NAMED files and that a file with an explicit name resolves against the WORKSPACE ROOT instead; (2) a NEW KEY timeouts.idle appears; (3) imageResponses gains the value 'only' and its documented default is corrected from \"auto\" to \"allow\". NO key was renamed and NO key was removed, so the failure mode this snapshot exists for -- a renamed key discarded in silence by a bare JSON.parse -- did not occur. cli-help.txt ADJUDICATED: two flags ADDED (--idle-timeout, --profile-dir-name), two flags' help text CHANGED (--image-responses, --output-dir), ZERO removed -- the --output-mode class is clear. *** THERE IS NO --file-paths. *** PR microsoft/playwright#42673, which upstream set to close our ask microsoft/playwright#42497, is still OPEN and unmerged as of this review (last updated 2026-09-14T16:37:50Z), and #42497 is still open with no state_reason. So ask #1 is NOT resolved, no PLAYWRIGHT_MCP_FILE_PATHS exists, and there is nothing for the environment allowlist to judge. Recorded because the ABSENCE was checked rather than assumed. tools-list.json ADJUDICATED by diffing the two accepted snapshots entry by entry, schema included: the three resolvedFrom stamps and serverInfo.version moved with the versions; counts internalRegistry 80 -> 82, exposedMaximum 71 -> 73, defaultSurface 24 -> 26, skillOnly still 9. TWO TOOLS ADDED -- browser_webmcp_list (readOnlyHint true, empty schema) and browser_webmcp_call (readOnlyHint FALSE, destructiveHint TRUE, three parameters, and a description saying in upstream's own words that the tool output is page-provided and untrusted). ZERO removed, ZERO renamed. ELEVEN SURVIVING TOOLS CHANGED, each by EXACTLY ONE STRING and no other field: the filename parameter's description on browser_console_messages, browser_evaluate, browser_network_requests, browser_network_request, browser_pdf_save, browser_run_code_unsafe, browser_take_screenshot, browser_snapshot, browser_storage_state, browser_set_storage_state and browser_start_video. No type, no enum, no required list and no annotation moved anywhere. The order of the surviving tools is preserved, which is what lossless passthrough rests on, and the protocol ceiling is still 2025-11-25. *** THE ELEVEN DESCRIPTIONS ARE A DOCUMENTATION CORRECTION AND NOT A BEHAVIOUR CHANGE, AND THAT WAS ESTABLISHED BY MEASUREMENT AFTER READING SAID THE OPPOSITE. *** Reading the bundle first suggested nothing had moved at all: outputFile still resolves path.resolve(outputDir(options), fileName), outputDir is byte-identical, options.cwd occurs 9 times in both bundles and outputFile( 8 times in both. A probe was run anyway, with outputDir and the child's cwd deliberately set to DIFFERENT directories so that the answer could discriminate -- and an explicitly-named relative screenshot landed in the CWD, not in outputDir, falsifying the reading. The real path is resolveClientOutputFile, which sends an explicit suggestedFilename through workspaceFile (resolving against perCallWorkspaceDir ?? options.cwd) and only an auto-generated name through outputFile. THE SAME PROBE WAS THEN RUN AGAINST 0.0.80 AS A CONTROL AND BEHAVED IDENTICALLY -- explicit name to the cwd, auto name to outputDir -- and workspaceFile is byte-identical between the two bundles. So the behaviour has been this way for at least a version and upstream has now made its descriptions say so. The lesson is the repository's own rule: the reading was coherent, cited real byte-identical functions, and was wrong, because it had found A resolver rather than THE resolver. WHY IT REACHES NOTHING HERE, and it is by construction rather than by luck: BrowserAI launches the session child with its working directory set to <session>\\output (SessionManager) and writes outputDir as that SAME directory (BrowserConfiguration), so upstream's two allowed roots COINCIDE. Whichever base upstream resolves a relative name against, it is the session's own output directory. That design predates this bump and its comment already said so. The only genuine code change in that area is a HARDENING: checkFile now resolves symlinks on all three of output, workspace and the candidate before comparing (resolveSymlinks, 0 occurrences in the old bundle and 7 in the new), so a symlink leading out of the allowed roots is refused where it previously was not. Nothing of ours depended on that gap. browsers.json ADJUDICATED: chromium 1243 -> 1244 and chromium-headless-shell the same pair, browserVersion 153.0.8010.12 -> 154.0.8037.0; firefox 1542 -> 1544 with browserVersion UNCHANGED at 155.0, which is a rebuild of the same Firefox rather than a new one; webkit 2358 -> 2361, browserVersion 26.5 -> 26.6, its two mac revisionOverrides unchanged at 2251. ffmpeg 1011, winldd 1007 and android 1001 are unmoved, and no entry was added or removed. CONSEQUENCE, AND A RELEASE-NOTES LINE IS OWED AGAIN ONE DAY AFTER THE LAST ONE: every machine re-provisions, and a ROLLBACK RE-DOWNLOADS TOO. The test rig was re-provisioned as part of this batch -- chromium-1244 by the payload build and firefox-1544 by an explicit install-browser -- and THIS TIME NOTHING WAS PRUNED: PLAYWRIGHT_SKIP_BROWSER_GC=1 reached the payload build for the first time (added 2026-09-15, after the 0.0.80 rebuild deleted firefox-1539) and firefox-1542, chromium-1243 and chromium-1244 all survived both operations. That is the previous review's fix confirmed against the failure that motivated it. PAYLOAD: the mcp tree went 18,623,990 -> 18,619,618 b (-4,372) across 194 -> 196 files, still three packages, and no package in it declares an install script -- which the payload build asserts rather than assumes. node is UNMOVED at v24.21.0 and its entry is untouched. DECLINED, with reasons: (1) the whole devops half -- restoring npm publishing from GitHub Actions, and the dependabot github-actions group bump -- none of it is consumed here, because the payload build resolves from the npm registry by dist-tag and never touches upstream's CI; (2) the next dist-tag, which read 0.0.79-alpha-1787869812000 on this date, the same string it has read for two releases, and is deliberately not tracked; (3) server.json's version bump, which is the MCP registry's metadata and not something this product reads; (4) --profile-dir-name and its PLAYWRIGHT_MCP_PROFILE_DIR_NAME, which mean nothing without --extension -- BrowserAI never attaches to a running browser, and the variable could not reach the child in any case because ChildEnvironment is an allowlist. *** NOT ADOPTED, AND NOT ADOPTABLE BY THIS BATCH: THE TWO NEW TOOLS HAVE NO VERDICT. *** The same stop as the previous review, one version later, and the shape differs in two ways that matter to whoever judges them: these two are core rather than devtools, so they sit in upstream's DEFAULT surface, and browser_webmcp_call is the first tool ever to arrive here carrying destructiveHint TRUE together with a description stating that its own output is page-provided and untrusted. BrowserAI denies by default, so a call naming either is already refused at the door -- but an ABSENT row is a gap rather than a decision, so both are still advertised in tools/list, which is ToolVerdicts' deliberate design. tool-verdicts.json was LEFT UNTOUCHED, judgedAgainst included: moving that stamp to 0.0.81 / 1.64.0-alpha-2026-09-14 would assert that the file HAS been re-judged against this tool set, and it has not. The last complete judgement really was against 0.0.80, and the stamp says so. TWO FURTHER QUESTIONS ARE REPORTED RATHER THAN TAKEN, both new config keys and therefore product stances under this repository's written-rather-than-omitted doctrine: (a) timeouts.idle and (b) imageResponses: 'only'. Neither was written into the generated config, so today's behaviour is upstream's default in both cases and nothing changed. WHAT WAS MEASURED ABOUT timeouts.idle SO THAT THE DECISION IS NOT TAKEN ON A DOC STRING: config.d.ts says 'The CLI shuts the whole session down instead of relaunching', which read against a product whose sessions are the point is alarming. It does not apply here. defaultIdleTimeout is 60*60*1e3 = one hour; createBrowserWithInfo applies it only when ownership is own AND the browser is headless; and its action on the MCP STDIO SERVER PATH -- the one BrowserAI runs -- is info.browser.close(). The shuts-the-session-down sentence belongs to the separate playwright-cli session daemon, which BrowserAI does not run. Measured with --idle-timeout 4000 against a real child: after twice the timeout had elapsed with no tool call the node child was STILL ALIVE, and the next tool call answered normally in 354 ms with no error. That is the same shape as BrowserAI's own BrowserIdleTimer, whose shipped period is TEN MINUTES -- so BrowserAI's timer fires first, its close disposes upstream's through the disconnected handler, and upstream's hour is unreachable under the shipped configuration. RE-VERIFICATION rows a @playwright/mcp move puts in play, answered by name: row 11 (tool counts) RE-MEASURED off the accepted snapshot, 80/71/24 -> 82/73/26, and the DEFAULT moved this time where it did not last time, because the arrivals are core and core is unconditional; row 12 (per-capability breakdown) RE-MEASURED, config+vision+devtools 44 -> 46 and with storage added 61 -> 63, with core 21 -> 23 the only membership change and storage still 17; row 17 (PLAYWRIGHT_MCP_* count) RE-MEASURED WITH THE OLD BUNDLE AS A POSITIVE CONTROL, which returned 41 + 2 = 43 exactly as recorded, against 42 + 3 = 45 now -- and the row itself was found STALE at 42, a figure the 0.0.80 review moved in the kb article and not in the row, so the two disagreed for a day and the row now says so; row 19 (artifact prefixes) unchanged, the prefix set in the snapshot is identical. Rows 69 and the DECISIONS.md surface numbers are VERDICT-DEPENDENT and were deliberately NOT moved. CHECKED AND UNCHANGED, each read against BOTH bundles rather than assumed: the install-browser remediation template is BYTE-IDENTICAL, so ProvisioningRemediation's anchored regex and its install-browser marker still match; scaleImageToFitMessage is still ABSENT (0 occurrences in both, and the constant 1568 with it), so the server instructions' sentence that nothing downscales an inline image is still true and the 0.0.80 decision behind it still holds; imageResponses's effective default is unchanged (!== \"omit\" became ?? \"allow\" with === \"omit\", the same behaviour, and the \"auto\" in config.d.ts that it replaced was stale documentation rather than a live default); firstRootPath and allRootPaths and the MCP roots machinery are byte-identical, so how the workspace root is derived did not move; outputDir and outputFile are byte-identical. NO MODEL-FACING STRING CAME CLOSE TO THE BUDGET: the longest string anywhere in the new surface is 340 UTF-16 characters (browser_find's description) against the 2,048 budget, and the eleven changed filename descriptions run 108-208. *** WHAT WAS MEASURED ABOUT THE TWO NEW TOOLS, SO THE VERDICT IS NOT TAKEN ON THE SHAPE OF THEIR NAMES. *** This is the same discipline the 0.0.80 pair got (Q163 = d, measure blocking first), and it returns the OPPOSITE answer. (1) browser_webmcp_call BLOCKS. Measured against a real child on a page registering two WebMCP tools through navigator.modelContext, one resolving at once and one whose invokeTool returns a promise that never settles: the well-behaved one answered in 521 ms, and the never-settling one HAD NOT ANSWERED AFTER 45,002 ms. The session itself was unharmed -- the next navigate answered in 40 ms -- so it is the call that hangs rather than the server. The code says why: listWebMCPTools wraps its per-frame evaluate in withTimeout(kFrameTimeout = 5000), and the CALL path wraps nothing, so the await is bounded by whatever the page chooses to do. THAT IS THE browser_annotate LIVENESS SHAPE -- a call with no self-timeout waiting on something outside this product's control -- and it is reachable from any page rather than requiring a human at a dashboard. (2) browser_webmcp_list is fast and is an INJECTION CHANNEL BY DESIGN. It answered in 2 ms off a cache, and what it returned was the page's own tool names, descriptions and inputSchemas, under upstream's own heading that they are page-provided and untrusted. The listing is collected during snapshot capture and on tab switch rather than when the tool is called, which is why it is cheap and also why it is collected whether or not anybody calls it. (3) A CONSEQUENCE THAT SURVIVES A DENY, AND IS THE ONE THING A VERDICT CANNOT SWITCH OFF: renderTabHeader now emits a line `- N webmcp tool(s) available on the page` whenever the count is non-zero. It was observed on a plain browser_navigate in this probe -- `- 2 webmcp tools available on the page`. It carries the COUNT ONLY and none of the page's text, but it reaches the model through every tab header regardless of what tool-verdicts.json says about the two tools, so denying them yields a model told that N tools are available and a product that refuses both. That is the shape the unjudged-tool refusal text was corrected for on 2026-09-15, arriving by a different route, and it is reported here rather than acted on. *** ADJUDICATED 2026-09-15, LATER THE SAME DAY, BY THE MAINTAINER; EVERYTHING ABOVE THIS SENTENCE IS THE MEASURING AND EVERYTHING BELOW IT IS THE DECIDING. *** The review above stopped at three things no agent may settle: two verdicts and two config stances. All are now taken and landed, and the payload was RE-RESOLVED before they were: npm latest still reads 0.0.81 carrying playwright-core 1.64.0-alpha-2026-09-14, node still v24.21.0, ModelContextProtocol and Velopack still 2.2.0 and 1.2.0 read from nuget.org stable -- so the adoption is against exactly the versions this review measured and no delta was re-opened. (1) VERDICTS (Q176 = b). browser_webmcp_call is DENY, since 2026-09-15, and browser_webmcp_list is ALLOW. The deny's why is the refusal a caller reads and it names the measurement and the alternative: liveness, no timeout, 45 s measured, use the list and then the ordinary tools. ADOPTED because the measurement above is the browser_annotate argument with a wider door -- the annotation daemon needed a human at a dashboard and this needs only a page the agent was told to visit -- and DECLINED for the list, which upstream bounds at 5,000 ms and which is the only way to see what a page offers at all. This is the first build to withhold more than one tool; every count that rests on 'exactly one' moved with it, re-counted rather than incremented: DECISIONS' surface sentence (71/70 -> 73/71), SessionPolicyTests.Advertises (70 -> 71 with a new Withholds = 2 beside it), VerticalSliceTests (Names.Count + 70 -> + 71), ModelSurfaceTests' newly-granted base (60 -> 61, which moves by ONE because the pair was judged in two directions), ToolVerdictTests' denied count (1 -> 2, and now by name), RecordedCountTests' withheld (1 -> 2), kb tools-and-artifacts (70 of 71, one row -> 71 of 73), re-verification row 69, and the suite's own RepositoryVerdicts.TheOneDenial, which became TheDenials plus an ADenial chosen by oldest `since` rather than by file order. judgedAgainst was moved to 0.0.81 / 1.64.0-alpha-2026-09-14 in the same change, which is the stamp saying a human really re-judged rather than that a build resolved. REPORTED AND NOT CLOSED BY THE DENY, again, because it is upstream's: renderTabHeader's `- N webmcp tools available on the page` still reaches the model. It carries the count and none of the page's text. (2) timeouts.idle (Q177 = a): ADOPTED, WRITTEN EXPLICITLY AT UPSTREAM'S OWN DEFAULT of 3,600,000 ms, under the written-rather-than-omitted doctrine that already governs allowUnrestrictedFileAccess. It CANNOT FIRE -- BrowserAI's own BrowserIdleTimer is ten minutes and both are reset by a tool call -- and that is the reason to write it rather than a reason to omit it: an omitted key records no decision, browser_get_config cannot read back a key the file never carried, and the day upstream moves its default the change would arrive as behaviour nobody chose. It is in BrowserConfiguration.RequiredSessionOpinions, so the generator dropping it is red, and ConfigRoundTripTests requires the running child to hand it back. The alarming sentence in config.d.ts -- 'The CLI shuts the whole session down instead of relaunching' -- belongs to the playwright-cli daemon and was measured above to be false of the MCP stdio path this product runs. The one thing the single value costs is stated rather than hidden: upstream defaults a HEADED browser to no timeout, so writing one value gives a headed session an hour it would not otherwise have, still unreachable behind ten minutes. (3) imageResponses: 'only' (Q178 = a): DECLINED, and the decline is the record. BrowserAI goes on writing 'allow'. 'only' would drop the text part of any response carrying an image, which is the part naming the file that was written, the page that was on screen and the events line -- so it trades the whole of a response's context for the tokens of a caption. Nothing about it is new behaviour: the key's effective default is unchanged, and no config this product writes ever asked for the old 'auto' that upstream's stale docs described. Re-open it only if a caller is ever measured to be paying for the text part of an image response, which nothing has been. (4) --profile-dir-name / PLAYWRIGHT_MCP_PROFILE_DIR_NAME: adjudicated CLOSED by the review itself rather than by the maintainer, and it stays closed. It means nothing without --extension, which BrowserAI never uses, and ChildEnvironment is an allowlist so the variable cannot reach the child in any case. It is nonetheless counted: it is the third PLAYWRIGHT_MCP_* variable outside the config mapping, which is why re-verification row 17 moved 42 -> 45 with a positive control on the previous bundle. *** STILL OWED, AND OWED HONESTLY. *** Re-verification rows 21, 34, 38 and 85 are NOT answered by this adoption and were not faked to close it. Each needs a measurement session against the newly-resolved browsers (chromium 1244, firefox 1544, webkit 2361) rather than a reading, and a release may be cut with them owed exactly as the 0.0.80 review was: they are marked in the index as what they are. The recording API rename noticed above (_enableRecorder -> _startRecording) is in the same class -- a re-measurement is owed if anything ever leans on it, and nothing does today."
    },
    "playwright-core": {
      "reviewed": "1.64.0-alpha-1789764292000",
      "date": "2026-09-21",
      "source": "https://github.com/microsoft/playwright",
      "why_it_matters": "Carries the actual tool implementations and the browsers.json that pins the Chromium revision. Arrives as @playwright/mcp's own exact dependency, not as a choice we make.",
      "snapshots": {
        "tools-list.json": "changed, and ADJUDICATED IN THE @playwright/mcp ENTRY FOR THE SAME ROLL, which is where the diff is visible: the two WebMCP tools became skillOnly and tools/list became dynamic and page-driven. It is recorded here because the IMPLEMENTATION is in this package -- packages/playwright-core/src/tools/backend/webmcp.ts -- and one half of it is a product stance the maintainer took, not a passthrough.",
        "cli-help.txt": "changed, and ADJUDICATED IN THE @playwright/mcp ENTRY FOR THE SAME ROLL. That package's cli.js is what this snapshot is captured from; this package is what cli.js re-exports. PLAYWRIGHT_MCP_WEBMCP is this package's one new environment variable, read inside configFromEnv, taking the count 46 -> 47 with the outside set unchanged at three -- re-verification row 17, re-measured with the previous bundle as the positive control.",
        "config-schema.d.ts": "changed, and ADJUDICATED IN THE @playwright/mcp ENTRY FOR THE SAME ROLL: config.d.ts ships with that package. What THIS package contributed is the filePaths implementation the wrapper's typings have now caught up with. NOT TO BE CONFUSED WITH THIS BUMP'S HEADLINE: 'what moved behind an IDENTICAL SCHEMA' is about the TOOLS' JSON schemas, which did not move, and not about the config typings, which did.",
        "browsers.json": "changed, read out of the rebuilt payload's own browsers.json: chromium 1245 -> 1246 at the same browserVersion 154.0.8037.0, firefox 1548 -> 1549 with 155.0 -> 156.0, webkit 2361 -> 2365 at the same 26.6, ffmpeg 1011 and winldd 1007 unmoved. ADOPTED. Re-verification row 21 was re-measured in this batch rather than left owed; rows 26, 34 and 38 are marked [STALE] and owed, with the reason on each."
      },
      "notes": "Reviewed 1.64.0-alpha-2026-09-17 -> 1.64.0-alpha-1789764292000 on 2026-09-21. THE HOW IS THE HEADLINE: this version arrives through @playwright/mcp 0.0.82's OWN EXACT DEPENDENCY, which is the ordinary way, and the entry below records the one build in this project's history that did not. THE DATED OVERRIDE IS RETIRED. build/payload/package.json carries no `overrides` block, the payload resolves whatever the wrapper pins, and DECISIONS.md's 'The two exceptions to the versioning policy' records the second exception as ENDED by addition -- opened 2026-09-17, ended 2026-09-21 -- with the sentence that neither is a precedent left standing.\n\nTHE SHAPE OF THE VERSION IS THE ONE THING WORTH CARRYING FORWARD. 1789764292000 is epoch milliseconds for 2026-09-18T20:44:52Z, and the package was published 2026-09-18T20:49:55.524Z. Upstream's daily cadence is unchanged and is still alpha-YYYY-MM-DD -- 1.64.0-alpha-2026-09-18 through -09-21 were all published at about 05:2x -- so the wrapper pinned an OUT-OF-BAND build from a different shape family. Nothing in this tree orders these strings any more, deliberately, so the shape costs nothing; it is recorded because the next reader of a version here should not assume a date can be parsed out of it.\n\nWHAT WAS ADOPTED, measured against the resolved tree rather than read off the release: the --file-paths fix the override existed for now arrives by the wrapper's own pin. lib/coreBundle.js is 3,551,719 bytes against the override's 3,536,648, PLAYWRIGHT_MCP_FILE_PATHS appears TWICE in it, `file-paths` 3 times and `filePaths` 8 -- identical to the override's own bundle, against 0 and 2 in 1.64.0-alpha-2026-09-14. The negative control for that grep is that a deliberately-wrong needle returns nothing from the same file, and the only other place in the tree carrying the name is @playwright/mcp's README, which documents --file-paths for the first time.\n\nWHAT MOVED BEHIND AN IDENTICAL SCHEMA, and it is this bump's real content: the WebMCP tools became skillOnly, tools/list became dynamic and page-driven, and page-authored text began arriving in every snapshot-bearing tool result. That is adjudicated in full in the @playwright/mcp entry, because both changes are visible on that package's own wire and one of them is a product stance for the maintainer. It is recorded here too, because the implementation is in THIS package: packages/playwright-core/src/tools/backend/webmcp.ts.\n\nBROWSER REVISIONS MOVED WITH IT, read out of the rebuilt payload's own browsers.json: chromium 1245 -> 1246 at the same browserVersion 154.0.8037.0, firefox 1548 -> 1549 with 155.0 -> 156.0, webkit 2361 -> 2365 at the same 26.6, ffmpeg 1011 and winldd 1007 unmoved. Re-verification row 21 was re-measured in this batch rather than left owed; rows 26, 34 and 38 are marked [STALE] and owed, with the reason on each.\n\nTHE winldd NO-OP IS UNCHANGED, which is the one thing UPSTREAM-REVIEW.md tells a reviewer to watch in this package's diff: the bundle still passes [\"chrome-win\"] to _validateHostRequirements for Chromium while Chromium extracts to chrome-win64, so the check is still a permanent no-op and this roll brought no cold-start latency with it. Re-verification row 10.\n\nPLAYWRIGHT_MCP_WEBMCP IS THE ONE NEW ENVIRONMENT VARIABLE, read inside configFromEnv, taking the count 46 -> 47 with the outside set unchanged at three. Row 17, re-measured with the previous bundle as the positive control.\n\nPREVIOUS ENTRY, 2026-09-17 @ 1.64.0-alpha-2026-09-17:\nReviewed 1.64.0-alpha-2026-09-14 -> 1.64.0-alpha-2026-09-17 on 2026-09-17. THIS IS A SUCCESSOR ENTRY AND NOT A CORRECTION OF THE ONE BELOW, which said truthfully of 0.0.81 that THERE IS NO --file-paths. There is one now, and nothing in that sentence has been rewritten.\n\nHOW THIS VERSION ARRIVES IS THE WHOLE OF WHAT IS UNUSUAL. @playwright/mcp `latest` is still 0.0.81 and still pins playwright-core 1.64.0-alpha-2026-09-14 EXACTLY, so no ordinary resolution reaches this build. build/payload/package.json carries an npm `overrides` entry that pulls it forward, taken by the maintainer on 2026-09-17 (Q210 = a) as a DATED EXCEPTION WITH A WRITTEN EXIT: the entry is deleted the day @playwright/mcp `latest` pins a playwright-core at or above 1.64.0-alpha-2026-09-17, and two independent instruments go red on that day -- PayloadTests.TheDatedPlaywrightCoreOverrideIsStillNeeded off the committed lock, and build/Build-Payload.ps1 off the live resolution. Both were planted red before this was landed. The exception is recorded beside the SQLite one in DECISIONS.md, 'The two exceptions to the versioning policy', which says explicitly that it is a different KIND of exception and that neither is a precedent for a third.\n\nWHAT WAS ADOPTED. (1) --file-paths=absolute, the `filePaths` config key and PLAYWRIGHT_MCP_FILE_PATHS, from PR microsoft/playwright#42673, merged 2026-09-16 and closing microsoft/playwright#42497 -- THIS PROJECT'S OWN ASK, filed 2026-08-27 and transferred upstream by upstream. It is the entire reason the override was taken. BrowserAI now writes filePaths: 'absolute' explicitly in every generated child config, under the written-rather-than-omitted doctrine already used for `timeouts.idle` and `allowUnrestrictedFileAccess`: upstream's default is `relative`, and a stance taken by silence is a stance nobody can find. The key is in BrowserConfiguration.RequiredSessionOpinions so a generator that drops it is red, PLAYWRIGHT_MCP_FILE_PATHS is in ChildEnvironment.Refused because the generator owns the decision, and ConfigRoundTripTests makes the running child hand the key back. (2) browser_emulate_media, ONE new tool, capability `core` and so in upstream's default surface, judged `allow` by the maintainer (Q209 = a) with its reasoning in tool-verdicts.json's own row. (3) The browser revisions that came with it: chromium 1244 -> 1245 and firefox 1544 -> 1548. Nothing was declined at this version, because there is nothing else in it.\n\nTHREE OF THE FOUR SNAPSHOTS MOVED AND THE FOURTH DID NOT, AND THE FOURTH IS THE FINDING. tools-list.json: exactly one tool added and none removed, renamed or reshaped -- the survivors' JSON is byte-identical and their order is preserved; internalRegistry 82 -> 83, exposedMaximum 73 -> 74, defaultSurface 26 -> 27, skillOnly unchanged at 9, and declaredCapabilities, capabilitiesCarryingNoTool, unconditionalCapabilities, serverCapabilities, the protocol probe and artifactPrefixes all unchanged. cli-help.txt: four lines, the `--file-paths <mode>` option and its wrapped description. browsers.json: chromium and chromium-headless-shell 1244 -> 1245 at the same browserVersion 154.0.8037.0, firefox 1544 -> 1548 at the same browserVersion 155.0, webkit unmoved at 2361, ffmpeg 1011 and winldd 1007 unmoved. config-schema.d.ts DID NOT MOVE, AND THAT IS NOT AN ABSENCE OF CHANGE: config.d.ts ships with @playwright/mcp, which is still 0.0.81, while the option is implemented in playwright-core, which is what the override moved. So the `filePaths` key is NOT DECLARED in the typings this project diffs. Adopted with that stated rather than worked around -- loadConfig is a bare JSON.parse with no schema validation, so the typings were never what made a key work, and the key was MEASURED to round-trip instead: a real child started with filePaths: 'absolute' hands it straight back from browser_get_config, and the bundle's own config key TYPE MAP carries the pair filePaths -> string. The day the wrapper rolls, config-schema.d.ts will move on its own and that diff is a confirmation rather than a change.\n\nWHAT THE FIX ACTUALLY DELIVERS, MEASURED RATHER THAN READ OFF THE PR. The mechanism is exactly two call sites, which is what the ask itself predicted: Response._printablePath returns path.resolve(fileName) when filePaths is absolute, and the snapshot renderer is passed logRelativeTo = undefined instead of the client workspace. Every pointer shape this project cares about goes through one of them, and the measurement is in kb/playwright/tools-and-artifacts.md with the rig at docs/probes/2026-09-17-file-paths/. All the shapes the ask named are ABSOLUTE: the screenshot, PDF and storage-state links, the snapshot link, the console log link (both the `filename` form and the `#L1-L2` pointer in the Events section), the download line, the binary response body line and the trace links. The two the PR body did not name -- binary body lines and trace links -- are covered too. ONE SHAPE WAS NOT DRIVEN: the paused-debugger location, which the PR body did name. It is the fourth _printablePath call site and so is covered by construction, and that is a reading of the bundle rather than a measurement; it is recorded as such and not as a result.\n\nTHE RE-VERIFICATION ROWS, ANSWERED BY NAME. Row 17 (PLAYWRIGHT_MCP_* count) RE-MEASURED with the previous bundle as the positive control, as the row itself requires: 1.64.0-alpha-2026-09-14 returned 42 inside the mapping + 3 outside = 45, exactly what the row recorded, and 1.64.0-alpha-2026-09-17 returns 43 + 3 = 46. The single addition is PLAYWRIGHT_MCP_FILE_PATHS, read inside configFromEnv, so the OUTSIDE set is unchanged at three and still PING_TIMEOUT_MS, EXTENSION_TOKEN and PROFILE_DIR_NAME. Rows 21, 34 and 38 are OWED AGAIN and are NOT re-measured in this batch: all three trigger on a browser revision move, chromium moved 1244 -> 1245 and firefox 1544 -> 1548, and re-running them is a measurement session rather than a gate. Saying so is the answer; adjusting the numbers would not be. Row 10 (the winldd chrome-win / chrome-win64 mismatch) was re-read against the new bundle and is UNCHANGED, so Chromium's dependency check is still a permanent no-op and no latency regression arrived with this move. Nothing else in the index triggers on a playwright-core move that this version touched.\n\nPREVIOUS NOTE, kept in full: Reviewed 1.63.0-alpha-2026-08-31 -> 1.64.0-alpha-2026-09-14 on 2026-09-15. A MINOR LINE MOVE rather than another daily alpha of the same line, which is a wider door than the last roll was, and it arrives as @playwright/mcp 0.0.81's own EXACT dependency -- there is no intermediate version this build could resolve. THE RESOLUTION TRAP FIRED FOR A THIRD TIME AND IN A THIRD DIRECTION: npm latest for playwright-core read 1.63.0 on this date, a STABLE release that is now BEHIND the alpha that actually ships, because the pin crossed into the 1.64 line. On 2026-08-15 latest was older than the pin, on 2026-09-14 it was newer, and today it is older again -- no reading of latest is safe in either direction, which is exactly what drift-check.json's _how_to_resolve table exists to prevent. It was not taken; the version was read out of 0.0.81's own dependencies, and the payload build asserts that upstream's pin is still exact and would throw if it had been loosened to a range. HOW IT WAS READ: the resolved payload's own bundle was diffed against the previous version's published tarball, because the bundle is what runs. coreBundle.js 3,469,825 -> 3,503,383 b (+33,558), 645 hunks. FOUND, each adjudicated: (1) TWO TOOLS ADDED -- browser_webmcp_list and browser_webmcp_call, plus the machinery behind them (listWebMCPTools, Tab.updateWebMCPTools, a webmcpToolCount field in the tab header, and a WebMCP refresh on tab switch and on snapshot capture). They are upstream's, they arrive here, and they have NO VERDICT; see the @playwright/mcp entry, which owns that stop. (2) THE ELEVEN `filename` DESCRIPTIONS NOW NAME THE WORKSPACE ROOT, AND THE BEHAVIOUR DID NOT CHANGE. Established by measurement, not by reading -- and the reading had said the opposite. outputDir, outputFile, workspaceFile, firstRootPath and allRootPaths are ALL byte-identical between the two bundles, and a probe with outputDir and the child's cwd pointed at DIFFERENT directories put an explicitly-named relative screenshot in the cwd under BOTH versions, with an auto-named one going to outputDir under both as the positive control. So this is upstream's documentation catching up with code that already behaved this way. BrowserAI is unaffected by construction: it writes the child's working directory and outputDir as the SAME directory, <session>\\output, so the two allowed roots coincide. (3) checkFile IS HARDENED, and it is the one real change on that path. It now resolves symlinks on the output root, the workspace root AND the candidate before comparing -- resolveSymlinks occurs 0 times in the old bundle and 7 in the new -- so a name that escapes the allowed roots through a symlink is refused where it previously was not. ADOPTED AS A STRICT IMPROVEMENT with nothing to do on this side: BrowserAI's containment IS upstream's file-access roots (its own filename gate was deleted on 2026-08-26 as a weaker duplicate), so a tightening there is a tightening here, and nothing in this tree depended on the gap. (4) A NEW IDLE TIMER. defaultIdleTimeout = 60*60*1e3 (one hour) and an IdleTimer class arrive in tools/backend/idleTimer.ts, reachable as config timeouts.idle, --idle-timeout and PLAYWRIGHT_MCP_IDLE_TIMEOUT. createBrowserWithInfo arms it ONLY when ownership is own AND launchOptions.headless, which is BrowserAI's shape, and its callback on the MCP stdio server path is info.browser.close() -- NOT a session shutdown. config.d.ts's sentence that 'The CLI shuts the whole session down instead of relaunching' describes the separate playwright-cli session daemon, which this product does not run. MEASURED rather than deduced from that, with --idle-timeout 4000 against a real child: after twice the timeout with no tool call the node child was still alive, and the next tool call answered in 354 ms with no error. NO PRODUCT CHANGE TAKEN: BrowserAI's own BrowserIdleTimer closes the browser after TEN minutes and upstream disposes its timer on the disconnected event, so upstream's hour is unreachable under the shipped configuration. Whether to write timeouts.idle explicitly is a product stance under the written-rather-than-omitted doctrine and is REPORTED, not decided. (5) imageResponses GAINS 'only', AND THE EFFECTIVE DEFAULT DID NOT MOVE. The guard was `config.imageResponses !== \"omit\"` and is now `const imageResponses = config.imageResponses ?? \"allow\"` with `imageResponses === \"omit\" ? [] : ...`, which is the same behaviour for an unset key; 'only' additionally drops the text part when a response carries an image and is not an error. config.d.ts's old '\"auto\", which sends images if the client can display them' was STALE DOCUMENTATION rather than a live default -- the CLI help has said 'allow' throughout -- so this is a doc correction on upstream's side and not the changed-default class. BrowserAI does not write the key, so nothing moved. Whether to write it, and whether 'only' is ever wanted, is REPORTED, not decided. (6) --profile-dir-name AND PLAYWRIGHT_MCP_PROFILE_DIR_NAME ADDED. Row 17 RE-MEASURED rather than incremented, with the old bundle as the positive control: the same predicate returned 41 in the config mapping + 2 outside = 43 against the old pin, exactly the recorded figure, and 42 + 3 = 45 against the new. PLAYWRIGHT_MCP_IDLE_TIMEOUT joins the configFromEnv mapping; PLAYWRIGHT_MCP_PROFILE_DIR_NAME is read straight off process.env in the resolver that picks the --extension channel, so the OUTSIDE set grew for the first time since it was recorded. No product change: the child environment is an ALLOWLIST, so a variable nobody named is absent by construction, and neither was added to ChildEnvironment.Refused because neither redirects a decision the config generator took. (7) THE RECORDING API WAS RENAMED UNDERNEATH THE TWO TOOLS ADOPTED YESTERDAY. browserContext._enableRecorder/_disableRecorder (4 occurrences, old) became _startRecording/_stopRecording (3 occurrences, new), and the mode/recorderMode/omitCallTracking arguments are gone in favour of language alone. browser_start_recording and browser_stop_recording still work -- they move in lockstep with the core they are pinned to, and the guard that throws 'Recording requires a newer version of Playwright' now tests the new name. RECORDED RATHER THAN RE-MEASURED, and stated as such: yesterday's decision to allow those two rested on a measurement (non-blocking, 40-46 ms, other tools answer during a recording) taken against the OLD implementation, and the implementation beneath them has been replaced. The tools' schemas are byte-identical and nothing observable changed, so no arm is red and nothing was adjusted; a re-measurement is OWED if that decision is ever leaned on again. CHECKED AND UNCHANGED, each read against BOTH bundles rather than assumed: winldd's chrome-win against Chromium's chrome-win64 is still the permanent no-op (row 10, 2 occurrences of each in both), so no new per-cold-start validation cost arrived; outputMaxSize still has no default (row 8); setupExitWatchdog is BYTE-IDENTICAL and gracefullyCloseAll's occurrence count is unmoved (row 36); the cdn.playwright.dev mirror list and the /dbazure/download/playwright prefix are unchanged (row 72); redactSecrets is still a substring replaceAll over the whole serialised response (row 113); the dashboard's headedness is still gated on PWTEST_DASHBOARD_APP_BIND_TITLE, so browser_annotate's refusal is still earned (row 94); the chromiumSandbox resolution chain and the launcher's --no-sandbox push are unmoved in both occurrence count and shape (rows 3 and 102 -- the fix that ARRIVED in the last review has not been reverted, and BrowserAI is still sandboxed on both sides because it passes --sandbox on the command line); the install-browser remediation template is BYTE-IDENTICAL, so ProvisioningRemediation's anchored regex and its install-browser marker still match; scaleImageToFitMessage is still ABSENT and the constant 1568 with it, so the deletion adopted in the last review has not been reverted and the server instructions' 'nothing downscales it' sentence remains true. DECLINED, with the reason: dropping BrowserAI's own ten-minute BrowserIdleTimer now that upstream ships one. Upstream's is an hour, is a default rather than a contract, has just been introduced and could move again, and closes the browser without writing the session record BrowserAI's own close writes. Two timers where the shorter one always wins is belt and braces; deleting ours would make this product's session lifecycle rest on a default that has existed for one day. RE-VERIFICATION rows a playwright-core move puts in play, answered by name: rows 3 and 102 -- UNCHANGED, the arrived sandbox fix is still in place, re-read against both bundles; row 10 -- UNCHANGED, still a permanent no-op; row 8 -- UNCHANGED; row 17 -- RE-MEASURED with a positive control, 43 -> 45, and the row was also found STALE at 42 and corrected; row 36 -- UNCHANGED, setupExitWatchdog byte-identical; row 72 -- UNCHANGED; row 94 -- UNCHANGED; row 113 -- UNCHANGED; rows 11 and 12 -- RE-MEASURED off the accepted snapshot and answered in the @playwright/mcp entry; rows 13, 14, 20, 22, 67 and 106 are unchanged by inspection of the same anchors. ROWS 21, 34, 38 AND 85 ARE OWED AND WERE NOT RUN, and they were owed before this bump: 21 (payload and browser sizes) and 34 (Firefox cost ratios) and 38 (resume cost) all trigger on a browser revision move, and BOTH revisions moved AGAIN -- chromium 1243 -> 1244 and firefox 1542 -> 1544 -- so the figures those rows carry are now two revisions stale rather than one. Nothing was adjusted and no number was carried forward as though it still held; re-running them is a measurement session rather than a gate. The two PAYLOAD figures in row 21 were re-measured by this build (node.exe unchanged at 93,580,104; the mcp tree 18,623,990 -> 18,619,618). browsers.json is adjudicated in the @playwright/mcp entry, which is where the snapshot lives."
    },
    "ModelContextProtocol": {
      "reviewed": "2.2.0",
      "date": "2026-08-14",
      "source": "https://github.com/modelcontextprotocol/csharp-sdk",
      "why_it_matters": "The protocol implementation on both sides of the proxy. Its typed layer drops unknown properties and throws on unknown content types, which is correct for a client and lossy for a proxy.",
      "snapshots": {
        "tools-list.json": "unchanged, and NOT IN PLAY: the C# SDK ships no tools of its own and feeds none of the four golden snapshots. Baseline entry, 2026-08-14: nothing had moved when it was written.",
        "cli-help.txt": "unchanged, and NOT IN PLAY: the SDK has no CLI in this build. Baseline entry, 2026-08-14.",
        "config-schema.d.ts": "unchanged, and NOT IN PLAY: the typings are @playwright/mcp's. Baseline entry, 2026-08-14.",
        "browsers.json": "unchanged, and NOT IN PLAY: browser revisions are playwright-core's. Baseline entry, 2026-08-14."
      },
      "notes": "Baseline. Tier 1 SDK under the Linux Foundation's Agentic AI Foundation; day-to-day engineering by the Microsoft .NET team. Declares IsAotCompatible=true on every non-netstandard2.0 target -- the author's claim about their code, not a proof for our usage. Full 2026-07-28 protocol support. Note 2.x drops MTP v1 in the 4.0.0 line of xUnit only; unrelated to this package."
    },
    "Velopack": {
      "reviewed": "1.2.158",
      "date": "2026-09-22",
      "source": "https://github.com/velopack/velopack",
      "why_it_matters": "Owns the update path, which is the most hazard-dense section of the charter. Five of its nine documented landmines were hit in production by a shipped product examined for this project rather than avoided.",
      "snapshots": {
        "tools-list.json": "unchanged, and NOT IN PLAY. From the note, verbatim: Velopack ships no tools and feeds none of tools-list.json, cli-help.txt, config-schema.d.ts or browsers.json.",
        "cli-help.txt": "unchanged, and NOT IN PLAY: see tools-list.json. The vpk CLI is a build-time tool and is not what this snapshot captures.",
        "config-schema.d.ts": "unchanged, and NOT IN PLAY: see tools-list.json.",
        "browsers.json": "unchanged, and NOT IN PLAY: see tools-list.json."
      },
      "notes": "Reviewed 1.2.0 -> 1.2.158 on 2026-09-22, end to end per UPSTREAM-REVIEW.md, after `dotnet restore --force-evaluate` moved all five committed locks. 1.2.158 is a STABLE release published 2026-09-21T22:37:04Z; 1.2.0 was published 2026-06-03T23:39:49Z. 158 commits, and the compare's file list is capped by GitHub at 300. ADOPTED. Nothing in this repository's PRODUCT code changed to absorb it and nothing was pinned back; ONE TEST DID, and it is item D below arriving as a red gate run rather than as a prediction. *Corrected 2026-09-22, an hour after this entry was first written (previously \"Nothing in this repository changed to absorb it and nothing was pinned back\", which was written before the gate had been run and was false by one file).* `RealInstallerTests.TheSuitesPackAndTheShippingPackDifferOnlyWhereTheIdAppears` normalises the pack id out of each entry's NAME before comparing the two packages, and velopack#985 moved the embedded stub's name from `mainExe` to `packTitle ?? packId` -- so `lib/app/BrowserAI_ExecutionStub.exe` in the shipping pack now faces `lib/app/BrowserAI (suite)_ExecutionStub.exe` in the suite's, and the name sets stopped matching. The normalisation was widened to that ONE entry, keyed on the `_ExecutionStub.exe` suffix rather than on the title wherever it appears, for the same reason the content rule refuses the shipping title: `BrowserAI` is in every binary's name, so stripping it would collapse `BrowserAI.exe` and `BrowserAI.Server.exe` into one key in one pack and leave them alone in the other. Two controls were added in both directions. THE SHIPPING PACKAGE IS UNAFFECTED: its title and its mainExe base are both `BrowserAI`, so its stub is still `BrowserAI_ExecutionStub.exe`, verified by reading both archives' entry lists. AND THE RENAME ARRIVES IN A SECOND PLACE, which the same arm found on the next run: `[Content_Types].xml`. `vpk` emits one declaration per extension in FIRST-SEEN order, so the pack whose stub is `BrowserAI (suite)_ExecutionStub.exe` meets an `.exe` before its first `.xml` and the shipping pack does not -- `...,ico,exe,xml,...` against `...,ico,xml,exe,...`, the same 24 declarations in the same 1,692 bytes, different order. THAT IS NOT NONDETERMINISM AND IT WAS CHECKED RATHER THAN ASSUMED: every shipping pack on this machine, 20 of them across both Velopack versions and back to 0.1.1, hashes that part to the same value, and the suite's pack of the same run is the only outlier. So this part is now compared as a SET of declarations rather than as bytes, for that one entry, with controls in three directions -- re-ordered is not an offence, a missing declaration is, and an empty read is never 'the same'.\n\nBETWEEN THE TWO THERE IS EXACTLY ONE RELEASE OBJECT IN ANY FORM, which is why this is one review rather than the split UPSTREAM-REVIEW.md prescribes for a large jump: the 1.2.110-ge826545 PRE-RELEASE of 2026-07-16. The patch component is a build counter rather than a patch level, so 1.2.0 -> 1.2.158 is 158 builds and one shipped version, not 158 versions nobody reviewed.\n\nTHE CLI SURFACE WAS DIFFED BY RUNNING BOTH VERSIONS, NOT BY READING THE CHANGELOG. `vpk --help` and `vpk pack --help` were captured at 1.2.0 before `dotnet tool update -g vpk --version 1.2.158` and again after. NO FLAG THIS PROJECT PASSES WAS REMOVED OR RENAMED: --packId, --packVersion, --packDir, --packTitle, --packAuthors, --channel, --outputDir, --icon, --mainExe and --shortcuts are all present with the same meanings. Three real surface changes, none of which reach us: (1) boolean options stopped declaring a value token, so 1.2.0's `--legacyConsole, -x <legacyConsole>` is now `--legacyConsole, -x` -- this matters only because build/New-Release.ps1 reads the tool version out of `vpk --help --legacyConsole`, and that call was re-run at 1.2.158 and still returns the banner, which is how the version check passed; (2) --msiBanner and --msiLogo became --msiTopBanner and --msiDialogBackground (velopack#1009), INERT HERE because --msi is never passed and never will be, for the reason in DECISIONS: --msi PerMachine installs to Program Files and makes the updater self-elevate, and a UAC prompt cannot be answered by a background MCP server; (3) four new top-level commands -- login, logout, publish, flow -- which is Velopack Flow (velopack#999).\n\nDECLINED, WITH REASONS, SO THAT THE NEXT BUMP DOES NOT RE-LITIGATE THEM.\n\n1. VELOPACK FLOW, the hosted release service now a first-class update source across every client library (#999). Declined. Releases here go to GitHub Releases through `gh`, the feed is a release asset, and the whole update lane is built on that. Adopting a hosted service would add an account, a credential and a second publish path to the area the charter calls the most hazard-dense in the product, in exchange for nothing this project lacks.\n\n2. THE WINDOWS INSTALLER CHANNEL-OVERRIDE TAG READERS (#987), which let Setup.exe and the MSI carry a channel that patches the manifest at install time. Declined. There is ONE track and no beta channel (DECISIONS -> Update tracks), and landmine 1 requires the channel to be set explicitly rather than composed; a tag that can repoint an installer's channel is a second way to set it and a second way to get it wrong. Upstream's own new tests are the clearest statement of what it does: `commands::install` grew a fourth parameter and `test_install_channel_override_patches_manifest` asserts the nuspec's <channel> is rewritten in `current\\sq.version`.\n\n3. THE --delta MODE stays at its default. #1010 removed the bsdiff fallback, so zstd is the only delta patch format; that is what these packs were already getting and the measurement below says the cost did not move.\n\nADOPTED AS BEHAVIOUR, BECAUSE IT ARRIVES WITH THE PACKAGE AND THERE IS NOTHING TO CONFIGURE. Each is measured here rather than taken from the release notes.\n\nA. THE WINDOWS BOOTSTRAPPER IS 64-BIT NOW (#944). `vpk pack` ships setup_x64.exe and stub_x64.exe for a win-x64 target instead of the i686 ones. MEASURED on the packed artifact: PE machine Amd64, and Setup.exe is 62,585,857 b against the 59,490,421 b this project last recorded, +3,095,440 (+5.2%). Upstream's motivation is that a 32-bit Setup.exe memory-maps its embedded package and caps isize at ~2 GiB; our package is 55 MB, so the fix buys us nothing and the size is what it costs. It is adopted because pinning back is not available under the versioning policy and because a 64-bit bootstrapper is the direction upstream is going, not because anything here needed it.\n\nB. EstimatedSize IS A REG_DWORD (#1051). Velopack wrote `folder_size / 1024` as a u64, which Windows stores as REG_QWORD and then ignores, so Add/Remove Programs showed no size at all. 1.2.158 saturates to u32. MEASURED against the maintainer's own live install on 2026-09-22: HKCU\\...\\Uninstall\\BrowserAI.app carries EstimatedSize as REG_QWORD 140209 today, which is exactly the defect, so the next install or update is the one that starts showing ~137 MB where Windows showed nothing. This is the only change in the bump that a user will SEE without being told to look. *Added 2026-09-24*: THE LIVE INSTALL HAS BEEN REWRITTEN, AND THE VALUE IS A REG_DWORD NOW. HKCU\\...\\Uninstall\\BrowserAI.app read EstimatedSize REG_QWORD 140209, InstallDate 20260917 and DisplayVersion 1.0.0 in the clearance reading taken at 06:57:31Z on 2026-09-24, and EstimatedSize REG_DWORD 140351, InstallDate 20260924 and DisplayVersion 1.1.0 in the one taken at 09:01:46Z, and it read the same in all 19 clearance readings of the re-run below. So the installed 1.0.0 applied its update to 1.1.0 between those two readings, and the value's type says which code wrote the key: 1.2.158's registry.rs saturates the size into a u32 (lines 21-24, #1051), while 1.2.0's apply writes the same key through the same update_uninstall_entry as a u64. The rewrite therefore ran 1.2.158's code. Whether Add/Remove Programs now displays the size was not looked at: that is a screen, and nobody read it. Both bracketing readings are kept in docs/evidence/2026-09-24-velopack-rows/.\n\nC. UNINSTALL SHOWS A PROGRESS DIALOG (#1011). `Update.exe --uninstall` ran with no UI until the completion dialog; it now shows an indeterminate xdialog progress window for the duration, across all 39 locales, SUPPRESSED BY --silent as before. The suite uninstalls with --silent and is unaffected; a human uninstalling from Add/Remove Programs now sees it.\n\nD. THE EMBEDDED STUB IS NAMED FROM packTitle RATHER THAN mainExe (#985, fixing #982). THE REAL PACK IS UNAFFECTED AND THE SUITE'S PACK IS NOT, and that asymmetry is the thing to know. For the shipping pack packTitle is `BrowserAI` and mainExe is `BrowserAI.exe`, so both naming rules produce `BrowserAI.exe` and nothing moves. For the suite's own pack packTitle is `BrowserAI (suite)` and mainExe is still `BrowserAI.exe`, so the root launcher that pack produces changes name. Nothing in this product reads that name: RegistrationTarget keys on being inside a `current\\` directory and on the console subsystem, never on a file name, so landmine 3 -- never register the execution stub -- cannot be broken by a rename.\n\nE. APPLY GREW TIMING LOGS (#974), for diagnosing slow applies on EDR machines. More lines from Update.exe on a path this project does not parse.\n\nF. SETUP'S DISK-SPACE ERROR IS LOCALISED AND IN BYTE UNITS (#932); MSI quiet-install default location and packTitle bracket escaping were fixed (#970, MSI-only); vpk's argument validation moved to the command layer with JSON output added (#934), which is what produced the boolean-option change measured above.\n\nOUR OWN OPEN ASK IS STILL OPEN, AND IT IS THE FIRST THING THIS REVIEW WENT LOOKING FOR. velopack/velopack#1056 -- \"Option to start the app without a console window after install\", filed 2026-09-15 by SixFive7 -- is state `open`, has no comments, no linked pull request, and `updated_at` equal to `created_at`. It is NOT in 1.2.158; the release notes list #1055 and #1057 and skip it. So Setup.exe still starts --mainExe with show_window = true and nothing suppresses that start, the two-binary split stays the answer, and re-verification rows 124 and 126 are unmoved for the reason they were written.\n\nRE-VERIFICATION, ROW BY ROW. Every manual row a Velopack bump puts in play, answered by name.\n\nRow 24 -- the landmines (feed-URL composition, SetAutoApplyOnStartup, the stub, force_stop_package). ANSWERED, PARTLY BY MEASUREMENT AND PARTLY BY THE SUITE, and the gap is named. Three real packs at 1.2.158 all exited 0 and produced a real feed and a real delta (below). Feed-URL composition is unchanged: the channel is still a `--channel` argument and never a URL segment. SetAutoApplyOnStartup(false), VelopackApp.Build().Run() and WaitExitThenApplyUpdates(release, silent, restart) all still compile against the shipped assembly with analyzers at error severity -- three Release publishes and three packs prove it. force_stop_package still matches by image path. WHAT WAS NOT RUN HERE is a real N->N+1 apply against a real install; that is the suite's Update layer and it runs in the release gate, not in this review.\n\nRow 26a -- the compiled-in licence text. RE-FETCHED AND UNCHANGED. velopack.nuspec at 1.2.158 records repository commit 3c7f52c1bf17d10ad21b794b006d5ebd1a879a3b; its LICENSE is 1,091 bytes, SHA-256 91845db83551c877ebbb1118e0fb92e4e527290d23b995c55dcd438b3293943f, BYTE FOR BYTE what commit f2edcbcafb81da5b3c884aaea330e225ad91d8b6 served for 1.2.0, with the same two copyright lines and the same years. THIRD-PARTY-NOTICES.txt carries the new version, the new commit and a paragraph saying the text did not move.\n\nRow 40 -- a legitimately empty channel 404s exactly like a misconfigured feed URL. RE-MEASURED 2026-09-22 and the fact HOLDS. `releases/latest/download/releases.win.json` returns HTTP 200 and a 260-byte manifest; `releases.nosuchchannel.json` returns HTTP 404 with a 9-byte `Not Found`. Velopack has NOT started returning an empty release list, and the two cases are still indistinguishable to anything reading the status code.\n\nRow 41 -- UpdateExe.Start's parameter order and ApplyUpdatesAndRestart already restarting. ANSWERED AND NARROWED. This project does not call UpdateExe.Start at all; its whole Velopack surface is VelopackApp.Build().SetLogger().SetAutoApplyOnStartup(false).Run(), new UpdateManager(...), CheckForUpdatesAsync, DownloadUpdatesAsync and WaitExitThenApplyUpdates(release, silent:, restart:). The compile half is answered by three green Release publishes at 1.2.158; the exactly-one-relaunch half is UpdateTests and is automated.\n\nRow 85 -- the update lane's own numbers. RE-ESTABLISHED LIKE FOR LIKE, and the delta did not move. Two packs of one unchanged tree into a scratch feed, 1.0.1-reverify.5 and 1.0.1-reverify.6: full package 55,023,117 b and 55,023,105 b, N->N+1 DELTA 138,791 b, shipped bytes 143,719,387, compression 0.3829, Setup.exe 62,585,857 b. Against the 2026-09-22 figures at Velopack 1.2.0 -- full 54,981,749, delta 138,943, shipped 143,715,555, compression 0.3826, Setup 59,490,421 -- the delta moved by MINUS 152 BYTES and the installer by +3,095,440, which is item A and nothing else. THE 6,226,697-BYTE DELTA IN THE GATE PACK IS NOT A REGRESSION AND IS RECORDED HERE SO NOBODY READS IT AS ONE: that pack's baseline is 1.0.1-alpha.0.25 from 2026-09-17, five days of real commits away, and vpk reported `0209 files, 0008 patched`. A delta is only comparable against the same tree, which is why this row's procedure packs twice. The two same-tree full packages differ by 12 bytes, which is the NativeAOT rebuild nondeterminism that row already records at 6.\n\nRow 123 -- one Add/Remove key per app id per user. NOT RE-RUN, and the reason is the row's own: it needs a second install of this product on the maintainer's machine against the one registry key the real install depends on. NOTHING IN THE DIFF TOUCHES THE KEYING; what changes is the TYPE of one value inside the key, which is item B and is measured there. RE-RUN 2026-09-24 @ 1.2.158, AND THE REASON ABOVE WAS WRONG: the fact is about ids, so the suite's test id BrowserAI.app.test measures it without writing the real key at all. VERDICT: UNCHANGED, in both uninstall orders. A second silent install into another scratch root rewrote InstallLocation, DisplayIcon, UninstallString and QuietUninstallString to name it; uninstalling either root deleted the key while the other root still held a complete install; the uninstall that then found no key logged os error 2 and exited 0. registry.rs:40 creates the subkey from get_manifest_id() alone and registry.rs:65 deletes it unconditionally, reached from uninstall.rs:38. The real BrowserAI.app key was never written. The row's method and its manual reasoning are corrected in place; it stays manual because nothing automates it yet, and whether it becomes a suite arm under the test id is a question put to the maintainer (Q275).\n\nRow 124 -- a non-silent Setup.exe starts the app with show_window = true. UNCHANGED, and this is the row #1056 exists for. The issue is still open and unaddressed; nothing in the release notes touches start_package's creation flags. The 64-bit bootstrapper changes which binary runs and not what it does. Not re-run: it needs a real non-silent install. RE-RUN 2026-09-24 @ 1.2.158, installing the suite's test pack without --silent from a DETACHED_PROCESS parent into a scratch root. VERDICT: UNCHANGED ON VELOPACK'S SIDE. Setup's own verbose log printed PROCESS_CREATION_FLAGS(134218752) for the --veloapp-install hook and PROCESS_CREATION_FLAGS(1024) for the app start, with VELOPACK_FIRSTRUN=true logged just before the start, and Setup.exe exited 26.9 ms after the started app's creation time; process_win.rs is byte-identical to 1.2.0. NOT ESTABLISHABLE: the console-window half, because no pack this repository builds has had a console-subsystem --mainExe since 2026-09-15; what the start produced was the configuration app's task dialog, logging (FirstRun). InstallerHandoffTests ran 9 of 9 green, filtered, once from each shell, against a fresh published slice.\n\nRow 126 -- VELOPACK_FIRSTRUN is not a reliable signal. UNCHANGED; nothing in the diff touches the variable or the stub's second entry point. Not re-run: it needs a real install. The product does not depend on the answer, as that row says. RE-RUN 2026-09-24 @ 1.2.158. VERDICT: UNCHANGED, AND THE OPEN QUESTION IS ANSWERED. VelopackApp.Run() reads the variable at VelopackApp.cs:232 and clears it at VelopackApp.cs:237, on an installed process only, because VelopackApp.cs:227-230 returns first when CurrentlyInstalledVersion is null; the file is byte-identical at 1.2.0. Measured with the variable set, through the orphan-console rig, on two byte-identical copies of the test pack's BrowserAI.Server.exe: the copy inside an installed scratch root logged Startup[9], the copy outside any install logged Startup[8]. The root stub's Update.exe start produced a configuration app that logged (Ordinary), against (FirstRun) for the start Setup.exe performed. A CONSEQUENCE FOR THIS PRODUCT, recorded in the kb and put to the maintainer as Q276: src/BrowserAI/Program.cs:228-232 reads the variable after line 131 has run VelopackApp.Run(), so that branch cannot fire on an installed process.\n\nRow 127 -- the two-binary pack, its sizes, its ratio and the app's window time. RE-ESTABLISHED IN BOTH HALVES. The sizes and ratio are row 85's numbers above. The window time was re-measured three times against the published configuration app by polling EnumWindows for a visible #32770 owned by the launched pid: 215, 157 and 152 ms, window 556 x 426 -- against a recorded 155-243 ms and 556 x 426, so the size is identical to the pixel and the low reading is 3 ms under the old band, which is machine noise on a three-sample poll.\n\nRow 130 -- Setup stops and asks before installing over a non-empty directory, and the button says Repair on a same-version re-ship. NOT RE-RUN, for the row's own reason: it installs this product over the maintainer's own install and waits on a modal dialog with nobody to answer it. Nothing in the diff touches `is_dir_empty` or the button labels. #932 changes a DIFFERENT setup dialog (disk space) and #1011 adds an uninstall dialog, so both are adjacent rather than on this path. RE-RUN 2026-09-24 @ 1.2.158 against the suite's test pack over a scratch root, AND THE REASON ABOVE WAS WRONG: that reproduces the dialog without touching the maintainer's install. VERDICT: UNCHANGED IN WHAT IT SHOWS, AND THE RECORD CORRECTED ON TWO POINTS 1.2.0'S OWN SOURCE ALREADY CONTRADICTED. It does not wait indefinitely: left alone, the dialog closed 299.9 s after it appeared and Setup.exe exited 0 having installed nothing, logging Overwrite/repair dialog timed out, treating as cancel. setup.rs:118-120 arms the timeout on every non-silent run, and 1.2.0 armed the same at setup.rs:110-112. And the body changes with the version comparison as well as the label: Update over an older install, Downgrade over a newer one, Repair over the same version and over a directory holding one file and no install. ADOPTED: the corrected record, in the kb and in the row. It stays manual, because the dialog is a modal window on the developer's screen for up to 300 s; a real Cancel click was not made, and consent for it is a question put to the maintainer (Q277).\n\nWHAT THE BUILD ESTABLISHED. HALT-A was in force for every publish and did not fire: six ILC passes across three packs, `ILC output for the configuration app is clean` and `ILC output for the MCP server is clean`, 0 complaints in each, and no ILC, trim or AOT warning anywhere in any publish log. The four golden snapshots are @playwright/mcp's and are untouched by a Velopack bump.\n\nTHE FOUR SNAPSHOTS AND tool-verdicts.json ARE NOT IN PLAY. Velopack ships no tools and feeds none of tools-list.json, cli-help.txt, config-schema.d.ts or browsers.json.\n\nRE-VERIFICATION RE-RUN, 2026-09-24 @ 1.2.158: THE FOUR ROWS THIS ENTRY LEFT NOT RE-RUN, 123, 124, 126 AND 130, HAVE ALL BEEN RUN. Taken by a read-only researcher against the suite's test pack in scratch roots, with CLAUDE_CONFIG_DIR, CODEX_HOME and BROWSERAI_ROOT at scratch, and with the clearance snapshot identical in all 19 readings: the real BrowserAI.app key and the Start Menu shortcut hashed the same in every one, and no Velopack temp directory was left behind. The clients' own configurations were deliberately not read, because reading one through its CLI can write it; the hooks ran against the scratch homes. Verdicts, in each row's paragraph above: 123 unchanged; 124 unchanged on Velopack's side with the console-window half not establishable; 126 unchanged with its open question answered; 130 unchanged in what it shows, with the record corrected. Nothing was adopted into the product and nothing was declined; what changed is the record, and the four rows carry it. The version reviewed is unchanged at 1.2.158, so this entry's date is the review's and not the re-run's. Evidence: docs/evidence/2026-09-24-velopack-rows/."
    },
    "node": {
      "reviewed": "v24.21.0",
      "date": "2026-09-14",
      "source": "https://github.com/nodejs/node",
      "why_it_matters": "Bundled in the payload as a single node.exe. A major bump changes size and can change process/stdio behaviour.",
      "snapshots": {
        "tools-list.json": "unchanged. A patch inside the same Krypton LTS line moves no upstream artefact: node is what RUNS cli.js, not what produces its text. The payload was rebuilt for this review and the build regenerates and diffs all four snapshots on every build, so a move would have been a red build; there was none.",
        "cli-help.txt": "unchanged, and this is the one of the four where node could in principle show: the snapshot is cli.js's --help output, captured by running it under this node.exe. It did not move. Established the same way -- rebuilt payload, regenerate-and-diff on every build.",
        "config-schema.d.ts": "unchanged; see tools-list.json. The typings are a file in the wrapper's package, and node does not read them.",
        "browsers.json": "unchanged; see tools-list.json. Browser revisions come from playwright-core's registry."
      },
      "notes": "Reviewed v24.20.0 -> v24.21.0 on 2026-09-14. Same Krypton LTS line. nodejs.org/dist/index.json -- which is what the payload build reads -- gives the release date as 2026-09-07, and the changelog's own header says September 8; the one-day difference is upstream's and changes nothing here. npm 11.19.0 and V8 13.6.233.17 are UNCHANGED; openssl 3.5.7 -> 3.5.8 is the only component that moved; security:false. WHAT WAS ADOPTED: the whole patch. The payload was rebuilt so the build's own resolver took it. node.exe grew 93,381,448 -> 93,580,104 b (+198,656); archive node-v24.21.0-win-x64.zip, sha256 158f7685b44de51f6c0df1d153526cbcd3e1bc739a8dfc607721cef75de9e541; LICENSE still at node-v24.21.0-win-x64/LICENSE and still 160,555 b. HOW IT WAS READ: this product uses node.exe for exactly two things -- running @playwright/mcp's cli.js over stdio pipes, and running cli.js install-browser -- so the release was read against those rather than as a whole. CLEARED BY MEASUREMENT RATHER THAN BY READING: 'crypto: update root certificates to NSS 3.126' and 'deps: update OpenSSL to 3.5.8' are the two items with a live failure mode, because provisioning downloads from Playwright's CDN over TLS. Both were EXERCISED in this batch rather than reasoned about -- install-browser firefox under this exact node.exe fetched firefox-win64.zip from cdn.playwright.dev and completed, and the suite's first-run arm then pulled 203.8 MB from the CDN in 23.6 s. WATCHED: 'child_process: keep SIGWINCH from killing on Win' (#64510) is the one Windows item touching the shape this product runs -- node as a parent of the browser processes -- and it is covered by the containment tiers rather than read: JobContainmentTests, JobObjectTests, BrowserContainmentTests and VerticalSliceTests.KillingThePublishedBinaryLeavesNoNodeAndNoBrowser are all green under v24.21.0. 'fs: fix realpath of namespaced drive paths' (#65378), 'fs: add windowsHandle option to file streams' (#63851) and 'src: expose Windows-only fs open flags' (#64775) are the other Windows-adjacent items; none changed a path taken here, and the whole-slice arms exercise the ones that could. DECLINED, each with its reason: (1) the two task-runner commits (#65217 escaping Windows environment variables, #64907 matching cmd.exe case-insensitively) -- that is node --run, which nothing here invokes; (2) the 16 stream commits -- overwhelmingly web streams and their hot paths, which playwright-core does not use for a piped child, and the classic-stream items are covered by the real-child and smoke tiers rather than by reading them; (3) every SEMVER-MINOR in the release -- crypto STORE-loader private keys, the histogram rework, net.BlockList performance, perf_hooks hypothesis testing, util.MIMEType.parse -- none is on a path BrowserAI takes, and NODE_OPTIONS and NODE_PATH are refused at ChildEnvironment.Refused so no new flag can arrive through the environment; (4) 'deps: update Undici to 7.29.1' -- playwright-core brings its own HTTP for the CDN, and nothing here calls fetch from node; (5) 'tty: add raw-vt and io raw modes' -- the child's stdio are pipes and every launch in this tree sets CreateNoWindow, so there is no tty to put in a raw mode; (6) npm is unchanged in any case, and the payload extracts only node.exe and LICENSE from the archive, so the bundled npm ships nowhere and resolves nothing. RE-VERIFICATION (kb/re-verification.md), every row a Node move puts in play, answered by name: rows 2 and 2b (job containment end to end, and from the published NativeAOT binary) -- ANSWERED GREEN by the gate under v24.21.0, ILCompiler 10.0.12 and SDK 10.0.401; row 21 (payload sizes) -- the two PAYLOAD rows were re-measured by this build and both figures are updated in place, and the FOUR BROWSER figures are OWED AND NOT RUN because both browser revisions moved, which the row now says in place rather than carrying the old numbers forward as if they still held; row 26 (payload licensing as shipped) -- re-read, Node's LICENSE is still the single aggregated file at the same size and the payload's licensing composition is unchanged; row 46 (Node's LICENSE is not published beside node.exe, and the extraction is keyed on the archive path) -- ANSWERED by the build, which found and extracted node-v24.21.0-win-x64/LICENSE and would have thrown had the archive been restructured; row 49 (npm lock keying, and npm ci not rewriting the lock) -- ANSWERED, the rebuild parsed the lock with -AsHashtable and npm ci left it byte-identical, which the build asserts rather than observes; row 50 (PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD gates only the postinstall paths) -- ANSWERED, and for the first time on a build that actually installed: the variable was set throughout and the explicit install-browser chromium still provisioned, which is the behaviour the payload build's comment predicts and the reason it fails loudly on the chrome.exe assertion if upstream ever extends the gate; row 85 (the update lane's own numbers) -- OWED AND STILL NOT RUN, and the kb entry says so in place, dated, now naming a THREE-sided gap rather than one: node.exe +198,656 on the day and +754,688 since the figures were taken, the payload mcp tree DOWN 373,255, and the published BrowserAI.exe at 19,186,688 against the recorded 17,853,952. NOT ONE FIGURE WAS ADJUSTED, and the two payload halves moved in opposite directions, so there is not even a single delta a reader could be tempted to add. Rows 16 and 16a are NOT in play: their trigger is an SDK change to the discover probe, not a Node move. Row 102 IS in play but on the @playwright/mcp axis rather than this one, and it is answered in the playwright-core entry -- it ARRIVED. v26 is Current rather than LTS and is deliberately not tracked; it read v26.8.2 on this date."
    }
  },
  "_chromium_is_derived": "Chromium, ffmpeg and winldd revisions are NOT listed here. They follow from the resolved playwright-core's browsers.json and are recorded in the release's resolved-set manifest instead. Reviewing playwright-core covers them."
}
