<!-- SPDX-FileCopyrightText: 2026 Jori Huisman -->
<!-- SPDX-License-Identifier: LicenseRef-BrowserAI-FSL-1.1-MIT-5yr -->

# Decisions

**This is the charter's argument half.** [`README.md`](README.md) says what
BrowserAI is, how to install it, how to use it and where the scope boundary
sits; this file says *why*, and it is where a decision goes to be settled and
not relitigated. The two were one document until 2026-08-18, at 84 KB, which
put a table of settled decisions between a first-time reader and the install
instructions.

Nothing here is a specification. How the product is put together, and which code
satisfies which part of it, is [`ARCHITECTURE.md`](ARCHITECTURE.md). What is
known to go wrong is [`HAZARDS.md`](HAZARDS.md). What was measured is
[`kb/`](kb/README.md). Work settled in intent but not yet done is
[`TODO.md`](TODO.md).

---

## The setup this replaces

BrowserAI replaces a PowerShell launcher that ran `npx -y @playwright/mcp@latest`
and registered **four** MCP servers, one per mode. It was designed to be
*copied* into a repository, which means every copy became a fork the moment it
landed.

As of **2026-08-13**, a filesystem sweep of the maintainer's source tree (depth
7) found **13 copies of `launch.ps1` across 10 repositories**. Nine of the ten
were byte-identical to each other and all nine differed from the tenth, the only
one anybody maintained; the same held for the accompanying configuration hook.
Three of the thirteen were worktree or backup copies inside a single repository.
If the real number is higher, the remainder live outside the tree that was swept
or deeper than seven levels.

**Every stale copy still carried bugs that had been diagnosed and fixed in the
maintained one on 2026-08-12/13:**

| Fixed in the maintained copy | Still broken in every other copy |
|---|---|
| `--output-mode` flag removed | Passes a flag deleted in `@playwright/mcp` 0.0.79 → `error: unknown option`, exit 1, **all four servers dead** |
| Process handle cached before `WaitForExit` | `.ExitCode` reads back `$null`, so a hard startup failure logs identically to a clean shutdown -- this is why the above went unnoticed for five days |
| Bundled-Chromium preflight | Missing browser revision → MCP handshake *succeeds*, `tools/list` returns normally, first `browser_navigate` fails |
| Detached installer via `cmd /c start "" /b` | `Start-Process` redirection does not prevent stderr-pipe inheritance; a client reading stderr blocks for the entire ~300 MB download (measured 11.71s → 0.37s after fix) |
| Error-shaped stderr detection | Warned on any stderr; Playwright prints a benign `Session: <path>` line on every healthy start |
| Typed artifact folders + prefix classification | Everything flat in one `output/`; grew to 346 session dirs / 1.5 GB in ~3 months |
| Screenshot-hook allowlist widened | Denies saving into any folder other than `output/` |

The lesson worth carrying into BrowserAI: **every one of those defects was invisible.** The setup reported healthy while being broken. Observability is a feature requirement here, not a nicety. The two measurements that generalise beyond that setup are in the knowledge base with their method -- [the stderr-pipe inheritance that cost 11.71 s per spawn](kb/windows/processes.md#stdio-exit-codes-and-process-startup), and [the uncached exit code that logged a hard failure as a clean shutdown for five days](kb/windows/processes.md#stdio-exit-codes-and-process-startup).

---

## Why this project needs to exist

### 1. There is no update path, and that is the actual problem

A fix made today reaches one of thirteen checkouts. Nothing propagates. The setup was designed to be *copied*, which means every copy is a fork the moment it lands.

This is the core justification for BrowserAI. It is **not** token cost -- see [Non-reasons](#non-reasons----do-not-relitigate-these).

### 2. The version chain floats, and it breaks silently

`launch.ps1` runs `npx -y @playwright/mcp@latest`. The dependency chain below that point is **exact** -- no semver ranges anywhere:

```
@playwright/mcp 0.0.79
  └── playwright-core 1.63.0-alpha-2026-08-05   (exact pin, no ^ or ~)
        └── browsers.json → chromium rev 1237 (152.0.7977.8)
```

`browsers.json` pins several browsers; the one at the end of *our* chain is **`chromium-1237`**, the full build. `chromium_headless_shell-1237` is pinned in the same file and [is never provisioned](#processes-browsers-and-session-modes) -- naming it here, as an earlier draft did, points the reader at a binary that never reaches a machine.

⚠️ **That chain is the one in force when this section was written, and the sentence above calling `chromium-1237` *the one at the end of our chain* has not been true of what ships for some time.** *Corrected 2026-09-21 @ `@playwright/mcp` 0.0.81 / `playwright-core` 1.64.0-alpha-2026-09-17 / chromium 1245 (previously the worked example stood alone, with no successor beside it, so its present tense read as a claim about today, not as a record of one day).* **Corrected by addition, not by replacement:** the 0.0.79 chain is a true record of what `launch.ps1` resolved on the day this argument was made, and the argument is about the *shape* of the chain, not about any link in it. What the payload ships today, read from [the payload lock](build/payload/package-lock.json) and [the committed `browsers.json` snapshot](upstream-snapshots/browsers.json), not from memory:

```
@playwright/mcp 0.0.82
  └── playwright-core 1.64.0-alpha-1789764292000
        └── browsers.json → chromium rev 1246 (154.0.8037.0)
```

**Each chain is a snapshot of one day and neither is the source of truth** -- the live values are `packages` in [`build/payload/package-lock.json`](build/payload/package-lock.json) and the `chromium` entry in [`upstream-snapshots/browsers.json`](upstream-snapshots/browsers.json), both committed, both regenerated from the resolved payload, and both held to the four versions printed above by `PayloadTests.TheWorkedExampleStatesTheChainTheCommittedRecordsState`, so this example goes **red, not stale**. *Corrected 2026-09-21, later the same day (previously `@playwright/mcp` **0.0.81** → `playwright-core` **1.64.0-alpha-2026-09-17**, annotated *the DATED OVERRIDE, above the wrapper's own exact pin of 1.64.0-alpha-2026-09-14* → chromium rev **1245**, with a following sentence reading "**The middle link is the one variation the 0.0.79 chain could not show:** since 2026-09-17 it is a dated override sitting above upstream's own exact pin, not upstream's pin itself -- a second way for the chain to move, and the reason that exception carries a written exit").* **The chain has three links again.** `@playwright/mcp` 0.0.82 ships a `playwright-core` carrying the fix the override existed for, [the exception ended](#the-two-exceptions-to-the-versioning-policy), and the middle link is upstream's own exact pin once more -- so the parenthetical that named a fourth version has nothing left to name. **The variation it showed was real and is worth remembering**: a chain whose middle link came from somewhere other than the link above it, for four days.

⚠️ **The middle link is no longer a date, and nothing here parses it as one.** `1.64.0-alpha-1789764292000` is epoch milliseconds, not upstream's daily `alpha-YYYY-MM-DD`; every comparison this repository makes on a `playwright-core` version is for **identity** against another recorded string, so the shape costs nothing and is recorded only so the next reader does not try to read a date out of it.

So the package is pinned to a browser revision, but *which package* is not pinned at all. An upstream publish silently invalidates the local browser cache and changes the CLI surface. Both failure modes fired this month, on the same day. The exactness of that chain, and upstream's daily-alpha cadence, are in [kb: tool surface](kb/playwright/tools-and-artifacts.md#the-tool-surface-and-the-package-shape).

### 3. Two failure classes exist that no configuration can fix

- **CLI flags fail loudly.** An unknown flag crashes at startup with a clear message -- recoverable once you can see stderr.
- **JSON config keys fail silently.** `loadConfig` is a bare `JSON.parse` with no schema validation. A key that upstream renamed or removed is simply ignored. `--output-mode` turned out to have been a **no-op for its entire life** -- a hardcoded literal in 0.0.78's bundle, never read from config ([kb: upstream config](kb/playwright/configuration.md)).

A setup that cannot distinguish "this setting is applied" from "this setting is silently discarded" cannot be trusted to be opinionated. BrowserAI must validate its own config against the runtime it ships.

### 4. Exclusivity is keyed on the wrong thing

Mutexes are named `Global\<RepoName>-PlaywrightInteractive`. That locks a *repository folder name*, which is arbitrary: two clones of the same repo collide, and two different repos wanting the same profile do not. The thing that actually requires exclusivity is **the browser profile directory**.

> **And nothing else enforces it in the mode that matters.** Measured 2026-08-14: the **full** chromium build writes a `lockfile` into the profile and a second instance is refused (`Browser is already in use for <dir>`). **`chrome-headless-shell` writes no `lockfile` at all** -- two headless instances opened the same profile directory, both launched, both worked, and no error was raised anywhere. Two browsers writing one profile's cookie and storage databases is silent corruption, and headless is the default mode. An earlier draft of this document said *"Chrome's own `SingletonLock` already gets this right for the persistent mode"*; that is true only of the headed build. **[locking](ARCHITECTURE.md#locking-ownership-and-the-sweep)'s directory-keyed lock is therefore the only protection that exists, not defence in depth.** Both launches are recorded in [kb: image-path detection](kb/windows/detection.md#process-image-path----the-fully-documented-detection-path).

### 5. Relative paths make silent data loss possible

All four `config.json` files use paths relative to the working directory, including `userDataDir: "playwright/persistent/profile"`. The launcher currently guarantees cwd via `Set-Location $RepoRoot`. Any change to how the process is started -- a proxy, a plugin, a service -- re-opens this, and the failure mode is **a fresh empty Chrome profile with every saved login gone, and no error raised**.

### 6. Profiles are fragmented by accident

Thirteen checkouts means thirteen `persistent/profile/` directories. A login established in one repository does nothing for the other twelve. This was never a decision; it is a side effect of copy-paste distribution.

### 7. Distribution to colleagues has no story

The current setup requires a repo, a `.mcp.json`, hook registrations, a `.gitignore` section, and a working Node installation. Onboarding another person means replicating all of it by hand.

---

## Versioning policy: everything floats, the build freezes it

Adopted from a sibling project's versioning policy -- *never pin*, and the modernity doctrine beside it -- and applied here **without exception, including to the shipped payload**.

**Every dependency floats to the latest release at build time.** NuGet packages, `@playwright/mcp` and the `playwright-core` it carries, `node.exe`, both Chromium builds, `ffmpeg`, `winldd` -- the build resolves each to the newest available version and then **freezes what it resolved into the artifact**. For .NET, the newest **GA** major, adopted at each annual GA, **including when that major is STS, not LTS**. `LangVersion` is `latestMajor`, never `preview`.

**Every version number in this document is a floor and a provenance stamp, never a target.** The versions named in [Implementation stack](STACK.md#implementation-stack) and [the runtime](ARCHITECTURE.md#the-runtime-it-ships) record what was current when each claim was verified, so a reader can tell how stale the prose is. **The build does not read them.**

> **Why:** stale dependencies are a defect, not a safety measure. Riding the newest release keeps each upgrade small instead of saving them into one nobody ever takes, and [Testing](TESTING.md#testing-a-hard-requirement-and-the-release-gate) is what catches breakage. A version pin is not a substitute for a test suite -- it is a way of not finding out.

**Latest patterns are the house style**, not merely latest versions. Code is written with the newest stable idioms the GA toolchain actually compiles, and rewritten toward them; familiar-but-dated patterns are style defects. Style is law, not advice -- analyzers run at error severity, and a severity is never weakened to make code pass.

### The pin is an output, not an input

This is the whole model, and it needs stating precisely because the charter's founding complaint was *about* floating:

| | Today (`launch.ps1`) | BrowserAI |
|---|---|---|
| **When** the version resolves | Every spawn | Once, at build time |
| **Where** it resolves | The user's machine | Ours |
| **What validates it** | Nothing | The full suite, which must be green |
| **What the client runs** | Whatever npm served that minute | Exactly the bytes we tested |
| **Failure surfaces** | Silently, in production, five days later | As a red test, before anything ships |

[Why-reason 2](#2-the-version-chain-floats-and-it-breaks-silently) is not weakened by this -- it is sharpened. **The defect was never that the version moved. It was that it moved untested, unobserved, and on someone else's machine.** Moving it under a test suite before anything ships inverts every row in that table. The artifact a client installs is still exactly fixed; what changed is that its contents are *derived from a green build* instead of hand-typed into a config file and left there.

### The five rules that make floating safe

1. **The resolved set is recorded, not remembered.** For NuGet this is **two steps, not one**: `dotnet restore --force-evaluate` to resolve the float, then a locked-mode restore to verify. They are mutually exclusive in a single invocation -- with a lock file present and no `--force-evaluate`, NuGet **does not re-resolve**, and the float is silently dead -- measured 2026-09-23 @ SDK 10.0.401 / NuGet 7.9.0-rc.42413 against a local-only feed carrying two versions of one floated package: `dotnet restore --locked-mode` exited 0 with **0 warnings and 0 errors** and said nothing about the newer version beside it. A one-step `--locked-mode` build is the `browserName: "chromium"` failure again. *Corrected 2026-09-23 (previously this cited [NU1512](https://learn.microsoft.com/nuget/reference/errors-and-warnings/nu1512) for it, "warned by default from the .NET 11 SDK")* -- NU1512 describes the opposite arrangement, both properties set, and it is loud; it was reproduced here under `-p:SdkAnalysisLevel=11.0.100` as the control proving it is reachable and simply does not fire in the one-step case. [RELEASING](RELEASING.md) carries the full measurement. It also yields the cheapest possible drift detector: `git diff --exit-code -- "**/packages.lock.json"` after the resolve ([kb: interop](kb/toolchain.md#nuget-floating-versions-and-central-package-management)). Then the resolved `package-lock.json` for the vendored npm tree, and browser revisions read from the resolved package's own `browsers.json`, never a hand-typed URL. **An artifact that cannot state exactly what went into it is not releasable** -- that is also what makes a rollback meaningful and a regression bisectable.
2. **The shipped artifact never floats.** The client resolves nothing at runtime: no `npx`, no `@latest`, no network at spawn. What was tested is what runs. This property is the non-negotiable one, and it is why [the runtime](ARCHITECTURE.md#the-runtime-it-ships) vendors the tree at all.
3. **GA is a hard floor.** No preview or RC builds a released artifact. Upstream Playwright publishes **daily alphas**, but `@playwright/mcp@latest` is the released dist-tag -- the `playwright-core` alpha beneath it arrives as that package's own exact dependency, not as a choice we make ([kb: tool surface](kb/playwright/tools-and-artifacts.md#the-tool-surface-and-the-package-shape)).
4. **Green is the only gate, and it gates the *release*, not the *update*.** The response to a breaking upstream change is to make the newest version work. Holding the previous version is not a fix, and "pin it back for now" is the failure this policy exists to prevent. The consequence is accepted and not softened: **a break with no fix-forward blocks releases for as long as it takes** -- see [Locking, logging, versioning and registration](#locking-logging-versioning-and-registration).
5. **Updating is the *first* step of touching this project, not the last step before a release.** Re-resolve everything to latest, fix whatever that breaks, and only then do the work that was asked for. A release may be cut only from a tree that has been fully re-resolved and is green throughout, and **nothing is held at an old version without the maintainer knowing it was held.** This is the rule that keeps the project following the lead: an update deferred to release time is an update deferred forever, because the day a release matters is the day nobody wants to absorb an unrelated upgrade. Rules 1-4 say *what* floating requires; this one says *when*, and without it they describe a policy that is technically in force and never exercised.

> ⚠️ **This heading read "The four rules" until 2026-08-16**, when rule 5 was added. Rules 1-4 are unchanged; what was missing was the ordering, which had been left as an implication and therefore never happened.

### The two exceptions to the versioning policy

**The section above says "without exception". There are two, they are different kinds of thing, and neither is a precedent for a third.** They are listed together so that a reader meeting one finds the other, and so that the count in this heading is the thing that has to move before a third is added.

✅ **THE SECOND ONE ENDED ON 2026-09-21, four days after it was taken, and it is recorded as ENDED and not deleted.** Opened **2026-09-17**, closed **2026-09-21**. Why it ended: `@playwright/mcp` **0.0.82**, published 2026-09-18T23:38Z, pins `playwright-core` **1.64.0-alpha-1789764292000**, whose own bundle carries [microsoft/playwright#42673](https://github.com/microsoft/playwright/pull/42673) -- measured, not assumed, by the same grep and the same negative control the override was taken on: `PLAYWRIGHT_MCP_FILE_PATHS` twice, `file-paths` three times and `filePaths` eight, identical to the override's own bundle against 0 and 2 in the build the wrapper used to pin. So the lag the exception existed for closed, which is exactly the condition it was dated against. The `overrides` block and its `//overrides` note are gone from [`build/payload/package.json`](build/payload/package.json), the wrapper's own pin floats again, and **the row below is kept in the past tense and not removed** so that a reader meeting the SQLite exception still finds out what the second one was and how it was allowed to be one.

⚠️ **The two instruments in the row below are RETIRED, and the ordering underneath them is not coming back wider.** `PayloadTests.TheDatedPlaywrightCoreOverrideIsStillNeeded`, its positive control `.TheExpiryComparisonFiresInBothDirections`, and `build/Build-Payload.ps1`'s `Compare-PlaywrightVersion` all existed only to answer *has the exit fired*. **They did their job by refusing to answer.** Both parsed `<major>.<minor>.<patch>[-alpha-YYYY-MM-DD]` and **threw** on anything else, and the version the wrapper actually rolled to is a 13-digit epoch-milliseconds alpha from a different shape family -- so on 2026-09-21 the exit was *unreadable*, not *unfired*, which is the state a human adjudicates and which both instruments said so in. Widening the regex to accept the epoch shape was available and was **declined**: silently ordering a shape nobody designed for is how an override survives its own exit, and the correct answer to *this needs a human* is a human, once. With the exception gone nothing in this tree ranks a `playwright-core` version at all; every comparison left is for **identity** against another recorded string, so a version is an opaque string. **The invariant that survives is the one that was always the real one**, and it came back to exactly where it was before 2026-09-17: `PayloadTests.TheLockRecordsUpstreamsOwnExactPinOfPlaywrightCore` asserts the resolved version **equals** the declared one -- planted red 2026-09-21 against a doctored lock -- and `build/Build-Payload.ps1` makes the same check against the live resolution. **That is also what would catch an override being added again**, which is the mechanism this section needs and did not have while the exception stood.

| | **1 -- the vendored SQLite pin** | **2 -- the dated `playwright-core` override** |
|---|---|---|
| **What it is** | `third-party/sqlite/sqlite3.c` and `sqlite3.h` are source-pinned at a version a human swaps, and compiled into the executable by [`build/Sqlite.targets`](build/Sqlite.targets) | [`build/payload/package.json`](build/payload/package.json) carries an npm `overrides` entry resolving `playwright-core` to a version newer than the one `@playwright/mcp` pins |
| **Why** | **An absence.** No maintained package ships a win-x64 static library, measured with a positive control. There is nothing to float | **A lag.** A merged upstream fix exists and one wrapper release has not carried it yet. There is something to float and it is one build behind |
| **Direction** | Holds a version **still**, because nothing moves it | Moves a version **forward**, ahead of what the wrapper resolves |
| **When it ends** | The day a win-x64 `e_sqlite3.lib` ships inside `SourceGear.sqlite3`: the compile step is deleted and the native floats again | The day `@playwright/mcp` `latest` pins a `playwright-core` at or above the override: the `overrides` block is deleted |
| **What ends it** | A human noticing. The watch is [`drift-check.json`](drift-check.json)'s `vendored` row, which names the issue and the glob that answers it | **It ENDED 2026-09-21.** What was supposed to end it: a red build on the day itself, from `PayloadTests.TheDatedPlaywrightCoreOverrideIsStillNeeded` off the committed lock and [`build/Build-Payload.ps1`](build/Build-Payload.ps1) off the live resolution. **What actually happened**: both refused to order the epoch-stamped version the wrapper rolled to and said a human must adjudicate, which is the designed behaviour and is how the condition surfaced. Both are retired |
| **Taken** | 2026-08-26 | 2026-09-17, by the maintainer, to reach [microsoft/playwright#42673](https://github.com/microsoft/playwright/pull/42673) -- `--file-paths=absolute`, which closes [this project's own ask #42497](https://github.com/microsoft/playwright/issues/42497) -- without waiting for a wrapper roll |
| **Ended** | Not yet; the absence it rests on is still there | **2026-09-21**, when `@playwright/mcp` 0.0.82 pinned a `playwright-core` carrying that fix. **Four days**, which is what a dated exception is supposed to look like |

**The second is a time-limited pull-forward, not a pin, and the distinction is the whole of why it is allowed.** [Rule 4](#the-five-rules-that-make-floating-safe) forbids *holding the previous version because the newest breaks something*; this holds nothing back. `@playwright/mcp` is still resolved from the `latest` dist-tag on every payload build, and what the override changes is one transitive dependency **upwards**, for as long as the wrapper's own exact pin is below it. The moment that stops being true the override is doing the forbidden thing, which is exactly the condition the two instruments fire on.

**It rests on upstream pinning exactly.** `@playwright/mcp` declares `playwright-core` as a byte-for-byte version, not a range -- [re-verification row 48](kb/re-verification.md) -- so an override moves the tree by *exactly one build*. Against a range it would open a second floating axis, and this exception would not be available. That is also why the row's own check changed shape instead of being relaxed: the declared pin must still be exact, and the resolved version must equal the override when there is one.

**The deletion was written down as a procedure, not left to whoever met the red build**, and that is what it ran on. [`TODO.md`](TODO.md#upstream-asks) carried a standing watch item -- *WATCH for the `@playwright/mcp` release that carries #42497* -- naming the condition, the exact object to delete, the rebuild, the review the roll then needs, and what to record here afterwards; it is **closed**, condition met 2026-09-18, acted on 2026-09-21. Two things monitored it and neither was a person remembering: the [daily drift check](CLAUDE.md#the-daily-drift-check) resolved `@playwright/mcp` `latest` and its exact `playwright-core` dependency on every day of work, and it is the one that actually reported; and the two instruments above went **unreadable, not red**, which was their designed answer to a version shape nobody had designed for, and which put the decision in front of a human on the right day.

⚠️ **This section still says "two" and the heading still counts two, deliberately.** *Corrected 2026-09-21 (previously "**When the override goes, this section describes one exception and its heading says so**").* An exception that ran its course and ended is a **record of how one is allowed to work**, not a slot that reopens: the count in the heading is of exceptions this project has taken, and lowering it back to one would delete the only worked example of a dated exception being taken, monitored and closed. **A third is still a change to this section, not an application of it**, and the paragraph below is unchanged.

⚠️ **Neither of these is a precedent, and a third exception is a change to this section, not an application of it.** The SQLite argument is an absence and expires when the absence does; this one is a lag and expires on a date nobody has to remember. A pin taken for a preference, for convenience, or to get past a break has neither property, and *"there are already two"* is not an argument -- it is the count this heading exists to make visible.

### Every release builds against the latest Playwright, and only a human may say otherwise

**The rule.** A release is cut against the newest `@playwright/mcp` the resolve
returns -- with the `playwright-core` alpha that arrives as *its own exact
dependency*, never npm `latest` for that package. This is rule 4 applied to the
one upstream that moves daily and is the whole reason this product exists.

**The exception, and who owns it.** A **human may force a crunch override** --
ship against a version that is not the newest, because the newest breaks
something and the release cannot wait. That is a judgement about a deadline, and
judgements about deadlines belong to the person with the deadline.

⚠️ **An agent may never take that decision, under any circumstances, however
reasonable it looks at the time.** Not to unblock a red suite, not to finish a
batch, not because the break "is upstream's". An agent that meets a break fixes
it forward or stops and says so; *"I pinned it back for now"* is the failure this
whole policy exists to prevent, and it is the failure an agent is most likely to
commit, because reverting to green is locally the cheapest correct-looking move.

**An override leaves a trace in the release artifact.** It is not a decision that
may be taken in a shell and forgotten: the resolved set is recorded beside the
package ([RELEASING item 11](RELEASING.md#11-the-resolved-set-is-recorded-beside-the-artifact)),
so the manifest states the version that was actually shipped, and the override is
stated with it -- what was held, at what version, against what newest version, why,
and the name of the human who took the decision. **A release whose manifest does
not say it was overridden is a release claiming it was not.**

⚠️ **That sentence was false for as long as it stood, and it is true since
2026-08-26.** [`build/Write-ReleaseManifest.ps1`](build/Write-ReleaseManifest.ps1)
emitted `version`, `tag`, `package`, `sha256` and the resolved versions read out
of seven copied files, and **had no override field and no place for one** -- so no
manifest could ever say it was overridden, and by the bolded sentence's own logic
every release claimed it was not, *including one that was*. The script takes five
`-Override*` parameters now and always emits the key: `"override": null` for an
ordinary release, a five-part block for a held one. **An absent key is not a
statement; `null` is** -- which is what lets a reader a year later tell *this
release says it was not overridden* from *this manifest was written by a build
that could not say*. A half-stated override refuses the manifest instead of
writing one, for the same reason a missing file does.
`ReleaseScriptTests.TheManifestStatesWhetherTheReleaseWasACrunchOverride` and
`.AnOverrideStatedInPartRefusesTheManifestRatherThanWritingHalfAClaim`, both
watched red -- the first on the field being absent altogether, the second on
*"A parameter cannot be found that matches parameter name 'OverriddenPackage'"*.

*Written down 2026-08-26, at the maintainer's decision. Nothing enforces the
"agents may never" half and nothing can -- it is a rule about who is asking, and
the build cannot see that. What the build does hold is the trace: the manifest is
emitted by [`build/Write-ReleaseManifest.ps1`](build/Write-ReleaseManifest.ps1)
and copied and not transcribed, so the version in it is evidence -- and, since
the correction above, the override itself is a field, not a claim about
one.*

### Never assert a version from memory

**If it was not looked up this session, it is unverified -- say so.** Model training knowledge lags this toolchain by design, and a confident stale version is worse than an admitted gap. Same discipline as the `Verified <date> @ <version>` stamps in [Provenance stamps](#provenance-stamps), applied to the act of writing a version, not to the value written.

> **In-house evidence, and it is three weeks old.** A sibling project pins `ModelContextProtocol` **1.4.1**, with a csproj comment reading *"1.4.1 = latest stable on nuget.org as of 2026-07-23 (2.0.0 is still preview)."* That was true when written. Re-checked against nuget.org's flat-container index on **2026-08-14**: 2.0.0, 2.1.0 and 2.2.0 have all shipped stable, so the comment's central claim is now false and nothing in that build says so.
>
> The comment was **correctly stamped with its date**, which is the only reason the staleness is detectable at all -- an unstamped "latest stable" claim would still read as current. Stamp the date; never trust one that lacks it. ([kb: package provenance](kb/packaging/dependencies.md#package-provenance-as-looked-up))

---

## What this improves over the setup it replaces

| | Today (the PowerShell launcher) | BrowserAI |
|---|---|---|
| Update path | Copy-paste to 13 checkouts | One release, one channel |
| Playwright version | `@latest`, re-resolved at every spawn, on the user's machine, untested | Resolved to latest at build time, gated by the suite, frozen into the artifact |
| Chromium | Downloaded on first use by `npx`, on the user's machine, untested, with no integrity check | Downloaded on first use **by us**, at a revision the build resolved and the suite gated, once per machine, not once per spawn |
| Node | Must exist on the host | Bundled (`node.exe`, 88.5 MB) |
| .NET / Chrome | n/a / required for headed modes | Neither -- NativeAOT single-file, and a Chromium we provision, not the user's |
| Browser patching | Chrome self-updates | **Ours to ship.** A Chromium CVE is now a release obligation |
| Headed behaviour | Google Chrome | **Chromium, which is not Chrome:** no proprietary codecs, a different UA and fingerprint surface, no Widevine. DRM video, some enterprise SSO and anything fingerprinting the build will behave differently. **Verify the portals actually in use before cutting over** -- this is the cost of dropping the host dependency, and it is paid by whoever migrates, once |
| Tree teardown | `Get-CimInstance` walk + `Stop-Process`; nothing survives a hard kill of the launcher | Job object -- the kernel reaps the tree even if BrowserAI is `TerminateProcess`d |
| Lock granularity | Repository folder name | Resolved profile directory |
| Output directories | Static, relative, cwd-dependent | Explicit argument to `init` |
| Profile | 13 separate ones by accident | One, deliberately -- or per-directory by choice |
| Config validity | Unknown keys silently ignored | Validated against the shipped runtime |
| Colleague onboarding | Clone repo, replicate 6 files, install Node | Run installer |
| MCP registrations | 4 | **1**, with `init`-issued handles routing to instances |

---

## What BrowserAI does not defend against

**Adversarial and hostile-caller defence is an explicit NON-GOAL of this
product.** The maintainer's words, 2026-08-25:

> *"We are building this tool on the premise that the LLM tries to behave and we
> try to steer it correctly where it would make honest mistakes but we do not
> guard against hostile intentions."*

Guarding at that level is *"hopeless and thus meaningless"* -- the calling agent
chooses the session directory, the browser profile and its cookie database are
created inside it, and the agent runs as the same Windows user, so anything a
refusal declines to do is reachable with a file tool the agent already holds.
**Measured 2026-08-18** and not argued: a second process as the same user
recovered a cookie from a session this product configured, using
`CryptUnprotectData` and AES-256-GCM alone, with App-Bound Encryption not in
force for the provisioned Chromium
([kb](kb/chromium/profiles.md#chromiums-cookie-store-and-what-it-takes-to-read-one----measured-2026-08-18)).

**What this licenses, and what it does not.**

- **It licenses application-level invariants.** *The writer of `browserai.data`
  is the lock holder* is kept by the code path that reaches it and by nothing
  else; a caller that opens the store itself and writes to it is out of scope by
  charter instead of defended against and losing.
- **It licenses steering over refusing.** A hazard whose only closure would be a
  door refusal aimed at a hostile string gets a sentence in an answer instead --
  which is what [Q128](TODO.md#upstream-asks) settled for the two output-filename
  hazards, alongside an upstream ask.
- **It does not license leaving a defect unrecorded.** Everything above stays in
  [`HAZARDS.md`](HAZARDS.md), open, with what it would take to close it. A
  non-goal is a decision about what to build, never a reason to stop knowing.
- **It does not license carelessness about honest mistakes.** Every refusal in
  `SessionErrors` exists because a model that meant well would otherwise get a
  wrong answer or a silent one. That is the whole product.

⚠️ **Written down 2026-08-26, and several of the arguments that preceded it
would have gone differently had it been here.** The tool-permission matrix
deleted on 2026-08-18, the `browser_get_config` secrets guard, the filename gate
deleted on 2026-08-26 -- each was argued at length as a boundary before somebody
noticed it was not one against the only caller there is. **It also retires a
standing item**: *headless-with-storage is still refused on a reason the same
pass declared void* sat in [`TODO.md`](TODO.md) from 2026-08-18 to 2026-08-26,
asking which way that inconsistency should be resolved. This is which way -- and
session modes were deleted in between, so there is no combination left to refuse.
This section exists so the next one is a paragraph, not a day.

---

## Known trade-offs

Recorded so they are inherited as decisions, not rediscovered as surprises.

### The `init` design weakens a security boundary

Today, four separate processes give **process-level isolation**. The `interactive` mode exists so a human can type credentials the agent must never capture, and its server process is launched without the `storage` capability -- the 17 cookie/localStorage/`storageState` tools do not exist in that process ([kb: tool surface](kb/playwright/tools-and-artifacts.md#the-tool-surface-and-the-package-shape)). There is no code path to reach them.

Under one server with `init`, that becomes a **runtime check in BrowserAI's own code**. The tools exist in the shared surface; correctness depends on the session-state lookup being right, including under concurrency.

To be precise about the size of this: it is *not* a demotion to "the model must behave" -- BrowserAI enforces it server-side, and a model that calls `browser_cookie_list` in an interactive session gets refused. It *is* a demotion from "the capability does not exist in this process" to "our code declines to use it." Weaker, and worth the eyes-open acknowledgement.

The session design ([sessions](ARCHITECTURE.md#sessions)) narrows this considerably -- a call is judged against the mode recorded in the `browserai.json` of the directory it names, never against the caller's word -- but it does not change the *kind* of guarantee. It remains our code declining, not a capability that does not exist. *(Both halves of that sentence are historical: session modes were deleted 2026-08-20 and `browserai.json` on 2026-08-26. The paragraph is kept unchanged because the correction two paragraphs down is written against it, and rewriting the thing a correction quotes is how a correction stops being readable.)*

⚠️ **Corrected 2026-08-16 at the plan's final audit, previously "a server-minted handle cannot be forged for a session type the agent never created."** **The mint is gone.** The session is a directory path, which a model can compose freely, so nothing prevents an agent *naming* a `persistent` directory it did not create. What the enforcement rests on instead is the file at that path: the mode comes from its `browserai.json`, so the refusal holds and the *forgery* argument does not. The residual gap, stated plainly: a connection holding both an `interactive` and a `persistent` session can route a call to the persistent one, which grants nothing new, and **the `interactive` guarantee, the one a human relies on, holds.**

⚠️ **Corrected 2026-08-18: the enforcement was removed, and the section above is kept because it is the reasoning a reader has to have before the correction makes sense.** *(Previously the section ended: "**Requirement:** session-type enforcement must be centralized in exactly one place, deny-by-default, and unit-tested against every tool in the surface. A new upstream tool must be *unreachable* until explicitly classified -- never reachable by default. With N concurrent instances in one process, the handle→type lookup is now shared mutable state on the hot path of every call: it must be correct under concurrency, and that is a test, not an assumption.")* All four properties were built and all four held. **The mistake was one level up: this was never a boundary against the caller at all.** The calling agent chooses the session directory. The browser profile -- cookie database included -- is created inside it. The agent runs as the same Windows user, so DPAPI decrypts for it. An agent holding any file tool reads what `browser_cookie_list` declined to return, so refusing the tool bought one extra step and cost a lookup on every call. ⚠️ **That last sentence was the whole argument and it was unmeasured when the removal was made; it was measured on 2026-08-18 and it holds** -- from a second process as the same user, `CryptUnprotectData` with no entropy plus AES-256-GCM recovered a cookie out of a session BrowserAI configured, and App-Bound Encryption is **not** in force for the provisioned Chromium (`v10` rows, no `app_bound_encrypted_key`) ([kb](kb/chromium/profiles.md#chromiums-cookie-store-and-what-it-takes-to-read-one----measured-2026-08-18)). **Recorded this way round on purpose:** the decision was right, and it was still taken on an assumption. The paragraph above says the `interactive` guarantee "holds"; against a caller with file access it does not, and that sentence was a justification stated as fact and never tested against the obvious bypass.

**Prompt injection is real and is not solved at this layer.** A model tricked into wanting the cookies is capable of opening the file. A defeated motivation cannot be answered by increasing execution complexity.

**What replaces it.** Change control moved to [the release gate](TESTING.md#the-upstream-review-gate), which already covered more: four golden snapshots -- `tools-list.json` carrying all 72 tools **with their `inputSchema`s**, `cli-help.txt`, `config-schema.d.ts`, `browsers.json` -- are regenerated from the resolved payload and diffed on every build, and `upstream-review.json` blocks a release until a human adjudicates what moved. A tool whose *schema* changed, or a new CLI flag, is caught there; deny-by-default on a tool *name* caught neither.

**What survives, and why neither is a permission.** `session` stays **mandatory** -- a call naming none is refused instead of reaching the run's own child -- because that is **routing**: a proxy holding N children has to be told whose call this is. And `browser_annotate` is **withheld from `tools/list` entirely**, in every mode, and refused wherever a caller names it anyway, as a **liveness** guard: it blocks with no self-timeout until a human draws, and its window belongs to a second, non-headless browser under a daemon that outlives the session. Neither carries a security claim. *Corrected 2026-08-18 (previously "`browser_annotate` is still refused on a mode that opens no window") -- the same measurement that dated the refusal took the ground out from under permitting it anywhere; the decision and what would reverse it are two rows down.* ⚠️ **And since 2026-08-26 that withholding is a row in [`tool-verdicts.json`](tool-verdicts.json), not a constant in C#** -- one row per tool, `allow` / `deny` / `answer`, shipped inside the payload it describes; `browser_annotate`'s refusal is byte-identical to the doc comment it replaced, because the reasoning that was a doc comment is now that row's own `why`. ⚠️ **It is the only `deny` again since 2026-09-21** -- *corrected that day (previously "**And since 2026-09-15 it is no longer the only `deny`** *(previously "`browser_annotate` is still the only `deny`")*: `browser_webmcp_call` arrived with `@playwright/mcp` 0.0.81 and was denied on the same ground, one row down in this table")*. `browser_webmcp_call` did not stop being dangerous and nobody reversed the judgement: `@playwright/mcp` 0.0.82 marked it `skillOnly`, so it is on no wire in any configuration, and a verdict row naming a tool the snapshot does not carry is a judgement about nothing. The row was deleted and **its reasoning kept**, in [`tool-verdicts.json`](tool-verdicts.json) and [`upstream-review.json`](upstream-review.json), because that is what a future judgement would need. **The count in the paragraph above moved with it and was re-counted and not decremented**, which is the only way a published number is allowed to move here. **What the file adds is deny-by-default**: a name with no row is refused at the door too, and `ToolVerdictTests` compares the file against the golden snapshot in both directions on every build, so a Playwright bump that adds a tool cannot go green unadjudicated. That is [not a reinstatement of the `(tool, mode)` matrix](ARCHITECTURE.md#the-verdicts-file-and-why-a-tool-nobody-judged-is-refused): it is not a permission, and what it buys is that a name this build has never been told about does not start a browser to be told there is nothing to run.

⚠️ **Corrected 2026-08-20: the last capability boundary went too, and it went deliberately.** *(Previously: "**What genuinely was a capability boundary is untouched.** A `headless` or `interactive` session's child is still launched **without the `storage` capability**, so those 17 tools do not exist in that process. That is the 'the capability does not exist' form this section was worried about losing, and it never went anywhere -- it is the mode's own `Storage` flag reaching `BrowserConfiguration.ForSession`.")* **Session modes were deleted on 2026-08-20 and every capability is now granted to every session**, so there is no longer a session whose child lacks `storage` -- or `network`, or `pdf`, or `testing`, three capabilities BrowserAI had never named at all. **Of the 72 tools a fully-capable child exposes, BrowserAI's `tools/list` now carries 71 tools**, one fewer only because `browser_annotate` is withheld for liveness. *Corrected 2026-09-21 (previously "Of the **74** tools a fully-capable child exposes, BrowserAI's `tools/list` now carries **72** tools", two fewer, naming `browser_webmcp_call` beside `browser_annotate`). Re-counted off the regenerated snapshot and not decremented: `@playwright/mcp` 0.0.82 marked `browser_webmcp_list` and `browser_webmcp_call` `skillOnly`, so both left the exposed surface entirely while keeping capability `core`, and both figures moved by two. **The withheld set is back to one and nobody decided that** -- a tool upstream takes off the wire takes its verdict row with it, which is `UPSTREAM-REVIEW.md`'s own instruction and the direction `ToolVerdictTests` refuses on purpose. The deny's reasoning is preserved in [`tool-verdicts.json`](tool-verdicts.json) and [`upstream-review.json`](upstream-review.json), because the tool can come back.* *Corrected 2026-09-17 (previously "Of the **73** tools a fully-capable child exposes, BrowserAI's `tools/list` now carries **71** tools"). Re-counted off the regenerated snapshot and not incremented: the [dated `playwright-core` override](#the-two-exceptions-to-the-versioning-policy) added `browser_emulate_media`, `core` and so unconditional, judged `allow`, so both figures moved by one and the withheld set is unchanged at two.* *Corrected 2026-09-15 (previously "Of the **71** tools a fully-capable child exposes, BrowserAI's `tools/list` now carries **70** tools, **one** fewer only because `browser_annotate` is withheld for liveness"; **69 / 68** before that on 2026-08-20). Re-counted off the regenerated snapshot and not incremented: `@playwright/mcp` 0.0.81 added `browser_webmcp_list` and `browser_webmcp_call`, both `core` and so both unconditional, and the two were judged in opposite directions on 2026-09-15 -- the list `allow`, the call `deny`, on liveness. **This is the first build to withhold more than one tool**, which is why the clause says "two fewer" instead of naming one.* **What that costs is exactly what the paragraph above already said it was worth:** the boundary was never against the caller, who owns the session directory and reads the profile inside it as the same Windows user, so what the missing capability withheld was reachable one file read away and one restart of the same session away. What it bought was a caller who had to destroy a `headless` session and recreate it to read a cookie it already owned. The ten tools that became reachable for the first time are recorded as a deliberate grant in `SessionToolSurface.NewlyGrantedTools` and asserted by name in `ModelSurfaceTests`; the one of them that can mislead a **human** -- `browser_route`, whose mocked response renders as if it came from the server -- is warned about in the server `instructions`.

### A web page's own tools are reached through one tool of ours

**Settled 2026-09-21, by the maintainer, on a question this charter had not
anticipated.** `@playwright/mcp` 0.0.82 let a **page** put tools on the child's
`tools/list` -- names, descriptions and `inputSchema`s written by whoever wrote
the page, unbounded in number, different on every page and gone the moment the
tab navigates ([kb](kb/playwright/tools-and-artifacts.md#a-page-can-add-tools-to-the-childs-toolslist-and-its-own-text-reaches-a-caller----measured-2026-09-21)).
Deny-by-default refused every one of them at the door, which is that rule working
exactly as designed against an adversary it was never written for -- and it left a
real capability unreachable. The maintainer's framing, verbatim:

> the directive to not pass through unknown tools is so that we can keep tight
> control on the upstream's capabilities together with the pinned version, so
> that we never expose tools that have not been classified into one of the
> running profiles. The client-side web MCP keys, however, are a new beast ...
> page-offered MCP tools that can then be called by whoever is driving
> Playwright. Since that is 100% dynamic and can never be altered by us in an
> allowlist or blocklist ... those should be passed through.

**So page tools are reached as a CLASS, through one tool that IS classified:**
[`browserai_page_tool`](ARCHITECTURE.md#browserai_page_tool-and-how-a-page-tools-name-is-resolved),
the eighth authored tool, carrying an `answer` row in
[`tool-verdicts.json`](tool-verdicts.json) with its own `why`. **A fourth verdict
class was not available and is not a thing that can exist**: that file is keyed
by name, `ToolVerdictTests` holds it against the golden snapshot in *both*
directions, and a page's names are invented by the page. What the class
judgement buys back is everything a row would have given: the call is bounded by
BrowserAI's own timeout, which upstream has none of; the name is re-resolved
against the live tab on every call, so a wire name that is a different page's
code after a navigation is refused and not run; the caller's `why` is
recorded on the session like any other call; and `session` and `why` never reach
the page. **The door is not widened.** A `webmcp_*` name arriving straight from a
client still has no row and is still refused -- this tool is not a new door, it is
the only door.

**Direction A is recorded as the follow-on NOT taken.** It was to make the door
itself accept a page tool by *rule*, not by row -- upstream's `webmcp_`
prefix, plus its unconditional `[UNTRUSTED: ...]` description prefix, plus absence
from the golden snapshot. It is strictly more capable and it costs two things
this design keeps: *every advertised name has a row* becomes *every advertised
name has a row or matches a rule*, which is a weaker sentence to audit; and the
call would be unbounded unless a timeout were added anyway, at which point the
bounded caller exists regardless. It stays available if a page tool ever needs to
be callable by a client that has never read a snapshot.

**`webmcp: true` is written explicitly (Q219).** Upstream's default is on, and
[the generator writes it anyway](src/BrowserAI/Runtime/BrowserConfiguration.cs) --
the `allowUnrestrictedFileAccess` and `timeouts.idle` argument, that an omission
records no decision and `browser_get_config` cannot read back a key the file
never carried, plus one more of its own: **the snapshot block that key switches
on is the only catalogue there is.** A model learns a page offers tools at all by
reading `- webmcp tools (page-provided, untrusted):` in a `browser_snapshot`
answer; with the key `false` there is no block, no tab-header count and no
dynamic tools, so the caller would have a tool and no way to know what to name.
`false` was the recommendation until the caller existed, on the ground that the
block was page-authored text with no capability behind it. There is a capability
behind it now. **What it costs is unchanged and is [its own hazard row](HAZARDS.md#hazard-index):**
the page's own words still reach the model, exactly as before.

**BrowserAI does not declare `listChanged` (Q220).** Its list does not change:
`tools/list` is answered from [the run's own child](ARCHITECTURE.md#the-mcp-server),
which never navigates, so a page cannot move the advertised surface however many
tools it registers -- measured 78 before a page and 78 after. Declaring the
capability anyway would be cheap and **in practice irreversible**: clients cache
what they are told at `initialize`, so withdrawing it later is a promise broken
against every session already open. `VerticalSliceTests` asserts the literal
`{"tools":{}}` against the published binary, with a not-equal control, and that
is the mechanism. **Direction B is recorded as the direction not taken**:
announce `listChanged`, merge each session's page tools into the advertised list
namespaced per session, and forward the child's notifications. It is the version
a client could use without reading a snapshot, and it costs a client dependency
BrowserAI does not have today, it stops `SessionToolSurface.Rewrite` being a pure
function of one pageless child, and it puts page-authored text inside the system
surface -- where [the client's silent truncation budget](kb/mcp/protocol.md#what-2kb-each-means----measured-2026-08-18--claude-code-21234)
is measured per string and a page controls the strings.

### Storage tools capture bearer tokens

`browser_storage_state` and the cookie tools return `httpOnly` cookies, which JavaScript cannot read. These are session bearer tokens. Any mode permitted to call them must be treated as credential-bearing. `browser_storage_state` additionally never captures IndexedDB -- see [kb: tools that reach credentials](kb/playwright/tools-and-artifacts.md#tools-that-reach-credentials).

### `browser_get_config` redacts, and that is not a reason to set `secrets`

⚠️ **Corrected 2026-08-20 @ 0.0.79 (previously "### `browser_get_config` does not redact -- Its handler is `JSON.stringify(context.config, null, 2)` with no filtering, so it would emit `config.secrets` in plaintext if that key were ever set. `Verified 2026-08-13 @ 0.0.79`: not set").** **The handler reading was correct and beside the point.** Every tool response is serialised through `sanitizeUnicode(this._context.redactSecrets(serializedText))` on the way out, so the unfiltered `JSON.stringify` is redacted one layer above the handler that produces it -- reading the handler alone could never have answered the question. **Measured 2026-08-20** against the bundled child started with `secrets: {"MY_TOKEN": "sk-live-..."}`: the answer comes back `"MY_TOKEN": "<secret>MY_TOKEN</secret>"` and the literal value is nowhere in the frame. Upstream's own `config.d.ts` -- the copy in `payload/`, unchanged since before 0.0.79 -- has said so all along: *"Secrets are used to replace matching plain text in the tool responses to prevent the LLM from accidentally getting sensitive data. It is a convenience and not a security feature."*

**None of that is a reason to start setting `secrets`, and the correction must not be read as one.** `redactSecrets` is `text.replaceAll(secretValue, ...)` -- a substring match on the **value**, over the whole response. Measured in the same run with a third secret whose value was `chromium`: `"browserName": "chromium"` came back as `"browserName": "<secret>COMMON</secret>"` and `chromiumSandbox` as `<secret>COMMON</secret>Sandbox`. So a short or common value corrupts unrelated text, and a value the page never renders verbatim -- anything encoded, split across nodes or hashed -- is not redacted at all. BrowserAI still never writes the key and never passes `--secrets`.

⚠️ **Corrected 2026-08-18 (previously "BrowserAI should either redact before forwarding or refuse to expose the tool").** It refused the answer for a while -- a guard that scanned every `browser_get_config` result for `"secrets"` -- and that guard was **removed**. BrowserAI never writes the key and never passes `--secrets`, so the only way a config could carry one is upstream starting to populate it, which is a change to `config.d.ts` and therefore a **snapshot diff a human has to adjudicate before a release** ([the release gate](TESTING.md#the-upstream-review-gate)). Guarding the answer duplicated that at the cost of scanning every config call, and it belonged to the same removed layer that was described as a security boundary and was not one ([kb: tools that reach credentials](kb/playwright/tools-and-artifacts.md#tools-that-reach-credentials)).

### The child's environment overrides the config file BrowserAI generates

The merge order is **config file → environment → CLI**, and `@playwright/mcp` reads **40** `PLAYWRIGHT_MCP_*` variables covering nearly every option: `BROWSER`, `HEADLESS`, `USER_DATA_DIR`, `EXECUTABLE_PATH`, `OUTPUT_DIR`, `ISOLATED`, `CONFIG`, `SECRETS_FILE`, `STORAGE_STATE`, `CAPS`, and 30 more -- **42 in total**, [two of them read outside that mapping](kb/playwright/configuration.md#environment-merge-order-and-startup-output).

So a stray variable in the user's environment silently overrides BrowserAI's opinions -- and **`PLAYWRIGHT_MCP_CAPS` triggers the same replace-not-merge wipe documented below for `--caps`**, meaning there is an environment route to a bug the "never pass `--caps`" rule does not close.

**Requirement: build the child environment allowlist-style, never inherited-and-patched.** `ProcessStartInfo.Environment` is pre-populated with the inherited block and assignment *merges*, so `Environment.Clear()` must come first. Also strip `INIT_CWD`, `NODE_OPTIONS`, `NODE_PATH`, `DEBUG` and `DEBUG_FILE`, and set `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1` and `PLAYWRIGHT_SKIP_BROWSER_GC=1` -- the latter because Playwright's stale-browser GC deletes any registry directory not referenced by a `.links` entry, and the blast radius is "deletes BrowserAI's shipped Chromium."

Do **not** set `PLAYWRIGHT_SKIP_VALIDATE_HOST_REQUIREMENTS`: it writes a line to stderr, which trips the error-shaped-stderr detection in §E.

### `capabilities` replaces, it does not merge

`mergeConfig` spreads defined overrides, so passing `--caps` on the command line **silently wipes** the config file's capability list. The current launcher passes only `--config` and `--output-dir`, which is why this has not bitten. BrowserAI generates the child config and must not introduce a `--caps` argument alongside it -- nor allow `PLAYWRIGHT_MCP_CAPS` to survive into the child environment ([kb: environment and merge order](kb/playwright/configuration.md#environment-merge-order-and-startup-output)).

### Windows process spawning

Node's `spawn` cannot execute `.cmd` shims without `shell: true` -- a live Claude Code bug ([#58510](https://github.com/anthropics/claude-code/issues/58510)) for plugin-shipped servers using bare `npx`. Since BrowserAI ships its own Node, it must invoke the resolved `cli.js` with the bundled Node executable directly and never depend on PATH resolution or shell shims:

```
<install>\node\node.exe  <install>\mcp\node_modules\@playwright\mcp\cli.js  --config <abs path>
```

**This is also why the SDK's own `StdioClientTransport` cannot be used** -- it prepends `cmd.exe /c` unconditionally on Windows. See [Implementation stack](STACK.md#implementation-stack).

Set `WorkingDirectory` explicitly on every spawn. Left unset, .NET passes `null` to `CreateProcess` and the child inherits BrowserAI's cwd -- whatever Claude Code happened to have ([kb: stdio and exit codes](kb/windows/processes.md#stdio-exit-codes-and-process-startup)). That is reason 5 above, verbatim.

### Being in the data path is a new failure domain

The current launcher is a **supervisor, not a proxy** -- it inherits stdio and never touches a JSON-RPC message. BrowserAI is in the data path by design. A crash in it takes down browser automation entirely, where today a crash in one mode leaves the other three working. Error propagation, cancellation, progress notifications, and binary/image passthrough all become BrowserAI's responsibility.

Prior measurement of an equivalent Node prototype: images passed through byte-identical (509,620 base64 bytes), error shapes preserved, ~50 ms added latency on a 500 KB payload, ~300 ms one-off child spawn. The overhead is acceptable; the ownership is the real cost. It was a *Node* prototype, so it predicts BrowserAI's overhead instead of measuring it -- [kb: timings](kb/playwright/provisioning-and-timings.md#timings-spawn-resume-idle-close-proxy-overhead) says so explicitly.

Two pieces of ownership the SDK does **not** hand you:

- **Progress notification relay.** The child's `notifications/progress` arrives keyed to the token BrowserAI minted; re-emitting it upward with the caller's token needs your own token map. Nothing bridges this automatically.
- **Cancellation relay.** A `CancellationToken` cancels the outbound child request, but it is unverified whether an upstream `notifications/cancelled` produces a downstream one, not just a local abort. Needs a test -- it is precisely the kind of thing that fails invisibly.

### Why C#, and what it costs

Recorded because the research raised it from two directions and the answer is not obvious.

**C# is not required for the update story.** Velopack is language-agnostic -- the same 1.2.0 release ships `lib-nodejs`, `lib-rust` and `lib-python`, and the Rust `Update.exe` doing the real work is identical for all of them. In Node, "bundle a Node runtime" would also stop being a payload shipped *alongside* the app and become the app's own runtime.

**C# is chosen for §D and §E.** Node's `child_process` has **no job object support at all** -- that half is Node's own documented API surface ([kb](kb/windows/job-objects.md)). The sentence that used to follow it, *"every Node process supervisor on Windows falls back to `taskkill /T /F` or a native addon, and none survive a hard kill of the supervisor"*, is ***relabelled 2026-08-18 as reasoned, not surveyed***: it is an inference from the first half, no supervisor was ever enumerated, and "every"/"none" cannot be established the way it was written. It is left here because it is load-bearing for this decision and deleting it would leave the decision with no stated reason at all. Named mutexes are the same story, and carry the same caveat: `System.Threading.Mutex` is a first-class kernel object with automatic abandonment on crash, where Node is *believed* to need `proper-lockfile` and its stale-detection heuristics -- **also unmeasured**, and asymmetric besides, since a native addon reaches `CreateMutexW` by exactly the route this argument concedes for job objects. Those two primitives are exactly the locking and lifecycle requirements above, and they are the things that actually failed in the current setup.

**The price is two failure modes .NET makes *worse*, both silent, both already named above**: `Process.ExitCode` throwing after `Dispose()` (§E), and stdio encoding defaulting to CP437 with CRLF and BOM hazards (§E). Neither is hard to handle; both must be **invariants owned by a single wrapper type**, not conventions maintained by discipline. If those two are handled properly, the choice pays for itself.

### One path function: normalise what is cheap, refuse what is not

**Settled 2026-08-26, replacing *"Refusing network paths and aliased spellings at
the door"* (settled 2026-08-19).** The heading is the change: an aliased spelling
is no longer refused, it is **resolved**, and what is still refused is refused at
**every** door, not at two. The predicate is
[`CanonicalPath`](src/BrowserAI/Sessions/CanonicalPath.cs).

**Why the alias half inverted.** The old refusals were already computing the
answer they declined to use. `\\?\` is four characters off the front; a `subst`
is one `QueryDosDeviceW` whose target *is* the accepted spelling; a junction is
one `GetFinalPathNameByHandleW` on a volume already proven local. All three were
formatted into a sentence telling the caller to call again with that answer, so
handing it back as a path costs **no syscall at all** and saves the caller a
turn -- every turn, for a caller whose alias is not its own choice: a `subst` in
the user's shell, a redirected profile, a junctioned `AppData`.

**Why the network half widened to every door.** The 2026-08-19 decision ran the
refusals at `browserai_init` and `browserai_resume` only, *"because a session
that predates this build on a network path must still be listable and still be
removable"*. Nothing was ever distributed, so that population is empty -- and the
cost of the exception was measured on 2026-08-26, through the wire:
`browserai_destroy` on `\\10.255.255.1\share\a-session` took **21 seconds** to
answer *that is not a session*. **The maintainer's decision was `b`: both halves,
everywhere, uniformly.**

**The one refusal the inversion kept is `\\.\`**, and it is kept because the two
prefixes are different objects: `\\?\` is a length-and-parsing prefix over an
ordinary path, while `\\.\` is the device namespace, where `\\.\NUL` and
`\\.\PhysicalDrive0` name devices. The deleted `filename` gate refused it in
exactly those words, so this makes the directory rule agree with the filename
rule instead of adding a rule. It names the accepted form, so it is one turn.

**And the shapes Windows would silently rewrite are refused too**, which is new:
a segment ending in a dot or a space, a reserved device name, an alternate data
stream, a wildcard or a control character. Measured 2026-08-26 on .NET 10.0.11:
`Path.GetFullPath(@"C:\work\sess.")` answers `C:\work\sess` and
`Path.GetFullPath(@"C:\work\NUL")` answers `\\.\NUL`
([kb](kb/windows/detection.md#pathgetfullpath-rewrites-three-name-shapes-instead-of-rejecting-them----measured-2026-08-26)).
None of those fails; each hands back a directory that is not the one the caller
asked for, which is the two-spellings failure arriving through the name.

**The migration was accepted and not mitigated.** Every derived identity is
computed from the case-folded path, so no mutex, index key or lock file moves for
an ordinary local directory. What moves is the **spelling** in every answer and
every record: `GetFinalPathNameByHandleW` reports the drive letter upper-case
always, so a session opened from a shell that spelled it `c:\` gains one
`directory` statement on its next resume. That is the whole of what the identity
change looks like from outside, and it is named here and not suppressed. A
session whose own path traverses a junction, a `subst` or a `\\?\` spelling does
change identity: its index entry is swept and not orphaned, and the next
`init` or `resume` records it again.

**What "normalise" costs, in the order the questions are asked.** Characters
first, then the object manager, then one directory open per level climbed --
because the filesystem call is the thing being defended against.

**Two origins, and the axis is *may this call touch the filesystem*.** A path a
caller named pays for the whole sequence. A path BrowserAI stored, or a stranger
published in a window title, runs the subset that costs nothing -- **zero
syscalls by construction** -- because everything this build writes is canonical
already, and a stored path that is not was not written by this build. The index
already knows what to do with one of those. That is what keeps the machine-wide
read path free: it is one check per entry, not one directory open per entry.

### Refusing network paths and aliased spellings at the door

**Superseded 2026-08-26 by
[the section above](#one-path-function-normalise-what-is-cheap-refuse-what-is-not).**
The heading survives because a released
[changelog](CHANGELOG.md) entry links to it and a released record is not
rewritten to suit a later decision. What it said was that an aliased spelling is
refused and that `browserai_destroy` and `browserai_list` are deliberately not
guarded; both halves are now the other way round, and the reasons are above.

**The measured problem, first half.** One `File.Exists` against a share that has
stopped answering costs **22,210 ms**, measured 2026-08-19 -- and that number is
through a mapped **drive letter**, not a UNC spelling
([kb](kb/windows/detection.md#a-mapped-drive-letter-is-a-network-path-and-costs-the-same-22-seconds)).
Several such calls happen inside `LockScopes.PerDirectoryGate`, which is
per-directory and machine-wide, so **the caller who names the dead share is not
the one who waits**: every other process contending for that directory does. The
alternative was to move the slow calls out of the critical section, which is a
redesign of the most safety-critical code in the product to accommodate a
configuration nobody should be in. **Refusing is one predicate; the argument is
that the alternative is twenty-two seconds inside a machine-wide mutex.**

**The measured problem, second half.** `Path.GetFullPath` resolves neither
`\?\`, junctions, `subst` nor mapped drives, so two spellings of one directory
produce **two mutex names and one session directory**: the per-directory gate
stops serialising while every signal still reads healthy, and
[review A4](docs/reviews/2026-08-18-adversarial-locking.md) gives two
interleavings, one ending with two processes driving one browser profile and the
other destroying a session's purpose and history. The correct fix is to
canonicalise through the filesystem's own final name and hash *that*. **It was
not taken because it rewrites the identity of every mutex name, index key and
lock path in the product** -- every session directory in existence would acquire a
new identity on the day it shipped, and the two spellings would still both be
accepted, just as one session. Refusal is bounded, and BrowserAI creates its own
session directories in the common case.

**What "network path" means operationally.** Two tests, in this order, and
**neither touches the filesystem**:

1. The spelling begins with two path separators -- `\host\share`, `//host/share`,
   `\?\UNC\...`, `\.\...`. Characters only, no syscall at all.
2. `GetDriveTypeW` answers `DRIVE_REMOTE` for the drive letter. This is the
   operating system's own classification, so SMB, WebDAV and NFS are all covered
   without this repository maintaining a list of redirector device names.

⚠️ **And `QueryDosDeviceW` is asked before `GetDriveTypeW`, not after --
corrected 2026-08-26.** The 0.9 ms figure holds for a letter that is not a
substitution and for no other. Measured that day, `subst W: V:\dir` over a `V:`
mapped to an unroutable address: `GetDriveTypeW("W:\")` took **21,035 ms** and
then answered `DRIVE_NO_ROOT_DIR`, while the mapping itself answered
`DRIVE_REMOTE` in **1 ms**
([kb](kb/windows/detection.md#getdrivetypew-through-a-subst-onto-a-mapped-drive-costs-the-full-21-seconds----measured-2026-08-26)).
It resolves the substitution and classifies what is at the end of it, so on that
one shape the call the whole ordering was built around *was* the network call.
Unwinding the substitution first means `GetDriveTypeW` is only ever asked about a
letter that is not one -- and `StraySweep`'s title guard follows the same chain,
because a title on a letter `subst`ed onto a mapped drive is
[processes finding 8](docs/reviews/2026-08-18-adversarial-processes.md) wearing
one more layer.

**What "an alias" means operationally**, and this is the half where what is *not*
resolvable matters as much as what is:

| Alias | Resolved by | Cost |
|---|---|---|
| `\\?\` extended-length prefix | characters -- the path is the string minus four | none |
| `\\.\` device namespace | **not resolved -- refused**, and it names the accepted form | none |
| a `subst`ed drive letter | `QueryDosDeviceW` returns a target under `\??\`, and that target is the answer | ~0.02 ms, object manager only |
| a junction, a directory symlink, a volume mount point | `GetFinalPathNameByHandleW` on the deepest **existing** ancestor | ~0.07 ms, one directory open on a volume already proven local |
| an 8.3 short name | **nothing here -- `Path.GetFullPath` already expanded it** | none |
| a UNC path or a mapped drive letter | **not resolvable without a redirector, so refused** | none |

**Three candidate definitions of *aliased* were rejected in 2026-08-19 and the
argument survives the inversion.** *A string check for the `~N` shape* -- it would
refuse a directory genuinely named `data~1`, and it cannot see a junction at all.
*A reparse-point walk of every component* -- one `GetFileAttributes` per level to
learn what one `GetFinalPathNameByHandleW` answers outright. *Refusing instead
of resolving* -- which is what shipped in 2026-08-19 and is what this decision
replaces.

**What it knowingly leaves open, stated and not implied:**

- **A component this process cannot open.** The ancestor walk stops at the first
  failure that is not *the name does not exist*, and the path is then accepted
  unverified -- so a junction underneath a directory this token cannot open is
  invisible. Turning *unknown* into a refusal would refuse ordinary directories
  on a locked-down machine, which is the worse error.
- **Anything that becomes true after the check.** This is a **door, not a
  guard**. A `subst`, a `mklink /J` or a drive remapped while a session is open
  moves the directory underneath a session that was admitted correctly, and
  nothing re-checks. **The honest answer to "what about a local path that later
  becomes a network path" is: `resume` catches it on the next open, and a
  session already live is not protected at all.** Re-checking per call would put
  the guard on the hot path and still lose the race, because the remap can land
  between the check and the call it was protecting.
- **Case.** Two spellings differing only in case are one session by design, so
  the comparison is case-insensitive and `C:\A` is not an alias of `c:\a`.

**What it costs a caller.** A caller whose sessions live on a share is refused,
and told to run locally and copy afterwards. That is the only capability this
takes away, and it is taken away at **every** door.

⚠️ **Corrected 2026-08-26 (previously "**`browserai_destroy` and `browserai_list`
are not guarded**: a session that predates this build on a network path must
still be listable and still be removable, and a refusal that left a directory
unremovable would be a worse trap than the one it closes").** There is no such
session: nothing was ever distributed, so the population the exception protected
is empty -- and the exception's cost was 21 measured seconds inside a tool that
then answered *that is not a session*. A directory on a share is now something
BrowserAI will not create, open, list or delete; removing one is the
filesystem's job, and the refusal does not pretend otherwise.

---

## Open design decisions

### Shape and packaging

**Settled 2026-08-13.**

| Decision | Outcome |
|---|---|
| **MCP registrations** | **One.** `init` names a session directory and every other tool takes that absolute path in a required `session` parameter, which is what routes the call. See [sessions](ARCHITECTURE.md#sessions). *(**Corrected 2026-08-16, previously "`init` returns a short server-minted handle".** There is no minted handle: the directory **is** the identity, deliberately, because it is the one thing a model can always reconstruct.)* *(**Corrected 2026-08-18, previously "⚠️ The registration itself is not built -- see [the outstanding work](TODO.md)".** It is built: `src/BrowserAI/Registration/McpClientRegistration.cs`, decided and delivered on 2026-08-16 -- see [Locking, logging, versioning and registration](#locking-logging-versioning-and-registration).)* |
| **Profile and artifact locations** | **Arguments to `init`**, not global policy. The caller states where its data goes, per session -- so "shared or per-directory" is the caller's choice, not BrowserAI's. This is what removes the relative-path hazard. |
| **Where BrowserAI writes, outside a session directory** | **Nowhere the caller did not name.** ⚠️ **Settled 2026-08-29, by deletion, and it had never been written down** -- which is how it came to be false for thirteen days without anybody deciding it should be. Every file this product writes lands either under the app root (the session index, the browsers root, the process log, the live markers, the generated Playwright config, `mcp-registration.json` beside the install root) or *inside* the session directory `init` was handed. **One thing broke it**: `browserai-sessions.json`, a per-root roll-up added with artifact routing on 2026-08-16 and inherited by `Sessions/SessionRollUp.cs` when the rest of that machinery was deleted on 2026-08-26. It was written to the **parent** of the caller's directory -- a session at `C:\repo\.browserai` put an unignored file in `C:\repo\` -- which is the one thing [§7](#7-distribution-to-colleagues-has-no-story), [`README.md`](README.md) and the installer's own registration message all promise BrowserAI does not do. **The maintainer's answer was to delete it and not re-home it**: nothing read it -- not the product, not a script, only a test helper reading back what had just been written -- and its content is delivered live by the sibling-sessions line in the `init`/`resume` answer and by `browserai_list` at any scope. **Nothing enforces this and a test cannot**: *a path the caller never chose* is not a property of a string, and the nearest thing to a mechanism is that `SessionLayout` and `IAppPaths` are the only two places a destination is composed. What a reader can do is check the list -- every write site in `src\` was enumerated on 2026-08-29 and each one resolves to one of those two. |
| **Path validation** | **Any path is accepted.** Correct use is the calling agent's responsibility. See below. |
| **Child process model** | **One node child per handle.** Stays firmly on the proxy side of the scope boundary; costs ~300 ms and one process per instance. |
| **How many executables ship** | ⚠️ **TWO since 2026-09-15, in one installer, over one data root** *(previously one, and the word "single-file" in the row below still means each binary is one file -- it never meant there was one binary)*. **The maintainer's decision, verbatim:** *"go with the two binaries. One mcp server and one config app. The config app then has the icon, start menu, autostart on velopack install and everything. This gui app should be very minimal. It's only purpose is to allow the user to see some information."* **The forcing fact is not taste.** A non-silent `Setup.exe` finishes by starting `--mainExe` itself with `CREATE_UNICODE_ENVIRONMENT` and nothing else (`show_window = true`, hardcoded at `util_windows.rs:106`), the stub and `Update.exe start` launch it with no flags at all, and **nothing suppresses that start** -- 36 `vpk` options, 16 manifest fields, 7 environment variables and every hook return were enumerated, and only `--silent` skips it, as a side effect of hiding every dialog. A console-subsystem main exe therefore gets a console, which on this machine is a **1506×1490 Windows Terminal window** over the user's work. A Windows-subsystem binary is never allocated one, so the main exe is now `BrowserAI.exe`, the **configuration app** -- and `BrowserAI.Server.exe` is the MCP server, which no installer ever launches and which a client starts by absolute path. **The five things `--mainExe` decides at once** are why the name went that way, not the other: the post-install start, the root stub's name, what `Update.exe start` launches, what all four hooks run on, and what the Start Menu shortcut points at. **What both share is one library**, `BrowserAI.Core` -- one data root, one registration, one update feed, one census -- because two copies of any of those would be two answers to the same question. See [the two executables](ARCHITECTURE.md#the-mcp-server). |
| **What the configuration app is allowed to do** | **Show state, and change nothing without one explicit click.** It reads the installed version, the install and data locations, and what is registered with Claude Code; it offers check-for-updates, register at user scope, unregister, register in a project you pick, and open the logs. **Never acts on inferred intent** -- a first run shows what happened instead of asking -- and **Close is the default button on every page**, because Enter is the key a person presses without reading and every other button edits a file. ⚠️ **It never adopts, overwrites or removes a `browserai` entry it did not write**: an entry whose command is not under our install root is another BrowserAI's, and it is reported with its location and left alone. **The model is OutlookAI's, down to the words** -- *"for all my Claude Code projects"* for user scope and *"in a specific project"* for a committed `.mcp.json` -- because two products in one estate describing one mechanism differently is how a person learns it twice. **What did NOT transfer is its hand-rolled JSON splicer**: BrowserAI writes through `claude mcp add`, and project scope is the same call with `--scope project` and the chosen folder as the working directory. Reading is ours and writing is the client's, and the asymmetry is deliberate -- a read that misunderstands the file shows a wrong sentence, a write that does corrupts somebody's configuration. |
| **Browser and packaging** | **Full Chromium, NativeAOT single-file.** No host dependency on Node, .NET or Chrome. Installer **53,567,930 bytes -- 51.1 MiB**; browsers provisioned on first run (207.3 MB down, 437.24 MiB on disk, ~10.8 s at chromium 1244 -- *re-measured 2026-09-16; previously 203.8 MB, 430.48 MiB, ~12.6 s at chromium 1237*), once per machine, not once per update. See [the runtime](ARCHITECTURE.md#the-runtime-it-ships). *(**Corrected 2026-08-18, previously "Installer ~117 MB".** That was written on 2026-08-13, before anything was built, and it was the **installed payload**, not the installer: `current\` weighs 124.39 MiB and the payload inside it 116.40 MiB ([kb: component sizes](kb/playwright/provisioning-and-timings.md#component-sizes)), so every other `~117 MB` in this repository is about that and stays. The self-extracting installer is LZMA2-compressed and is less than half of it. **Re-measured, not computed:** the installer as `build/New-Release.ps1` packed it for `v1.0.0` on 2026-08-17 -- then named `BrowserAI-win-Setup.exe`, and `BrowserAI.exe` since 2026-09-15; the bytes are the same file -- the `0.1.3` run of the same script produced 53,511,950 bytes, so the figure is stable across versions to within 0.1%. The `-full.nupkg` beside it is 49,106,362 bytes and the portable zip 49,105,337. **No test holds this number and none can:** `Releases/` is gitignored, so the artifact does not exist on a clean clone or on CI, and a test asserting it would be red everywhere it was not just built. Re-establish by running the release script and reading the file size.)* |

**On accepting any path.** `init` does not constrain `userDataDir` or artifact paths to a sanctioned root. The consequence, recorded so it is inherited and not rediscovered: an agent may point an instance at the user's **real Chrome profile** and read live browser state, or write artifacts anywhere the process can reach.

This is consistent with the trust model already in force -- the current `persistent` mode grants any calling agent every login stored in its profile, and the workspace runs in `bypassPermissions`. BrowserAI is not the boundary; the agent's own instructions are.

Two things follow, and they are design obligations, not caveats:

- **The `init` tool description is a security surface.** It is the only place the calling agent is told what a path argument means. Write it as guidance an agent will actually follow -- name the sensible default location, and say plainly what pointing at an existing browser profile does.
- **Log the resolved absolute paths at instance creation.** If BrowserAI does not enforce a boundary, it must at least make crossings visible after the fact. This is the same principle as §E: the failure that cannot be seen is the one that costs five days.

**On one child per handle.** Research verified a single node process *can* serve several configurations -- two servers with correctly divergent surfaces (42 vs 59 tools), no module-global browser state, browsers created lazily on first tool call ([kb: tool surface](kb/playwright/tools-and-artifacts.md#the-tool-surface-and-the-package-shape)). That path is rejected not on capability but on scope: it is reachable only through the programmatic `createConnection` API, which means writing a JS shim and moving toward the boundary [Scope](README.md#scope-proxy-not-implementation) forbids. Spawning `cli.js` per handle keeps the proxy a proxy.

### Licence, release policy and the tool surface

**Settled 2026-08-14.**

| Decision | Outcome |
|---|---|
| **License** | **Source-available**, under a bespoke five-year variant of the Functional Source License 1.1 (MIT Future License). Fixed before any code exists, and it constrains dependency selection from here. See [License](README.md#license). |
| **Repository visibility** | **Public**, opened at `v1.0.0`. Source-available is the licensing posture and publishing was always a separate decision; this row recorded that the decision had not been made. It has. *Corrected 2026-08-17 (previously "Private for now").* |
| **Third-party payload** | Keeps its own terms. Bundling creates redistribution obligations that bite at first installer handoff *regardless* of which license BrowserAI itself carries -- enumerated under [Third-party components](README.md#third-party-components). |
| **Update tracks** | **One.** No beta channel. A second track doubles the release matrix and makes the version string load-bearing -- a shipping in-house product derives its runtime track from a `-beta` suffix, so a formatting change breaks track detection silently. A single track still requires the channel to be set explicitly, for the reason in [updates](ARCHITECTURE.md#updates) landmine 1. |
| **The update pass's timers** | ⚠️ **FOUR, AND ALL FOUR SIT INSIDE THE CRASH TRIPWIRE. Decided 2026-09-24, the maintainer verbatim:** *"Wrap the check in its own timer. So all three timers sit in the tripwire's time."* The check gets 15 minutes, the download 30 absolute and 60 s of no progress, and the tripwire is 45 -- so `check + absolute` is 45 against 45 and **no pass in which every timer behaved can reach the outer deadline**, which is the sentence that deadline is named for. **Added by addition: what this row replaces is an absence, not a claim.** Until this decision the check ran under the tripwire alone and was bounded by nothing this product controls -- Velopack 1.2.158's `CheckForUpdatesAsync()` takes no `CancellationToken`, so a stalled manifest fetch ended only on its own 30-minute `HttpClient.Timeout`, and 30 + 30 against 45 meant a healthy pass could trip the wire. Worse, a network timeout arrives as a `TaskCanceledException`, so the one log line that means *our own timers failed* was also the line an ordinary slow feed produced. **The directions not taken:** discriminating the catch alone, which would have fixed the attribution and left the unbounded call; and raising the tripwire above 60 minutes, which would have made the arithmetic true by making the crash detector slower than the thing it detects. **The budget is applied by AWAITING the call through its token, not by passing the token on** -- past the point where the call reaches Velopack a token is inert, so the pass ends on time and Velopack's call finishes into nothing. That cost is accepted: the alternative is a pass that cannot be ended. `UpdateTests.ACheckThatNeverAnswersEndsOnItsOwnBudgetAndSaysSo` holds it, and `.TheOuterDeadlineIsATripwireRatherThanASecondBudget` holds the arithmetic |
| **Deltas** | ⚠️ **NONE. Every release packs FULL PACKAGES ONLY, and that is a decision of record, not a property of how a cut happened to be run. Decided 2026-09-22, the maintainer's words verbatim:** *"always produce full packages only. The sizes are so small, and internet speeds nowadays are so fast that we don't want to exert any effort in creating deltas. Full downloads are always just easier."* `build/New-Release.ps1` passes `--delta None`, which is `vpk`'s own name for it -- resolved from the tool, not from memory, by handing `--delta` a value it cannot parse and reading back the modes it names. **Without the argument the default is `BestSpeed`**, so this is an argument that has to be passed, not a behaviour that has to be avoided. ⚠️ **It looked like it was already true and it was not:** no release before 1.1.0 ever carried a delta, and not because anybody chose it -- the clean re-pack empties `Releases/`, so `vpk` never had a previous package to compare against. The first cut that left one there would have started emitting deltas with nothing to say so, which is the shape this row exists to stop. **What it costs, measured 2026-09-22:** a same-tree N→N+1 delta is **138,791 b** against a full package of **55,022,705 b**, so every install on every release now downloads about 55 MB where it could have downloaded 139 KB. That is the trade, stated and not hidden. **What it buys** is one artifact per release and a rollback that is always a plain download. `ReleaseScriptTests.EveryReleasePacksFullPackagesOnlyAndTheFeedCarriesNoDeltaRow` holds both halves -- that the argument is passed, and that it means what this row says -- with the positive control that the same pack without it does produce a delta. |
| **When the feed is asked, and how often** | ⚠️ **Once per server start of an installed, non-pre-release BrowserAI, and never on any other schedule. Q225 b, the maintainer's answer, added 2026-09-22.** There is no timer, no interval and no retry: `UpdateService.StartInBackground` runs one pass and the type holds no `PeriodicTimer`. **A session is not a check and a browser is not a check** -- sessions and browsers are children of one server per client connection, so a hundred browsers in one session cost zero extra requests, which is the question that was asked: *"is there not a risk of flooding the release page with update checks if an agent starts 100+ browsers in sustained bursts?"* **Two of the three guards are ours, not Velopack's**, and that is the half that was written down as an inference and is now read: `StartInBackground` returns before any network call when `!isInstalled`, and again when the build carries a pre-release suffix; Velopack supplies only the predicate, through `VelopackLocator.Current.CurrentlyInstalledVersion` being null. So a checkout, a `dotnet run`, every test host and every suite-started server ask for nothing at all. **The monitor is the asset's own download counter and it discriminates**: on 2026-09-22 `releases.win.json` read **48** against **1** for every other asset on the release, while the suite had started thousands of servers in the same window -- and the flat `1` on the packages says no install anywhere has ever applied an update. **The pathological case is named and accepted**: a client spawning a fresh server per task in bursts costs one 260-byte GET per start and inflates that counter, which degrades the monitor, not the service. **The alternative was offered and declined** -- a machine-wide check stamp in the shared data root, skipping the feed when any instance of this install had checked within the hour -- because the check it would suppress is one request per process start. See [kb](kb/packaging/velopack.md#how-often-the-feed-is-asked-and-by-what----measured-2026-09-22). |
| **Where the feed and the package are hosted** | ⚠️ **GitHub release assets on this repository's own releases, reached through `releases/latest/download/`, and nowhere else. Q237 = f, decided 2026-09-23, the maintainer's answer verbatim:** *"Q237 f"*. The question was raised by the asset trim -- if a release carries three files, does it need to carry them at all -- and was researched in full before it was answered ([evidence](docs/evidence/2026-09-23-feed-hosting-research/README.md), [kb](kb/packaging/velopack.md#where-the-feed-and-the-package-are-hosted-and-the-four-places-they-could-have-gone-instead----read-2026-09-23)). **One fact governs every alternative at once: a published release asset cannot be redirected.** No GitHub facility makes `releases/latest/download/releases.win.json` serve from somewhere else, so any move strands every installed build that has not first updated through the old URL; the only runtime lever is `BROWSERAI_UPDATE_FEED`, which is a thing a person sets by hand on one machine. **A hosting move is a one-way door for the installed base**, and that is true of all five directions below. **And what the current arrangement gives for free is freshness**: the alias answers `Cache-Control: no-cache` and a new release is visible immediately, measured 2026-09-23. **The five directions not taken, each with the fact it rests on.** **(a) GitHub Pages for both** -- the published-site cap is 1 GB, about **18 packages live at once**, and the soft bandwidth limit is 100 GB a month, about **1,860 downloads** of a 55 MB package, against release assets that GitHub documents as having no size or bandwidth limit at all; a Pages feed is also served from a Fastly edge at `max-age=600`, so *immediate* becomes *up to ten minutes*. **(b) Pages for the feed only, the package left on the release** -- Velopack's `SimpleWebSource` accepts an absolute `FileName` in code while its documentation requires the package in the same folder as the feed, and `vpk pack` regenerates `releases.{channel}.json` with bare names on **every pack**, so a split feed is not a configuration but a post-processing step somebody has to remember, on the one file a client cannot do without. **(c) A second repository's releases** -- the only arrangement that keeps 55 MB per release out of a contributor's clone, because a default `git clone` fetches every branch's objects and an orphan branch does not avoid it; it costs a second repository to keep in step, for a problem this project does not have while it publishes assets instead of commits them. **(d) A pinned pre-release carrying the feed** -- the base URL would have to become tag-specific, which is the one thing `releases/latest/download/` exists to avoid. **(e) An external host** -- a bill, a second set of credentials and an availability story this project does not have, for a 260-byte file that costs nothing where it is. **What would re-open this**: a release asset acquiring a bandwidth limit, or a reason to stop publishing releases at all. |
| **Dependency versioning** | **Everything floats at build time; the build freezes it.** Nothing is pinned by hand, the payload included. The build resolves latest, the suite gates it, the release records exactly what shipped, and the client resolves nothing at runtime. Adopted from a sibling project and applied without exception. See [Versioning policy](#versioning-policy-everything-floats-the-build-freezes-it). |
| **Release trigger** | **Manual, by the maintainer, through the agent.** No release pipeline, no scheduled publish, no auto-merge on green. Green is necessary and not sufficient -- a human decides when a green build becomes a release. See [The release gate](TESTING.md#the-release-gate). |
| **Generated code** | ⚠️ **WITHDRAWN 2026-09-24 BY THE MAINTAINER: GENERATED CODE MAY SHIP.** Q274 = c, his words verbatim: *"q274 c be liberal with the license interpretation. I really believe it is ok."* **What may ship** is generated code of every kind, CsWin32 output generated from Microsoft's Windows metadata and C#/WinRT projections included. **The licence contradiction recorded below is resolved by his liberal reading of it**, which is his decision and not a legal opinion this repository has acquired. **One obligation stays, and it is the whole of the rule now: the first commit that ships generated output from third-party metadata adds that metadata's notice to [`THIRD-PARTY-NOTICES.txt`](THIRD-PARTY-NOTICES.txt), under the notice rules the rest of that file follows.** The mechanism is `ForbiddenDependencyTests.AProjectUnderSrcThatReferencesTheCodeGeneratorShipsTheMetadatasNotice` -- *inverted 2026-09-24 (previously `NoProjectUnderSrcReferencesTheCodeGenerator`, which refused any reference under `src/`)* -- and it requires the notices file to name every package the generator reads, as the test project's lock file resolves them, beside any `src/` reference to it. **The C#/WinRT half has no mechanism and needs a reader**: a projection arrives through a target framework carrying a Windows SDK version or a `Microsoft.Windows.CsWinRT` reference, and that scan reads neither. **Q273 = b, taken the same day: the toast interop will use .NET's `[GeneratedComInterface]` and `[GeneratedComClass]`**, whose generators read this repository's own declarations and no third-party metadata. The layout oracle is unchanged, a test-project reference at `PrivateAssets="all"`. **Everything after this paragraph is the rule as it stood from 2026-08-20 to 2026-09-24, kept by addition as the record.** ⚠️ **NONE OF IT EVER SHIPS, AND THAT IS A STANDING RULE AND NOT A *FOR NOW*. Decided 2026-08-20 by the maintainer, in the form the record carries: *no generated code will ever ship.*** Not vendored, not committed, not emitted at build time into the product, not published in a public repository, at any future version. **Moved here 2026-09-23 from [`QUESTIONS.md` section 12](QUESTIONS.md#12-the-cswin32-metadata-licence----moot-2026-08-20-and-the-entry-stays), which is where it was taken and the wrong place for it to live**: that entry is a question, and a rule kept only inside a question nobody has to answer any more is a rule nobody will find. Everything under that heading stays exactly as it is -- the terms, the five questions, the recommendation -- because it is the brief a future maintainer needs in order to reverse this. **What the rule buys is that the CsWin32 metadata licence contradiction never has to be resolved.** `Microsoft.Windows.CsWin32` is MIT; the metadata it reads is governed by the Windows 10 SDK EULA, and `win32metadata`'s own `README.md` calls the one file CsWin32 reads MIT, so the package and the repository disagree about the same bytes. That is real, unresolved, and **unreachable**, because the only act that would engage it has been ruled out. **CsWin32 stays where it is and this is what it is for:** a direct reference of the test project at `PrivateAssets="all"`, a *layout oracle*. The product hand-writes its seven interop structs and the suite asserts their sizes and field offsets against Microsoft's own metadata instead of against a comment, so deleting the reference would remove the only independent check that the hand-written interop matches what Windows expects. **The mechanism is `ForbiddenDependencyTests.NoProjectUnderSrcReferencesTheCodeGenerator`**, which reads every build file for the `Include=` attribute and refuses one under `src/`. The reference is what is asserted because generated code has no mark in a binary and no file in the tree, so the reference is the only earlier signal there is; the two that must stay are asserted by count, so losing the oracle is a red build too. **What re-opens all of it:** any proposal to ship generated code. At that point direction (b) of that entry -- the five questions to a lawyer -- is the next step, and the brief is already written. |
| **Test framework** | **TUnit**, already the house standard in a sibling project. Source-generated, reflection-free, MTP-native, MIT. See [Implementation stack](STACK.md#implementation-stack). |
| **What the gate's clearance reads, and how** | ⚠️ **The client registration is read by parsing `~/.claude.json`, never by starting the client. Decided 2026-09-24 by the maintainer, Q281, in his words verbatim: *"Q281 a"*, and permanently.** *Added by addition; no row named the reading before.* The gate compares six readings either side of every run -- `build/Get-ClearanceSnapshot.ps1` -- and the third, the real `browserai` registration the install and uninstall hooks rewrite, ran `claude mcp get browserai` on every gate until that day. That verb starts the client, which health-checks the server it names and can write `~/.claude.json`: the reading could change the one file the comparison exists to prove untouched. The entry is now opened for reading with every sharing mode, parsed with `ConvertFrom-Json -AsHashtable`, and printed key by key. **The mechanism is `SuiteCoverageTests.TheClearanceSnapshotReadsTheRegistrationWithoutStartingTheClient`**, which reads the script as code and refuses a call to the client's MCP verbs or a reading that parses no file; planted red against the script as it stood. ⚠️ **And each client's reading is its BrowserAI entry and no other part of its file -- Q292, decided 2026-09-24 by the maintainer, verbatim: *"Q292 a - Same for claude code"*.** *Added by addition.* The Codex reading hashed the whole `~\.codex\config.toml` until that day, and the Codex desktop app rewrites it when it starts, so a gate that spanned a Codex start stopped on a change that was not BrowserAI's. It is the `[mcp_servers.browserai]` entry now, line for line, beside Claude Code's `mcpServers.browserai`; `SuiteCoverageTests.TheClearanceComparesOnlyEachClientsBrowserAiEntry` drives the script against a scratch profile and was planted red against the whole-file hash. |
| **What the real-installer arms install** | ⚠️ **The suite's own installer, packed from the tree before each gate run, never published and never tagged. Decided 2026-09-24 by the maintainer, Q287, in his words verbatim: *"Q287 a"*.** *Added by addition.* The arms that run a real `Setup.exe` installed whatever `Releases\test-pack` a release cut had left, so between releases no real installer ran a hook the tree had changed: on 2026-09-24 they were installing a 1.1.0 build that wrote the old registration record. `build/New-Release.ps1 -TestPackOnly` packs `BrowserAI.app.test` from the two publishes the run tests and a shipping-id twin beside it for the one comparison that needs two packs of one publish, and touches nothing a release is made of; every gate driver runs it under the installer lock before its first run. The `release installer` capability refuses a pack that is not byte for byte the published slices, and the arms then hold the publish to the tree. **What it costs:** about forty seconds per gate half, and at a release cut the arms install the publish the suite tests and not the release publish item 7 makes, and ILC makes the two different bytes of one tree -- recorded in [RELEASING](RELEASING.md#8-run-everything). |
| **The two things product code carries only for the suite** | ⚠️ **TWO, NAMED HERE SO THAT THEY STAY TWO. Decided 2026-09-24 by the maintainer, Q293, in his words verbatim: *"Q293 a"*.** *Added by addition.* The rule everywhere else is that the product has no switch a test flips, and both of these were taken as exceptions by the writer who added them and put to him as such. **(1) The server forwards `PWTEST_SERVER_REGISTRY` to every child it starts** (`ChildEnvironment`'s inherited allowlist). The variable is Playwright's own, nothing in the product sets it, and it overrides no key BrowserAI's generated configuration writes; forwarding it is the only way the child that writes the descriptors and the reap that collects them read one directory, which is what lets `ServerRegistryReapTests` exercise the reap against a scratch registry instead of the maintainer's own. **(2) `JobLauncher.Start` takes an optional `desktop`, and the product never passes one** (Q279): a Windows-subsystem child shows its window whatever `CREATE_NO_WINDOW` says, and a desktop nobody is looking at is the only thing that keeps the installer arm's dialog off the screen, so `RealInstallerTests` and `WindowWatchTests` launch through the product's own launcher with a desktop named, and a second launcher in the harness would carry the handle-inheritance defects this one already closed. **What the alternatives were:** for (1), overriding `LOCALAPPDATA` for the test host, which moves every Windows path a child reads and reaches every child every other arm starts, or no arm at all; for (2), a harness-side `CreateProcessW` of about 120 lines. **A third is a new decision, not a precedent.** Nothing enforces the count; `ChildEnvironment`'s and `JobLauncher`'s own remarks name each one where it lives. |
| **SDK test fixtures** | **Not vendored. We write our own harness.** The MCP SDK's `ClientServerTestBase` + `tests/Common/Utils/*` (1,082 lines, Apache-2.0, unpublished to NuGet) wire *one* pipe pair; a proxy needs two hops. Copying them would mean a permanent three-way merge against an upstream that edits `tests/` weekly, and would lock the framework to xUnit. See [We write our own harness](TESTING.md#we-write-our-own-harness). |
| **Instance teardown** | **There is no close tool, and that is the decision.** Teardown is stdin EOF, the client-liveness watcher (an `OpenProcess` handle on the client PID -- never ping-based; `ping` is removed at 2026-07-28) and the browser-idle timer. `browserai_destroy` **deletes** a session; nothing ends one while keeping it resumable, because nothing needs to. **Expiry is reclaim, not destruction:** a torn-down session stays resumable against its recorded directory, since the durable thing is the profile, not the process. Measured 2026-08-14: resume costs **515 ms** and loses only `sessionStorage` ([kb: timings](kb/playwright/provisioning-and-timings.md#timings-spawn-resume-idle-close-proxy-overhead)). Timer values are settled, not open: [exactly one timer exists](ARCHITECTURE.md#sessions), browser-idle at ~10 minutes. |
| **`browser_run_code_unsafe`** | ⚠️ **Corrected 2026-08-18: available in every mode** *(previously "**Hidden in `interactive` sessions** ... Hiding it in the one mode that exists to keep human credentials from the agent makes §trade-offs' claim true for the first time; it stays available elsewhere as an escape hatch")*. **The measurement stands and is not what changed:** demonstrated 2026-08-14, against the default 24-tool surface with zero `browser_cookie_*` exposed, `async (page) => page.context().cookies()` returned an `httpOnly` bearer token ([kb: tools that reach credentials](kb/playwright/tools-and-artifacts.md#tools-that-reach-credentials)); it is in `core`, so no capability setting disables it, and it was the only hole -- `browser_evaluate` → `document.cookie` returns `""`, and `browser_network_request` strips `Cookie` and `Set-Cookie`. What changed is the conclusion drawn from it. Hiding it made the claim true only against a caller with **no file access**, and the caller chooses the session directory and reads the profile inside it as the same Windows user. The whole tool-permission policy went for that reason; see [§trade-offs](#the-init-design-weakens-a-security-boundary). |
| **Artifact placement** | ⚠️ **The output directory is FLAT and BrowserAI adds nothing to it, 2026-08-26** *(previously "**Routed on the way in, not sorted on the way out.** The child's cwd is the instance output root, `filename` arguments are normalised into typed subfolders, and every result carries the resolved absolute path")*. Routing is deleted: the child's cwd is still the session's output root, and that is now the whole of it -- a `filename` reaches the browser server as the caller spelled it, and upstream's own file-access roots refuse anything that resolves outside. ✅ **The half of the deleted note that mattered came back on 2026-09-17, as upstream's, not ours** *(added by way of correction; the sentence above is unchanged)*: the routed answer used to name every artifact absolutely, and from 2026-08-26 until that date the child's own relative pointers reached a model unaccompanied. The config now writes `filePaths: "absolute"` -- [upstream ask #1](TODO.md#upstream-asks), granted as [microsoft/playwright#42673](https://github.com/microsoft/playwright/pull/42673) -- and [every pointer shape was measured absolute, before and after and end to end](kb/playwright/tools-and-artifacts.md#every-artifact-pointer-a-tool-result-carries-is-absolute----measured-2026-09-17). One config key, no bytes rewritten, so the *nothing between the two servers* doctrine is not weakened by it. **There is no default root, because there is no default** -- `init` requires an explicit session directory and refuses an empty or relative one. Per-instance paths stay unconstrained; per-call filenames do not. *(**Corrected 2026-08-16 at the plan's final audit, previously "The default root is outside any repository."** An earlier draft did default to `%LocalAppData%\BrowserAI\sessions\<label>\`; [the output directory](ARCHITECTURE.md#the-sessions-output-directory) removed it and the shipped `init` refuses instead. A safe default answers the symptom -- files landing in repo roots -- while preserving the cause, which is that nobody chose where they should go.)* See [the output directory](ARCHITECTURE.md#the-sessions-output-directory) and [the `init` contract](ARCHITECTURE.md#sessions). |
| **Tool naming** | **Never renamed.** Names are upstream's byte-for-byte; the names BrowserAI authors carry the `browserai_` prefix -- see [Processes, browsers and session modes](#processes-browsers-and-session-modes). (This row originally read "the only name BrowserAI authors is `init`", which the design outgrew.) Descriptions are append-only. A `deny` hook keyed on `browser_take_screenshot` exists in ten repositories and a rename would disable it silently -- but the deciding argument is maintenance: upstream renamed nine of its own tools sixteen days after its first release -- 0.0.9 (2025-04-01) to 0.0.10 (2025-04-06), where lib/tools/screenshot.js became lib/tools/screen.js and every name in it moved, and lib/tools/common.js split, taking browser_choose_file to browser_file_upload and browser_go_back to browser_navigate_back with it -- and renamed browser_wait to browser_wait_for at 0.0.22 (2025-05-07) to 0.0.24 (2025-05-12), same file, same binding, same capability. Measured 2026-09-23 from npm's own published tarballs, each pairing confirmed against the defining file in both versions. Corrected 2026-09-23 (previously "upstream renamed one of its own tools inside four months"), which named no version pair and understated it by eight tools. The deny hook above is an illustration and not an instance: browser_take_screenshot is one of the names that survived, defined in lib/tools/snapshot.js in both versions. A rename map is a second surface to re-review on every bump. |
| **Upstream review** | **Gated by evidence, not approval.** A version bump cannot reach a release until the build has diffed four snapshots against the resolved payload, the suite is green, and the marker entry adjudicates whatever actually moved. An approval prompt was tried first and abandoned -- measured inert against sub-agents, and against a human it proved only a click. See [The upstream-review marker](TESTING.md#the-upstream-review-gate). |

**Why not permissive.** Apache-2.0 was the runner-up and is the smoothest technical fit: it is what `@playwright/mcp` and the C# SDK already carry, and its §4(b) change-statement requirement is real protection for something distributed as a binary installer nobody reads the source of. It was rejected only because it gives the commercial market away outright. AGPL-3.0 was considered and rejected on the merits -- BrowserAI is stdio-only, one machine, one user, **no processes and no ports**, so §13's network-interaction clause could never fire and the license would be inert boilerplate.

### Processes, browsers and session modes

**Settled 2026-08-15.**

| Decision | Outcome |
|---|---|
| **Process containment** | **One job object per instance, `KILL_ON_JOB_CLOSE` and nothing else, assigned at creation via `PROC_THREAD_ATTRIBUTE_JOB_LIST`.** Verified end to end against real Chromium and Firefox trees: 16 runs, 106 processes, **0 escapees, 0 survivors**. The guarantee is conditional on our implementation, not on the browsers -- two of the failure modes were proven fatal by measurement. See [the job object contract](ARCHITECTURE.md#process-containment-and-observability). |
| **Never by image name** | **Structural, not procedural.** BrowserAI can only terminate a process belonging to a job it created, or one identified against a path it owns. `GetProcessesByName`, `taskkill /IM` and name-filtered WMI are forbidden and analyzer-enforced. See [`NeverByImageNameTests`](tests/BrowserAI.Tests/NeverByImageNameTests.cs). |
| **Chromium binary** | **Full Chromium always**, headless runs included; `chrome-headless-shell` is not shipped. It makes the payload ~120 MB *smaller*, gives Chromium's own `lockfile` and `Chrome_MessageWindow` whether or not a window appears, and removes a branch. The shell's only advantage -- it cannot be resurrected after a reboot -- is worth less than being findable when it leaks, and the sweep it would avoid must exist anyway for a headed run. *(Reworded 2026-08-20, previously "in every mode ... removes a per-mode branch ... must exist anyway for the three headed modes"; session modes are gone and the property is now unconditional, which is what it always described.)* |
| **What stops a provisioning run** | ⚠️ ***Corrected 2026-08-19 (previously a 45-minute ceiling on the whole install).*** **A STALL, not a total.** A total-time cap can only ever fire on a link that is slow and working -- 203,824,344 B in 2,700 s is 0.60 Mbps -- while a link that has died is caught by upstream's own 30-second socket timeout twenty times sooner, so the old cap punished the one case it could reach. Progress is **bytes on disk under the browsers root**, which is one number only because BrowserAI points the installer's `TEMP` at `<browsers root>\.downloads`: upstream downloads into `os.tmpdir()`, so without that the root does not grow at all until the unzip starts. The cap is **ten minutes with nothing written**, set by upstream's own `__dirlock`, which legitimately writes nothing for up to **470 s** while an install queues behind another ([kb](kb/playwright/provisioning-and-timings.md#what-grows-on-disk-while-an-install-runs-and-when----2026-08-19)). The refusal a browser call meets meanwhile became a **progress report** in the same change, because upstream emits no progress notifications at all and it is therefore the whole mechanism. |
| **Chromium sandbox** | **Passed as the `--sandbox` CLI flag, never the config key**, which is silently discarded. Asserted by a test on the child's resolved browser command line. |
| **Firefox restart registration** | **Disabled on every launch** via `firefoxUserPrefs: { "toolkit.winRegisterApplicationRestart": false }`. The pref is observed at runtime and calls `UnregisterApplicationRestart`. The only place browser resurrection can be prevented outright instead of cleaned up after. **Built and measured 2026-08-16** (Firefox support): the config generator writes it for every Firefox config, and a live launch answers `ERROR_NOT_FOUND` on every process where an upstream-default launch leaves one registered. It is delivered over juggler, not through the profile's `user.js`, which is the BiDi launcher's path and not the one upstream takes. |
| **Windows `RestartApps`** | **Never touched.** The maintainer's machine has it enabled, which is the direct cause of the resurrection incident, but it is a personal, global, per-user setting. BrowserAI reads nothing and writes nothing there. |
| **`browser_annotate`** | ⚠️ **Corrected 2026-08-18, later the same day: REMOVED from the model-facing surface entirely** *(previously "refused wherever no window was promised, and permitted on the two modes that open one"; and "**`interactive` sessions only**" before that)*. It is filtered out of `tools/list` in every session -- filtering is in scope by [the charter](README.md#scope-proxy-not-implementation), renaming is not -- and a caller that names it anyway is refused, because a model knows upstream's tool names from everywhere except this server's list. **Four measured facts, each its own reason** ([kb](kb/playwright/tools-and-artifacts.md#what-browser_annotate-actually-does----measured-2026-08-18), three runs): it **has no self-timeout** -- the control run stood silent for a full 90 s, and its wait is `await new Promise(resolve => client.on("exit", ...))`, with the only bounded arm being the daemon failing to start at 15 s; its window is a **second, non-headless Chromium** at upstream's `100,100,1280x800`; there is **no configuration in which it runs headless**, because the dashboard's headedness is `headless: !!process.env.PWTEST_DASHBOARD_APP_BIND_TITLE`, an upstream *test* variable no session config reaches; and it **escapes the session's containment**, spawning a `detached`, `unref`'d, per-user-singleton daemon that writes its profile into `%TEMP%` -- a parent walk after the probe exited found **zero** of the 18 processes the job object had collected. **What it would take to reverse this**, so the decision is not a dead end: a **bounded** call, so no unattended run can hang on it; a dashboard **inside the session's own containment**, dying with it instead of living in `%TEMP%` under a user-wide singleton; and a **headless path that does not depend on a `PWTEST_*` variable**. No two of the three are enough. The cost paid: a human at the keyboard of a headed session can no longer annotate through this server -- take a screenshot and have them mark up the file. |
| **`browser_webmcp_call`** | **Withheld from `tools/list` and refused if named anyway, on liveness -- judged 2026-09-15, the second tool ever denied here and the first denied for something a *page* can do.** It invokes a tool the page itself registers and waits for it **with no timeout at all**: measured 2026-09-15 against a page whose `invokeTool` never settles, the call stood silent for **45,002 ms**, where a well-behaved tool on the same page answered in **521 ms** and the session was unharmed afterwards -- so it is the call that hangs, not the child. Upstream wraps the *list* path in `withTimeout(5000)` and wraps the call path in nothing. That is `browser_annotate`'s shape through a **wider door**: the annotation daemon needed a human at a dashboard, and this needs only a page the agent was told to visit. **`browser_webmcp_list` is `allow`** -- it answers in 2 ms off a cache, and a model that can see what a page offers can act on it with the ordinary tools. ⚠️ **A deny does not fully close it, and that is recorded and not fixed**: upstream's `renderTabHeader` now emits `- N webmcp tools available on the page` on every tab header whose count is non-zero, so a model is told the tools exist whatever this file says. The line carries the count and none of the page's text. **What would reverse this**: a bounded call path upstream -- the same `withTimeout` the list already has. |
| **`--isolated`** | **Never set, ever.** It puts the profile in a temp directory deleted on close. Three of the four legacy modes set it; BrowserAI gives every session a real directory and deletes nothing automatically. *(Reworded 2026-08-20, previously "in any mode ... every mode a real directory".)* |
| **`--output-max-size`** | **Never set, and `PLAYWRIGHT_MCP_OUTPUT_MAX_SIZE` stripped from the child's environment.** It is a recursive oldest-first deleter pointed at directories agents choose. Retention is the calling agent's decision, supported by an explicit cleanup tool, not by an eviction threshold. |
| **Console level** | **Exposed on `init`.** The upstream default of `info` silently drops `debug` messages; which trade-off is right is per-session, not global. |
| **Authored tool names** | **`browserai_` prefix on every tool BrowserAI authors** -- `browserai_init`, `browserai_resume`, `browserai_list`, `browserai_destroy`, `browserai_set_purpose`, `browserai_reinstall_browser`. Not `browser_*`: **MCP spec SEP-2567 names `destroy_*` and `list_*` as the documented companions to a creation tool**, so upstream shipping `browser_list` is the expected pattern, not a hypothetical -- and since upstream names are never renamed, a collision would be unresolvable. Corrected 2026-09-23 (previously "Bare names are worse still: MCP tool names share a flat namespace with every other server's, and a bare `destroy` is a name a model could reach for meaning something else entirely") -- there is no such namespace. The MCP specification 2025-06-18 scopes a tool name to one server, `tools/call` carries only `params.name` and is routed by the connection it is written to, and Claude Code 2.1.281 hands the model `mcp__browserai__<tool>`, so a bare name could not collide with another server's even if one were written. The collision this row is actually about is the one inside a single surface, which the SEP-2567 sentence above already carries on its own, and the prefix stands on it. |
| **Browser reinstall** | **A tool that refuses and does not coordinate.** Takes a machine-wide mutex, then **refuses if any session anywhere has a live browser**, naming what is live; only when nothing is running does it delete and re-provision. Reuses the sweep's own detection to answer "is anything live". ⚠️ ***Corrected 2026-08-18 (previously "Downloading alongside and swapping does not work -- Windows will not rename a directory holding open executables, and live browsers hold `chrome.exe`", cited to an article about mutex naming that does not discuss renames at all).*** **That is false, and it closed a design option that is in fact open.** Measured 2026-08-18 on Windows 10.0.26200 / .NET 10 against a running executable whose current directory was deliberately elsewhere, so that the [already-measured cwd rule](kb/windows/processes.md) was not the confound: `Directory.Move` of the executable's **parent** and of its **grandparent** both **succeeded**, and `File.Move` of the **running `.exe` itself** succeeded. Only `File.Delete` of the running image was refused, with `UnauthorizedAccessException`. Renaming a live tree aside and dropping a new one in its place is the ordinary Windows update pattern, and Windows permits it. **The refusal below is left exactly as it is** -- and ⚠️ ***the evidence that was owed arrived on 2026-08-19 and supports it***, *previously "it may well still be right ... nothing here has established what Chromium then does ... re-opening it is a decision to be made on evidence rather than one already closed"*. Measured against a live headless Chromium 152.0.7977.8 running as ten processes with its own current directory deliberately elsewhere: `Directory.Move` of **`chrome-win64`** was refused with a sharing violation and `Directory.Move` of **`chromium-1237`** with `ERROR_ACCESS_DENIED`, while **both succeeded in the same script seconds later with the browser killed first** ([kb](kb/windows/processes.md#the-win32-interop-surface), re-verification row 103). So the 2026-08-18 retraction was right that Windows has no such general rule and **too broad in the other direction**: swapping a tree *under a live browser* is not available for this browser, which is exactly what the refusal assumes, and swapping one while nothing is running works -- which is the only state this tool ever acts in. **Nothing is relaxed and nothing here changes**; what changes is that the position rests on a measurement of Chromium, not on a retracted sentence about Windows. The mechanism behind the two refusals is `[UNVERIFIED]` -- they carry different Win32 errors, so they are not the same cause. ⚠️ ***Corrected 2026-08-19, later the same day (previously "and Firefox was not tested").*** **It has been, and it matches Chromium exactly** -- a live headless Firefox 153.0 from `firefox-1539` refuses `Directory.Move` of `firefox` with a sharing violation and of `firefox-1539` with `ERROR_ACCESS_DENIED`, and both succeed with the browser killed first, over two runs ([kb](kb/windows/processes.md#the-same-measurement-for-firefox-and-for-what-both-families-share----2026-08-19)). **So there is no product finding and nothing here changes**: the refusal was already family-agnostic and is now measured for both families and not generalised from one. **The shared components go the other way, and that is new:** `ffmpeg-1011` and `winldd-1007` rename freely while a browser of either family is live, because neither is running -- but the **browsers root itself** is refused under both, with the same error as the revision directory, which is further up than the running-executable measurement predicts. Recorded, not acted on; in particular it does not license narrowing `shared`'s deliberately wider refusal, whose reason is that *a process is running from this tree* and *a session is using it* are the same question for a browser and are not for `ffmpeg`. A force flag is not offered, because force here means terminating browsers other sessions are using. If refusal proves too restrictive, the fallback is deferral: mark the install bad and let the next process that finds nothing running do the work. ⚠️ ***Changed 2026-08-19 at Firefox support (previously "It takes no arguments because there is nothing to name: the install is shared by every session on this machine").*** **The stated reason expired instead of being overruled** -- with two families provisioned there are two trees, and the caller's broken browser is one of them -- so the tool now takes **one required `browser` argument**, and the refusal narrows with it: an open Chromium session no longer blocks a Firefox reinstall. **Reinstalling both** was rejected because it makes the blast radius worse in the one situation the tool exists for: a caller with a broken Firefox pays 331 MB, loses a working Chromium for the length of its own re-download, and a network failure part-way leaves two broken browsers instead of one. **Optional-with-a-default** was rejected as the worst of the three: a broken Firefox, a Chromium deleted and re-downloaded, and an answer saying the reinstall succeeded. Required instead of optional-with-no-default follows `mode` on `init`, which is this product's settled shape for an argument whose omission cannot be answered honestly. ⚠️ ***Changed 2026-08-19, later the same day (previously "`ffmpeg` and `winldd` are shared by both families, each carries its own completion marker, and neither is touched by either family's reinstall -- so a corrupt `ffmpeg` is not repairable through this tool, recorded as a limitation rather than discovered as one").*** **It is repairable now**, through a third value on the same required argument: **`shared`**, which deletes both component trees and runs one `install-browser ffmpeg` -- measured 2026-08-19 to rebuild whichever of the two is missing, because each carries its own marker ([kb](kb/playwright/provisioning-and-timings.md#one-install-browser-ffmpeg-rebuilds-both-shared-components----2026-08-19)). **Having a family reinstall verify and repair them instead was rejected**, and rejected on this table's own reasoning: that is a repair tool which can break something working, which is exactly why the argument is required and not defaulted. **`shared`'s refusal is wider than a family's, and the widening is the decision:** it refuses while **any** session is open, of **either** family. For a family, *a process is running from this tree* and *a session is driving this browser* are the same question -- `chrome.exe` lives for the session's life and holds its own image open -- and for the shared components they are not, because `ffmpeg-win64.exe` exists only while a recording runs. A process-only gate would therefore answer *nothing is using it* on a machine full of live sessions and delete the tree under the next one to record, and the filesystem offers no second line of defence there the way an open `chrome.exe` does. `shared` is **not** offered on `init`: a session's browser is a thing that renders web pages, and the two accepted sets are separate lists with a test asserting they differ. |
| **Restart-registration lever** | ⚠️ **Corrected 2026-08-16 at Firefox support (previously "None shipped. A test instead").** **Chromium: none shipped, a test instead. Firefox: a lever, on every launch.** The original reasoning was measured and is unchanged *for Chromium* -- Playwright's command line overshoots Windows' 1023-character `RegisterApplicationRestart` limit by **531-807 characters** in every shippable configuration, so registration already fails and `GetApplicationRestartSettings` on a live browser returns `ERROR_NOT_FOUND`. **It generalised from one browser family to both, and the other one does not obey it.** Firefox's call site is gated on a preference, not on a length: measured 2026-08-16 against the Firefox BrowserAI provisions, an upstream-default launch leaves **exactly one process registered** and BrowserAI's own generated config leaves **none**. So the pref below is not belt-and-braces, it is the only thing preventing a Windows update from resurrecting an unclaimed Firefox. `--browser-test` remains rejected for Chromium on the original grounds: it is **not** web-detectable (0 differences across 486 fingerprint fields, 65 launches), but it suppresses something that is not happening and drags in unrelated behaviour changes. Both families are asserted on live processes, so a change in either direction is a red build. See [kb: resurrection](kb/chromium/resurrection.md#the-firefox-half-measured-on-both-sides----2026-08-16). |
| **Validate every path before launch** | **Required, and it prevents a hang, not untidiness.** An unusable `--user-data-dir` makes Chrome fall back to a default profile -- invisibly to the MCP client, with 8 healthy processes and both `initialize` and `browser_navigate` returning OK -- while its message window is titled with the **fallback** path, so our own stray detector goes blind to exactly the broken instances. In other configurations the same condition raises a native `#32770` dialog that **blocks startup entirely** until dismissed (measured: 1 process at 6 s; 10 processes after `WM_CLOSE`), which on a background server is an invisible hang that `--noerrdialogs` does not suppress. Third native-dialog trap after Firefox's profile-lock modal. |
| **Our own Chrome for Testing, never `channel: "chrome"`** | With `channel: "chrome"` the fallback profile is `%LOCALAPPDATA%\Google\Chrome\User Data` -- **the user's own browser**. A stray detector extended to cover fallbacks would identify a personal Chrome as ours, and Chrome's `ProcessSingleton` would forward the launch into the user's running browser. Cited 2026-09-23 @ Chromium `chrome/browser/process_singleton_win.cc`, refs/heads/main: the singleton is keyed on the user data directory ("Look for a Chrome instance that uses the same profile directory"), the running instance advertises that directory as its message-window title ("so other Chrome instances can decide if they should forward to us"), and a second launch naming it has its command line delivered over `WM_COPYDATA` and answered `PROCESS_NOTIFIED`. The trigger is the shared user-data-dir, which is exactly what a `chrome` channel's fallback profile supplies and what a per-session BrowserAI profile cannot. Using the CfT build BrowserAI provisions makes both structurally impossible. **"Our own" means the one BrowserAI manages, not one shipped inside the installer** -- the redistribution position is unresolved and provisioning stays first-run download. |
| **Session modes** | ⚠️ **None. Deleted 2026-08-20.** *(Previously: "**Three -- `headless`, `interactive`, `persistent` -- with `tracing` a boolean on any of them.** `tracing` was never a mode; it is `interactive` plus a flag. Promoting it removes a mode *and* adds capability. Headless-with-storage is deliberately not offered: it is the one combination granting full credential access with no visible signal, and that should be its own decision, not a side effect. Mode is bound at `init`, recorded in `browserai.json`, and read back by `resume`. Discoverability is a hard requirement across all four model-facing channels, generated from one table and pinned by tests.")* **The reasoning above is kept because it is what a reader needs before the deletion makes sense.** A mode was two things: a window, and a capability set. **The window became a per-run argument** -- `headed` on `init` and on `resume`, regenerated at every child launch and never written to the record -- because a session created headless and later needed headed had to be destroyed and recreated, and nothing on disk differs between the two. **The capability set became the full union**, `network`, `pdf` and `testing` included, for the reason [§trade-offs](#the-init-design-weakens-a-security-boundary) had already established twice over: it was never a boundary against the caller, who owns the session directory and could reach whatever it withheld by resuming as a different mode. **The one argument this row made that survived scrutiny was headless-with-storage** -- "full credential access with no visible signal" -- and it did not survive the observation that `headless` already persisted cookies, that `browser_run_code_unsafe` is `core` and reads the cookie jar in any session, and that the visible signal a window provides is a signal to a **human**, who is not present in the case the row was worried about. What replaces the signal is the `browser_route` warning in the server `instructions`, which is aimed at the human who *is* watching. **What it cost:** ten tools became reachable for the first time in this product's history -- see the row below -- and `browserai.json` moved to schema 3, so an existing session record is refused with the delete-and-init recovery it has always carried. |
| **The ten newly-granted tools** | **`browser_route`, `browser_route_list`, `browser_unroute`, `browser_network_state_set` (`network`); `browser_pdf_save` (`pdf`); `browser_generate_locator`, `browser_verify_element_visible`, `browser_verify_text_visible`, `browser_verify_list_visible`, `browser_verify_value` (`testing`).** *Added 2026-08-20 with the row above.* **Recorded as a deliberate grant and not left as a consequence**: they arrived because session modes were deleted, and a side effect nothing names reads as an accident at the next review. They are listed in `SessionToolSurface.NewlyGrantedTools` and asserted by name, in process and off the wire, by `ModelSurfaceTests` and `VerticalSliceTests`. ⚠️ **`browser_run_code_unsafe` is NOT among them.** It is `core`, so it has been reachable in every session this product has ever opened including every `headless` one, and a reader meeting this row must not come away thinking otherwise -- the measurement that makes it interesting is three rows up. ⚠️ **`browser_route` is the one that can deceive a person, not a model.** A mocked response renders as if it came from the server: the address bar keeps the real origin, and nothing on screen says a rule is in force, so a human watching a headed window is looking at something the agent made up. The warning is in the server `instructions` -- **not** appended to the tool's description, because upstream descriptions pass through byte for byte and [the rewrite path that could have done it was deleted on 2026-08-18](#licence-release-policy-and-the-tool-surface). |
| **Browser families offered** | **Two -- `chromium` (the default) and `firefox` -- bound at `init`, and since 2026-08-20 the only thing that is.** ⚠️ ***Corrected 2026-08-19 (previously Chromium only: "`browserai_init` refuses `browser: "firefox"`").*** A profile belongs to the browser that made it, so `resume` reads the family back out of `browserai.json` and **refuses** to be told a different one. *(Corrected 2026-08-20, previously "-- the same refusal `mode` gets, for the same reason", followed by "**`browser` has a default where `mode` deliberately has none, and the asymmetry is the decision:** a mode chosen by omission is a security posture nobody decided on, while a browser chosen by omission is a rendering engine, and every session in this product's history has been Chromium." Session modes are gone, so `browser` is now the **only** refusal `resume` makes and there is no asymmetry left to explain. The half that still holds is why it defaults at all: a browser chosen by omission is a rendering engine, and every session in this product's history has been Chromium.)* ⚠️ **And the ground under the choice of Chromium is stated 2026-09-17, in the maintainer's words: _"the reason for the default is that chrome is the most widely used"._** It is a **decision, not a measurement**, and it is written down that way on purpose. *Corrected 2026-09-17 (previously the only ground on offer anywhere was the four Firefox-against-Chromium cost ratios, recorded in [the kb](kb/playwright/provisioning-and-timings.md#firefox-against-chromium-the-standing-cost-ratios) as "the whole of the evidence behind Chromium being the default family").* **Those four stopped supporting it on 2026-09-16** -- [re-verification row 34](kb/re-verification.md): RAM 2× → 1.19×, first navigate 10× → 4.62×, profile disk 20× → 2.76×, and **idle CPU reversed sign**, ~24× → 0.77×, so on that axis Firefox is the cheaper browser. Three of the four collapsed by between 1.7× and 7× and the fourth pointed the other way, which leaves a cost argument that cannot carry the decision it was being asked to carry. ⚠️ ***Corrected 2026-09-18 by addition (previously the clause above ended "so on that axis Firefox is the cheaper browser" and "the fourth pointed the other way")*** -- **that reading of the fourth axis is withdrawn, and the conclusion it supported is unchanged.** The 0.77× was re-taken the same evening at **1.31×**, a second sign reversal inside one day, against a Chromium binary identical to the byte; the two distributions overlap completely and the run's own control moved **−41%** on that axis. So **idle CPU is retired as not established** (Q215 = a, [not-established](kb/not-established.md)) instead of pointing either way, and this paragraph now argues from an axis that cannot be measured on this machine instead of from Firefox being cheaper on it. **Three of the four still collapsed by between 1.7× and 7×**, which is the half that was ever load-bearing, and the default is in any case no longer argued from any of them. **Nothing about the default changes**; what changes is that it now rests on a reason its owner gave, not on numbers that moved underneath it. A dated market-share citation would be external colour and `[FLOATS]`, and none is claimed here. **What it cost to offer:** a measured per-family download size, because the refusal a caller reads while a browser is still downloading quotes one and Chromium's was the only measurement there was ([kb](kb/playwright/provisioning-and-timings.md#firefox-measured-the-same-way----2026-08-19)); and the `browserai_reinstall_browser` decision two rows above. **What it did not cost:** the advertised tool surface, which was measured the same day not to depend on `browserName` at all -- 42 and 59 tools, same names, same order, identical schemas under both ([kb](kb/playwright/tools-and-artifacts.md#does-the-surface-differ-by-browser-family----measured-2026-08-19)). WebKit is not offered and is not owed a reason yet: nothing has asked for it, and the [containment contract](ARCHITECTURE.md#process-containment-and-observability) has never been demonstrated against it. |
| **Free disk is not checked** | ⚠️ **Settled 2026-09-17, in the maintainer's words: _"Remove the free space check. Checking for free space is out of scope and makes our project more complicated. I do not want to check for that at all."_** Until that day `browserai_init` asked the volume how much room it had and refused below 640 MiB, naming the number, before it created anything. **The whole of it is gone** -- the constant, the refusal, the catalogue row that worded it, and the injected reader the suite reached it through -- and what is left is the closure that was always carrying the hazard: an installer that exits 0 without writing its marker is a failure, the partial tree is removed, and nothing reports a browser installed on the strength of a directory existing. **What this costs, named and not implied:** a machine that runs out of room now discovers it partway through a 207.3 MB download instead of in a sentence at `init`, which is exactly what already happened on any volume that could not answer the question in one call -- a network share, most often -- so the change makes one behaviour universal instead of inventing one. **What made it an easy call:** the guard was arithmetic, not a measurement (nobody ever sampled free space across a run), its margin had fallen to ~5 MiB, and keeping it meant re-deciding a shipped threshold at every browser roll. `HouseRuleTests.NothingAsksAVolumeHowMuchRoomItHas` scans the whole tree so it cannot come back quietly; [the hazard row](HAZARDS.md#hazard-index) and [the kb figure](kb/playwright/provisioning-and-timings.md#first-run-provisioning) are amended and not deleted, because a peak a reader budgets for is still worth having written down. |
| **`browserai_resume` repairs a dead child** | ⚠️ **Added 2026-09-17. Resume is no longer a pure no-op on a session this process already owns.** Until that day its question was *do I already own this directory*, and the answer is still **yes** when the `node` child behind the session has been killed -- so it answered *"This session is already open in this BrowserAI; nothing was changed"* in **7.68 ms** about a session that could no longer do anything at all, and the next browser call was still outstanding at **900,000 ms** ([hazard row](HAZARDS.md#hazard-index), [evidence](docs/evidence/2026-09-17-resume-wedge/README.md)). It now asks the second question too -- *is the child behind it still there* -- and, when it is not, **starts a replacement with the same launch options and says so**, in `SessionManager.ChildWasRelaunched`. **Liveness is read from the transport's own closed state and the child's process handle, never from a pid lookup by name**, so a child on its way out counts as alive until one of the two says otherwise and a healthy session's resume still changes nothing (`DeadChildTests.AResumeOfALiveSessionStillChangesNothing` is that control). **What a relaunch does not restore is named in the answer**: the profile is on disk and survives, so cookies and storage are there; the pages, the tabs and anything a script left in memory were in the process that died. **What was rejected:** *tearing the session down and re-opening it* -- that releases the directory lock, and another process may take it in between, so a repair would sometimes hand the session away; and *relaunching lazily on the next browser call* -- a call that silently starts a browser and answers normally hides the gap, and the gap is the thing a reader needs. A replacement that will not start is its own refusal, `SessionErrors.BrowserServerCouldNotBeRelaunched`, which says the session is still open and still held instead of inviting a re-init. |
| **Playwright's own server registry** | ⚠️ **Added 2026-09-24, in the maintainer's words: _"I want to avoid complexity. Does throtthling not mean we need to implement anything ourselves? I'm leaning towards c."_**, confirmed at the close against the mechanism written here, **At session close BrowserAI starts Playwright's own `list` command from the payload, detached, never awaited, and that is the whole mechanism.** `playwright-core` writes one descriptor per browser bind into `%LOCALAPPDATA%\ms-playwright\b`, which `PLAYWRIGHT_BROWSERS_PATH` does not move, and the only code that unlinks a dead one is inside `ServerRegistry.list()` -- upstream's own comment on the call is *List early to GC*. **Nothing calls it, so the directory only grows**: 3,815 descriptors stood on this machine on 2026-09-24, at about 499 a day from the suite and 3.5 a day from the installed product. ⭐ **The cost of reading it is QUADRATIC and that is what makes the growth matter**, measured 2026-09-24 against copies of the real directory in a scratch registry: 125 entries 261 ms, 250 entries 678 ms, 500 entries 2,101 ms, 1,000 entries 9,911 ms, 2,000 entries 36,310 ms, and **3,762 entries 141,616 ms** -- 2.4 minutes for one call, about 16 minutes at 10,000 and about 26 hours at 100,000. What degrades is Playwright's own dashboard, `list` and attach, never this product and never the disk. ⚠️ **Three things are accepted, by name.** A detached call that never runs, fails or hangs is one nobody hears, and that is the price of not awaiting it at the one moment a session is closing. **No throttle and no concurrency arm**: eight concurrent `list()` callers over a registry holding one live descriptor reaped nothing live, 8 of 8, and beyond eight the pipe-busy false positive is **unmeasured** -- a caller that cannot connect to a live browser's pipe would unlink a live descriptor, which costs discoverability and no browser. And the first call on a real backlog costs those 2.4 minutes once, in a detached process, after which every later one is milliseconds. **Two directions were dropped**: owning the unlink ourselves, which would reap a peer's descriptors as readily as our own on a machine-wide *cannot connect* judgement, and an upstream ask for a bounded registry, where retention is deliberate and the honest ask would be a settable path, not a delete-on-close. ⚠️ **BUILT 2026-09-24, and one detail of the sentence above is corrected by addition: it is upstream's one-line `serverRegistry.list()` and not the CLI client's `list` command** *(previously "BrowserAI starts Playwright's own `list` command from the payload")*. **Both reach the same reaper** -- the command's own call to it is the *List early to GC* line -- **and the command brings two things a session close has no business bringing**: it resolves a WORKSPACE by walking up ten directories from its working directory looking for `.playwright`, so the same code would do different things depending on where a session directory happens to sit; and having resolved one it DELETES that workspace's dead daemon session configs under `%LOCALAPPDATA%\ms-playwright\daemon`, a second registry this product never writes and was never asked to prune. `src/BrowserAI/Runtime/ServerRegistryReap.cs` is the whole of it: **four close paths** -- a destroy, an idle close, a client going away or a shutdown, and the stray sweep's own kill, which is the one close no session is left to reap after -- each firing **only when the child's job held more than the node child**, because a descriptor is written at a browser *bind* and a session that never bound one made nothing dead. The launch is in **no job and inherits nothing** (`JobLauncher.StartDetached`, `bInheritHandles: FALSE`, no standard handle named, `CREATE_NO_WINDOW`), its working directory is the **user profile** -- never a session directory, which a destroy is about to delete, and never `current\`, which an update replaces -- and its pid and creation time go to the PROCESS log at **event id 90**, with 91 for a launch that failed and 92 for a payload with no module. ⚠️ **`PWTEST_SERVER_REGISTRY` joins the forwarded allowlist as a documented test hook and nothing in the product ever sets it**, which is a narrower thing than the growth-reduction direction that was declined: without forwarding, the child that writes the descriptors and the reap that collects them read two different directories, and the only arm that could exist would be one that pruned the developer's own registry. **Measured on this machine 2026-09-24**, single process over planted dead descriptors in a scratch registry: **1,000 in 10.19 s** and **4,000 in 309.5 s**; the real directory stood at **4,059** when it was reaped for the first time that morning and was **empty** when that run ended -- 4,059 unlinked in 1,053 s, with two callers running at once because the measurement was started twice, and a second reap over the pruned directory costing **0.062 s** ([kb](kb/playwright/tools-and-artifacts.md#a-session-close-now-starts-upstreams-own-reaper----measured-2026-09-24), re-verification row 154). The hazard row closed on its own condition |

### Locking, logging, versioning and registration

**Settled 2026-08-16.**

| Decision | Outcome |
|---|---|
| **Machine-wide lock scope** | **`Global\` only. There is no `Local\` fallback.** If the machine-wide lock cannot be created there is no lock, and therefore no session -- a hard blocker surfaced to the calling LLM with the reason, not a quiet downgrade. A `Local\` degraded mode was rejected because it does not protect the **cross-login** case, which is the one that matters: [headless Chromium writes no lock file of its own](#4-exclusivity-is-keyed-on-the-wrong-thing), so BrowserAI's lock is the only protection that exists in the mode that is the default. A lock that is silently weaker than it claims is the exact failure class this project exists to eliminate -- a degraded mode would report healthy while being broken. |
| **Lock acquisition never waits** | **A fast attempt; on contention, fail immediately, naming the holder and when it was taken.** No timer, no backoff, no configurable wait. **Whether to retry, and how long, is the calling LLM's decision** -- consistent with correct use being [the calling agent's responsibility](#shape-and-packaging). A wait inside BrowserAI makes that judgement once, silently, and identically for every caller, and the caller is the only party that knows whether the work is worth queueing behind. One exception, and it is not a hole in the rule: the **per-directory create-or-take gate** keeps a short bounded internal wait, because asking a model to retry a 5 ms operation would be absurd. *(**Corrected 2026-08-16 at the plan's final audit, previously "the machine-wide mutex held for milliseconds around a session-index write."** The session index takes **no lock at all** -- asserted by `SessionIndexTests.TheIndexTakesNoLockAndDeletesNoDirectory`, and [locking](ARCHITECTURE.md#locking-ownership-and-the-sweep) exists partly to kill that phrasing, because a machine-wide lock on the hot path of every session start is precisely the trade the index was designed to avoid. The bounded wait is real, and it is the gate that serialises close→rename→re-open on one directory.)* |
| **Where the program goes and where the data goes** | ⚠️ **Two roots, siblings, settled 2026-09-15 by the maintainer**: the program installs into `%LocalAppData%\BrowserAI.app` and the data lives in `%LocalAppData%\BrowserAI`, and neither is inside the other. **The reason is that an install root is destroyed twice over by design** -- `Setup.exe` renames a non-empty one aside and, on success, deletes it (which is what a repair or an overwrite install *is*), and uninstall empties it -- so the old layout, where every path was a sibling of `current\` under that root, put 768 MB of provisioned browsers, the session index and the log inside the one directory the installer removes. **The lever is the Velopack pack id and there is no other**: it is what the install directory is derived from, it is immovable at 1.2.0, and an id cannot carry a path -- so the id became `BrowserAI.app` and `build/New-Release.ps1` renames the downloads back after the pack -- to `BrowserAI.exe` and `BrowserAI.zip`, *(previously "renames the download back to `BrowserAI-win-Setup.exe`", one artifact, until later the same day: the portable archive was renamed with it and vpk's own `-Setup` / `-Portable` vocabulary went too, on the maintainer's instruction that the release outputs be friendly-named)*. **The data root is a CONSTANT and is never derived** from the install root or from the image path: `--installto` and the portable zip both move the binary, and a hook's own image is `<install root>\current\BrowserAI.exe`, so a derived root would abandon the browsers whenever the program moved -- and the uninstall hook would offer to delete a directory nobody had been using. **`BROWSERAI_ROOT` stays and means the data root only**; the install root is Velopack's and nothing in this product moves it. **One thing stayed behind on purpose**: the live-instance markers and their `Global\` mutex are keyed to the *install* root, because the census answers *is any other process running out of this install?* -- which is exactly the set `force_stop_package` terminates by image path. ⚠️ **And since 2026-09-15 the configuration app is a member of that census for exactly as long as its window is open** *(previously the census had one kind of member, the server)*. It is the same question answered about a second binary: `force_stop_package` matches image path and never name, so an update applied by a **server** while somebody is reading the dialog would take the dialog with it, mid-click. The app's own apply is the one exception and is deliberate -- `restart: true`, so the window it kills is the window that comes back saying *Updated to ...*. Asserted from outside the process, on the marker file itself, by `RealInstallerTests.TheInstalledMainExecutableOpensOneDialogAndNoConsoleWindow`: a marker exists under the install root while the window is up and is gone once the process has exited. **Uninstall asks before deleting the data root, Keep is the default, and a `--silent` uninstall keeps**; an upgrade never asks and never touches it. Held by `UpdateTests.NoDataPathResolvesUnderAnyInstallRoot` (a scan that fails against the wiring this replaced), `ReleaseScriptTests.ThePackIdIsTheInstallDirectoryAndTheInstallerIsRenamedBack`, the four uninstall arms in `RegistrationTests`, and -- when this machine has packed one -- `RealInstallerTests.InstallingTwiceOverOneRootLeavesTheDataRootByteIdentical`, which runs a real `Setup.exe` twice over one install root with Velopack's own `Renaming existing directory` line as the positive control. See [the install layout](ARCHITECTURE.md#updates) and [kb](kb/packaging/velopack.md#where-state-may-live----the-finding-the-provisioning-design-rests-on). |
| **BrowserAI will not serve out of a root two Windows users could share** | **Settled 2026-08-20, in the maintainer's words: _"L1 a"_** -- direction (a) of [QUESTIONS.md §12](QUESTIONS.md), refuse at startup. The predicate is **the app root is inside the current user's profile**, and it is answered through the filesystem, not through strings: both sides go through `VolumeIdentity.DeepestExistingFinalName`, the same walk `CanonicalPath` uses on a caller's session directory, so a junction, a `subst`ed letter, an 8.3 short component and an extended-length spelling all resolve to what the filesystem calls them -- and a junction *inside* the profile pointing outside it, which every string comparison accepts, is refused. **The refusal carries the remedy**: the root it found, why a shared root is unsafe, and that clearing `BROWSERAI_ROOT` restores the per-user default. It runs **before** the sweep, the live marker, the instance directory and every session, so a refused root gains nothing but a log line. **What was knowingly bought**: *outside the profile* is not the same predicate as *shared*, so a single-user install at `D:\Tools\BrowserAI` is refused for nothing -- the table in §12 said so and the decision was taken anyway. **What could not be established is served with a warning, never refused**, because a background MCP server that will not start on a locked-down machine is a worse failure than the one being prevented. See [`Hosting/InstallRootScope.cs`](src/BrowserAI.Core/Hosting/InstallRootScope.cs). |
| **A reinstall holds the machine's browsers root** | ⚠️ **Became a READER/WRITER claim on 2026-08-20, in the maintainer's words: _"any init or resume should take a system level lock. No matter the browser type. These locks are cumulative. And reinstalling the browser should be an exclusive lock."_** Every session opens `<browsers root>\reinstall.lock` `FileAccess.Read`/`FileShare.Read` and holds it for its whole life; a reinstall opens it `FileAccess.ReadWrite`/`FileShare.Read`, which Windows refuses while any reader holds it. **The kernel is the gate and the census is only a sentence** -- `SessionManager.LiveSessions` survives to name what the caller must close, and the family filter is gone, so a live Firefox session refuses a Chromium reinstall. **No intent marker, no drain, and writer starvation accepted**, all three his: _"If anything is busy then the reinstall should be refused with the list... But it should not start a drain/preventstart process of sorts. Keep it simple. Let the user solve the open sessions block."_ *Previously settled 2026-08-19: _"No reinstall if there is any session running system wide. Including any reinstall sessions."_* `browserai_reinstall_browser` takes a machine-wide claim before it looks at anything and holds it for the whole call; `browserai_init` and `browserai_resume` are refused while it is held, and so is a second reinstall. **It is a lock FILE and not a named mutex, and that is forced and not chosen** -- the claim spans a 203.8 MB download inside an `async` method and a named mutex is owned by the thread that waited on it. **Nor a named semaphore**, which does span threads: a semaphore's count is not restored when its holder dies, so one crashed reinstall would refuse every `init` on the machine until a reboot; Windows closes a file handle however the process ends. **Keyed on the browsers root**, exactly as the provisioning mutexes are, because two installations with different roots are genuinely independent. **No drain and no wait** -- a reinstall that finds sessions open releases the claim and refuses at once, naming how many. See [`Runtime/MaintenanceLock.cs`](src/BrowserAI/Runtime/MaintenanceLock.cs) and [`ARCHITECTURE.md`](ARCHITECTURE.md#the-mcp-server). |
| **Git detection** | **Out of scope. BrowserAI performs no git detection of any kind.** Considered and rejected on 2026-08-16, recorded here so it is not rediscovered as a gap. The concern was well-shaped: `storage-state` artifacts are session bearer tokens, and a session directory inside a working tree is both untracked *and* unignored -- one `git add -A` from being pushed. A detector was designed (refuse, require explicit acknowledgement) and then dropped on two grounds. **Where an agent puts its data is the agent's decision**, consistent with [any path is accepted](#shape-and-packaging); and **the premise does not hold** -- a browser session writes hundreds of files, so it is plainly visible in the staged list and the failure was never silent, which was the entire basis for the concern. This says nothing about this repository's own `.gitignore`, which is repository hygiene, not a runtime feature. |
| **A renamed session directory** | **The directory is the identity. The path recorded in `browserai.json` is provenance.** On `resume`, when the recorded path differs from the actual one: recorded path **gone** → it was *moved*, so repair the record, log it, carry on. Recorded path **still present** → it was *copied*. **⚠️ Re-answered 2026-08-18: the copy is resumed and told what it is, not refused** (*previously "so refuse and require explicit acknowledgement"*, through an `acknowledgeCopy=true` argument that is now deleted). The refusal was necessary only while `browserai.json` was a **snapshot**, because taking the copy over overwrote the one piece of evidence that it was a copy. Schema 2 made every field an ordered list of timestamped statements, so a resumed copy **appends** its path beside the original and the answer returns the whole history -- which is strictly more than the refusal ever conveyed, and leaves the product with **zero confirmation flags**. **No extra identifier is needed** -- the recorded path is already the discriminator, and a proposal to add a random fingerprint field is dropped. The rationale is narrower than it first looks, and it is exactly this: **a copy corrupts nothing.** Each copy has its own profile folder, and each browser writes only to its own. What the copy inherits is an *ownership record* naming a process that may still be live against a **different** directory, which produces either a wrong refusal or a confusing takeover of another session's recorded purpose and history. **Legibility, not data loss** -- the same reason `resume` exists at all. |
| **Logging** | ⚠️ **There is no per-session log file, since 2026-08-26** -- *previously "**Session logs live in the session directory, beside `browserai.json`.**"*. Everything it carried is on stderr, which the session's logging stack already wrote to at every level, and what a session itself did is rows in `browserai.data`. The rest of this row is unchanged and is about the machine-wide log. Everything that happens outside a session -- startup, the stray sweep, first-run browser provisioning, applying an update -- has no session directory, so it goes to **one rolling process log under the app data root, outside `current\`**, resolved from `VelopackLocator.Current.RootAppDir` and never `AppContext.BaseDirectory`. Outside `current\` because an update replaces that folder, and a log wiped by the update is a log missing exactly the event you came to read. **Every line is forced to stderr by configuration, not by rule** -- [`stdout` is the protocol channel](CLAUDE.md#rules-a-mechanism-enforces), and a rule is a habit that fails inside a `catch`. Append, never wipe on start; **every record is one unbuffered write, so there is nothing to flush and no flush hook exists**; no destination that can block or open a window; reading from the console is banned alongside writing to it. *(**Corrected 2026-08-16, previously "flush before exit, including on the crash path."** [containment and observability](ARCHITECTURE.md#process-containment-and-observability) asked for a flush; the sink was built with nothing to flush instead, and a no-op `Flush()` would be a mechanism that only looks like one. `ILogSink` carries no `Flush` member, so the absence is structural, not an omission, and `ProcessLogTests.AnUnhandledExceptionStillLeavesItsLastLogLineOnDisk` asserts the property the flush was for. **Still unverified, and named and not assumed:** whether the framework's console provider drains its own queue at an abnormal exit -- every record also goes to stderr through it.)* **Debug level is an optional argument on `init` and `resume`**, replacing an earlier idea of a differently-named executable -- the agent that wants diagnostics is the one opening the session, and it should not have to relaunch the server to get them. |
| **Automated checks** | ⚠️ ***Corrected 2026-08-20 (previously "**There is hosted CI as of 2026-08-18** -- `build.yml` builds the payload, provisions both browsers, publishes the NativeAOT binary and runs the **whole** suite on every push and pull request", which had itself corrected "**None. No CI, no scheduled job, no git hook.**" on 2026-08-19). **The original decision stands again**, and both `previously` clauses are kept because this row has now been wrong in both directions inside three days.*** Hosted CI existed from 2026-08-18 to 2026-08-20 and was removed at the maintainer's decision, verbatim: *"Remove CI completely. Let all the tests run on my machine only. I want no CI and no github runner. Add to the todo that we will add CI back in later. But that requires me adding infrastructure for self-hosted runners and I am considering leaving github before we do so."* **None. No CI, no scheduled job, no git hook.** Bringing it back needs self-hosted runner infrastructure that does not exist yet and must not assume GitHub Actions; the item, and the audit of what is unverified anywhere while CI is gone, is [`TODO.md`](TODO.md#continuous-integration). The original text follows, unchanged, because it is what holds. -- The [release checklist](RELEASING.md) is the only gate that exists. That buys many commits without re-running everything on each of them, and keeps the project off hosted CI entirely. **The cost is recorded and not softened: the gate works when it is invoked, and nothing makes it fire.** It is accepted because [the release trigger is already manual](#licence-release-policy-and-the-tool-surface) -- the checklist runs at the one moment a human is deliberately doing something. **To be reviewed once the product is finished**, when the shape of the suite and the release cadence are known and not predicted. ⚠️ **DECIDED 2026-09-23, BY ADDITION, AND THE REVIEW THIS ROW ASKED FOR IS CLOSED WITH IT.** The maintainer, verbatim: *"Only I drive when there is a release. You can only suggest to me when it is a good moment."* The review was to be taken against a real cadence instead of a predicted one, and what it was for was the arrangement this row describes: a gate nothing makes fire, accepted because the release trigger is manual. **The trigger is now manual by decision and not by default**, which is a stronger footing than the one the review was meant to test, so `TODO.md`'s item asking for it is deleted in the same commit and its whole remainder is this paragraph. An agent may observe that the moment looks good and say why; an agent may not start a release. **What is NOT decided here is automation**: a self-hosted runner that ran the suite on every push would take nothing from the sentence above, because running the gate and cutting a release are different acts. That question is [`TODO.md`](TODO.md#continuous-integration)'s own item, which stays. ⚠️ **AND A VERSION'S EDITING IS GATED ONCE, AT ITS END, WHICH IS A SECOND ANSWER TO THE SAME QUESTION -- added 2026-09-24.** The maintainer's words, at the start of the work: _"I want to start dev on the next version and only run all the tests after we are done editing instead of in between all the steps because iterating is going to slow right now."_, and at its end: _"Q260 a"_, which was the instruction to close. **The arrangement is:** ungated work goes on a branch, pushed to origin so nothing is lost, while `master` stays at the last gated commit; a filtered single-test run to watch a planted red stays allowed as **iteration** and is never verification; the [ordinary two-shell gate](TESTING.md#continuous-integration) runs once when the editing is done, and `master` fast-forwards to the branch only after it. That produced a branch of more than eighty commits between one gate and the next. ⚠️ **Two properties of it are the reason it is written here and not left as a habit.** Deciding that the editing is *done* is the maintainer's call and not an agent's, on the same footing as [item 14](RELEASING.md#14-a-human-decides): an agent may report that the work in hand is finished and may not declare the version finished. And work that is unfinished at that moment becomes the **next** version's work, stated in [`TODO.md`](TODO.md) and carried across nowhere else -- nothing half-built crosses the boundary, because a branch gated once cannot tell a deliberate omission from a forgotten one. |
| **Version numbers** | **Derived from git tags -- three parts plus a pre-release suffix**, which is the shape `vpk` accepts. Tag `1.2.0` and that build is `1.2.0`; five untagged commits later it is `1.2.1-alpha.0.5`. Auto-incrementing, nothing hand-edited. **The house four-part `base.commitcount` convention cannot be carried**: `vpk` rejects four-part versions outright, so this is a constraint imposed by the packaging tool, not a preference. One consequence removes a design problem instead of adding one -- **no magic development-build version number is needed.** An untagged build already carries the not-a-release suffix, so "never self-update from a build that is not a release" is readable straight off the version string instead of depending on a sentinel someone has to remember to set. **The mechanism was settled on 2026-08-16 and is [MinVer](https://github.com/adamralph/minver)**, taken from the one sibling repository that already derives its version from tags. Tag prefix `v`, product project only, and it floats like everything else (7.0.0 on the day). Two consequences are load-bearing, not incidental: a build that derives `0.0.0`, which is what an unfetched tag or a shallow clone produces, **fails** instead of stamping; and **nothing reads `AssemblyVersion`**, because MinVer sets it to `{Major}.0.0.0` by design, so the number a caller would naturally reach for is `0.0.0.0` for the whole 0.x line. |
| **The plan's lifetime** | **A plan is consumed and then deleted**, and this one was, on 2026-08-17. It was written as one file per section behind a short index -- a section was marked built, recorded what implements it, and was removed on its own, so the ending was a folder that emptied as the build proceeded. The rejected alternative was one document deleted in a single motion at the end, which makes the last step frightening enough never to be taken. What outlived it is what was never a section of it: the [hazard index](HAZARDS.md#hazard-index), whose rows gain a status and the evidence that closed them instead of being deleted, and the [architecture map](ARCHITECTURE.md) the `Implemented by` column became. |
| **How BrowserAI is registered with a client** | **At *user* scope, through the client's own supported command: `claude mcp add --scope user`, run by the Velopack install hook and undone by the uninstall hook.** User scope *is* [the MCP server](ARCHITECTURE.md#the-mcp-server)'s founding sentence -- *"registered once at system or user scope, available in every repository, with no per-repo files"* -- and it is the promise [§7 above](#7-distribution-to-colleagues-has-no-story) says the current setup has no answer to. **Decided 2026-08-16 in the maintainer's absence**, having been asked twice; the alternative was continuing to ship an installed, self-updating binary that no client is configured to talk to, which the plan's audit found and named as the largest gap in the product. **Three alternatives rejected, and the reasons are the durable half:** writing the client's configuration file directly means owning another product's on-disk format *and* its merge semantics forever, for a file the maintainer edits daily; a registry key is read by only some clients, so it registers with nothing on the machine this was built for; a documented manual step abandons the one promise that distinguishes this from the setup it replaces. **The mechanism lives in exactly one file** -- `src/BrowserAI/Registration/McpClientRegistration.cs` -- so revisiting it is reading one file, not an installer. Three properties are non-negotiable for any replacement: it registers `current\BrowserAI.Server.exe` and never [the 392,704-byte execution stub](ARCHITECTURE.md#updates), it is idempotent across install/update/repair/reinstall (**the client's own `add` is not** -- a duplicate exits 1), and it can neither fail an install nor fail silently: every outcome writes a log record *and* `<root>\mcp-registration.json`, carrying the command to run by hand. ⚠️ **A fourth property since 2026-09-16, because it was written here as a promise and kept by one intent out of three:** it never adopts, overwrites or removes a `browserai` entry whose command is not under this install root -- **on install and on uninstall as well as on update**. Until that day `Reassert` ran `mcp remove` then `mcp add` with no check and `Remove` ran `mcp remove` unconditionally, so installing this BrowserAI overwrote another one's registration and uninstalling it deleted one. `McpRegistrar.NotOursToTouch` decides it once for every intent and `RegistrationTests.NeitherAnInstallNorAnUninstallTouchesAnEntryThisInstallDidNotWrite` holds it over constructed inputs. There is **no exit code** on that path by design -- these run inside Velopack fast-exit callbacks -- so the honest signal is `isWhatWasAskedFor: false` in the record, naming the foreign path. ⚠️ **Corrected 2026-09-15 (previously "it registers `current\BrowserAI.exe`", and the mechanism file was `src/BrowserAI/Registration/McpClientRegistration.cs`)** -- the names swapped when the product became two binaries and the mechanism moved to `src/BrowserAI.Core/Registration/` with the library both executables link. ⚠️ **AND A FOURTH PROPERTY IS GONE, WHICH IS THE LOAD-BEARING HALF OF THIS CORRECTION.** `RegistrationTarget` registered `Environment.ProcessPath` verbatim, so *"the registered path and the running binary cannot disagree: they are the same string"* -- an identity, not a check. The hooks run on `--mainExe`, which is the configuration app now, so what a client is given is **composed**: the running image's directory plus the server's file name. A composed path is a guess until something checks it, and **two checks replace the identity**: the sibling must exist, and its PE optional header must declare the console subsystem (`IMAGE_SUBSYSTEM_WINDOWS_CUI`, 3). Either failing is a refusal naming the file, in the log and in `mcp-registration.json`. **A name check would not have been enough** -- a file called `BrowserAI.Server.exe` that is really the configuration app passes every check an extension can make and fails at the worst possible moment: a client starts it expecting stdio, a window appears, and the client waits forever for a handshake a dialog will never send. The subsystem is a field the linker writes and the loader obeys, and it is the one property a rename cannot forge. **The update hook gained a job with it**: an entry of ours naming a file that is no longer there is **re-pointed**, one that still resolves is left exactly as it is, and a foreign one is reported and never touched. ⚠️ **AND SINCE 2026-09-16 "STILL RESOLVES" MEANS "IS THE SERVER" -- added, because the previous reading left every pre-split install broken by its own update.** `McpRegistryView.Classify` answered `OursAndPresent` for any file under our install root that **existed**, and every registration written before 2026-09-15 names `current\BrowserAI.exe`, which is now the configuration app. So an install that updated kept an entry pointing at the **window**: `Repair` left it exactly as it is by design, the client started a dialog and waited forever for a handshake, and there was nothing in any log because nothing had failed. The classifier reads the same `IMAGE_SUBSYSTEM_WINDOWS_CUI` the composition check reads -- one discriminator, not two -- and anything else under our root is `OursAndStale`, which is the state `Repair` re-points and the state the window offers *Register* out of. **The two causes of stale stay one state on purpose**, because the remedy is identical; what differs is the sentence, and `AppState.StatusSentence` says *"Registered to the wrong binary"* when the file is there and *"which is not there any more"* when it is not. Held by `RegistrationTests.TheSiblingServerIsRegisteredRatherThanTheAppThatComposedIt`, `.AnInstallWithNoServerBesideTheAppIsRefusedByName`, `.AFileAtTheServersNameThatIsNotAConsoleBinaryIsRefused`, `.AnUpdateRepairsOurOwnStaleEntryAndLeavesEveryOtherKindAlone` and `.TheReaderTellsAbsentFromOursFromStaleFromForeign`, each over inputs constructed out of bytes, and by `ConfigurationAppTests.TheStatusSentenceAndTheOfferedActionsFollowTheState` for the two sentences. |
| **A break with no fix-forward** | **Blocks releases for as long as it takes. That is the intended answer, not a gap.** Pinning back is forbidden by [rule 4](#the-five-rules-that-make-floating-safe), so the only move left is to make the newest version work, and until that lands there is nothing releasable. Written down as a decision because it had been left as an implication -- and an unstated consequence gets rediscovered under deadline pressure, at which point "just pin it this once" reads as a judgement call, not as the thing the policy exists to forbid. |

### Session lifetime, the file boundary, and moving a session

**Settled 2026-09-21.** Four things were already true of the product and said in
no string a model reads, and two more were asked for and deferred. The
maintainer's words, both answers verbatim: _"Skip the move and copy tool for
now. But add your recommended mcp text additions."_ and _"let's keep the sandbox
like it is today and have only the mcp instructions be more clear about it."_

**None of the four is a behaviour change.** Every one of them describes what
BrowserAI already did; what changed is that the model is told, in the string that
is read at the moment the fact matters. The four are held against the published
binary's own wire by `ModelSurfaceTests`, which reads the published slice's own
`initialize` and `tools/list`, not the constants they are assembled from.

| Decision | Outcome |
|---|---|
| **Who deletes a session** | **The agent that created it, and nothing else ever does.** BrowserAI removes nothing on a schedule and nothing at a size -- that half was already on `browserai_init` as *"nothing here expires"* -- and a retention policy with no owner is half a sentence. The owner is now named: destroy the session when the work is done, and **promptly when it held a login**, because cookies and logins arrive from navigation, live in the profile, and stay on disk until the directory goes. Stated in the server `instructions`, on `browserai_init` and on `browserai_destroy`, which are the three moments at which it is actionable. |
| **What a destroy takes** | **Everything in the directory, screenshots and downloads included.** `browserai_destroy`'s description said *"deletes the whole directory"*, which a caller reads as a statement about the session, not about the artifacts it was asked to produce. It now says to move out what must be kept first, beside the instruction -- unchanged -- to call `browserai_catch_up` before destroying, because the sizes are the only thing that says what is about to go. |
| **What `browser_file_upload` can reach** | **Only a file inside the session's `output` folder.** That is not upstream's default but this project's choice: `allowUnrestrictedFileAccess` is written `false` and both of upstream's roots are written as `<session>\output`, so `checkFile` refuses anything else -- see [known trade-offs](#what-browserai-does-not-defend-against) and `FileAccessRootTests`. The consequence for a caller is one it cannot guess: copy the file into `output` first, and the copy goes when the session does. **It is said on `browserai_init`, never on the tool.** `browser_file_upload` is upstream's and every upstream description passes through byte for byte; `LosslessPassthroughTests` holds that, and the arm that asserts the sentence asserts the tool is still upstream's own bytes in the same breath. |
| **Moving a session** | **By hand, with ordinary file tools, while no browser is open on it -- then resume it at its new path.** The directory is the identity and `resume` already repairs a record whose path moved; what was missing was any sentence saying a caller may do it. Said on `browserai_resume`, with the warning that **copying** one instead duplicates every login it holds into a second directory nothing is tracking. |
| **A move tool and a copy tool** | ⚠️ **Considered and DEFERRED 2026-09-21**, in the maintainer's words: _"Skip the move and copy tool for now."_ The argument against a move tool is that a directory is the session, so moving one is a file operation every client that has file tools can already do, and a tool would earn its place only for a client that has none. The argument against a copy tool is stronger and is not about cost: **a copy duplicates whatever logins the profile holds**, which makes the convenient tool the one that silently multiplies credentials. Recorded here so the absence reads as a decision, not a gap, and the sentence on `browserai_resume` is what a caller gets instead. |
| **Widening the read roots** | ⚠️ **Considered and DEFERRED 2026-09-21**, in the maintainer's words: _"let's keep the sandbox like it is today and have only the mcp instructions be more clear about it."_ Three shapes were weighed and none was taken: **widening the roots to the client's workspace**, which turns a boundary a caller cannot cross by accident into one it can; **transparent staging**, where BrowserAI copies a named file into `output` on the caller's behalf, which hides the boundary instead of stating it and makes the copy's lifetime invisible; and **asking upstream for separate read and write roots**, which is the principled fix and is somebody else's release schedule. The sandbox stands as it is, **for reads and writes alike**, and what changed is that the surface says so. |

### The update lane, the sessions that hold it, and the second client

**Settled 2026-09-24.** A staged update applies when this process is the last
live BrowserAI on the machine. **Nothing on disk holds it.** The census reads one
file per live server under `<root>\live\`, each held open by the kernel for as
long as that process runs, so an abandoned session directory holds nothing and a
**running** server holds everything. Every Claude Code or Codex session keeps one
server alive for its whole life: **22 were live on this machine on 2026-09-23,
14 of them never used, and the staged 1.1.0 had been waiting behind them since
the day before.** The maintainer's first reading was that a forgotten session on
disk would block updates forever -- _"That makes no sense. I'd expect many agents
to forget closing sessions meaning any old stale session on disk will block
updates forever. Are you sure this is right?"_ -- and it does not. The
distinction decides everything below it: the remedy for a stale directory is
housekeeping this product could do by itself, and the remedy for a live server is
a conversation with the person at the keyboard.

| Decision | Outcome |
|---|---|
| **Nothing exits itself to let an update in** | **Settled 2026-09-24, in the maintainer's words: _"No relay. The toast linking to the gui that lists the sessions and allows control and informs you of the dangers feels like the right way to go."_** BrowserAI never ends a session's server to unblock its own update -- not on a timer, not on an idle condition, not when a client is known to re-launch it. A blocked update **asks the person** and closes nothing on its own. Four directions were measured and dropped with this one: an idle exit gated on `clientInfo.name` (which is what the research recommended), an unconditional idle exit, an exit tied to stdin EOF, and a pre-exit message asking the model to restart the server. ⚠️ **The last of those is closed by measurement and not by preference**: no channel reaches the model except a tool result, `notifications/message` is dropped by Claude Code outright, stderr reaches only `--debug-file`, and a model cannot start a server in any case. ⚠️ **What this accepts** is that a machine whose sessions are never closed never applies an update, and that the only thing between that state and a user is a person reading a toast. The maintainer's earlier leaning -- _"I'm leaning towards keeping the servers online during normal usage. But only stopping the servers if there are no browsers and sub processes running and only if there is an update available."_ -- is what the research was commissioned against, and the answer recorded here is the one it came back with |
| **A blocked update asks once, from the tray, and the X means the dropdown** | **A Windows toast, raised when an update has been staged and the census says this process is not alone.** Title *BrowserAI 1.1.0 is ready to install*; a body naming what holds it, how many sessions and how many of those are Codex-hosted; two buttons, **Review N sessions** (green, opens the sessions page, and a click on the body does the same) and **Ask me later**, bound to a selection input titled *If not now, ask me again* offering 10 minutes, 6 hours, after the next reboot, and not for this version. **Default 6 hours, and dismissing the toast with its X takes that default** -- a dismissal is a snooze and never a decline, because the alternative is an update that stops asking and a user who never finds out why. ⭐ **Five labelled buttons were tried first and cannot work**: Windows lays every toast button in ONE row and never wraps, so five collapse to their first word -- *Revie*, *Ask*, *Ask*, *Not*, *Skip* -- with the emoji rendered as a small icon above. That is a platform layout limit, seen at 3840x2160, and it is why the snooze choices are an input and not buttons. The three renderings are kept beside this decision at [`docs/design/toast-2026-09-24`](docs/design/toast-2026-09-24/README.md) |
| **The sessions page, and what it warns about** | **A page of the configuration window listing every live session: its client, its purpose, when it was last used, and whether a browser is open on it.** It closes the ones the person selects and nothing else, which is the rule [who deletes a session](#session-lifetime-the-file-boundary-and-moving-a-session) already states: BrowserAI removes nothing on a schedule. ⚠️ **Two warnings are load-bearing and they are the _informs you of the dangers_ half of the instruction.** A **Codex-hosted** session does not get its server back: Codex re-launches a dead stdio server only on a refresh, and nothing on the failure path fires one, so closing that server ends MCP for that Codex thread until the host asks for a reload or the user opens a new thread. A **recently active** session may be mid-task, and closing it takes its browser with it, because the job object is `KILL_ON_JOB_CLOSE` by design. Claude Code carries neither warning: it re-dials transparently on the next tool call, 3/3 in every scenario measured |
| **A re-connected session is told its tool list is stale, once** | **Settled 2026-09-24, in the maintainer's words: _"Q261 b"_**, against his own framing of the question a few hours earlier: _"How will restarted codex or reconnected claude code sessions learn about the new version and that it needs to refresh the tool list? Can we save per session the last tool call's browserai version inside of browserai and refuse any call until a tool list has been called? Or is this over enginering and are there simpler ways. Or should we just accept it as tool mistmatches will most certainly trigger the llm to re-tool? what do you think?"_ **The mechanism is a per-connection flag: has a `tools/list` arrived since this connection's handshake.** A `tools/call` that arrives before one is **refused once**, with a message naming the running BrowserAI version and the remedy for the client at the other end -- for Claude Code, that the list-changed notification has been sent and the call can be retried; for Codex, that a new thread lists fresh -- and `notifications/tools/list_changed` goes out with that refusal. **A first connect lists before it calls, so a new session pays nothing**, and the refusal is once per connection and never a wall. ⚠️ **Three directions were dropped and the reason is the same in each case.** *The notification alone* was the earlier recommendation and is measured to move Claude Code (3/3, with the client's own debug line) and to be ignored by Codex, so on its own it leaves a Codex thread calling names that are gone with nothing said. *Refusing every call until a list arrives* is what the question proposed and it turns a working session into a failing one for as long as the model does not happen to list. *Accepting the mismatch* rests on a model re-tooling after an error, which it does in practice and which is not a mechanism: the error a missing tool produces reads to the model as its own mistake. **The measurements behind all four are in [`kb/mcp/protocol.md`](kb/mcp/protocol.md)** and the release rule it does not replace is [item 4](RELEASING.md#4-the-four-snapshots-and-the-verdict-file-adjudicated). ✅ **BUILT 2026-09-24** -- *corrected the same day (previously "the implementation is the next version's work in [`TODO.md`](TODO.md)")*. `BrowserProxy` holds the flag, `SessionErrors.ToolListPredatesThisServer` is the sentence, `KnownClients` carries the two identifiers as read values, and the courtesy line is `SessionManager.ServedByADifferentVersion`. ⚠️ **ONE CLAUSE OF THIS ROW WAS FALSIFIED BY THE IMPLEMENTATION'S OWN MEASUREMENT AND IS CORRECTED AND NOT QUIETLY DROPPED.** The row said the notification *is measured to move Claude Code (3/3, with the client's own debug line)*, which is true of a connection the client established and listed from -- and **false of a re-dialled one**: measured 2026-09-24 @ 2.1.281 against the published slice, 3/3, with 5.1 s of idle connection left between the refusal and the retry, **no `tools/list` arrived at all**. So on the one path this decision exists for, *the notification alone* would have done nothing, which strengthens the choice and does not weaken it -- and the remedy clause no longer tells a model its list has been refreshed. The refusal itself reaches the model byte for byte and the call after it is answered, 3/3; a Codex thread lists before it calls, 3/3, so it never meets the refusal at all ([kb](kb/mcp/protocol.md#what-q261s-refusal-does-at-the-other-end----measured-2026-09-24), [evidence](docs/evidence/2026-09-24-q261/README.md), re-verification row 153). It closes [the frozen-tool-list hazard row](HAZARDS.md#hazard-index) on the condition that row set for itself, and what it does not close is named there |
| **One hidden coordinator, started only when it is needed, owns the apply and the conversation with the person** | **Settled 2026-09-24, in the maintainer's words: _"Q280 b - Some questions however. Research how we can reliably start earlier than vscode starting dozens of servers simultainiously. Also research how velopack handles process kills on updates because I have a hard time believing it only monitors the process calling update. That would mean it kills all instances of a multi instance app!? Or is this no longer an issue if only the gui app can run velopacks update after it's own checks? I do not want to build logic if velopack already ships this. Also, we should still be able to start the gui process even if it is already running invisible. Even if it is single instance? How do the servers communicate with the gui app? Named pipe? How do we prevent multiple gui instances then? Help me design this out to be minimal but effective. Research everything you need to before answering me."_** The design his four answers in the rows below completed, recorded as one. **`BrowserAI.exe`, the configuration app, becomes a hidden coordinator**: one per user, single instance through its own named pipe, and the only process that raises the update toast, shows the sessions page, stops a server gracefully through that server's pipe, and asks Velopack to apply. **It is not resident.** A per-user logon task starts it at sign-in, where it applies a staged update if nothing else runs from the install, and a server that finds an update blocked starts it through the same task. A second start of it, from the Start Menu or a click on the toast, hands over through the pipe and exits, which answers *"we should still be able to start the gui process even if it is already running invisible"*. **Velopack keeps what it ships**: the wait, the swap, the kill pass, its packages lock and the hooks, with `SetAutoApplyOnStartup` still off. What it does not ship is any notion of a second instance, measured: an apply terminates every process under the install root, not only the one that asked ([kb](kb/packaging/velopack.md#what-an-apply-does-to-every-process-under-the-root----read-and-measured-at-12158-2026-09-24)), so knowing who else runs is the one thing the coordinator adds. The live markers are unchanged, and nothing is written to disk for another process to read. ⚠️ **Four directions were set aside, each for a reason recorded where it was measured.** *Servers agreeing among themselves on who applies*, through a lock or a mutex: a server waiting on one is a process under the install root, and the kill pass ends it with the rest. *The first server after a reboot applying on its own*: it does so only when it is alone, which it was on the morning of 2026-09-24, and a dozen restored sessions starting in the same second each see the others. *A coordinator started as a plain child of a server*: the client kills its server's process tree when it exits ([kb](kb/mcp/protocol.md#what-a-client-does-when-the-server-exits-and-what-the-pipe-decides----measured-2026-09-24)). *A record each server rewrites in a file*: it is read torn, which is the row on the channels below. **What exists of it**: phase 1 built the servers' half, the pipe each server answers on and the refusal of Q286 b below; the coordinator, its logon task, its toast and its sessions page are phases 2 to 4 and none of them exists yet |
| **The sign-in step is a per-user logon task** | **Settled 2026-09-24, in the maintainer's words: _"Q282 a"_.** The install and update hooks register a scheduled task with a **logon trigger scoped to the installing user**, and the uninstall hook removes it; it starts the coordinator, which applies a staged update if the install is otherwise idle. **It is the earliest start an installer can register, and it is not a guarantee.** On the morning measured, a logon task's process started 1.249 s after sign-in, before `explorer.exe`, while the `Run` keys began at +20.8 s and ran one command at a time, the editor started at +274.5 s, and the first BrowserAI server of the day, whose launcher no record names, came at +104.8 s ([kb](kb/windows/processes.md#what-starts-first-after-sign-in-and-what-a-task-started-process-may-do----measured-2026-09-24)). **A user-scoped trigger registers without elevation, and a trigger for any user is refused**: registered in 56 ms against *Access is denied* in 23 ms, re-measured from a non-elevated token; the refusal is what the logon sweep task met on 2026-08-16, whose trigger named no user ([kb](kb/windows/detection.md#the-logon-sweep-task)). Not taken: a `Run` key or the Startup folder, which start later and behind everything else registered; a task for any user, which a non-elevated installer hook is refused; and no sign-in step at all, which leaves the apply to whichever server happens to be first. **Not built yet**: phase 2 |
| **A blocked server starts the coordinator through the logon task** | **Settled 2026-09-24, in the maintainer's words: _"Q283 a"_.** A server that finds a staged update it may not apply runs the same scheduled task on demand, so the coordinator is the task scheduler's child and not the server's, and **it outlives the client**: a client kills its server's process tree when it exits, `taskkill /T /F` ([kb](kb/mcp/protocol.md#what-a-client-does-when-the-server-exits-and-what-the-pipe-decides----measured-2026-09-24)), and a coordinator started as the server's own child would go with it. **What it costs is the foreground**: a process the task scheduler starts holds no right to it, measured three times, so the coordinator brings a window to the foreground only after a process that holds the right grants it ([kb](kb/windows/processes.md#what-starts-first-after-sign-in-and-what-a-task-started-process-may-do----measured-2026-09-24)), which is why a second start learns the coordinator's pid through its pipe before it asks for the window. **Not built yet**: phase 2 |
| **Processes talk over raw named pipes, and nothing is written for another process to read** | **Settled 2026-09-24, in the maintainer's words: _"Q284 a"_**, which is also his answer to Q268, where he had asked of a per-server record: _"but why does it need to be checksummed? Also, if servers start talking over named pipes for q280. Maybe we need to review the file based communication and leverage this path as it is already existing then?"_ **Two kinds of pipe.** Each server serves `\\.\pipe\BrowserAI-<its live marker's name>` and answers two requests, `describe` from its own memory and `stop` by acknowledging and then stopping; the coordinator serves one pipe of its own, created with `FILE_FLAG_FIRST_PIPE_INSTANCE`, which is both its single-instance guard and the channel a second start hands over through. **Raw `CreateNamedPipeW`**, with `PIPE_REJECT_REMOTE_CLIENTS` and a DACL whose one entry is the current user. The live markers stay exactly as they are and remain the census. **Why a checksum ever came up, and why nothing needs one now**: a record rewritten in place in a file is read torn, and the torn read parses, 22 of 3,000,000 reads at full rate and 2 in 1,976 writes at ten a second; a checksum would catch it, and a pipe answering from memory has nothing to tear ([kb](kb/windows/processes.md#a-per-server-named-pipe-answers-from-memory-and-cannot-tear----measured-2026-09-24)). Not taken, each measured: that record, with a checksum or a sequence counter; the framework's pipe class, 121,856 bytes against 3,584 for the raw call and unable to set the remote-client flag; a named mutex and event for the single instance, which decide the winner faster and tell a second start no pid; and a message-only window, which works and is found only from its own desktop. ✅ **The servers' half is BUILT, phase 1**: `ServerPipe`, `ServerPipeClient` and `ServerPipeTests`. The coordinator's own pipe is phase 2 |
| **An update applies when a path scan under the install root finds only the coordinator** | **Settled 2026-09-24, in the maintainer's words: _"Q285 a"_.** Before it asks Velopack to apply, the coordinator lists every process whose image path is under the install root, and it goes ahead only when that list holds nothing but itself. **The scan is Velopack's own kill set, measured**: `force_stop_package` ends exactly the processes under that root, every one of them, after every hook and once more before the swap ([kb](kb/packaging/velopack.md#what-an-apply-does-to-every-process-under-the-root----read-and-measured-at-12158-2026-09-24)), so whatever the scan finds is what an apply would terminate. Not taken: the census of live markers as the gate, which counts the BrowserAI processes that joined and not everything the kill pass would end; and a lock among the servers, which the kill pass ends with them. **Not built yet**: phase 2 |
| **A call an update meets is refused with a sentence, never dropped** | **Settled 2026-09-24, in the maintainer's words: _"Q286 b"_**, answering his own question of that afternoon: _"Can we, during a gracefull shutdown of a server instance, tell the llm to wait 1 min. before using the server again? This would allow the update process to finish? Or is that not part of the mcp shutdown protocol? Or can we refuse a badly timed start with a refusal message explain to retry as well?"_ **The first half is not something MCP carries**: no shutdown message reaches a model -- Claude Code drops log notifications and Codex only logs them, measured -- so a tool result is the only channel there is, and both moments an update meets a model are answered through it, with one catalogue row, `SessionErrors.UpdateIsBeingInstalled`. **(1) A server stopped through its pipe** answers every tool call still in flight with that refusal before it exits, and refuses at the door any call that arrives after the stop began; the first answer for a call claims it at the door out, so an answer arriving after the refusal is dropped and not sent twice. **(2) A server that starts while its own install's `Update.exe` is running** -- the full image path `<install root>\Update.exe`, read with `QueryFullProcessImageNameW`, detection only and nothing terminated -- answers `initialize` normally, refuses every tool call with the same sentence, starts no browser server and never checks the feed; the updater's kill pass ends it and Claude Code starts one again on its next call. ⚠️ **Three things were added to that text while building it, each for a reason stated where it lives.** The in-flight sentence says the call **may have partly happened** and to check before repeating it, because a call cut off in flight had already been forwarded and the proposal's *nothing was run* would be false of it. A server that started during an update **ends its own conversation when the updater it found has gone**, because the 2026-09-24 kill run started four servers after Velopack's kill pass and before its updater exited, running the new version with nothing left to end them -- left alone, each would have refused every call for the rest of its session. And `tools/list` on such a server is refused as a JSON-RPC error carrying the same sentence, because there is no child to list from and a partial list would be cached by the client for its whole session. **The accepted gap, named when the direction was put and accepted with it**: a server killed before its handshake finishes is lost for that session, because the client sees a server that failed to start and does not start it again -- an installed server took **0.39 s at the median** from its first log record to serving stdio, 0.30 s at p10 and 2.17 s at p90, over 414 starts read out of the process log on 2026-09-24. **The same gap has a second face this build adds and does not close**: a session that starts in the few seconds of an apply meets a tool list that failed, and so has no BrowserAI tools until the client reconnects. `UpdateInProgressTests`, `ErrorCatalogueTests.TheUpdateRowIsEmittedByACallCutOffByAStopAndByACallMadeAfterIt` |
| **The session record carries the version that served it, and says so kindly** | **Settled 2026-09-24 with the row above.** Every call stamps the serving BrowserAI version into the session record, and `browserai_resume` and `browserai_catch_up` carry a **courtesy line** when the version that last served this session differs from the one answering now. ⚠️ **It is never a refusal**, and that is the whole distinction from the direction above it: an agent meeting a session it did not create learns that the product under it moved, at the moment it is already reading what the directory holds, and nothing it can do about that should stop it working. The version stamp is a fact about the record, not a gate on the caller |
| **The relay was prototyped, it works, and it is not taken** | ⛔ **Measured 2026-09-24 and dropped by the maintainer in the same breath as the decision above.** Both clients decide a stdio server is gone from the **pipe** and not from the child's exit, so a relay spawned by the client, never exiting, forwarding stdio to a server it re-spawns across the apply, keeps the transport alive through an update: **3/3 Claude Code in one session, 3/3 `codex exec` in one turn, and 3/3 Codex app-server on the SECOND TURN OF THE SAME THREAD with no reload and no user action**, swap 308-365 ms. The handover shape -- the server hands its pipes to a helper and exits -- works 3/3 under Codex and collapses 3/3 under Claude Code, because an inherited stdin reports EOF at the parent's exit. **What it would have cost is why it is recorded here and not built:** two processes per session, a relay living outside the install root and needing an update lane of its own, `ClientLiveness.ForParentProcess` threaded through it, Velopack's `WaitExitThenApply` keyed on the server and never on the relay, and an in-flight request recovered only by **re-sending the frame** -- at-least-once delivery, which no side-effecting tool can accept and which would have to be read off `tool-verdicts.json` per call. The measurements are the evidence for this decision and are kept at [`docs/evidence/2026-09-23-client-reconnect`](docs/evidence/2026-09-23-client-reconnect/README.md) |
| **Registering with a second client** | **Settled 2026-09-24, in the maintainer's words: _"Q258 Your recommendation but skip c."_** BrowserAI registers with **Codex** the way it registers with Claude Code -- through the client's own supported command, `codex mcp add`, at **user scope**, which is the only scope that command has. **Project scope goes through the client's own binary too**, with `CODEX_HOME` pointed at the repository's `.codex` directory, which makes Codex write its own project file: an undocumented lever, recorded as one, whose alternative was owning a TOML merge. **The app-server protocol is the migration target** and not today's mechanism -- `config/value/write`, `config/batchWrite` and `config/mcpServer/reload` are marked experimental and only a host can call them, and the last of those is also the only thing that recovers a dead server with no user action. ⛔ **The plugin route was skipped by name.** A self-hosted marketplace plus a `.codex-plugin/plugin.json` and `.mcp.json` pair does register a local Windows stdio executable by absolute path -- measured, and 62 of 62 curated plugins use that form -- but a plugin install is always user-global, the public directory needs a public HTTPS endpoint no local stdio server can offer, and it would put the exe path in a second place that has to agree with the first. **Separate control per client** is the maintainer's third ask and is not a consequence of this row: it is a GUI obligation, and it is listed in [`TODO.md`](TODO.md). ✅ **BUILT 2026-09-24, steps 2 to 6, and each of his four asks is answered by name.** *"I want the system level and repo level registration to also work for codex and not only for claude code"*: the install, update and uninstall hooks run one pass per client, each with its own ownership read -- ours by command path, another install's refused and left alone -- and its own entry in `mcp-registration.json`, which is schema 2 with a `clients` array; a machine with no Codex gets Codex's named refusal in that entry and an install that still succeeds. Project scope for both clients goes through `McpRegistrar.ApplyToProject`, which gave project scope the ownership gate it never had. *"I want the system level registration and unregistration to be an option in the gui"* and *"I easy I want separate control over system level registration between codex and claude"*: the configuration window gives each client its own state line and its own links, every link names its client, and every identifier resolves to exactly one -- register (whose label follows the state: register, repair, register again), unregister, register in a project, and remove from the project a registration of ours was found in. *"I want the same update effects to be tested on coded"*: `ClientReconnectTests.ABrowserAiRegisteredInCodexServesACallAndLeavesNothingThatHoldsAnUpdate`. ⚠️ **Four things the implementation settled that this row left open, recorded by addition.** (1) **A Codex project entry carries the absolute server path.** The portable `${LOCALAPPDATA}` spelling works through Claude Code's own expansion, and Codex's documentation of `command` describes none. (2) **Codex ends a server by TERMINATING it, not by closing its stdin**, measured 3/3, and the live marker it leaves is not held -- so a Codex-hosted server whose host has gone does not hold an update. (3) **Codex hands a stdio server an allowlist of 20 environment variables**, so `BROWSERAI_ROOT` reaches a Codex-started BrowserAI only through the registration's own `env` table. (4) **A repository with no `.codex` directory is asked nothing**: with `CODEX_HOME` at a missing directory every `codex mcp` verb exits 1, and asking first made the first project registration impossible until the real client showed it ([kb](kb/mcp/protocol.md#what-codex-hands-a-stdio-server-and-how-it-ends-one----measured-2026-09-24), re-verification row 155). **The row's pointer to `TODO.md` above is history**: that item is done and deleted, and the hazard it named as owed is written -- a Codex thread whose server has gone loses MCP for that thread, in [the hazard index](HAZARDS.md#hazard-index) |
| **Another product's configuration file is still not ours to write** | **`config.toml` is not written by BrowserAI, for the reason that rejected writing Claude Code's JSON on 2026-08-16: owning another product's on-disk format and its merge semantics forever, for a file the maintainer edits daily.** ⚠️ **It is a closer call for Codex than it was for Claude Code, and that is worth recording instead of hiding.** OpenAI documents `config.toml` as a file a person edits, the schema is published, and `codex mcp add` is itself lossy in one narrow way: comments inside the `mcp_servers` region do not survive it. So the argument against is no longer *it is undocumented*. It is that a comment-preserving TOML writer is a dependency and a format contract this product does not need, while the client's own command is idempotent in both directions -- three adds exit 0, and a remove of an absent server exits 0 -- and is the thing OpenAI supports |

### Still open

1. ~~**Firefox's `parent.lock` preflight and its own stray detection.**~~ **Closed 2026-08-16** at Firefox support, and it is a charter requirement now, not a design. The preflight opens `parent.lock` for write before every Firefox launch and refuses on a sharing violation with [error row 11](src/BrowserAI/Sessions/SessionErrors.cs); attribution is `RmStartSession` → `RmRegisterResources` → `RmGetList`, intersected with the image-path candidate set and guarded on `ProcessStartTime`; and the restart-registration preference is written into every Firefox config. Measured against the developer's own running Firefox as the negative subject: a foreign browser is attributed to none of our sessions and cannot become a candidate. ⚠️ ***Corrected 2026-08-19 (previously "What remains open is not §D's: `browserai_init` still refuses `browser: "firefox"`, because offering it needs a per-family download size for row 6 and a decision about what `browserai_reinstall_browser` reinstalls when there are two trees -- both carried in TODO.md").*** **Both are done and Firefox is offered.** The download size is measured per family -- Firefox 127.2 MB down, 340.15 MiB on disk, from the CDN's own `content-length` and two clean provisioning runs ([kb](kb/playwright/provisioning-and-timings.md#firefox-measured-the-same-way----2026-08-19)) -- and `browserai_reinstall_browser` names the family it reinstalls (the row above). A real Firefox session now runs the front door end to end: `FirefoxSessionTests.AFirefoxSessionRunsFromInitThroughAnArtifactToDestroy`, which asserts by **full image path** that the browser that came up was Firefox, because every other assertion in that arm is satisfied just as happily by a Chromium.

2. ~~**Nothing here is built, and the first vertical slice is what will say which of it survives.**~~ **Closed 2026-08-17 at `v1.0.0`.** The three decisions this named as *"settled on paper and unexercised"* were all exercised: the three lock scopes under real concurrency (300 tool calls and 50 session open/destroys outstanding at once across three modes), `PROC_THREAD_ATTRIBUTE_JOB_LIST` in a published AOT binary, and the session-index file layout. The prediction that *"at least one will move"* was correct several times over, and each move is recorded where the decision lives, not here. (The SDK and NativeAOT halves closed earlier, on 2026-08-15; see [kb: the 2026-08-15 spike](kb/mcp/sdk.md#driving-the-whole-sdk-aot-passthrough-filters-and-cancellation).)

   > **All three are now exercised, and the expectation was right -- twice.** Closed 2026-08-16, one line each:
   >
   > | Decision | Verdict | What actually happened |
   > |---|---|---|
   > | `PROC_THREAD_ATTRIBUTE_JOB_LIST` in a published AOT binary | **Held** | Zero trim/AOT warnings and correct containment under ILC at the job object, closed against a real browser from the published binary at the vertical slice. What *did* move is an expectation about it: a denied breakaway does **not** always fail the launch -- nested inside libuv's permissive job it succeeds and lands in our job anyway, so the acceptance test had to assert on where the process ended up, not on the return value |
   > | The three lock scopes under real concurrency | **Held, and forced a change** | 16 processes racing one directory on every suite run, 64 twice by hand, one acquiring and every other told who had it. **But the plan had a collision it did not know about**: an open handle cannot be the lock *and* the target of an atomic rename, because Windows refuses the rename under every share mode -- not even with `FILE_SHARE_DELETE`. Create-or-take is now close → rename → re-open **inside** the per-directory mutex, which makes that mutex load-bearing, not orderly (the session directory and the three lock scopes) |
   > | The session-index file layout | **Held, and the *predicate* moved** | The file, its name, its contents and the absence of a lock are exactly as designed -- 2,000 concurrent renames over one name from 8 processes leave one valid file, twice, with zero failures. What was wrong was one word in the self-cleaning rule: *"no **readable** `browserai.json`"* removes precisely the entries that can never come back, because a session whose record is corrupt is refused by `TryAcquire` and so can never re-assert its own entry. Sweeping it would make a directory that still exists permanently invisible to the only inventory there is (the session index) |
   >
   > **The prediction -- "expect at least one to move" -- was conservative.** Nothing was found to be *wrong*; all three shipped. But each of the three cost a correction to a neighbouring claim that had never been exercised, and in every case the correction ran in the same direction: **the paper version was less careful about the case where the recovery does not exist.** A breakaway that cannot fail, a rename that cannot be blocked, an entry that can always be rebuilt -- three assumptions, none of them stated, all three false.

3. **Dash style for the whole repository.** ⚠️ **Open, and deliberately deferred on 2026-09-22, not settled in a release batch.** The maintainer's choice among the directions offered, verbatim as the direction he took: *decide dash style for the whole repository later, as its own item, applied to new entries and unsealed documents only.*

   **The facts it rests on, none of them in dispute.** `ChangelogTests.NothingThatReachesAReleaseBodyCarriesACharacterAPersonWouldNotType` refuses the em dash, the en dash, the four curly quotes, the ellipsis and the non-breaking space in **everything that reaches a release body** -- the section preamble, every entry headline, the legend -- and it is a deny list with a backticked-span exemption. **Everywhere else those characters are the house voice**: the changelog's entry detail, this file, [`CLAUDE.md`](CLAUDE.md), [`HAZARDS.md`](HAZARDS.md), [`TESTING.md`](TESTING.md) and every [`kb/`](kb/README.md) article use them freely, and nothing reads them. **The three entries written on 2026-09-22 use a double hyphen throughout**, so the changelog is now split between two styles by accident, not by anybody's decision.

   **What forced the question and what it costs.** A wording reader found **26 detail lines in the `1.1.0` section** carrying an em or en dash beside three new entries that do not. Those 26 are sealed with the release and **are not touched**: a released section is an append-only record, and re-cutting one to change punctuation is the sweep [`AppendOnlyRecordTests`](TESTING.md#the-dated-records-are-append-only) exists to refuse. So the question is about **new entries and unsealed documents only**, which is exactly the scope the direction names.

   **Why it is not decided here.** It is a convention over roughly forty documents, it touches nothing a user or a test can see outside the release body, and settling it inside a release batch would mean choosing it under time pressure and applying it to whatever the batch happened to touch. The three directions that were on the table: leave it (the repository's voice, and the release body is already protected by a test); sweep the whole tree to one style now; or defer it as its own item with a stated scope. **The third was chosen.** What would make it urgent: the character test growing to cover entry detail, which would make the split a red build, not an inconsistency.

**Recently closed, listed so they are not reopened by habit:** what ends an instance (one browser-idle timer, stdin EOF as backstop, explicit `browserai_destroy`; reclaim is forever); which capabilities ship (unchanged -- `vision`, `devtools`, `config` everywhere, `storage` on `persistent`); how far to curate the surface (upstream names never renamed, and no description is rewritten at all now; *corrected 2026-08-18 twice, previously "`browser_annotate` classified to `interactive`" and then "refused only where no window was promised" -- it is withheld from the surface outright, which is the one filter this build applies and the reason nothing is left to append to*); and whether BrowserAI ships a second stray-sweep trigger at all -- **it does not**.

> ⚠️ **Corrected 2026-08-16 (previously "whether a Velopack install hook can register the logon sweep task -- verified 2026-08-15, the hooks run as the user, non-elevated, and `schtasks /Create /XML` with `LogonType=InteractiveToken` succeeded, survived update and rollback, and was removed by the uninstall hook").** That spike result is real and still recorded in [kb](kb/packaging/velopack.md#nativeaot-hooks-and-vpk-output) -- but it was measured in a spike directory and **generalised to the wrong subject**. The stray sweep then measured registration from BrowserAI's own token and it is **denied**: `schtasks /Create /XML` and the `Schedule.Service` COM API both answer `Access is denied` / `0x80070005`, for a minimal definition as much as for ours ([kb](kb/windows/detection.md#the-logon-sweep-task)). **The logon task is therefore dropped**, decided 2026-08-16 at the update lane, and its code, its tests and its plan text are deleted and not left standing. BrowserAI's own startup sweep is the only trigger, and it is the one that matters: a stray matters when something is about to contend for a lock, which is exactly when a client starts.

> ⚠️ **Corrected 2026-09-24, by addition (previously, and still above, "The stray sweep then measured registration from BrowserAI's own token and it is **denied**").** It is denied for the shape it was measured with. The definition's `<LogonTrigger>` carried no `<UserId>`, which makes it a trigger for any user's logon, and that is the shape a non-elevated token may not register; a trigger scoped to the registering user registers without elevation, measured twice on 2026-09-24 ([kb](kb/windows/detection.md#the-logon-sweep-task)). **Q282 a's per-user logon task rests on that second half.** The drop recorded above stands as it was decided: the task Q282 a registers starts the coordinator, not the sweep, and the startup sweep is still the sweep's only trigger.

---

## Non-reasons -- do not relitigate these

**Token cost is not why this project exists.** Measured 2026-08-13 with `tiktoken` `cl100k_base` against live `tools/list` payloads from `@playwright/mcp` 0.0.79:

| | Eager clients (Claude Desktop, Cursor, older Claude Code) | Claude Code with deferred tool loading |
|---|---:|---:|
| Four servers as registered today | ~23,000 tok | **~985 tok** |
| One perfectly-curated proxy | ~11,600 tok | **~330 tok** |

Claude Code defers MCP tool schemas -- they arrive as bare names and load on demand. **The entire achievable saving in that client is ~650 tokens**, around 0.3% of a 200k window. Dropping the `devtools` capability from four JSON files saves a comparable amount for no engineering effort at all.

The number only becomes significant in clients without deferred loading, where a consolidated surface saves ~65%. Worth knowing it exists. Not worth building for, and **not a justification to cite in design arguments.** Method and provenance: [kb: token cost](kb/packaging/dependencies.md#token-cost-of-the-tool-surface).

---

## Where these decisions came from

Feasibility research completed 2026-08-13 across five streams: MCP SDK capability, Windows auto-update, Node/Chromium bundling, .NET process supervision, and test harness design. Everything marked *measured* or *verified* in this document was executed on a real machine against `@playwright/mcp` 0.0.79 -- not inferred from documentation. Three claims in the first draft of this charter were wrong and have been corrected: the tool count, the framing of the protocol split as a risk, and the assumption that a bundled Chromium is used by default.

**Architecture is settled.** One MCP registration with `init`-issued handles · profile and artifact locations as `init` arguments · any path accepted, correct use being the calling agent's responsibility · one node child per handle · full batteries-included bundling as a NativeAOT single-file binary with no host dependencies.

> ⚠️ **Corrected 2026-08-18 (previously "The [remaining open items](#still-open) -- and the fact that nothing is built -- are implementation-shaping rather than architecture-shaping, with the exception of the last. Instance teardown policy, default capabilities and tool-surface curation were the three open items this paragraph used to name; all three closed on 2026-08-15 and are listed under [Recently closed](#still-open).").** Everything is built, so the clause asserting otherwise is gone and not left standing beside a shipped `v1.0.0`. The three named items did close on 2026-08-15 and are still listed under [Still open](#still-open)'s *Recently closed*. *(⚠️ **Corrected 2026-08-24**: read "beside a **tagged** `v1.0.0`". `v1.0.0` is an annotated, pushed tag over a build that was packed and never distributed, and "shipped" was doing work in design arguments elsewhere -- see [`README.md`](README.md#status). Everything else in this correction stands.)*

> ⚠️ **Corrected 2026-08-18 (previously "One verification task, not a decision: **confirm the MCP SDK is NativeAOT-compatible in our usage** before committing to single-file AOT. Partially discharged on 2026-08-14 -- the SDK declares `IsAotCompatible=true` on every non-`netstandard2.0` target, and the `JsonElement` passthrough at the proxy's core is AOT-friendly -- but a declaration is not a publish-and-run. The fallback, self-contained trimmed at ~70 MB, is noise against the browser download.").** It was discharged in full: the product publishes as a NativeAOT single-file binary with ILC reporting nothing, and the trimmed fallback was never needed.

---

## Provenance stamps

Two conventions, and together they are what makes a stale claim in this
repository detectable, not merely wrong.

**`Verified <date> @ <version>`**, on a default value or on a measured claim.
Upstream defaults drift, and a bare "Default: X" claim cannot tell you when it
was last true. The convention is carried over from the setup BrowserAI replaces,
whose settings table stamped every default the same way. Motivation lives only in
prose -- it cannot recover itself from code.

**Its companion, for a claim that turned out to be wrong: `Corrected <date> @ <version> (previously "X")`.** This repository already writes corrections that way -- [kb: the SDK spike](kb/mcp/sdk.md) carries one -- and naming it makes it a convention, not a habit. **The `previously` clause is the load-bearing half.** A reader who learned the old value needs to see that it was reviewed and replaced, not that it went missing; without the clause a correction is indistinguishable from a claim quietly dropped, and the next reader re-derives the same wrong answer because nothing records that it was already tried. Same reason the supersession callouts throughout this document quote what they replace.

The specification this product is written against is the
[MCP specification](https://modelcontextprotocol.io/specification/latest),
revision 2026-07-28 at the time of writing.
