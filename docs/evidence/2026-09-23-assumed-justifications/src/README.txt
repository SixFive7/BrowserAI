Probes for TODO.md's "27 assumed justifications", src/ sub-list.  2026-09-23.
Read-only research; nothing in the repository was modified.

  probe-E-unwind.txt        does an unhandled exception unwind the stack (finally/Dispose)?
  probe-F-cmdshim.txt       can a .cmd shim start without cmd.exe, and what does a shell cost?
  probe-C-negotiation.txt   what does ModelContextProtocol 2.2.0 do on a protocol-version mismatch?
  probe-G-stderr.txt        can a line a child's tree writes to stderr be lost at the 2 s drain?
  probe-AB-httptimeout.txt  what bounds a stalled Velopack download, and a stalled check?
  durable-count.txt         every "durabl*" occurrence in src/, raw
  durable-classification.txt the same, classified by sense, with the predicate quoted
  probes/                   the source of each rig (own Directory.Build.props /
                            Directory.Packages.props so nothing inherits the repo's
                            central package management or analyzer severities)
