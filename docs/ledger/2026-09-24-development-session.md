<!-- SPDX-FileCopyrightText: 2026 Jori Huisman -->
<!-- SPDX-License-Identifier: LicenseRef-BrowserAI-FSL-1.1-MIT-5yr -->

<!-- The decision ledger of the session opened 2026-09-24, appended to live by the architect and snapshotted here at commit 21de415 on 2026-09-25 at 00:58Z. This is an INTERIM snapshot: the session is still open, and a later snapshot replaces this one under the re-snapshot rule in README.md. It covers the night items (the 2026-09-24 drift check, Q261 against the real clients, T7 c and the Codex steps 2 to 6), the reboot and the maintainer's return, the Velopack rows re-run at 1.2.158, the toast design questions (Q262 to Q274), the focus incident and the testbed batch that followed it (Q275 to Q279, Q281), the coordinator design (Q280, Q282 to Q286) with its phases 1 and 1b (Q287 to Q294), the history rewrite (Q295 a), phase 2 steps 1 to 5 up to their pause, the client, Firefox and window research behind Q296, Q302 to Q304 and Q307 to Q310, Q312, and the zoom-out the maintainer asked for on 2026-09-25. -->
<!-- The live copy is still .work/STATE.md, and the architect is still appending to it. At the snapshot it held 199,686 bytes in 467 lines, sha256 7bd4e035bef4f12cd30a0105c43803ba1db4d26229923b5c58783b604f371659, last written at 00:50:08Z by its modification time. The last heading in the body is stamped 01:10Z, later than both, so that stamp was not read off a clock. -->
<!-- The body below is that live file with ONE exception, applied to this copy only and never to the live file, under the 2026-09-23 grant. Every edit is named here, so the next snapshot can apply the same ones to the live file before it checks the prefix: 52 em dashes became two hyphens, each keeping the space on either side, 25 en dashes became one hyphen, and the one bracketed ASSUMED marker lost its brackets, so the scan reads it as prose and not as a claim. Nothing else moved. The body held no curly quote, ellipsis character or non-breaking space, and the wording scan found no tell in it. None of the 78 swept characters sat inside a quotation: fourteen headings that go on to quote the maintainer verbatim had the dash after their timestamp swept, and every quotation is byte for byte what the live file holds. No fact, number, date, name or claim moved. The body's sha256 is 69ba43e29b982b1871a2af24e6e0363b018303992b7f5c95472882827740ee57. -->

# BrowserAI decision ledger -- night session opened 2026-09-24 ~07:50 local (the 2026-09-23 development session is closed and snapshotted at docs/ledger/2026-09-23-development-session.md; master = 99563ac, gate 807/0/0 both shells)

STANDING RULES: unchanged; near-verbatim in docs/ledger/2026-09-22-release-session.md under "RESUME NOTE FOR THE POST-COMPACTION SESSION" (model discipline: Fable plans and talks, Opus sub-agents do the work, one writer at a time; HALT-A/HALT-B; hostile-caller defence a non-goal; Q175 posts wait for his word; Q185 tag rules; release rules incl. "no trace of AI" (now a repository directive with a scan); repo rules; clearance conditions; harness path guard; instructions budget 2,026/2,048; a ledger is not a decision of record).

OVERNIGHT ORDERS (verbatim, from the 2026-09-23 snapshot's last entries): "continue your work autonomously. Get as far as possible. If there are any questions along the way I want you to keep a record of these questions and follow your own recommendations. When I am back I will say so. Then you may present me with all the choices (if any) that have been made, the alternative answers, the reasoning for your recommendation and the consequences if I chose to chose differently retroactively. ... Arm the watchdog. ... do not restart my system or anythign crazy like that." / "for large bodies of work. Call opus subagents not fable." / machine stays awake; merging to master after each green gate OK; toasts and headed windows on screen OK; the Codex hook steps OK on the test pack only.

NIGHT PLAN: branch `next` from master 99563ac; one Opus writer at a time; after each item the two-shell gate through build/Invoke-OrdinaryGate.ps1 and build/invoke-ordinary-gate.sh with the clearance conditions, on green fast-forward master and push; NO release (T6: only he drives a release). Order: (0) the 2026-09-24 daily drift check (owed: lastChecked is 2026-09-23); (1) Q261 as decided (per-connection flag + one informed refusal + list_changed + version stamp + courtesy line; stub arm, real Claude Code arm under a scratch config, Codex arm under a scratch CODEX_HOME; then an Opus sub-agent drives the real clients against the published slice through the research rigs in docs/evidence/2026-09-23-client-reconnect); (2) T7 c (detached Playwright list call at session close, kb, row); (3) Codex steps 2-6; (4) Q254 toast research then the toast and the sessions page. Feature-catalogue candidates untouched. Never touch the real install (%LOCALAPPDATA%\BrowserAI.app), ~/.claude.json, ~/.claude/, ~/.codex; never kill processes not ours; never restart the machine.

QUESTION LOG (for the morning; each with alternatives, the choice taken, the reasoning, and the retroactive consequence):
- N1: stale local branches `review/playwright-0.0.81` and four `worktree-agent-*` exist from earlier sessions. Alternatives: delete now / leave. Taken: LEAVE (deleting is his; no cost to leaving). Consequence of choosing differently: one `git branch -D` each, nothing else.
- N2: Q261 (b) says "the serving BrowserAI version is stamped into the session record on every call that names a session". Implemented as the ACQUISITION stamp plus dedup, which `SessionLock.Compose` has always done, and NOT as a per-call write. Alternatives: (a) append a `browserAiVersion` row on every session-naming call, dedup'd against the in-memory record -- in this process that branch can never fire, because acquisition already stamped this build, so it is an unreachable write and a string compare per call; (b) re-read the record from the store on every call and stamp on a real difference -- one SQLite read per forwarded call, and the only case it buys is a second BrowserAI reclaiming a live session's directory mid-flight, which is already a named hazard of its own; (c) what was done: the record always carries the serving build because the acquisition wrote it, `HowItGotHere` prints the whole chain once there is more than one, and the courtesy line compares the DISK record before re-acquisition. Taken: (c), and the observable half is held by tests (`CatchUpCarriesTheSameLineOnPageOneAndStampsNothing` asserts the record still names only the planted build after a read). Reasoning: an unreachable write is the thing `ErrorCatalogueTests`' census exists to forbid in the refusal catalogue, and the same smell applies to a stamp. Consequence of choosing differently later: (b) is about ten lines in `SessionLock.Append` plus an arm that plants a reclaim; nothing built tonight has to be undone.
- N3: the frozen-tool-list HAZARDS row was CLOSED (2026-09-24) and not left open. Alternatives: leave it open, because the residue is real -- the refusal informs and does not force, so a model that ignores the sentence still meets upstream's unknown-tool error; or close it on the condition the row itself set ("this row stays open until the refusal exists"). Taken: CLOSED, with both residues named in the Notes cell -- the informs-and-does-not-force half with release item 4 kept as the human gate, and the Codex thread that keeps an old server, which is owed its own row with the Codex work. Reasoning: the row named its own closing condition and that condition is met, and it is met by measurement against the real client and not by a stub alone. Consequence of choosing differently: one Status cell back to `open`, one Notes clause, and the published tally back to 57 open / 156 closed.
- N4: the acceptance recorded on 2026-09-24 asked the real-Claude-Code arm to show "the list refreshed on the notification". MEASUREMENT CONTRADICTS IT, 3/3: with 5.1 s of idle connection left between the refusal and the retry, no `tools/list` reached the re-dialled server at all. Alternatives: assert it anyway (a permanently red arm asserting something false); assert nothing about the notification; or assert what BrowserAI does and record what the client does. Taken: the arm asserts that the notification was SENT and that the refusal and the retry work, and the absent refresh is recorded as a measured fact in kb, in row 153, in the hazard row and in the product's own wording -- which was corrected before it shipped, because it had promised a refresh. Consequence of choosing differently: nothing to undo; making the client refresh is not in this repository's gift.
- N5: a new `SuiteCapability.CodexCommandLine` was added beside `ClientCommandLine` and the latter was not widened. Alternatives: one "an MCP client" capability covering both. Taken: two, resolved through the product's own `CodexRegistration.Locate`. Reasoning: they are installed independently and answer different questions, and one capability would report PRESENT on a machine with only one client while the arm for the other skipped silently. Consequence: merging them later is a small edit in `SuiteEnvironment`.
- N6: the probe rig writes its scratch app root to `C:\Users\jori\Downloads\tmp-q261-clients\<run>`, which is outside `.work/`. Alternatives: `.work/` (refused -- `InstallRootScope` requires the app root under the user's profile); `%TEMP%` (forbidden by the user-global rule); `%LOCALAPPDATA%\BrowserAI-test-scratch` (the suite's own `ScratchRoot.ProfileScratch`, which is what the SUITE arms use). Taken: the Downloads path for the hand-run rig only, and `ScratchRoot.ProfileScratch` for the suite arms. Reasoning: the rig is run by hand and its residue is disposable; the suite already has a sanctioned profile-anchored location and uses it. Consequence: pointing the rig at `%LOCALAPPDATA%\BrowserAI-test-scratch` too is a one-line change in each driver.
- N7: `docs/probes/2026-09-24-q261/` holds COPIES of `apistub.js` and `appserver.js` taken from the 2026-09-23 evidence batch's rig. Alternatives: have `ClientReconnectTests` read them out of `docs/evidence/`; move both under `build/`. Taken: copy into the probe directory. Reasoning: a suite arm may not depend on a file under `docs/evidence/`, which is a record of what a measurement was taken from and which nothing in the suite reads; and `build/` is governed by the tree-as-text scans the probes README explains rigs sit outside. Consequence: deleting two files and re-pointing two paths.

## 2026-09-24 ~05:45 local -- ITEM 0 AND ITEM 1 CLOSED, MERGED
- Drift check 2026-09-24: NO DRIFT on any of the five and none on the vendored row. @playwright/mcp 0.0.82 (435 versions, overrides object empty), playwright-core 1.64.0-alpha-1789764292000 out of 0.0.82's own dependencies and agreeing with the payload lock, ModelContextProtocol 2.2.0 (47), Velopack 1.2.158 (119), node v24.21.0. ONE COUNT MOVED WITH NO ROW BEHIND IT: node's lts-bearing count 287-of-866 to 288-of-868, because v22.23.3 (Jod, 2026-09-23) landed on an older LTS line and v26.10.0 on the untracked Current one; the tracked predicate is unmoved. SQLite 3.53.4 / 2,946,650 B / SHA3-256 628a44cf...27934e identical in all three fields; SourceGear.sqlite3 gap still nil. Positive control still discriminates (System.Text.Json 181 versions, newest any form 11.0.0-rc.1.26425.128, newest stable 10.0.12). `7f0c943`.
- Q261 (a) and (b): `59179da`. Planted red twice and watched -- flag forced set at the handshake (4 arms red, incl. the catalogue provocation) and the once-guard made to fire every time (the refusal arm red on the second call, which is the wall). Catalogue census 33 -> 34.
- Q261 real clients: `74e650e`. 11 runs, rig `docs/probes/2026-09-24-q261`, evidence `docs/evidence/2026-09-24-q261` (92 files, 878,686 B). CCQ7-9 3/3: the refusal reaches the model byte for byte and the retry is answered. CCQ4-6 3/3 with 5.1 s idle between refusal and retry: NO tools/list on the re-dialled connection at all -- so the notification is not what recovers the turn, and the product's own wording was corrected before it shipped. CXQ1-3 3/3: Codex lists before it calls, never refused, no notification. `~/.codex` hashed before and after every Codex run and unchanged. HAZARDS frozen-tool-list row CLOSED (tally 56 open / 157 closed); re-verification row 153 added, row 149 narrowed; kb entry added; TODO item deleted; DECISIONS row corrected.
- Rig fixes: `c5aaf41`. 22 reds on the first gate run, all the rig calling a tool without listing; RawStdioClient and ClientProbe now list at connect, and the new arms end the processes they start (5 leaked stubs identified by command line and ended).
- README re-measured: `46cbf99`.
- GATE, two-shell ordinary, both green: PowerShell ord-ps-3 814/0/0 in 2m 39.9s, drive letter UPPER; Git Bash ord-bash-1 814/0/0 in 2m 41.2s, drive letter LOWER. Both FULL RUN, both publish FRESH, every capability PRESENT (including the new Codex CLI row), clearance byte-identical either side of each. Commit charge read TIGHT on both (86.0% / 78.1%) and is recorded as machine load, not as a finding.
- MASTER = 46cbf99, fast-forwarded from 99563ac, 5 commits, pushed; `next` pushed; back on `next`, tree clean.
- NEXT: item 2 (T7 c), then Codex steps 2-6, then Q254.

## 2026-09-24 ~10:15 local -- NIGHT ITEM 1 LANDED: drift 2026-09-24 clean (7f0c943); Q261 (59179da flag + refusal + notification + stamp + courtesy; 74e650e real-client arms, rig docs/probes/2026-09-24-q261, evidence docs/evidence/2026-09-24-q261; c5aaf41 rig fixes); gate 814/0/0 both shells; master = 46cbf99 pushed. FINDING: on a RE-DIALLED Claude Code connection no tools/list follows the list_changed notification (3/3 with 5.1 s idle), unlike an established connection; the once-per-connection refusal is what recovers the turn; wording corrected before shipping; row 153, row 149 narrowed, HAZARDS frozen-tool-list row closed. Codex lists before calling (3/3) and never meets the refusal. N2-N7 logged by the writer. NEXT: an independent Opus verifier drives the published slice through the rigs (his acceptance line), then T7 c.

## 2026-09-24 ~11:00 local -- Q261 INDEPENDENTLY VERIFIED on real clients (Opus verifier, own rig and stubs, byte-exact against the C# literals): Claude Code 2.1.281 refusal 4/4 (894 chars, sha acede678...), list_changed sent immediately before it, NO tools/list within a 5.0 s idle 3/3, retry answered 3/3, first-connect control never refused; Codex 0.155.0-alpha.9.2 lists before calling on 14/14 connections, never refused, never re-dials a dead server (3/3 "Transport closed"; resume --last lists fresh 3/3); courtesy line 3/3 both tools (563 chars, sha ae3810d2...), absent when the stamp matches; filtered Q261 arms 32/32. CORRECTIONS TO FOLD IN (next writer, by addition): the published slice in the tree was STALE against the final Q261 commit (five src files newer than the exe; the verifier republished, now 1.1.1-alpha.0.68 and fresh); the kb's "79 tool definitions on every turn" is configuration-specific (121 when the repo's committed .mcp.json servers connect; browserai's own list is 79 on every connection); the probe README's claim that the rig takes codex.exe from AppData\Local\OpenAI\Codex\bin "outside ~\.codex" does not describe the suite arm, which resolves ~\.codex\plugins\.plugin-appserver\codex.exe (byte-identical today); Claude Code headless from a cwd inside the repo connects the repo's project-scope .mcp.json servers even with an empty .mcp.json in the cwd; the premise "a name has gone" is simulated by process identity in every rig, never by a real surface difference. N8: the verifier's republish changed build output only; nothing to revert. NEXT: T7 c writer.

## 2026-09-24 ~08:25 local -- NIGHT ITEM 2 LANDED: T7 c BUILT, GATE GREEN BOTH SHELLS, MASTER = 63c71a9
- SIX COMMITS on `next`, fast-forwarded to master and pushed: `ad4537b` the reap call; `6b22a79` the arm; `f2417a7` the record (kb, DECISIONS, HAZARDS, re-verification, ARCHITECTURE, CHANGELOG, TODO) plus an eleven-occurrence sweep of the banned comparison frame; `bcadbe3` the verifier's four corrections folded in; `8ec27f1` the two suite reds; `63c71a9` README re-measured at 817.
- THE MECHANISM: one detached `node -e "require('<payload>/mcp/node_modules/playwright-core/lib/serverRegistry.js').serverRegistry.list().then(...)"` per close that really put a browser tree down. Four paths: destroy (its own cause), idle close, client exit / shutdown, and the stray sweep's own kill. In no job, `bInheritHandles: FALSE`, no standard handle named, `CREATE_NO_WINDOW`, working directory the user profile. Event ids 90 (started, `pid@createdFileTime`), 91 (could not start), 92 (no module in the payload). `JobLauncher.StartDetached` and `ProcessLiveness.TryCreationTimeOf` are the new interop; `PayloadLayout.ServerRegistryModule` is deliberately NOT in `Verify()`.
- PLANTED REDS, WATCHED. (1) The reap made unreachable in `LiveSession.TearDownAsync`, slice republished: the arm failed on *the close should have recorded the reaper it started*, 1 failed / 1 succeeded in 5.58 s; restored and republished, 2/2 in 11.2 s. (2) `PasswordPromptTests`' guard dropped on the spot, under one real `--sweep` per second: 3/3 RED (two at about 3 s, one at 3m 01s, which is upstream's 180 s launch budget) and 3/3 GREEN with the guard held.
- MEASUREMENTS (single process, planted dead descriptors, scratch registry through `PWTEST_SERVER_REGISTRY`): 1,000 in 10.19 s; 4,000 in 309.5 s; an empty directory in 0.062 s. THE REAL DIRECTORY was reaped for the first time: 4,059 in, 0 left, 4,059 unlinked, 1,053.09 s -- and that timing is CONTAMINATED, because this session started the measurement twice and two reapers ran at once (29.4 s and 23.3 s of CPU across 22 minutes, i.e. waiting on pipes). The kb says all of that in those words. One gate run produced 114 reap records.
- GATE, two-shell ordinary, both green at the SAME publish height: PowerShell `t7-ps-4` 817/0/0 in 2m 43.5s, drive letter UPPER; Git Bash `t7-bash-2` 817/0/0 in 2m 44.5s, drive letter LOWER. Both FULL RUN, both publish FRESH, every capability PRESENT, no degraded path, clearance byte-identical either side of each. Commit charge TIGHT (86.9% → 78.4% and 78.6% → 79.0%), recorded as machine load. Both slices published with 0 ILC/trim warnings -- twice, once before the gate and once after the test-fix commit (see N16).
- THREE EARLIER GATE RUNS AND WHAT THEY FOUND: `t7-ps-1` 816/1 (PasswordPrompt), `t7-ps-2` 815/2 (PasswordPrompt plus my own liveness assertion), `t7-ps-3` and `t7-bash-1` 816/1 each (ClientReconnect, version-stale publish). Every cause is named and either fixed or logged; none was a product defect.
- MASTER = 63c71a9, fast-forwarded from 46cbf99, 6 commits, pushed; `next` pushed; back on `next`, tree clean.
- POST-GATE ADDENDUM: one more prose-only commit landed after the green gate, `f3320c0` -- the kb records that the suite's own descriptor residue is **0** immediately after the two 817-test runs, against the 499 a day this machine used to accumulate, with **679** reap records that day at event id 90 and **0** at 91 and 92. Verified by the four document scans (49/49) and by nothing else, because it changes no code. **MASTER = f3320c0**, pushed, `next` level with it, tree clean. Both slices were then republished at that height (1.1.1-alpha.0.75, 0 ILC/trim warnings each) so the next gate does not meet N16.
- NEXT: Codex steps 2-6, then Q254 (toast research, the toast and the sessions page).

QUESTION LOG, continued:
- N9: forwarding `PWTEST_SERVER_REGISTRY` to every child. Alternatives: (a) forward it as a documented test hook; (b) do not forward, and isolate a reap by overriding `LOCALAPPDATA` for the test host -- which moves every Windows path the child reads, so the reap would be exercised at a path production never takes, and the override reaches every child every other arm starts; (c) no suite arm at all, a hand-run probe instead -- and the only arm that could then exist would prune the maintainer's own registry, 4,059 entries and 17 minutes of it, which is the one thing the T7 probes were careful never to do. TAKEN: (a). Reasoning: the variable is upstream's own, it overrides no key the config generator writes, nothing in the product sets it, and forwarding is the only way the child that writes the descriptors and the reap that collects them read one directory. ⚠️ TENSION WITH THE RECORDED DECISION, named and not hidden: the 2026-09-24 close recorded *the suite variable is not used*, which declined recommendation 5 -- pointing the SUITE's own runs at a scratch registry to remove 99.3% of the growth here. This is narrower: the suite still writes to the real registry, and one arm hands the variable to one process. Consequence of choosing differently: one line out of `ChildEnvironment.InheritedWhenSet`, one arm out of `ChildEnvironmentTests`, and `ServerRegistryReapTests` either re-pointed at a `LOCALAPPDATA` override or deleted.
- N10: the one-line `serverRegistry.list()` instead of the CLI client's `list` command, which is what the T7 row said. Alternatives: (a) `node .../lib/tools/cli-client/cli.js list`; (b) the one-liner. TAKEN: (b), and the DECISIONS row is corrected by addition with what it previously said. Reasoning: both reach the same reaper -- `collectList` calls it before it needs anything from it, which is upstream's *List early to GC* line -- but the command also resolves a WORKSPACE by walking up ten directories from its working directory looking for `.playwright`, and then deletes that workspace's dead daemon session configs under `%LOCALAPPDATA%\ms-playwright\daemon`: a second registry this product never writes, pruned differently depending on where a session directory happens to sit. Consequence of choosing differently: one argument list in `ServerRegistryReap`, and the daemon-registry prune accepted.
- N11: the reap fires only when the child's job held more than the node child. Alternatives: (a) on every session close, as the decision's wording reads; (b) only when a browser tree was in the job; (c) a seam so the suite can switch it off. TAKEN: (b). Reasoning: a descriptor is written at a browser BIND, so a close that bound none made nothing dead and has nothing to collect; and the suite closes sessions against in-process doubles by the hundred, which would otherwise start a hundred node processes per run. (c) is a seam that exists for a test, which this repository refuses elsewhere in the same file. Consequence: delete two conditions -- and expect the suite's process count to rise.
- N12: the destroy got its own reap cause, which needed a `TearDownAsync(string)` beside `DisposeAsync()`. Alternative: one cause for all three teardown paths, leaving either an unused constant or a vaguer record. TAKEN: the explicit method, with `DisposeAsync()` delegating and the destroy calling it inside `await using` so CA2000 still sees the disposal. Consequence: collapse to one constant, about three lines.
- N13: the real-registry measurement was started TWICE, so two reapers ran at once and the 1,053.09 s is an upper bound and not a clean number. My error, recorded as such in the kb entry. The count (4,059 → 0) is what that run is quotable for, and the clean timings come from the single-process scratch runs. Consequence of wanting a clean one: it is unobtainable now -- the backlog is gone -- and would need another week of growth, or a scratch registry seeded to the same size, which is what the 4,000-entry run at 309.5 s already is.
- N14: `PasswordPromptTests` was edited, which is outside T7's subject. Alternatives: (a) hold the session guard for the child's life; (b) add `[NotInParallel("stray-sweep")]` as the sibling Firefox arm does; (c) log the red and leave it. TAKEN: (a). Reasoning: the sweeps come from every BrowserAI the suite starts -- about a hundred processes -- and not from a sibling arm, so a key cannot close it; and the product was doing exactly what it promises, which the sweep's own record proves by naming the pid the arm's Playwright log carried. (c) would mean merging on a red whose cause was known. Consequence: revert one holder class and one parameter.
- N15: the liveness half of the T7 arm was REMOVED after one run. *The reaper was still running when the destroy answered* holds only while the test host observes the answer inside the reap's own window, and at full parallelism this host is starved for seconds: it passed alone and failed in the suite with the product behaving identically. Alternatives: plant 4,000 descriptors so the window is minutes (a five-minute arm, and still a clock); assert nothing about ordering (which loses what the orders asked for); assert the ordering from the LOG. TAKEN: the log ordering, with the removal and its reason written where the assertion stood. Consequence: nothing to undo; a race-free liveness assertion would need a lever this rig does not have.
- N16 (FOR HIM, NOT DECIDED): a commit between the publish and the gate reddens `ClientReconnectTests.ARealClaudeCodeMeetsTheRefusalOnceAndItsRetryGoesThrough`, and the publish-freshness row cannot see it. Measured: the published server was 1.1.1-alpha.0.72 while the test host built 1.1.1-alpha.0.73; the arm composes the expected refusal from its OWN `BuildVersion.Current`, so the two strings differed by the version they name -- red in both shells, green in both after a republish at the same height. The freshness check compares FILE TIMES of `src/` inputs, so a test-only commit moves the version and nothing notices. Directions: (1) have that arm read the server's own version off the wire (the courtesy line already carries it) and compare against that; (2) teach `PublishedSlice.EnsureFresh` to compare the version baked into the binary against the tree's, which catches the whole class; (3) document it in TESTING.md and keep republishing by hand. Recommendation: (2), with (1) as the cheap immediate fix. Nothing was changed, because it is a decision about the gate's own instruments.

## 2026-09-24 ~14:40 local -- NIGHT ITEM 2 LANDED: T7 c (ad4537b reaper call, event ids 90/91/92, detached one-liner serverRegistry.list() per browser-tree close; 6b22a79 test with planted red; f2417a7 kb/DECISIONS/HAZARDS closed/rows 154+150/ARCHITECTURE/CHANGELOG/TODO; bcadbe3 the verifier's corrections; 8ec27f1 two suite reds fixed with planted reds; 63c71a9/f3320c0 README 817 + kb addendum). Gate 817/0/0 both shells. master = f3320c0 pushed. Measured: 1,000 dead entries 10.2 s, 4,000 → 309.5 s, empty 0.06 s; the real registry 4,059 → 0 (timing contaminated by a double start, recorded). N9-N16 logged by the writer. N16 DECIDED BY THE ROOT SESSION per the overnight rule: the ClientReconnect arm composes the expected refusal from the server's version read off the wire, not from the test host's BuildVersion, and PublishedSlice.EnsureFresh additionally compares the baked version with the tree's; folded into item 3's brief. Retroactive cost of choosing differently: a one-arm change. NEXT: item 3, Codex steps 2-6.

## 2026-09-24T08:20Z -- REBOOT, DIRECTIVE CHANGE, THE MAINTAINER IS BACK. (Clock note: the "local" stamps above this entry were estimates and drifted by hours; from here stamps are UTC from `date -u`.)

MAINTAINER (verbatim): "My system accidentally rebooted. Please continue. Slight change in the directive. Everything now runs under opus 5.5." and "Also, I am here now so present me with open questions when they arise by listing all open questions up to that point. For each of the open questions give me some a short primer, context, solutions directions and recommendations."

=> The machine rebooted at 07:13Z (boot 09:13:44 local). The item-3 writer died mid step 3: step 2 committed locally at d30fd68 (NOT pushed; origin/next = master = f3320c0); step 3's work uncommitted in nine modified files plus the untracked src/BrowserAI.App/ClientState.cs (+949/-259). MODEL DISCIPLINE FROM HERE: the root runs Opus 5.5 (claude-opus-5-5[1m]); sub-agents are dispatched as "opus" (Opus 5.5); large bodies of work still go to sub-agents, one writer at a time; the root does cheap verification itself. THE MAINTAINER IS BACK: the overnight question log N1-N16 (N8 is the verifier's republish, in the ~11:00 entry) is presented now; new questions are presented as they arise together with every still-open one.

FIELD OBSERVATION, the update lane in the wild: after the reboot the real install applied its staged update on its own. Process log browserai-20260924-000.log: pid 20368 started 07:40:36Z as 1.0.0, checked the feed, found 1.1.0 (fullPackageBytes 55,022,716), staged it in 0.6 s, found itself alone, and handed the apply to Update.exe ("BrowserAI is shutting down"); the Update hook ran at 07:40:40Z as 1.1.0 and left the user-scope registration alone ("already registered at user scope, so nothing was changed"); from 07:43:35Z every server reports manifestVersion 1.1.0, seven of the maintainer's servers now run from current\. ARP BrowserAI.app DisplayVersion 1.1.0; Start Menu BrowserAI.lnk rewritten 07:40:44Z; ~/.claude.json browserai unchanged (current\BrowserAI.Server.exe). Every pre-reboot clearance baseline is stale by the product's own update, not by the suite. Writer resumed with this state; it pushes `next` after every commit from now on.

## 2026-09-24 ~10:40 local -- ITEM 3 (Codex steps 2-6) IN PROGRESS, writer resumed after the 07:13Z reboot
- Step 2 `d30fd68` (hooks: every client, schema-2 record with one entry per client, clearance gained a 6th reading) -- pushed after the reboot, it had been committed and not pushed.
- Step 3 `31afbe1` (GUI per client, ApplyToProject in the registrar, re-register, project unregister, Codex project scope) -- pushed. Six planted reds, each on its own arm.
- Post-reboot census 08:20Z: 10 BrowserAI.Server.exe, ALL parented by claude.exe; the Codex desktop app-server pid 25364 (up since 07:39:03Z) holds none.
- Measured 08:20Z under a scratch CODEX_HOME: a project-scope add/list/remove leaves `tmp\` and `tmp\arg0\` as EMPTY DIRECTORIES beside a 0-byte config.toml.

QUESTION LOG, continued (item 3's writer):
- N17: the real-installer arm runs the hooks of the last PACKED test installer (it wrote `schemaVersion: 1`, `browserAiVersion: 1.1.0`), not this tree's, so the per-client record cannot be asserted there. Alternatives: (a) re-pack the test installer through build/New-Release.ps1 -- it is a by-product of a release cut, needs -AllowPreRelease, writes Releases/, and a release is his to drive (T6); (b) assert only when the packed version equals the tree's -- a test that silently stops covering; (c) hold the record's shape in the arms that run the hook body in this build and write the limit into the installer arm. TAKEN: (c). Consequence of choosing differently: after his next release cut, one foreach in the installer arm asserts both keys.
- N18: the clearance snapshot gained a SIXTH reading, `~\.codex\config.toml` by length and SHA-256 (the brief named five). Alternatives: keep five and hash ~/.codex by hand around each run; read it through `codex mcp get browserai`; read the file in the script. TAKEN: the file, in the script, with TESTING.md's table row corrected. Reasoning: the hooks now register Codex, `codex mcp add` has no scope flag, and that file is what this item newly puts at risk; a file read needs no CLI on the machine. Consequence: delete one block in build/Get-ClearanceSnapshot.ps1 and one clause in TESTING.md.
- N19: the window's shape. Alternatives: (a) a client selector (half the links, but the other client's actions sit behind a click); (b) two rows with a separate "register again" link (a fourth link per client); (c) two rows, one register link whose LABEL follows the state (register / repair / register again), project unregister offered only where a registration of ours was found at or above the working directory and with no folder picker. TAKEN: (c). Reasoning: his "separate control" is served by no link applying to both clients; a picker for a removal can be pointed at the wrong repository. Consequence: a selector is a toggle identifier plus one filter in ConfigurationDialog.Commands; a picker is one call in Program.UnregisterFromProject.
- N20: a Codex PROJECT entry carries the absolute server path; the portable `${LOCALAPPDATA}/...` spelling stays Claude Code's. Alternatives: write the portable spelling for Codex too (its docs say nothing of expansion in `command`, and an unexpanded variable is a path that resolves to nothing); measure Codex's expansion first (an app-server dial against a scratch home). TAKEN: absolute, with the note saying why. Consequence: if measured to expand, one line (`PortableCommandFor`) for Codex.
- N21: the brief's "its desktop daemon holds servers for days" could NOT be re-established: the reboot cleared every process, and the 08:20Z census found the Codex app-server holding no BrowserAI server. Alternatives: write it as asserted (uncited; an ASSUMED marker reddens the build by design); omit it; write the evidenced mechanism and name the duration as unmeasured. TAKEN: the last, in the HAZARDS row. Consequence: when a census shows a Codex-parented server older than a day, add that reading to the row by addition.
- N22: `ApplyToProject` removes an entry OF OURS before re-adding it, as the user-scope install does. The old window path treated Claude Code's "already exists" as success and reported "Wrote ..." over a stale entry it had not touched. Alternatives: keep add-only; remove-then-add. TAKEN: remove-then-add, planted red. Consequence: delete one block.

## 2026-09-24 ~11:20 local -- ITEM 3: STEPS 4, N16 AND 6 LANDED ON next (not yet gated)
- `a0d2314` step 4 (four real-Codex arms, the update-effect arm, two product defects fixed with planted reds, HAZARDS row OPEN, kb section, re-verification row 155); `11c6097` N16 (version in the freshness reading, wire version in the reconnect arm, a rendering defect the first live run showed); `b9aa4c4` step 6 (DECISIONS by addition, README, CHANGELOG, ARCHITECTURE, TODO item deleted). All pushed.
- Measured tonight under scratch CODEX_HOME: Codex hands a stdio server exactly 20 environment variables (APPDATA..WINDIR), never BROWSERAI_ROOT, 3/3; every `codex mcp` verb exits 1 under a CODEX_HOME that does not exist; Codex TERMINATES its server ~100 ms after the app-server's own stdin EOF (no end-of-stream line in the server's log), 3/3, and the marker left behind is unheld (4/4).
- The maintainer's ~/.codex/config.toml was rewritten at 07:39:11Z by the Codex desktop app starting after the reboot (len 3699 -> 3689, no `browserai` in it); every run of mine used a scratch home and began at 08:20Z.

QUESTION LOG, continued (item 3's writer):
- N23: the update-effect arm was briefed as "a BrowserAI server registered in Codex starts, serves one call and exits on stdin EOF the way Codex ends it". MEASUREMENT CONTRADICTS the EOF half, 3/3: Codex terminates the server; its log has no end-of-stream and no client-exit line, and its live marker is left behind. Alternatives: (a) assert an EOF line anyway (permanently red); (b) assert Codex's termination (red on a benign upstream change to a graceful close); (c) assert what the update lane needs -- the process is gone by pid+creation time and the marker is not held, with a HELD control while it serves -- and record the termination in kb, HAZARDS and the arm's own remarks. TAKEN: (c). Consequence of choosing differently: (b) is one assertion that the server's log lacks the end-of-stream line.
- N24: Codex forwards an allowlist of 20 variables and never BROWSERAI_ROOT, so the update-effect arm adds the variable through Codex's own `codex mcp add --env` after registering the product's way. Alternatives: run the arm against the default app root (its stray sweep would hunt the developer's own); register with the shim as the Q261 arm does (not the product's registration); the `--env` re-add. TAKEN: the `--env` re-add, with the registrar's own entry asserted first. Consequence: none to undo; if the product ever registers with `--env` itself, the re-add becomes redundant.
- N25: ClientReconnectTests.cs carried both step 4 and N16. Alternatives: one combined commit; two commits with the shared file split by reversing N16's hunks into a step-4 state, each state built and tested before its commit. TAKEN: two commits (a0d2314, 11c6097), each state verified. Consequence: none.
- N26: the CHANGELOG's step-1 entry ("BrowserAI knows how to register itself with Codex, and nothing yet asks it to") was brought up to the shipped state in place, with its history in one sentence, and not left beside a second entry. Alternatives: leave it and add a new entry (a release body saying "nothing yet asks it to" beside one saying it registers); rewrite it. TAKEN: rewrite in place; [Unreleased] is not sealed. Consequence: restore the old headline and paragraph from git history if he wants the landing order visible in the release notes.
- N27: README states "Codex registration arrives in the first release after 1.1.0; 1.1.0 registers Claude Code only". Alternatives: describe the unreleased behaviour with no caveat (misleading to anyone downloading 1.1.0 today); leave README alone until the release. TAKEN: the caveat. Consequence: delete one sentence at the next release, which RELEASING's checklist would meet anyway.
- N28: two product defects the real client found were fixed in-item and are named in the CHANGELOG entry and not as Fixed entries, because neither ever shipped (uninstall-over-nothing reported "Removed" for Codex; the first project registration was refused under a missing home). The pre-existing Claude Code project-scope defect (no ownership check; "Wrote" over a stale entry) DID ship in 1.1.0 and has its own Fixed entry. Consequence: none.

## 2026-09-24 ~11:15 local -- NIGHT ITEM 3 CLOSED: CODEX STEPS 2-6 + N16, GATE GREEN BOTH SHELLS FIRST ATTEMPT, MASTER = 5dc214d
- SEVEN COMMITS on `next`, fast-forwarded to master from f3320c0 and pushed: `d30fd68` step 2 (hooks, schema-2 record, 6th clearance reading); `31afbe1` step 3 (window per client, ApplyToProject, re-register, project unregister, Codex project scope); `a0d2314` step 4 (real-Codex arms, update-effect arm, two product defects, HAZARDS row OPEN, kb, row 155); `11c6097` N16; `b9aa4c4` step 6 (DECISIONS/README/CHANGELOG/ARCHITECTURE, TODO item deleted); `5dc214d` README status re-measured at 831.
- GATE, two-shell ordinary, both green on the first attempt at b9aa4c4: PowerShell `c3-ps-1` 831/0/0 in 3m 04.8s, drive letter UPPER, commit charge TIGHT (67.1% -> 77.3%, machine load); Git Bash `c3-bash-1` 831/0/0 in 2m 48.4s, drive letter LOWER, commit charge HEALTHY (59.9% -> 60.0%). Both FULL RUN, both publish FRESH with "exe and tree both 1.1.1-alpha.0.80" (N16's witness, live), every capability PRESENT incl. Codex CLI, no degraded path.
- CLEARANCE: fresh baseline `item3-baseline` taken after the reboot (ARP BrowserAI.app DisplayVersion 1.1.0, InstallDate 20260924, EstimatedSize 140351; ARP BrowserAI.app.test ABSENT; `claude mcp get browserai` -> ...\BrowserAI.app\current\BrowserAI.Server.exe, Connected; TEMP velopack_BrowserAI.app ABSENT; StartMenu BrowserAI.lnk len 2323 sha256 F99795CD...6145; Codex config.toml len 3689 sha256 E210B8FE...53FC6). Each half's own before/after identical; baseline vs post-gate `item3-final` identical in all six readings.
- Both slices published at 1.1.1-alpha.0.80 before the gate with 0 ILC/trim warning lines each (pattern positive-controlled), and republished at 5dc214d afterwards so the next gate does not meet N16.
- NEXT: Q254 (toast research, then the toast and the sessions page).
- POST-GATE ADDENDUM (item 3): an audit of the item's own remarks found five planted-red claims not yet true -- three real-Codex arm remarks naming plants never run against the real client, two hook-arm remarks describing a different plant from the one watched. The three were WATCHED NOW against the real client, each reverted: MeansAlreadyRegistered true on exit 0 -> "Expected false, found True"; the uninstall-over-nothing fix unreachable -> "Expected NothingToUnregister, received Unregistered"; File.Delete restored -> the real client's tmp\arg0 survived. The two remarks corrected. Commit `8149748` (comment-only; RegistrationTests 41/41, HouseRuleTests 20/20). **MASTER = 8149748**, pushed, `next` level. Both slices republished at 1.1.1-alpha.0.82 (0 warnings) because the plants moved two src mtimes. Hand-made junctions under .work/codex-eof-probe unlinked (4); publish dir intact.

## 2026-09-24T09:30Z -- ITEM 3 LANDED, master = 8149748 (d30fd68 step 2 hooks for both clients; 31afbe1 step 3 per-client window; a0d2314 step 4 real-Codex arms, HAZARDS row open, re-verification row 155; 11c6097 N16 version-aware freshness and the reconnect arm reads the wire; b9aa4c4 step 6 documents; 5dc214d README 831; 8149748 planted-red audit). Gate 831/0/0 in both shells; the clearance baseline was re-taken after the reboot and was identical after. N17-N28 logged by the writer.
ITEM 4 SPLIT, because the maintainer is present and the design touches other agents' live sessions: a design researcher goes first (toast from NativeAOT without generated code, activation and whether the dropdown reaches the app, the X, who raises the toast and when, snooze persistence, the sessions page's data, how the window closes a server, what "recently active" means); its questions go to the maintainer BEFORE any implementation. BESIDE IT, read-only: a researcher re-checks the owed Velopack rows 123/124/126/130 at 1.2.158 and returns replacement text for a later writer.
SUGGESTION FOR HIM (T6: only he drives a release): a good moment for the next release is after item 4 lands; N17 notes that the per-client hook path runs inside a real Setup.exe only once a new release pack exists.

## 2026-09-24T09:42Z -- FOCUS INCIDENT. MAINTAINER (verbatim): "Stop claiming focus or my mouse please" then "Ok nvm. You can have my pc for the next 30 min. But make sure this focus stealing is not something that ends up in the testbed."
=> Measured: no process of BrowserAI's researchers held a window (no live process referencing their scratch, the test pack or an input API; no visible window from a BrowserAI-named image outside the real install). The processes that appeared with the grab were two Stationeers game clients (rocketstation.exe, E:\StationeersRig hostie and joiner) started 09:36:57Z by C:\Source\SixFive7\StationeersPlus\TestRig\testrig.exe playtest under nohup -- another project's agent; both had exited by 09:40Z. Both researchers were told to go headless, then told that UI is allowed until 10:10Z and headless after. His second sentence is taken as a directive for BrowserAI's testbed: an audit reader was dispatched to find every way a suite run can show a window, pop a toast, take focus or drive input, and to propose a mechanism that fails a run that does (directions + recommendation, for him); the toast designer must base the suite's toast and sessions-page tests on an instrument with no popup (SuppressPopup + History read-back to be measured).

## 2026-09-24T10:18Z -- TOAST DESIGN RESEARCH IN (.work/q254-design/, MEASUREMENTS.txt; REPORT.md being written). Q262-Q274 POSED to the maintainer; implementation of item 4 waits on his answers.
Key facts: the Velopack shortcut already carries System.AppUserModel.ID = velopack.BrowserAI.app (rewritten on every update), no ToastActivatorCLSID; a NativeAOT raw-function-pointer prototype raised a toast (1,288,192 B, zero warnings); a protocol click loses the dropdown value, a COM activator via HKCU\Software\Classes\AppUserModelId\<id>\CustomActivator receives it (21-40 ms) without touching the shortcut; the X is visible only to a live raiser; a scheduled toast is delivered with no process alive; boot time via GetTickCount64 (Environment.TickCount64 changes meaning in .NET 11), logon time via WTS; live markers can carry a checksummed record readable with ReadWrite|Delete sharing, torn reads measured; a named stop event per server exits gracefully in ~60 ms; a SuppressPopup toast under a suite-only registry app id showed no popup and took no focus and can be read back and removed (taskbar badge unmeasured).
Mapping: Q262 click path (rec COM activator, CLSID derived from the app id) · Q263 raiser (rec the server + machine-wide stamp + non-waiting mutex, fixed tag/group) · Q264 snooze return (rec scheduled toast, fallback next server start) · Q265 "after the next reboot" (rec session logon time; GetTickCount64 directly) · Q266 the X (rec stamp now+6h at raise) · Q267 closing a server (rec graceful stop event, bounded wait, confirmed force close) · Q268 state publication (rec checksummed record in the held marker) · Q269 recency (rec two levels) · Q270 what applies after closing (rec the app when the census shows only itself; replaces today's "Install now", which applies with no census and whose label contradicts the measured re-dial) · Q271 page rows (rec one per server) · Q272 suite toast (rec release gate until the badge is measured, then every run) · Q273 interop (rec raw function pointers + WinMD oracle test) · Q274 the generated-code rule's scope (rec: clarify by addition that it forbids code generated from third-party metadata, CsWin32 and C#/WinRT, and allows .NET source generators over our own declarations, which already ship).
INCIDENTS during his 30-minute window: the researcher's UI Automation clicked "Settings for BrowserAI Q254 test (registry id)" in the notification centre by mistake; his already-open Settings window (pid 66872) may have navigated; the Notifications\Settings key list is identical to before (122 keys). The window "BrowserAI.test-installer" (pid 76976) held the foreground 09:42:21Z to at least 09:44Z: the Velopack re-check's test installer, inside the allowed window.
DEFECTS FOUND (for the next writer): patch-script residue in XML doc comments (`""" + W + """` at FirefoxProfile.cs:93, UpdateBudgets.cs:23, UpdateService.cs:130/:157/:594; `/// %s` at App/Program.cs:287 and AppendOnlyRecordTests.cs:114); kb/windows/notifications.md:11 and docs/design/toast-2026-09-24/README.md:70 say PowerShell 7 raised the toasts, measured false (only Windows PowerShell 5.1 loads the WinRT type); "Install now" in the configuration window applies with no census check and its label contradicts the measured Claude Code re-dial (Q270).

## 2026-09-24T10:25Z -- VELOPACK ROWS RE-CHECKED AT 1.2.158 (.work/velopack-rows/, replacement-rows.md): 123 unchanged (both uninstall orders, test id only, real key never written); 124 unchanged on Velopack's side (flags 134218752 / 1024, VELOPACK_FIRSTRUN=true, Setup exits 26.9 ms after the app starts; the console-window half not establishable, no current pack has a console main exe); 126 unchanged and the open question answered (VelopackApp.Run() clears the variable, installed processes only, VelopackApp.cs:227-238); 130 the RECORD corrected (the overwrite dialog cancels itself at 300 s with exit 0 and installs nothing; the body changes with the version, Update / Downgrade / Repair). Clearance identical in all 19 readings. Other findings: the server's installer-exit branch (src/BrowserAI/Program.cs:228-232) cannot fire on an installed process because Run() at :131 already cleared the variable; kb/packaging/velopack.md :6, :1252-1262, :1724-1731, :1759-1764 and upstream-review.json's "NOT RE-RUN" note are contradicted; the 300 s timeout applies to every non-silent Setup/Update dialog (source only); pwsh 7.6.6 under DETACHED_PROCESS exits without running its script.
⚠️ CORRECTION OF MY 09:42Z ATTRIBUTION: I told the maintainer the focus grab was not BrowserAI. Incomplete: the Velopack re-check ran a NON-SILENT install at 09:36:18-09:36:26Z whose configuration window Velopack explicitly lets take the foreground (AllowSetForegroundWindow, process_win.rs:402-404), and the toast researcher was running UI Automation and test toasts until the stop at ~09:39Z. The Stationeers game clients started at 09:36:57Z. So two of the three candidates were ours; my check at 09:40Z saw only live processes and missed both. Told to him in the next message.
QUESTIONS POSED: Q275 (row 123 and the installed half of row 126 as suite arms under the test id), Q276 (the server's installer-exit branch that cannot fire when installed), Q277 (consent for two UI-only measurements: row 124's console window, a real Cancel click).
WRITER DISPATCHED for work that waits on no answer: the four rows and the kb/upstream-review corrections, both evidence batches, the toast measurements into kb/windows/notifications.md with the PowerShell-version correction, the patch-script residue in five doc comments with a scan that catches the class, then publish, the two-shell gate, merge on green.

## 2026-09-24T10:40Z -- FOCUS AUDIT IN (read-only, nothing written to .work/focus-audit). REAL TODAY: RealInstallerTests.TheInstalledMainExecutableOpensOneDialogAndNoConsoleWindow (RealInstallerTests.cs:292-415) starts the installed test pack's configuration app, whose #32770 task dialog (556x426, taskbar button) shows on the interactive desktop in EVERY full run on this machine and asks for the foreground (TaskDialogPage.cs:258-263 never sets TDF_NO_SET_FOREGROUND; the grant succeeds when the launch chain holds foreground rights, which gates started from Claude Code in VS Code do); in the suite since 6602774 (2026-09-15); both of today's full runs passed through it. kb/windows/detection.md:1040-1048 ("zero visible windows and zero foreground events in a full run", 2026-08-17) is contradicted by it. Possible or latent: P1 TaskDialogLayoutTests --report stays windowless only because Main returns early; P2 UserPrompt MessageBox unreachable under --silent; P3 Velopack non-silent start; P4 the password control arm's headless "Save password?" windows (visibility unread); P5 headed real children (none today); P6 the future toast (shell-drawn, invisible to a tree-owned detector); P7 the gate drivers' `Start-Process pwsh -WindowStyle Hidden` may show a Windows Terminal window under WT delegation (unverified, microsoft/terminal#12464); P8 the CreateNoWindow scan reads .cs only and credits the flag at GUI launches where it does nothing; no SendInput/mouse/UIA anywhere in src/tests/build (with a positive control). Directions: A in-host WinEvent hook raised from the session hook (REC), B census, C out-of-process watcher in the drivers, D structural bans, E private desktop.
ACTIONS: the writer (Velopack rows, evidence, toast kb, comment residue) was told to HOLD the two-shell gate until the maintainer decides how that arm is handled; it publishes, commits and pushes, and stops before the gate. The root wrote .work/q254-design/REPORT.md verbatim from the researcher's final message (its own write was refused; the root's first heredoc attempt failed, the Write tool succeeded).
QUESTIONS POSED: Q275, Q276, Q277 (consent list for UI-only measurements, now including the audit's P7 and the toast researcher's six), Q278 (the no-window mechanism, rec A plus D's ban on the real toast API in tests), Q279 (the dialog arm, rec a private desktop), Q272 UPDATED with the audit's alternative (a notifier seam and a ban in ordinary runs; a real suppressed toast only in release runs).

## 2026-09-24T10:27Z -- WRITER (rows, evidence, toast kb, residue scan, changelog) STARTED on `next` = 8149748
- DIRECTIVE FROM THE ROOT SESSION, mid-batch: HOLD THE GATE. No full suite run until root says so -- RealInstallerTests.TheInstalledMainExecutableOpensOneDialogAndNoConsoleWindow puts a focus-requesting task dialog on the interactive desktop in every full run, and how that arm is fixed is the maintainer's decision. Steps 1-5 as briefed, publish both slices and check them for ILC/trim warnings, commit and push `next`, then STOP before the two-shell gate. Filtered runs that exclude RealInstallerTests are fine as iteration.

QUESTION LOG, continued (this writer):
- N29: upstream-review.json's Velopack `date` when the four rows' re-run was recorded. Alternatives: (a) move it to 2026-09-24 (UPSTREAM-REVIEW.md says `date` is today when an entry is written); (b) keep the review's 2026-09-22 and date the re-run inside the notes. TAKEN: (b), and the notes say so in their closing paragraph. Reasoning: `reviewed` did not move; the re-run is the owed half of that same review's re-verification, recorded by addition, and a moved date would read as a second review of 1.2.158. Retroactive cost: one field.
- N30: kb/packaging/velopack.md:6 "versions in force". The line is the baseline every un-stamped entry was measured under, so replacing 1.2.0 with 1.2.158 would re-stamp older entries as measured at 1.2.158. Alternatives: (a) replace outright; (b) keep the line and add a note beneath; (c) name each value with the date it moved and keep the old one beside it, with a previously clause. TAKEN: (c), and it moved MinVer (7.0.0 -> 8.0.0, lock commit 1b90342, 2026-09-15) and the .NET line (10.0.400/10.0.11 -> 10.0.401/10.0.12, 2026-09-14 per global.json's provenance) as well, because the lock files contradict all three and the brief named the line, not only its Velopack value. Cost: one line.
- N31: the kb sections behind rows 123 and 124 got a "Re-established 2026-09-24 @ 1.2.158" paragraph each (the brief named :6, :1252-1262, :1724-1731, :1759-1764 only). Alternatives: leave them, so the kb's own re-establish methods (export the real key) contradict the corrected rows; add the stamps by addition. TAKEN: the stamps; the old methods stay, marked superseded. Cost: delete two paragraphs.
- N32: the 300 s timeout paragraph carries NO new `[FLOATS]` marker. Alternatives: a marker, which moves the anchor sentence, the holes table and the velopack.md row (three published counts) for a fact row 130 already names; no marker, covered by the section's existing marker and by row 130. TAKEN: no marker. Cost: add it and re-count.
- N33: TODO.md's item "Four re-verification rows are owed a re-measurement" was DELETED in step 1 (TODO.md: an item is deleted when done). The [Unreleased] CHANGELOG sentence announcing the item is brought up to date in step 5, on N26's precedent. Cost: restore from git.
- N34: the kb and the review notes cite Q275/Q276/Q277 by number though they live only in this ledger and the chat. Alternatives: omit the numbers; cite them. TAKEN: cite -- this ledger is snapshotted into docs/ledger/ when the session closes, which is where the numbers resolve. Cost: none.
- N35: item B's "a 1.2.158 updater rewrote it" is DERIVED, not observed: the key read REG_QWORD 140209 / 20260917 / 1.0.0 at 06:57:31Z (clearance codex-check-2) and REG_DWORD 140351 / 20260924 / 1.1.0 at 09:01:46Z (item3-baseline); 1.2.158's registry.rs writes a u32 and 1.2.0's apply writes the same key as a u64, so the writer ran 1.2.158 code. Written with its derivation, both readings kept in the velopack-rows evidence batch. The installed root was not read (brief: never touch it). Cost: none.
- N36: the researcher's "pwsh 7.6.6 under DETACHED_PROCESS exits without running its script" is NOT promoted to kb: the kept probe files show only the positive case (a hidden-console start that ran), and re-measuring the negative risks a console window on the maintainer's screen. Recorded in the evidence README as the researcher's report with that limit named. Cost: a kb entry once somebody measures it headless.

## 2026-09-24T10:55Z -- MAINTAINER ANSWERS (verbatim): "Q262 b - but is there something cleaner and more modern than com? / Q263+Q264+Q266 (and maybe also Q262) I am getting the feeling that a central coordination point for this would be nice. Could the config app be that coordination point and toast control? / Q265 b - The goal is to defer this until after a system reboot. And since most reboots fix the in use problem that will probably solve it. Am I correct in asuming that the first server that starts after a reboot will apply the patch? Or will it lock it again? Also, maybe the config app also needs to coordinate and be the frist after reboot app here? / Q267 d - The config app already communicates things like "codex needs manual reconnects for x out of x sessions, are you sure?" / Q268 I do not understand. Where is this question coming from? / Q269 d - But just say busy if there have been calls within the 10 min. timeout. Because there is no semantic difference for the user between call in progress and call fired and finished 1 ms or 4 min. ago. In all instances it is a strong indication some llm is busy with it now. Be it between llm turns or inside a call. The goal is to try and predict if there will be a call incoming during the update process and warn for that. / Q270 Does velopack not manage this for us effectively providing a? And what about on a reboot? How does that work? / Q271 Do we have any information for those servers that have not done anything? Things like claude or codex or the path the claude session is coming from? If not just show connected but not used yet as an indicator maybe? / Q272 Do whatever the test suite needs. As long as it does not take focus or interferes with my other work. / Q273+Q274 Why may generated code not ship? Is that not a weird directive?"

=> DECIDED: Q262 b (COM activator; the Windows App SDK's AppNotificationManager registers the same COM server under the hood for unpackaged apps and adds the Windows App Runtime and generated projections, per Learn today). Q265 b (sign-in time). Q267 d, the confirmation naming how many of the selected sessions are Codex-hosted and need a manual reconnect. Q269 ONE level: "busy" = any call within the 10-minute idle window, in progress or finished, as a predictor of a call arriving during the update. Q271 one row per server; an unused server shows its client and version from the handshake, its project folder (Claude Code declares the roots capability, measured; the inherited working directory is the other candidate, unmeasured) and "connected, not used yet". Q272 = the suite's choice under "no focus, no interference": d (a notifier seam and a ban on the real toast API in ordinary runs; one suppressed real toast read back from history in release runs).
ANSWERED: first server after a reboot applies only if it is alone (this morning: yes, 07:40:36Z); many restored terminals starting in the same second can each see the others and none applies. Velopack's apply checks nothing: Update.exe waits for the asking pid and then force-stops every process under the install root. Q268 is plumbing for the sessions page. The generated-code rule is his own of 2026-08-20 (QUESTIONS.md section 12, DECISIONS.md:694), taken to make the CsWin32 metadata-licence contradiction unreachable; its wording is broader than its reason.
OPEN: Q280 NEW, the configuration app as the coordinator (replaces Q263/Q264/Q266 and carries the "first after reboot" sign-in step); Q268; Q270 (folded into Q280); Q273 (depends on Q274); Q274; Q275-Q279.
- STEPS 1-3 LANDED on `next`, pushed: 6b61fe3 (rows 123/124/126/130 verbatim, velopack.md corrections, review notes re-run + item B, TODO item deleted), 03c36ae (docs/evidence/2026-09-24-velopack-rows, 72 files), ae0540e (toast facts in kb/windows/notifications.md, rows 156-158, docs/evidence/2026-09-24-toast-design, 62 files, the PowerShell correction in two places). Filtered scans green at each; CLAUDE.md fragment count 1211 -> 1215 -> 1220 from the scan's own failure.
- N37: REPORT.md went into the toast evidence batch (the brief listed MEASUREMENTS, results, sources, screenshots). Alternatives: leave it out, because its recommendations are not decisions; keep it as the record of the research. TAKEN: keep, with its README saying the recommendations are one researcher's and the facts are what the kb records. Cost: delete one file.
- N38: the prototype's size. The report says 1,288,192 bytes; the binary on disk (last of four publishes, 09:57Z, the one whose source is kept) is 1,625,600. TAKEN: the kb and row 157 record 1,625,600, the batch README names the other as an earlier publish. Cost: none.
- N39: the PowerShell correction was RE-MEASURED by this writer before it was written: pwsh 7.6.6 answers "Unable to find type" for [Windows.UI.Notifications.ToastNotificationManager, ..., ContentType = WindowsRuntime]; Windows PowerShell 5.1.26100.9549 loads it from Windows.UI. Type load only, nothing raised. Cost: none.
- N40: which toast facts are [FLOATS]: three -- the Velopack app id (row 156), the NativeAOT raw-call path (row 157), TickCount64 under .NET 11 (row 158). The activation, dismissal, scheduling, suppression, get_Setting, marker-sharing and stop-event facts are Windows behaviour and are stamped [STABLE]/[MACHINE]. Alternative: mark the NativeAOT stop-event timing [FLOATS] too (ILC-built). Cost: one marker and one row.
- N41: the .NET 11 fact is READ (Microsoft Learn, looked up today, URL in kb and row 158), not measured -- there is no .NET 11 here. Cost: none.
- N42: the suppressed toast's one foreground change (pid 9576) matches the Velopack D1 installer's pid, but the kept lines do not time the run; written as a pid match with that limit, not as an identification. Cost: none.
- N43: the live-marker and stop-event facts went into kb/windows/notifications.md as the brief said, with a sentence saying why (the sessions page the toast opens reads them). Alternative: kb/windows/processes.md. Cost: move two sections.
- N44: the report's "Shut down under Fast Startup does not reset the boot time" is documentation, not a measurement here (Fast Startup is off on this machine); the kb says it was not measured. Cost: none.
- STEP 4 LANDED: e344274, pushed. The new arm HouseRuleTests.NoCommentCarriesTheResidueOfTheScriptThatWroteIt watched RED against the tree as it stood: 8 offences, both shapes -- the 5 concatenations and 3 %s, one of them src/BrowserAI.App/StatusReport.cs:91, which the brief's list did not name. Green after the restore: HouseRuleTests 21/21.
- N45: what the residues meant. git log -L shows each line was WRITTEN as residue (no earlier text). Alternatives: delete the token so the paragraph opens with <b>; another sign; the warning sign. TAKEN: the warning sign, because every sibling paragraph in the five writing commits (ce9f80d, ec2a695, 09b1e0b, 896eea0, 9c4dc0d) opens with it, and UpdateService.cs:130 replaced a paragraph that had opened with it (339d0a9). Cost: one character per line.
- N46: StatusReport.cs:91 was fixed in the same commit although the brief did not list it: the scan the brief asked for names it, so leaving it would have left the arm red. Cost: none.
- N47: the scan's corpus is code-file COMMENTARY only (the brief's "comment text"), not Markdown prose. Alternatives: add prose with fenced blocks and code spans blanked, which would catch the same residue in a kb article or a CHANGELOG entry written by a patch script. TAKEN: commentary. Cost: widen IsCode to prose files and blank fenced blocks.
- N48: the placeholder set is %s %d %r %i %f and %(name)s, with no flags, width or precision, because a URL encoding such as %2f would otherwise match; followed by neither letter, digit nor percent, which keeps %LocalAppData% and date formats such as %Y%m%d out. Cost: one regex.
- N49: e344274's message says "Four src files changed"; it was five (App/Program.cs, StatusReport.cs, UpdateBudgets.cs, UpdateService.cs, FirefoxProfile.cs). Pushed, so not amended. Cost: none.
- STEP 5 LANDED: c909d5d, pushed. Three [Unreleased] entries (a test entry under Added; two documentation entries under Changed) and the "next version's work" entry's owed-rows sentence brought up to date in place (N33). ChangelogTests and neighbours 71/71 filtered.
- N50: where the entries sit. The eight restored comments ride inside the ✅ scan entry under Added, not as a 🐛 under Fixed: they are doc comments, no behaviour moved and nothing shipped wrong. Alternative: a separate Fixed entry. Cost: split one entry.
- N51: README's status paragraph still reads 831. The new arm makes the suite one larger, and that paragraph is re-measured from a gate run (RecordedCountTests deliberately does not hold it), and the gate is held by the root session. Left for the gate. Cost: one re-measured sentence at the gate.
- N52: no clearance snapshot was taken, because build/Get-ClearanceSnapshot.ps1 reads the registration through `claude mcp get`, which can write ~/.claude.json (the Velopack researcher's note), and with the gate held there is no run for it to bracket. Cost: take it at the gate.
- 29e53fa (a wording fix in the index's newest count note), pushed. Slices published at c909d5d (1.1.1-alpha.0.87, 0 ILC/trim/AOT warning lines each, pattern positive-controlled against a synthetic IL2026 line) and again at 29e53fa so the version matches HEAD (N16).
- N53: the velopack-rows batch README said 72 files and git holds 73 with the README; the toast README now says "with this README" too. 03c36ae's commit message also says 72; pushed, not amended. Cost: none.

## 2026-09-24T11:00Z -- WRITER STOPPED BEFORE THE GATE, per the root session's HOLD. `next` = 81c4c59 pushed; master = 8149748, NOT fast-forwarded (no gate ran)
- Commits on `next` since 8149748: 6b61fe3 step 1, 03c36ae step 2, ae0540e step 3, e344274 step 4, c909d5d step 5, 29e53fa wording fix, 81c4c59 evidence counts.
- Both slices published at 81c4c59, 1.1.1-alpha.0.89 (server 19,318,784 B, app 10,526,720 B), 0 ILC/trim/AOT warning lines each; a filtered run's coverage block reads FRESH, "exe and tree both 1.1.1-alpha.0.89".
- NOT DONE, held: the two-shell gate, its clearance readings, README's status count (831 + the new arm, to be re-measured from the gate), the fast-forward of master.
- Left running: one VBCSCompiler (per-user compiler server, started 10:42:39Z during these builds), which exits on idle. No MSBuild node, no test host, no publish is left; .work/test-scratch is empty.

## 2026-09-24T11:30Z -- WRITER BATCH LANDED ON next = 81c4c59 (6b61fe3 Velopack rows + velopack.md corrections + review notes; 03c36ae evidence 2026-09-24-velopack-rows; ae0540e toast facts in kb/windows/notifications.md, rows 156-158, evidence 2026-09-24-toast-design, PowerShell corrections re-measured; e344274 comment-residue scan HouseRuleTests.NoCommentCarriesTheResidueOfTheScriptThatWroteIt, watched red on 8 residues incl. a ninth at StatusReport.cs:91, restored to the warning sign; c909d5d CHANGELOG; 29e53fa, 81c4c59 small fixes). Both slices published at 1.1.1-alpha.0.89 with no ILC/trim warnings. GATE NOT RUN (held on Q279); master stays 8149748. N29-N53 logged by the writer.
Q281 POSED: N52 says the clearance script runs `claude mcp get browserai`, which may write ~/.claude.json; every gate so far ran it. Rec: replace that reading with a read-only parse of the browserai entry (ConvertFrom-Json -AsHashtable works, measured by the root at 08:20Z), which answers the same question without starting the client.

## 2026-09-24T11:35Z -- MAINTAINER ANSWERS (verbatim): "Q280 b - Some questions however. Research how we can reliably start earlier than vscode starting dozens of servers simultainiously. Also research how velopack handles process kills on updates because I have a hard time believing it only monitors the process calling update. That would mean it kills all instances of a multi instance app!? Or is this no longer an issue if only the gui app can run velopacks update after it's own checks? I do not want to build logic if velopack already ships this. Also, we should still be able to start the gui process even if it is already running invisible. Even if it is single instance? How do the servers communicate with the gui app? Named pipe? How do we prevent multiple gui instances then? Help me design this out to be minimal but effective. Research everything you need to before answering me. / Q268 a - but why does it need to be checksummed? Also, if servers start talking over named pipes for q280. Maybe we need to review the file based communication and leverage this path as it is already existing then? Maybe we need a holistic repo wide review of how the ipc works. Help me figure this out. What would you recommend. Research this in depth. / q274 c be liberal with the license interpretation. I really believe it is ok. / q273 b / q278 your recommendation / q279 a / q275 a / q276 What are you talking about? / q277 a The machine is yours for the next +/- 15 min. so do it now and tell me when you are done." and "For each of the open questions give me some a short primer, context, solutions directions and recommendations."

=> DECIDED: Q280 b (coordinator only when needed + sign-in step), design pending research; Q268 a pending the IPC review; Q274 c (the no-generated-code rule is WITHDRAWN; liberal licence reading, his words); Q273 b ([GeneratedComInterface]/[GeneratedComClass]); Q278 a (in-host WinEvent guard + tests-only ban on raising real toasts + CreateNoWindow scan extended); Q279 a (the dialog arm on a private desktop); Q275 a (rows 123 and 126's installed half as suite arms); Q277 a NOW, screen until 11:46Z (toast researcher: six items; Velopack researcher: hidden pwsh, a real Cancel click, row 124's console window; `.work/ui.lock` and `.work/installer.lock` protocol). Q276 explained again in plain words.
DISPATCHED: a lifecycle researcher (Velopack's apply and force-stop from source + a measured multi-instance kill after 11:46Z under the installer lock; what starts VS Code at sign-in and the earliest mechanisms; single-instance show-window and foreground rights), an IPC reviewer (inventory of every inter-process mechanism; per-server details measured as checksum vs sequence counter vs per-server named pipe vs derived; minimal IPC architecture), and a writer (Q274 c records and the CsWin32 arm; Q278 guard; Q279 private desktop; Q275 arms; gate with the installer lock and a read-only parse instead of `claude mcp get`; merge on green). Open: Q276, Q281.

## 2026-09-24T11:50Z -- SCREEN MEASUREMENTS DONE (both researchers finished before the 11:46Z cutoff; locks released; everything installed is uninstalled; registrations removed; the Notifications\Settings key list identical to before).
TOAST (.work/q254-design/MEASUREMENTS.txt, second-window section): (1) taskbar badge INCONCLUSIVE, this taskbar shows no badge even for an ordinary toast moved to the Notification Centre; (2) a window opened by a real click on "Review" through the COM activator TAKES the foreground (SetForegroundWindow 1, held 2 s), a background-started one is refused; (3) a click in the Notification Centre after the X reaches the activator with the dropdown value (snooze='6h'), 37 ms; (4) the registry display name and icon under AppUserModelId\<id> WIN over the shortcut's, so the product must omit the display name or set it to exactly "BrowserAI" with the product icon; (5) protocol activation from a body click launches with the link and drops the dropdown value; (6) display scale NOT MEASURED (all monitors 100%, changing it would move other agents' windows). Side finding: changing LocalServer32's path did not take effect for the next activation (COM kept the old path).
VELOPACK (.work/velopack-rows/): (1) the gate's `Start-Process pwsh -WindowStyle Hidden` shows NO window here and is never handed to Windows Terminal (audit's P7 resolved: no); (2) a real mouse Cancel on the "already installed" dialog exits 0 and leaves the root untouched; (3) row 124's console half MEASURED at 1.2.158 with a probe pack whose main exe is the server: a non-silent install opened a visible Windows Terminal window (3049x1745) titled with the server path, in the already-running Terminal, which stayed open after the server exited ("You can now close this terminal..."); Setup exited 26.1 ms after the start; gone by 11:39:14Z, closer unknown. replacement-rows.md now carries updated rows 124 and 130 (the 6b61fe3 versions predate this). The root deleted I1-screenshot.png (it showed the maintainer's private windows) before any evidence cut.

## 2026-09-24T11:43Z -- WRITER (Q274=c / Q273=b, Q278 focus guard, Q279 private desktop, Q275 installer arms, gate) STARTED on `next` = 81c4c59; master = 8149748
- Brief from the root: five steps, one commit per step, a red planted before every behaviour change; the installer lock at `.work/installer.lock` around every run that includes RealInstallerTests and around every gate; nothing run may show a window or take focus; clearance readings around each gate half with the browserai entry read from ~/.claude.json by a read-only parse (never `claude mcp get`), logged as the substitute pending Q281.
- 11:43Z coordinator folded in three more items, one commit each, before the gate: (1) rows 124 and 130 replaced with the updated `.work/velopack-rows/replacement-rows.md` text, the I2/I3 files and the r124 Velopack log cut into docs/evidence/2026-09-24-velopack-rows (no screenshot showing anything beyond a BrowserAI or probe window); (2) the second screen window's toast facts into kb/windows/notifications.md with evidence; (3) the extended CreateNoWindow scan accepts `Start-Process -WindowStyle Hidden` in .ps1, citing `.work/velopack-rows/logs/I1-hidden-pwsh.txt`. Screen measurements finished; only my own rules and the installer lock apply from 11:43Z.

QUESTION LOG, continued (this writer):
- STEP 1 LANDED: `245edac`, pushed. Q274 = c and Q273 = b recorded by addition (DECISIONS Generated code row, QUESTIONS section 12, Directory.Packages.props comment). PLANTED RED, WATCHED: `<PackageReference Include="Microsoft.Windows.CsWin32" />` added to src/BrowserAI/BrowserAI.csproj as text -> the new arm exit 2 naming line 176 and all three packages (SDK.Win32Docs, SDK.Win32Metadata, WDK.Win32Metadata); project restored byte-identical (hash compared) before any build. ForbiddenDependencyTests 4/4; doc/house scans 91/91 filtered.
- N54: the T3 arm was INVERTED, not removed. Alternatives: (a) delete it, leaving the new rule's one obligation as prose; (b) invert it to "a src/ reference to CsWin32 needs THIRD-PARTY-NOTICES.txt to name every metadata package the generator reads", the list read from tests/BrowserAI.Tests/packages.lock.json; (c) (b) plus a C#/WinRT clause keyed on a windows10 TFM or a CsWinRT reference. TAKEN: (b). Reasoning: his decision kept exactly one obligation and it is mechanisable for CsWin32 by reference; for C#/WinRT the notice a projection needs is not established here, so the record says that half needs a reader. Retroactive cost: delete one arm (a), or add one clause (c).
- N55: CHANGELOG [Unreleased] "No generated code ever ships, and now something enforces it" (2026-09-23, unreleased) is REWRITTEN IN PLACE to the shipped state, with its history in one sentence, on N26's precedent. Cost: restore the old entry from git.
- N56: STACK.md's stale pointer ("the open licence question are in TODO.md"; that item went 2026-09-23) corrected by addition with a previously clause, outside the brief because the withdrawal made it doubly stale. Cost: none.

## 2026-09-24T12:20Z -- MAINTAINER (verbatim): "Q276 a / Q281 a" => DECIDED: Q276 a, delete the server's installer-launch exit branch (src/BrowserAI/Program.cs:228-232, which cannot fire on an installed process because VelopackApp.Run() clears VELOPACK_FIRSTRUN first, and Setup has started the configuration app since 2026-09-15) and its log event, retiring the id, keeping the general "launcher gone and stdin is a console" exit; Q281 a, the clearance check reads the browserai registration from ~/.claude.json by a read-only parse permanently and never runs `claude mcp get`. Both queued to the writer.

## 2026-09-24T12:25Z -- Q280 LIFECYCLE RESEARCH IN (.work/coordinator-lifecycle/, scratch roots were under Downloads and are removed). MEASURED at Velopack 1.2.158: Update.exe waits only on the asking pid (60 s max, then kills it too); force_stop_package TerminateProcess(1)s EVERY process whose image is under the install root, after every hook and before the swap, excluding only its own pid; no multi-instance notion, no census, no graceful stop (Velopack's own docs say other instances may be terminated); run 1: servers B and C plus every server started up to +2.5 s were killed, servers started from +2.9 s ran from the new current\; run 2: of two concurrent applies the kill decided, not the lock. AutoApplyOnStartup defaults true, is off in both BrowserAI binaries. SIGN-IN this morning: sign-in 07:38:51.685Z; a per-user logon task ran at +1.25 s; Run keys serially from +20.8 s (HKCU Run to +2 min 41 s, ~30 s waits per entry); Startup folder at +4 min 05 s; Code.exe from Startup\Visual Studio Code.lnk at +4 min 34.5 s; VS Code's servers from 07:43:35Z; the 07:40:36Z server came from a Claude Code CLI with cwd system32, launcher unknown. A per-user logon task registers without elevation when its trigger is scoped to the user (any-user trigger refused, 0x80070005); a task-started process has no foreground rights. No mechanism guarantees going first. A server-side apply mutex cannot help (a waiting server runs from current\ and is killed). SINGLE INSTANCE prototype: Local\ mutex + message-only window + AllowSetForegroundWindow + PostMessage, 20/20, end to end ~11 ms; the live foreground lock is 2147483647 here, only explicit grants work. Five open points with recommendations, held until the IPC review returns so Q280 and Q268 are presented as one design. Side findings for the records: kb/windows/detection.md:240-255 and DECISIONS.md:833-835 overstate "non-elevated task registration is denied" (only the any-user trigger); kb/packaging/velopack.md:77-79 says the kill runs on start (only the legacy branch at 1.2.158); every apply logs a spurious "Access is denied" wait line and a timed-out wait logs nothing.
- 12:1xZ coordinator folded in two more decisions, verbatim from the maintainer: "Q276 a / Q281 a". Q276 = a: delete the server's installer-launch exit branch (src/BrowserAI/Program.cs:228-232) and its log event, retire the id, keep the general exit, update InstallerHandoffTests' set arm, rows 124/126 and the kb by addition; the app's own first-run read is untouched. Q281 = a: the clearance reads the browserai registration from ~/.claude.json by a read-only parse, permanently, never `claude mcp get`; update Get-ClearanceSnapshot.ps1, TESTING.md's row, RELEASING and DECISIONS by addition with his words. One commit each, before the gate, a red planted wherever behaviour changes.
- Step 2 in progress: the positive control's first planted red (show events dropped from the watch) went red by the HANG path -- the child's planted arm waited ProcessHang for a sighting and the parent's own ProcessHang wait expired first, 10m00s, "a child test host running one arm that has not exited inside the suite's own hang detector is a hang". The arm's wait is now InProcessHang so a blind watch fails the child with a named reason inside the parent's wait.
- STEP 2 PLANTED REDS WATCHED (not yet committed): (a) pairing scan made strict, run WITHOUT the Desktop entry -> red naming "src\BrowserAI\Interop\JobLauncher.cs: Desktop is assigned and this scan does not know whether CreateProcessW reads it"; (b) the CreateNoWindow scan's GUI rule against the tree -> red naming RealInstallerTests.cs (AppFileName) and TaskDialogLayoutTests.cs (AppExecutable); (c) the positive control with IsOffence planted to "Baseline" -> child exit 0, parent "Expected to be equal to 10 ... but received 0" (the plant also flagged the parent's 97 baseline windows, so it was redone as (d)); (d) show events dropped from the watch -> red by the hang path, 10m00s; (e) THE UNMODIFIED RealInstallerTests.TheInstalledMainExecutableOpensOneDialogAndNoConsoleWindow run in a child test host ON A PRIVATE DESKTOP (so nothing reached the screen), installer lock held 12:19:38Z-12:19:55Z: exit 10, windows row SHOWN, 3 events, "shown: #32770 'BrowserAI' ... current\BrowserAI.exe at 1634,755 556x540, 12:19:50.252Z, a descendant of the test host" plus two "UAC Input Indicator"/"UAC_InputIndicatorOverlayWnd" windows of the same pid; the arm itself passed (total 2, failed 1 = the session hook, succeeded 1).
- ITEM 1 (coordinator) LANDED: `17b079b`, pushed. Rows 124 and 130 replaced with the updated replacement text; the kb sections behind both extended by addition; the [Unreleased] re-check entry gains two sentences in place; evidence batch 73 -> 88 files.
- N57: row 124's replacement DROPPED a sentence committed that morning ("The console-window half was not re-observed, and no pack this repository builds can show it"). Alternatives: (a) replace verbatim as the coordinator's text had it; (b) replace and keep the dropped sentence in a "Corrected 2026-09-24, later the same day (previously ...)" clause. TAKEN: (b), the house stamping rule. Cost: delete one clause.
- N58: the evidence cut includes run I1 (`logs/I1-hidden-pwsh.txt`, `rig/item1-hidden-pwsh.ps1`) although item 1 named only the I2/I3 files and the r124 log, because item 3 asks the scan to cite I1 and a scratch path may not be the only copy a conclusion rests on. PRIVACY CUTS: both I3b screenshots left out (one framed by the maintainer's windows and taskbar, one a 120-byte empty crop), and line 15 of the I3b observation (a second Terminal window of his own, titled with his work) cut into a `.trimmed.` copy; every cut named with the original's SHA-256. Cost: none; the originals stay in .work/velopack-rows until it is wiped.
- N59 (step 2): the private-desktop launch goes through the PRODUCT's `JobLauncher.Start` with a new optional `desktop` parameter the product never passes (the field sat unused at JobLauncher.cs:478). Alternatives: (a) a harness-side CreateProcessW with its own STARTUPINFO, pipes and handle list; (b) the product launcher, one pointer and one free. TAKEN: (b): the suite already drives JobLauncher directly in a dozen places, and (a) is a second launcher with the handle-inheritance defects JobLauncher already closed. Tension named: it is a parameter only tests use. Cost of (a): about 120 lines in the harness and the parameter removed.
- N60: the STARTUPINFO pairing scan now REFUSES a field it does not know, so the table entry for lpDesktop (no flag) has teeth; Cb and AttributeList are named with no bit beside it. Alternative: an entry that documents and changes nothing, since the old scan ignored unknown fields. Cost: one loop.
- N61: the CreateNoWindow scan's GUI rule ("a launch naming AppExecutable/AppFileName in the launch window or the 10 lines above it is an offence whatever the flag") makes the STEP-2 COMMIT RED on RealInstallerTests.cs until step 3 moves that launch, the same site the watch's red is about. Alternatives: move the GUI half of the scan into step 3's commit; carry an exemption. TAKEN: red for one commit, named here. Cost: none after step 3.
- N62: the tests-only toast ban is DORMANT as a banned symbol (neither test project resolves the WinRT projection), so `HouseRuleTests.NoTestRaisesARealToast` reads the tests' code for the runtime class name a raw RoGetActivationFactory route would need, and holds that both test projects read the file. Alternative: the banned-symbols entry alone, which ForbiddenDependencyTests' own remarks call coverage that is none. Cost: one arm.
- N63: coordinator item 3 (accept `Start-Process -WindowStyle Hidden` in .ps1, citing I1) is FOLDED INTO STEP 2's commit, not a commit of its own: it is a clause of the scan step 2 adds. `-NoNewWindow` is accepted as well (the child shares the caller's console and opens none). Cost: none.
- N64: the watch composes ScratchRoot.Path/ProfileScratch WITHOUT the reclaim (two new accessors), because a child test host taking ScratchRoot.Path would sweep the parent's live scratch. The planted arm in the child writes into the parent's scratch, handed over through the probe-report path. Cost: none.
- N65: an UNWATCHED run (hooks refused) fails only a release; an ordinary run proceeds and says why, and `WindowWatchTests.ThisRunIsWatched` asserts the hooks went in. Alternative: fail every mode. Cost: one switch arm.
- N66: the RealInstallerTests red (the brief: "the unmodified tree going red ... watched once before step 3 moves it") was watched in a child test host started ON A PRIVATE DESKTOP by `.work/w/Run-OnPrivateDesktop.ps1`, so nothing reached his screen; the child's own watch is on that desktop and saw the dialog. Alternative: the default desktop, which would have put the dialog in front of him. Cost: none.
- STEP 2 LANDED: `2cbbe6b`, pushed. STEP 3 (Q279) built: RealInstallerTests' dialog arm on a PrivateDesktop via JobLauncher/lpDesktop, EnumDesktopWindows, close from a thread attached to the desktop, desktop disposed; every assertion kept. Green 3/3 from a host on Default under the installer lock (12:29:58Z-12:30:13Z), each run's windows row CLEAN. HouseRuleTests 49/49 with the neighbours.
- N67: on a private desktop the app's pid shows two input-indicator windows (UAC_InputIndicatorOverlayWnd 0x0, UAC Input Indicator 50x50, 41/61 ms after the dialog as the watch read them). The "exactly one visible window, the dialog" assertion now LEAVES OUT THOSE TWO CLASSES BY NAME. Alternatives: (a) keep the assertion literal and risk a race-dependent red; (b) exclude by name, documented; (c) assert the dialog is among the visible windows only. TAKEN: (b). Cost: one filter line.
- N68: the analyzer `DoNotPassDisposablesIntoUnawaitedTasksAnalyzer` CRASHED (AD0001, NullReferenceException) on an inline Task.WhenAll of the child's two CopyToAsync calls inside that arm; the drain moved into a static helper and the crash went away. Not investigated further. Cost: none.
- N69 (Q281 = a): the brief for this writer said to take the gate's clearance with the substitute parse "pending Q281"; Q281 is now decided (a), so the substitute IS the script: build/Get-ClearanceSnapshot.ps1's third reading parses ~/.claude.json's `mcpServers.browserai` read-only (FileShare ReadWrite|Delete, ConvertFrom-Json -AsHashtable, keys sorted, ConvertTo-Json -Compress per value). PLANTED RED, WATCHED: the new arm SuiteCoverageTests.TheClearanceSnapshotReadsTheRegistrationWithoutStartingTheClient against the script as it stood -> two offences, "it starts the client ..." and "it does not read the browserai entry out of ~/.claude.json ...". Green after. DECISIONS named no clearance reading at all (grep), so the decision gets a row of its own by addition in "Licence, release policy and the tool surface", beside the test-framework rows. Trial snapshot q281-trial: the same six readings as item3-baseline (ARP 140351/20260924/1.1.0, lnk F99795CD..., Codex E210B8FE...) with reading 3 now `args = []`, `command = "...\BrowserAI.app\current\BrowserAI.Server.exe"`, `env = {}`, `type = "stdio"`. Cost of choosing differently: none, it was his call.
- ITEM 2 (coordinator) BUILT: the second window's toast facts into kb/windows/notifications.md (by addition under "A click carries the dropdown's value only to a COM activator", the old "Not measured" paragraph kept), the CHANGELOG research entry gains one paragraph in place, the toast batch 62 -> 77 files.
- N70: the activator's own lines for 11:35:59Z (snooze in the Notification Centre) and 11:37:14Z (Review) SURVIVE ONLY IN MEASUREMENTS.txt: the prototype's events.log under proto/publish/ was replaced when the new build was copied over the path LocalServer32 named. The kb says so and cites the transcription. Cost: none; re-measuring needs a screen.
- N71: PRIVACY: the four item1 badge screenshots (crops of the maintainer's notification area) are left out with their SHA-256; item4-identity.png (the probe toast's header only) is kept. `boot/dpi.txt` and its reader are kept because the kb cites the 96-DPI reading. The newer prototype source goes in beside the old as `proto/Program-second-window.cs` (the batch's Program.cs is the 09:57 publish's source), and the newer created-list as `reg/created-second-window.txt` (not a superset of the old). Cost: none.
- N72: no new re-verification rows for these facts: they are Windows behaviour, [STABLE]/[MACHINE], on N40's precedent. Cost: rows if he wants them.

## 2026-09-24T13:05Z -- Q268 IPC REVIEW IN (.work/ipc-review/, MEASUREMENTS.txt). 17 existing mechanisms inventoried; none uses a named pipe, event or shared memory today. Torn in-place reads parse as VALID JSON with wrong values (22 of 3,000,000 at max rate; 2 in ~1,976 writes at 10/s); a CRC caught all (374 detected, 0 accepted). A per-server named pipe answering from memory cannot tear; page refresh over 100 servers 21.9 ms sequential / 5.0 ms parallel; raw CreateNamedPipeW +3,584 B vs .NET +121,856 B, and only raw can set PIPE_REJECT_REMOTE_CLIENTS. The coordinator's own pipe with FIRST_PIPE_INSTANCE is a single-instance guard (20 simultaneous starts, one winner, three rounds); second start learns the pid, grants foreground, sends `show`, 19/19 in 2.8-31 ms. The census stays the liveness signal (85 µs; a pipe to a dead server costs its whole timeout). Working directory of installed servers is a repo in 143 of 162 starts.
SYNTHESIS (root): one per-user logon task (sign-in apply-if-alone + started on demand by a blocked server, so the coordinator lives outside the client's tree, which Claude Code taskkills /T /F at exit); the coordinator hidden, single-instance via its pipe, owns toast/X/snooze/page/graceful stop via server pipes/apply when a path scan under the install root shows only itself; every second start (Start Menu, COM toast click) hands over through the pipe and exits; markers unchanged, no record, no checksum; Velopack does wait/swap/kill/lock/hooks, AutoApplyOnStartup stays off. Where the two researches differed, the root took the lifecycle one on how a server starts the coordinator (a plain child dies in the client's tree kill) and on the apply gate (the path scan is exactly Velopack's kill set), and the IPC one on the show channel (a pipe carries the pid and is machine-wide; a message-only window is per-desktop). QUESTIONS POSED: Q282 sign-in trigger, Q283 how a server starts the coordinator, Q284 the channels (Q268's answer, raw pipes), Q285 the apply gate, Q286 a server starting during an apply.
- STEP 4 (Q275 = a) BUILT: `TwoRootsOfOnePackIdShareOneUninstallKeyAndEitherUninstallDeletesIt` (row 123, both orders) and `AnInstalledServerNeverSeesTheInstallersVariableAndACopyOutsideAnyInstallDoes` (row 126's installed half), silent installs of the test pack only, real ARP key and Start Menu held byte-identical; OrphanedConsoleStart gained an overload taking the executable. Green 2/2 in 17.96 s under the lock (12:43:35Z-12:43:53Z). PLANTED REDS, WATCHED (12:44:24Z-12:44:36Z, under the lock): the second install's InstallLocation expected to still name root 1a -> "received ...TWO-ROOTS-1B-..."; the copy's expectation flipped -> "Expected ... no client to serve ... but received started by the installer". Rows 123 and 126's last columns name the arms by addition.
- N73: "each arm holds the installer lock" is read as: the arms are installer arms and run only inside a run that holds `.work/installer.lock`, which whoever runs the suite takes. Alternatives: (a) each arm creates the lock file itself (then every gate, which must hold the lock for its whole length, would deadlock against its own arms); (b) the arms assert the lock file exists (a suite that fails on a developer machine for a coordination file of this session). TAKEN: neither; nothing in the tree reads the lock. Cost: an arm-level acquire with a gate-aware owner check.
- N74: THE Q276 / Q275 COLLISION. The brief's row-126 arm says the copy outside any install logs Startup[8]; Q276 deletes Startup[8] from the tree. The arm runs the LAST PACKED test pack (1.1.0, which still carries it), so it stays green today and would go red at the first pack cut after Q276. Alternatives: (a) assert Startup[8] as briefed (red after the next release); (b) assert only the installed half; (c) read the copy's binary for the installer exit's sentence and expect that exit iff the build carries it. TAKEN: (c). Cost: one byte search; (a) is one constant.
- Q276 (= a) BUILT: `Main`'s installer-exit branch and `StartupLog.StartedByTheInstaller` (EventId 8) deleted; `// RETIRED-EVENT-IDS: 8` in StartupLog with its reason; the configuration app's own read before Run() untouched (its remark corrected by addition); VelopackStartup's remarks say the server no longer calls it. PLANTED REDS, WATCHED against a server published at 388f35f WITH the branch (12:48Z): `InstallerHandoffTests.ThePublishedBinaryServesTheClientThatStartedItWithTheInstallersVariableSet` (the old installer-exit arm, inverted: a live client with the variable set is served and ends on EOF with exit 0) -> "Expected to be false ... but found True" (it exited instead of serving), and `ThePublishedBinaryExitsWhenItsLauncherIsGoneAndStdinIsAConsole(True)` -> "received started by the installer"; republished without the branch: InstallerHandoffTests 9/9 + ProxyLogTests green. ProxyLogTests' new StartupLog assertion watched red with the marker planted out. Rows 124/126, the velopack.md section, the closed HAZARDS row and the CHANGELOG (an in-place sentence plus a Removed entry) by addition.
- N75: an OWN ERROR, caught by the suite and repaired before any commit: a PowerShell `.Replace("### Removed\n\n", ...)` inserted the Q276 entry into EVERY released section's Removed heading, and a botched repair script then truncated CHANGELOG.md to its first 100 lines on disk. AppendOnlyRecordTests went red naming CHANGELOG.md#1.0.0 as REWRITTEN; `git checkout -- CHANGELOG.md` restored the committed file and the two edits were redone by index inside [Unreleased] only. Every other inserted passage of this batch was then counted at exactly one occurrence. Nothing wrong was committed. Cost: none.
- N76: upstream-review.json's Velopack notes still say Q276 was put to the maintainer; it is a dated record of a review and is left as written (its own house rule: "rewriting one would edit the account").

## 2026-09-24T13:10Z -- WRITER BATCH CLOSED: GATE GREEN BOTH SHELLS FIRST ATTEMPT, MASTER = acaaf86 (fast-forwarded from 8149748, 16 commits, pushed; next level)
- Commits of this writer: `245edac` step 1 (Q274 c / Q273 b); `17b079b` coordinator item 1 (rows 124/130 + evidence); `2cbbe6b` step 2 (Q278 window watch); `9d013d9` step 3 (Q279 private desktop); `2758d0d` Q281 a; `e2a2d76` coordinator item 2 (toast facts); `388f35f` step 4 (Q275 a); `38d5f97` Q276 a; `acaaf86` README 841. Coordinator item 3 folded into 2cbbe6b (N63).
- GATE at 38d5f97, installer lock held 12:55:39Z-13:03:37Z: PowerShell `w5-ps-1` 841/0/0, TUnit 3m 14.4s, driver 12:55:40Z-12:59:13Z, drive letter UPPER (took), FULL RUN, FRESH "exe and tree both 1.1.1-alpha.0.97", windows CLEAN (4192 events, 5 visible-root events of other processes, 97 baseline), commit charge HEALTHY 57.8%->58.9%, first-run bytes CDN 207.3 MB; Git Bash `w5-bash-1` 841/0/0, TUnit 3m 05.8s, driver 12:59:35Z-13:03:06Z, drive letter LOWER (took), FULL RUN, FRESH, windows CLEAN (3382 events, 0 visible-root, 97 baseline), commit charge HEALTHY 58.2%->58.3%, first-run bytes CACHED. Clearance (the Q281 parse, never `claude mcp get`): each half's before/after identical; w5-baseline vs w5-final identical in all six readings.
- Both slices published at 38d5f97 before the gate (0 ILC/trim/AOT warning lines, pattern positive-controlled, ILC ran in each) and republished at acaaf86 after it (0 warning lines) so the next gate does not meet N16.
- Left running: nothing of this writer's that I could see; a `testhost.exe` pid 85644 started 13:04:42Z is OutlookAI's test host (C:\Source\SixFive7\OutlookAI\...), not BrowserAI's, left alone. .work/test-scratch empty. installer.lock absent.

## 2026-09-24T13:25Z -- MAINTAINER (verbatim): "Q282 a / Q283 a / Q284 a / Q285 a / q286 Can we, during a gracefull shutdown of a server instance, tell the llm to wait 1 min. before using the server again? This would allow the update process to finish? Or is that not part of the mcp shutdown protocol? Or can we refuse a badly timed start with a refusal message explain to retry as well?"
=> DECIDED: Q282 a (a per-user logon task, always registered by the install/update hooks, removed by uninstall); Q283 a (a blocked server starts that task on demand, so the coordinator lives outside the client's process tree); Q284 a (raw named pipes: one per server for describe and stop, the coordinator's own FIRST_PIPE_INSTANCE pipe for single instance and handover; markers unchanged, no record, no checksum); Q285 a (the apply waits until a path scan under the install root shows only the coordinator, exactly Velopack's kill set).
Q286 REFRAMED for him: MCP has no shutdown message that reaches the model (Claude Code drops log notifications; Codex only logs them; measured); a tool result is the only channel. Proposal b: a server being stopped answers any call still in flight with "BrowserAI is installing an update; nothing was run; retry in about a minute" before it exits; a server that starts while this install's Update.exe is running (path-verified) refuses every call with the same text without starting a browser, until the updater's kill ends it, and Claude Code relaunches it on the next call from the new version; the first call on a relaunched connection already meets the Q261 refusal, whose text can name the new version. Remaining gap: a server killed before its ~0.34 s handshake finishes is lost for that session.
PLAN once the current writer is merged: the coordinator work in four gated phases by fresh writers: (1) server pipes (describe, stop) and the Q286 behaviour; (2) the coordinator core (single instance and handover, the logon task and on-demand start, UpdatePendingRestart, the path-scan apply gate); (3) the toast (GeneratedComInterface interop, XML, COM activator and handover, snooze, the X default, sign-in time, not-for-this-version); (4) the sessions page (rows per server, busy, purposes, close with the Codex count, force close) replacing "Install now".

## 2026-09-24T13:35Z -- TESTBED BATCH LANDED, master = acaaf86 (245edac Q274 c + Q273 b, CsWin32 arm inverted to a notices requirement; 17b079b rows 124/130; 2cbbe6b Q278 focus guard: in-host WinEvent watch, `windows` coverage row, private-desktop harness, window-show probe mode, strict STARTUPINFO pairing, Windows-subsystem launch rule, tests-only toast scan; 9d013d9 Q279 dialog arm on its own desktop; 2758d0d Q281 clearance parses ~/.claude.json; e2a2d76 toast facts; 388f35f Q275 two installer arms; 38d5f97 Q276 branch deleted, event id 8 retired; acaaf86 README 841). Gate at 38d5f97: 841/0/0 PowerShell (UPPER, windows CLEAN, 4192 events) and Git Bash (LOWER, windows CLEAN, 3382 events). N54-N76 logged by the writer. Known: 2cbbe6b red until 9d013d9 (N61); the positive control's red at the shorter bound not re-watched; the watch cannot attribute a Windows Terminal window or a toast.
PHASE 1 OF THE COORDINATOR WORK DISPATCHED: the per-server named pipe (describe, stop) with its tests, the client in Core, both research batches persisted (ipc-review, coordinator-lifecycle) with kb facts and the record corrections, DECISIONS rows for Q282-Q285; then gate and merge. Q286 still open (reframed).

## 2026-09-24T13:45Z -- MAINTAINER (verbatim): "Q286 b" => DECIDED: a server being stopped for an update answers every call still in flight with a refusal ("BrowserAI is installing an update; nothing was run; retry in about a minute") before it exits; a server that starts while this install's own Update.exe is running (full image path under its install root, verified) refuses every call with the same text, starts no browser child and skips its feed check, until the updater's kill ends it; the Q261 refusal on a relaunched connection already names the running version. Remaining accepted gap: a server killed before its ~0.34 s handshake finishes is lost for that session. Folded into phase 1.

## 2026-09-24T14:00Z -- MAINTAINER (verbatim): "For all the choices the writer made. Highlight the once you think I might want to review given the new reality and plan. And given the impact. For each of the these give me some a short primer, context, solutions directions and recommendations."
=> Seven highlighted as Q287-Q293 (the rest are record-keeping or cost a line to change): Q287 = N17+N74 the packed test installer is still 1.1.0, so no real install exercises this tree's hooks until a release (rec: rebuild a local test pack before each gate, never published or tagged); Q288 = N20 Codex project entries carry the absolute profile path (rec: measure Codex's variable expansion, use the portable spelling if it works); Q289 = N19 project unregister appears only when a registration is found at or above the working directory, which a Start Menu launch never provides (root's reading, not verified) (rec: list the projects from the app's own record with an unregister link each, and show a rendering of the whole window before phase 4); Q290 = N65 an ordinary run passes when the window guard fails to install (rec: fail every run); Q291 = N73 the installer lock is a convention (rec: the suite's session hook takes it, the drivers declare they hold it); Q292 = N18 clearance hashes the whole Codex config file, which the Codex desktop app rewrites on start (rec: compare only the browserai entry); Q293 = N9+N59 two test-only hooks in product code (rec: keep both, named as exceptions). Reminder, not a choice: N62's toast ban becomes real in phase 3.

## 2026-09-24T14:15Z -- MAINTAINER (verbatim): "Q287 a / Q288 a / Q289 b / Q290 a / Q291 a / Q292 a - Same for claude code / Q293 a"
=> DECIDED: Q287 a (the gate rebuilds a local test installer from the current tree before each run, never published or tagged); Q288 a (measure Codex's variable expansion in a server command, then use the portable spelling if it expands; a researcher dispatched now); Q289 b (a folder picker for project unregister, safe because only our own entry is ever removed); Q290 a (every run fails when the window guard did not install); Q291 a (the suite's session hook takes .work/installer.lock itself; the gate drivers declare they hold it); Q292 a, "same for claude code" (both clearance readings compare only the BrowserAI entry, the Claude Code one in ~/.claude.json and the Codex one in config.toml, never the whole file); Q293 a (keep the forwarded PWTEST_SERVER_REGISTRY and the launcher's desktop parameter, named as deliberate test-only exceptions in DECISIONS). All queued as phase 1b for the writer after phase 1 merges.

## 2026-09-24T14:40Z -- Q288 MEASURED (.work/codex-expansion/, summary.txt/json; stub logs carry machine paths, trim before any evidence cut): Codex 0.155.0-alpha.9.2 expands NO spelling in an MCP server's command: ${LOCALAPPDATA}, $LOCALAPPDATA, %LOCALAPPDATA%, ~ all 0 starts in 48 attempts (user layer and trusted project layer, both separators, three rounds), os error 3; absolute-path controls 45/45; args and env reach the server literally; `codex mcp add` stores the text literally (78/78). Source at rust-v0.155.0-alpha.9.2 (4607249e) confirms no expansion; main (282cd7b0) identical on the path; openai/codex#2680 (env expansion) open since 2025-08-25. So BrowserAI's absolute Codex project entry stays correct; remarks at RegistrationClient.cs:113 and App/Program.cs:459 get the measured reason.
Q294 POSED: what a committed Codex project file should contain, since no portable spelling exists: a) absolute path, per machine (current); b) bare BrowserAI.Server.exe plus the install adding current\ to the user PATH (the only direction without a measured defect; PATH change and uninstall cleanup, a Codex restart probably needed); c) a cmd.exe wrapper (fails with a space in the profile path); d) an env PATH trick (undocumented crate behaviour); e) no Codex project entries. Researcher and root recommend a for now.
PHASE 1b ADDITIONS (defects, no question): CodexRegistryView reuses McpRegistryView.Classify, which expands ${VAR} as Claude Code does, so a Codex entry spelled the Claude way would read as BrowserAI's and present while Codex cannot start it (read from code, plant the red); CodexRegistration.ManualProjectCommandFor is never called and its `set CODEX_HOME=... &&` line only works in cmd (in PowerShell it would write the user's real config): delete or fix; kb/mcp/protocol.md:410-413 says tmp\arg0 is not reliably produced, measured 18/18 today; each Codex mcpServerStatus/list starts an extra short-lived copy of every configured server, a full BrowserAI.Server.exe start with its feed check, worth a kb note and a hazard row.

## 2026-09-24T14:25Z -- PHASE 1 WRITER (pipe, persistence, DECISIONS, CHANGELOG, gate) STARTED on `next` = acaaf86; Q286 b folded in by the coordinator at ~13:50Z
- STEP 1 LANDED: `db7210d`, pushed. The per-server pipe (Core/Coordination: ServerPipe, ServerPipeProtocol, ServerDescription, ServerPipeClient; Core/Interop/NamedPipes; server half Proxy/ServerActivity, Proxy/ServerPipeResponder, BrowserProxy.HeldSessions, SessionManager.Held, LiveSession.BrowserIsOpen; Program opens it after the live join), ServerPipeTests 9 arms, the ipc-review evidence batch (167 files), kb/windows/processes.md section, ARCHITECTURE rows, two HAZARDS rows (open), InteropLayoutTests rows for NamedPipes.SecurityAttributes and TokenUser, Interop CLAUDE.md re-counted (78 declarations).
- PLANTED REDS, WATCHED (in-process, rounds A-D): DACL with an Everyone ACE -> "Expected to be 1 but found 2"; reject flag removed -> "Expected 8 but received 0"; FIRST_PIPE_INSTANCE removed -> squatter arm "no exception was thrown" (the second-instance arm stayed green: the instance limit alone refuses it, 231, measured); FIRST removed + max 2 -> second-instance arm "no exception was thrown"; short body accepted -> Why "not a Describe answer" instead of "closed after 246 of the 492 bytes"; bound/10 -> "Expected >= 00:00:00.5 but received 00:00:00.0552616"; IsMarkerHeld planted false -> "Expected true but found False"; census skipped -> "Expected NotRunning but received NoPipe"; pid check skipped -> "Expected NotItsServer but received Answered"; a missing name waited on -> "Expected NoPipe but received NoAnswer". PUBLISHED (one publish, both plants): working directory reported as the publish dir -> WorkingDirectory red; RequestStop as Environment.Exit(0) -> exit 0 but "Directory.Exists(instance) Expected false but found True". All restored byte-identical (sha256 -c), republished, 8/8 green; scans 121/123 with only staleness reds, then 8/8 after republish.
- OWN SLIP, caught and repaired before any commit: the dying-server helper waited for a connection the client never made when the census plant made it answer NotRunning; the filtered run hung 11 min until I stopped MY test host (pid 53040, path and start time verified). The helper now cancels its accept once the client has answered.
- N77: the ipc-review evidence cut and all ipc-review kb facts ride STEP 1's commit, not step 2's, because the kb entries step 1 writes cite the batch. Alternatives: (a) no kb entries in step 1; (b) cite a batch a commit before it exists (a red commit, N61's precedent). Step 2 carries the coordinator-lifecycle batch, its facts and the corrections. Cost: none, an order.
- N78: the client's I/O is a FileStream over an overlapped raw handle (CancelIoEx on the deadline, broken pipe read as end of stream). Alternatives: (a) NamedPipeClientStream, ~120 KB into the app and it retries a missing name until its timeout (the review's 493.7 ms); (b) hand-written OVERLAPPED with events (a raw event handle in native storage, the DangerousAddRef rule, ~80 lines of unsafe). Cost: swap one class.
- N79: a missing pipe name answers NoPipe AT ONCE and is never waited on. The brief's "a pipe connect to a dead server costs the whole timeout" was the framework client's behaviour. Consequence: a server between its join and its pipe (a few statements) answers NoPipe; the suite's rig retries on NoPipe. Alternative: wait on FILE_NOT_FOUND until the bound, which costs 500 ms per 1.1.0 server and per configuration-app marker on every page refresh. Cost: one branch.
- N80: "creation time" = the process creation FILETIME (identity); "start time" = when the server began answering its client (Serving or Updating); "last tool call" = the later of a call's arrival and its answer, and every tools/call counts, refused ones included (Q269's words). Alternatives: start = creation (redundant); last call = arrival only (a 15-minute call that ended a minute ago reads as 16 minutes ago). Cost: one field each.
- N81: "browser open" = more than the node child in the session child's job, the predicate the teardown already reads; a job that cannot be read answers false. Alternative: ask the child (a tool call, which is activity). Cost: none.
- N82: the server's pipe I/O is synchronous on one thread that owns the handle; Dispose wakes it by connecting once. A client that connects and never closes holds the thread, which is HAZARDS' new open row. Alternative: overlapped server I/O with a stop event and a per-connection read bound. Cost: rewrite ServerPipe.Serve.
- N83: describe carries `protocol: 1` and `state` (starting/serving/updating/stopping) beyond the brief's list. Alternative: leave the page to infer state. Cost: two members.
- N84: the client takes an optional bound; the product never passes one (CallBound), the suite's happy-path arms pass a hang detector because a starved test host can delay an async completion past 500 ms. Cost: one parameter.
- N85: the Interop CLAUDE.md counts were already stale before this change (38 against 37 in the server's directory, 13 against 8 in the app's); re-counted and the whole difference named. Cost: none.
- N86: the second-instance arm gained a squatter arm (0x80070005), because FILE_FLAG_FIRST_PIPE_INSTANCE is not what refuses a second instance of our own one-instance pipe (231 comes from the instance limit, with and without the flag, measured); the flag's job is the squatter: without it our creation JOINED the squatter's pipe. Cost: one arm.
- N87: the "server dying mid-answer" is the suite's own server, the framework's pipe class, closing its handle after half the answer (what the kernel does to a dying process's handles), not a separate process that dies. Alternative: a TestProbe mode that serves half and FailFasts. Cost: a probe mode.
- N88: HAZARDS gained two OPEN rows: a caller that never closes holds the one pipe thread; a server with no pipe (<= 1.1.0, or a squatted name) is counted and cannot be stopped through one. The tally sentence moved 56 -> 58 open, 158 closed. Cost: two rows.
- N89: my own reading of "what crosses a process boundary" lists 19 rows under its own predicate; the review's 17 is recorded as its count, its list not being on disk. The 143 of 162 was re-read read-only from the process log: 365 installed-server starts 2026-09-16T04:00Z..2026-09-24T13:29Z, 332 with a .git in the working directory, 334 inside a repository, 6 in System32, 2 gone; counts only, the directories are the maintainer's projects. Cost: none.
- Q286 b (coordinator's add-on, "Q286 b") LANDED: `0261c8f`, pushed. BrowserProxy refuses calls in flight on stop (claim at the door out, one answer per call), Program detects <install root>\Update.exe (BrowserProcesses.FirstRunning, WatchedProcess), UpdateInProgressServer, SessionErrors.UpdateIsBeingInstalled, UpdateInProgressTests (3 published arms), ErrorCatalogueTests arm (census 34 -> 35), DECISIONS row, HAZARDS row (open, 59/158), ARCHITECTURE row, Interop counts 79.
- PLANTED REDS, WATCHED: (a) door out lets a second answer through -> "Expected to be 1 but found 2"; (b) updater never looked for -> refusal arm "Expected true but found False" (served); (c) stop refuses nothing -> "The peer closed its stdout before answering 'tools/call' (id 5)"; (d) every server reads as updating -> control "Expected to not be equal to True ... but received True" with the update sentence; (f) cut-off call told nothing ran -> catalogue Match mismatch; (e) no updater watch -> "Expected to be true but found False" after 10m00s (the hang detector). Restored byte-identical, republished (0 warnings), 152 green in the filtered set with only the step-2 staging's three reds, which went away when that staging was set aside.
- N90: the in-flight sentence says the call "was already being carried out, so part of it may have happened: check what it was doing before you repeat it" and NOT the suggested "nothing was run", which is false of a call already forwarded. The refused-at-the-door sentence keeps "NOT run: nothing reached a browser". Cost: one clause.
- N91: an UPDATING SERVER ENDS ITS OWN CONVERSATION when the updater it found exits. Measured reason: kill run 1 started late08..late11 at +2.9..+4.15 s, after the kill pass and the swap, while Update.exe lived to +4.25 s; they ran the new version and nothing would have ended them, so under the text as given each would refuse every call for its whole session. Alternatives: (a) refuse for life, as given; (b) start the child and serve once the updater is gone (a deferred startup, a Program restructuring). Cost of (b): the restructuring; of (a): delete one watch.
- N92: tools/list on an updating server is a JSON-RPC error carrying the same sentence. Alternatives: an authored-only list (the client caches a partial list for its whole session), an empty list (silent), holding it until exit (the same outcome with no reason). Consequence recorded in HAZARDS and DECISIONS: a session starting in the few seconds of an apply has no BrowserAI tools until it reconnects. Cost: one branch.
- N93: an updating server also skips the stray sweep: a sweep that ends a stray starts a registry reap, which is a node process from the payload under the install root. Not in the brief. Cost: one condition.
- N94: the stop's refusal text assumes the stop is FOR AN UPDATE. The phase-4 sessions page's close will need its own reason (an argument on `stop`, or a second verb); the dispatch is one switch in ServerPipe.Dispatch and the reply shape already has `refused`. Cost: none now.
- N95: "the census records it" is read as the error catalogue's census (EveryRowInTheCatalogueWasTriggeredBySomethingAbove); the row is provoked in-process by an arm that also holds the one-answer property, count 34 -> 35. Cost: none.
- N96: the reframe's "~0.34 s handshake" had no source in the tree; DECISIONS and HAZARDS carry this writer's own read-only measurement instead: an installed server's first log record to Startup[2] "serving stdio", 0.39 s median, 0.30 s p10, 2.17 s p90, 414 starts (script .work/phase1/startup_to_serving.py). Cost: none.
- N97: the in-flight refusals are bounded by ServerPipeProtocol.CallBound, the bound a caller gives the stop; a write that cannot finish is abandoned and the stop goes ahead. Alternative: unbounded (a stop that never comes when the client is not reading its stdout). Cost: none.
- N98: the Update.exe stand-in is a COPY OF %SystemRoot%\System32\cmd.exe run /d /q /k, reading a stdin pipe nothing writes to, in a kill-on-close job the arm owns and ends by closing the job's handle; a renamed copy was measured to run headless and wait before it was used. Alternatives: node.exe (93 MB copy); the TestProbe (needs its directory of assemblies beside it). Cost: none.

## 2026-09-24T15:10Z -- MAINTAINER (verbatim): "All my personal SixFive7 repos have commits under jori@huisman.io. Why does this repo have commits under jori@voipfabric.com?"
=> FOUND: 22 of 587 commits carry jori@voipfabric.com as author AND committer, all made 2026-09-23 19:56-20:48 local (79f74ee..4b1b530), the wording sweep, all on master and origin. The configured identity is jori@huisman.io (~/.gitconfig, no local override, no includeIf, no GIT_* variables). The sweep's writers ran `git -c user.name="Jori Huisman" -c user.email="jori@voipfabric.com" commit ...` explicitly (found in this session's agent transcripts, working in the BrowserAI-wt-A worktree and the shard D rig); the likely source is the harness context line that gives every agent "The user's email address is jori@voipfabric.com ... such as for authorship". StatusAI has 2 such commits (2026-08-14, its first two). The phase 1 writer was told to never pass an identity to git. Q295 POSED: a) .mailmap plus prevention (rec); b) rewrite and force-push (every later hash changes, records citing hashes break); c) leave it. Prevention: a user-level PreToolUse hook refusing identity overrides in git commands, and a suite check that every commit's identity maps to jori@huisman.io.

## 2026-09-24T15:35Z -- MAINTAINER (verbatim): "Q294 b / Q295 I prefer b. But I want to know if it breaks the releases or not and if the hash records can be fixed."
=> DECIDED Q294 b: Codex project entries write the bare program name; the install hook adds BrowserAI's program folder (current\) to the user PATH and uninstall removes it; the Codex ownership check learns the bare name; Codex likely needs a restart to see the PATH (unmeasured). Queued for phase 1b.
Q295 FACTS: tags v0.1.0 (98cb78b), v1.0.0 (6e55df8), v1.1.0 (d3aabf1, 2026-09-23 13:29 +0200) all predate 79f74ee, so no tag, release, installer or feed changes; MinVer heights unchanged. A rewrite from 79f74ee changes 116 commits (to today's master, phase 1 included). No branch protection on master. git-filter-repo not installed; Python 3.12 with pip is. Citations of rewritten hashes: 10 in commit messages inside the range (filter-repo rewrites these, filter-branch does not); 72 in 4 tracked files: docs/ledger/2026-09-23-development-session.md 64 (not sealed by any test; ledgers are outside the append-only seal by design, but snapshots are by convention never edited, and the header carries a sha256), kb/windows/processes.md 4, docs/evidence/2026-09-23-assumed-justifications/README.md 3, docs/evidence/2026-09-24-ipc-review/README.md 1 (committed db7210d by the phase 1 writer). The running .work/STATE.md also cites many and is fixed before its snapshot. Must run with no writer active: after phase 1 merges, before phase 1b. StatusAI: its two work-email commits are its first two, so a rewrite changes every hash there; separate decision.

## 2026-09-24T15:45Z -- MAINTAINER (verbatim): "Q295 a - make sure to make a safety backup before messing with the history. leave statusai out of scope"
=> DECIDED Q295 a, StatusAI out of scope. SEQUENCE: when phase 1 has merged and no writer is committing, one agent: (1) SAFETY BACKUP first, outside the working tree and outside .work and Downloads (both get wiped): a `git bundle create --all` plus a `git clone --mirror`, both verified, under C:\Source\SixFive7\_backups\BrowserAI\2026-09-24-pre-email-rewrite\, and the pre-rewrite tips of master and next recorded; (2) `pip install --user git-filter-repo`; (3) `git filter-repo --mailmap` mapping jori@voipfabric.com to Jori Huisman <jori@huisman.io> for author and committer, which also rewrites hash citations inside commit messages; (4) verify: no commit carries the work email, every tag's hash unchanged, and the new master's TREE identical to the old master's; (5) one follow-up commit replacing every old abbreviation in the tracked files with its new one from filter-repo's commit map, yesterday's ledger snapshot included with its header fingerprint updated (his go-ahead is this answer), and the same replacement in .work/STATE.md; (6) the two-shell gate; (7) re-add origin and force-push master and next with an explicit lease on the old tips; (8) copy the commit map into the backup folder. The root holds all writes to .work/STATE.md while it runs.

## 2026-09-24T15:25Z -- PHASE 1 WRITER: STEP 2 LANDED, `f557b58` on `next`, pushed
- STEP 2: docs/evidence/2026-09-24-coordinator-lifecycle (102 files, about 1.7 MB): both Velopack 1.2.158 runs, the prototype, the three task-started probes, and three reads of mine under writer/ (sign-in order, logon-trigger registration, the Q286 handshake median with the cut-off that reproduces DECISIONS' 414). 12 logs trimmed by the predicate "a .lnk line naming neither BrowserAI (suite) nor *.lnk", originals' SHA-256 in the README; vp/ (37 files) cited by tag 1.2.158 = commit 3c7f52c1bf17d10ad21b794b006d5ebd1a879a3b, the nine files the kb cites compared byte for byte with that commit through gh api (all nine identical). kb: velopack.md new section (source table, run 1, run 2, the spurious wait line, the silent timeout, AutoApply still true at VelopackApp.cs:35) + corrections by addition at the provisioning paragraph and landmine 4; processes.md new section (sign-in order, the first server of the morning, logon trigger shapes, foreground, the mutex prototype); detection.md correction by addition in "The logon sweep task" + `[STALE]` on section 2 (kb/README count 2 -> 3); re-verification rows 159 (kill set) and 160 (the wait), 272 -> 274 markers, 158 -> 160 rows, 167 -> 169 lines, holes row velopack 23/14 -> 25/16. DECISIONS and LiveInstances.cs corrected by addition. CLAUDE.md fragments 1227 -> 1250 from the scan's own failure. Scans 89/89 (DocumentationLink, RecordedCount, ReVerificationIndex, HouseRule, NeverByImageName, HazardIndex, AppendOnlyRecord, ProcessLiveness, Changelog). No behaviour change, so no planted red: the only src/ edit is a doc comment.
- N99: the researcher's "a per-user logon task ran at +1.25 s" does NOT survive the re-reading: the process created at +1.249 s (parent: the svchost hosting Schedule) belongs to a third-party task whose logon trigger is for ANY user (principal the INTERACTIVE group, run level Highest); the only task with a user-scoped trigger last ran at 07:39:08Z (whole seconds) after a 15 s delay, so its trigger fired between +1.3 and +2.3 s. Recorded as re-read, the researcher's phrase quoted in the evidence README as the one figure not reproduced. Alternative: repeat it as given. Cost: none.
- N100: the sign-in order rests on event logs that roll over, so it is persisted as writer/sign-in.txt, generated by a read-only script, with the maintainer's programs counted and never named (only Code.exe, explorer.exe, taskhostw.exe, claude-code and BrowserAI appear). Also found: the Run enumerations number 5, not "HKCU to +2 min 41 s" (the event never names the hive; the long one is 10 commands, 3 of them held for 30.0-30.1 s); nothing logs the Startup folder by itself, so "Code.exe from Startup\Visual Studio Code.lnk" is recorded as consistent, not established (a pinned-icon click has the same parent). Alternative: cite the logs alone. Cost: none.
- N101: the logon-trigger split was RE-MEASURED by me, because the researcher's run left no file: two throwaway tasks registered non-elevated in the task root (user-scoped: registered in 56 ms; any user: refused, Access is denied, native 0x80070005, in 23 ms), both unregistered in the same pass and verified absent; neither trigger could fire. A reversible write to Task Scheduler the brief did not name. Alternative: cite the ledger. Cost: none now.
- N102: the 2026-08-16 refusal is explained, not just contradicted: the deleted LogonSweepTask.cs (at 874f9ff) wrote a <LogonTrigger> with no <UserId>, i.e. an any-user trigger, which is exactly the refused shape. Recorded in detection.md and DECISIONS by addition. The rule itself is marked [MACHINE]: Microsoft Learn documents what an empty UserId means and not what elevation it needs.
- N103: the Global\ DACL: stamped [STALE] and not re-measured, per the brief ("mark for re-measure"). The IPC review's EVENT (user, BA, SY; no logon-session SID) and the 2026-08-20 MUTEX (SY, logon-session SID, user) differ in two entries; I argue sections 3 and 5 are not contradicted (a standard second user holds no entry in either) and that a BA entry matters only to section 6's elevated peer. Alternative: read a Global\ mutex now (one read-only probe). Cost: one probe.
- N104: the Q286 handshake script moved out of .work into the batch (writer/startup-to-serving.py) with a cut-off argument: 2026-09-24T15:00:00Z reproduces 414 starts, 0.392 / 0.302 / 2.174 s; with no cut-off it read 415 at 15:10Z, percentiles identical. DECISIONS and HAZARDS keep 414. Cost: none.
- N105: sandbox/client/.claude.json is KEPT although it carries Claude Code's pseudonymous machineID/userID hashes, because it is the record that the installer hook's registration went to the sandbox and was gone after the uninstall. Alternative: cut it and name the cut. Cost: one file.
- N106: kept out of the records for want of a source on disk: the researcher's "Velopack's own docs say other instances may be terminated", and "by design" as a description of the kill. Cost: none.
- N107: the rigs' three .ps1 files stay .ps1.txt, and probe-noarg-[$(Arg0)].log is renamed probe-noarg.log, because the link scan reads .ps1 for relative paths and a PowerShell cast followed by a parenthesis reads to it as a Markdown link; the README names all four renames. Cost: none.
- STEP 3 LANDED: `0538824`, pushed. Five DECISIONS rows in the update lane, before Q286 b's: Q280 b's design as one row (his whole Q280 b message verbatim), then Q282 a, Q283 a, Q284 a (with his Q268 question verbatim) and Q285 a, each saying what is built (the servers' pipe, phase 1) and what is phase 2. Scans green; CLAUDE.md fragments 1250 -> 1258.
- STEP 4 LANDED: `0890d3e`, pushed. CHANGELOG [Unreleased]: two Added entries (the per-server pipe; the update refusal, naming which of its two moments is live now) and two Changed entries (what a Velopack apply ends, with the three corrections; the coordinator design as a decision of record). Headlines 85-88 characters. Scans green; fragments 1258 -> 1261.
- N108: my step-1 commit message and ledger say ServerPipeTests has "nine arms"; it has EIGHT test methods (the second-instance and squatter halves share one method, and the free, pipeless and foreign-pid cases share another). The CHANGELOG says eight. The commit message is left as written. Cost: none.
- N109: the "directions not taken" in the five DECISIONS rows come from what the two researches measured and the root's synthesis records, NOT from the lettered options put to the maintainer, which are not on disk for me; no row claims a letter it cannot quote. Alternative: ask for the option texts. Cost: one addition per row if the maintainer's options named something else.
- N110: the Q280 b row calls the coordinator "not resident" (from "coordinator only when needed"), and the Q282 a row says the hooks register the task "scoped to the installing user"; both are the ledger's DECIDED lines restated, not new decisions. Cost: none.
- STEP 5, GATE GREEN BOTH SHELLS FIRST ATTEMPT, MASTER = `c97188a` (fast-forwarded from acaaf86, 6 commits: db7210d, 0261c8f, f557b58, 0538824, 0890d3e, c97188a), pushed; next = master.
- Published both slices at 0890d3e before the gate: 0 lines matching (warning|error) IL####, 0 "will always throw", 0 warning lines of any kind, ILC ran in each (patterns positive-controlled on synthetic lines). Server 19,398,144 B, app 10,526,720 B.
- GATE at 0890d3e, installer lock taken exclusively 15:30:10Z, released 15:38:55Z. PowerShell `p1-ps-1`: 853/0/0, TUnit 3m 37.3s, driver 15:30:25Z-15:34:20Z, drive letter UPPER (took), FULL RUN, FRESH "exe and tree both 1.1.1-alpha.0.103", windows CLEAN (5007 events, 43 about visible root windows and none of them this run's, 133 in the baseline), commit charge TIGHT 78.4% -> 63.9%, every layer exercised, no degraded path. Git Bash `p1-bash-1`: 853/0/0, TUnit 3m 13.9s, driver 15:34:54Z-15:38:37Z, drive letter LOWER (took), FULL RUN, FRESH, windows CLEAN (3384 events, 8 visible-root, none this run's, 135 baseline), commit charge TIGHT 71.8% -> 81.3%. Clearance: each half's before/after identical; p1-baseline vs p1-final identical, 22 lines (the Q281 parse, never `claude mcp get`).
- README 841 -> 853 (`c97188a`), the +12 named test by test with the planted red each was watched against; doc scans 69/69 on it. Both slices REPUBLISHED at c97188a (0 warnings, ILC ran), both report 1.1.1-alpha.0.104, so the next gate does not meet N16.
- Left running: nothing started from the repository (checked by image path); .work/installer.lock absent; .work/test-scratch emptied after the last run.

## 2026-09-24T16:05Z -- PHASE 1 LANDED, master = c97188a (db7210d per-server pipe describe/stop + ServerPipeClient + IPC evidence; 0261c8f Q286 b update refusal; f557b58 coordinator-lifecycle evidence + kb + corrections + rows 159/160; 0538824 DECISIONS rows Q280 b, Q282-Q285; 0890d3e CHANGELOG; c97188a README 853). Gate 853/0/0 both shells, windows CLEAN. N77-N110 logged by the writer. To highlight for him: N82 (a caller that connects and never closes holds a server's pipe), N92 (an updating server answers tools/list with an error; what Claude Code and Codex do with that at a first connection is unmeasured and might leave a session without BrowserAI tools), N105 (evidence keeps a sandbox client config with anonymous ID hashes in a public repo), N101 (two throwaway scheduled tasks registered and removed), N94 (the stop refusal text assumes an update; phase 4 needs its own).
HISTORY REWRITE (Q295 a) DISPATCHED NOW, no writer active. The root makes no writes to .work/STATE.md until it reports.

## 2026-09-24T17:20Z -- MAINTAINER (verbatim): "Q300 a / Q301 b". Q295 a COMPLETE: history rewritten with git-filter-repo 2.47.0 (--mailmap), 97 commits re-hashed (the 22 plus 75 descendants), every tag hash and every tree unchanged, 10 message citations rewritten by the tool; b5367df replaced 70 prose citations (ledger 61 incl. its header's snapshot commit, kb/windows/processes.md 4, two evidence READMEs 3+1, src/BrowserAI/Interop/CLAUDE.md 1), left the 31 captured evidence files byte-for-byte with a mapping paragraph in each of the two batch READMEs (Q300 a), added the ledger header's new digest b7a1222b... with the previous c20d63c5... and one sentence in docs/ledger/README.md naming Q295 a as the second exception (Q301 b); .work/STATE.md's 161 citations replaced. Backup: C:\Source\SixFive7\_backups\BrowserAI\2026-09-24-pre-email-rewrite\ (bundle verified, --no-hardlinks mirror, raw .git copy, commit map, refs before/after, push log). Gate at b5367df: PowerShell run 1 852/1 (ServerPipeTests.AStopThroughThePipeEndsAPublishedServerTheGracefulWay, a headless browser still alive at an unwaited check), Git Bash 853/0, PowerShell re-run 853/0, windows CLEAN in all. ROOT'S CALL within Q295 a: pushed master = c97188a (the rewritten gated 1a6a7da, same tree) and next = b5367df, both leased on 1a6a7da; master moves to next after phase 1b's clean gate. GitHub keeps the force-pushed-over commits reachable by hash until its GC.
PHASE 1b DISPATCHED: Q287 a, Q294 b, Q289 b, Q290 a, Q291 a, Q292 a (both clients), Q293 a, and the defects (Codex classifier expanding ${VAR}; ManualProjectCommandFor; tmp\arg0 kb; extra server per Codex status query; the two remarks; the ServerPipeTests race; evidence README's "only one batch departs"; the Q288 evidence), then gate and master fast-forward. Q296-Q299 still open.

## 2026-09-24T17:10Z -- PHASE 1b WRITER STARTED on `next` = b5367df (master = c97188a)
- (ii) LANDED: `65ecc8d`, pushed. CodexRegistration.ManualProjectCommandFor deleted (never called; `set CODEX_HOME=... &&` works only in cmd, writes the real ~\.codex from PowerShell); the remark on ManualCommandFor says what was there. Dead code, no red.

QUESTION LOG, continued (phase 1b writer):
- Q293 a LANDED: `a906f0e`, pushed. DECISIONS row "The two things product code carries only for the suite" in the licence/tool-surface table, by addition: the forwarded PWTEST_SERVER_REGISTRY and JobLauncher.Start's desktop, each with its reason and the declined alternative; "a third is a new decision, not a precedent"; nothing enforces the count and the row says so. Doc scans 72/72.
- (vii) LANDED: `79323b6`, pushed. docs/evidence/README.md corrected by addition: not one batch but six depart from their captured bytes. Five READMEs say so themselves (feed-hosting-research, coordinator-lifecycle, ipc-review, toast-design, velopack-rows prepended SPDX headers; lifecycle renamed three rigs and a log; lifecycle and velopack-rows use .trimmed. for privacy cuts). Found by reading every batch README for "SPDX"; a README silent about headers was not checked against originals (gone).
- Q290 a LANDED: `f8f2e85`, pushed. WindowWatch.Decide refuses an unwatched run in every mode; row and refusal texts changed. PLANTED RED, WATCHED: WindowWatchTests.TheRowAndTheRefusalSayWhatTheWatchSaw with the expectations moved first -> "Expected to be equal to Refuse but received Proceed". Green 77/77 with the doc scans.
- N111: WindowWatch.Decide KEEPS its isReleaseRun parameter though it no longer changes the answer. Alternatives: (a) drop it and change the two callers and six test cells; (b) keep it, documented as not changing the answer. TAKEN: (b), smaller diff and the table is still asked in six cells. Cost: one parameter.
- N112: (vii) was widened from the one batch the brief named to all five whose READMEs declare a departure, because a correction naming only ipc-review would leave the sentence false four times over. Alternative: name ipc-review only, as briefed. Cost: none.
- Q288 EVIDENCE LANDED: `82744df`, pushed. docs/evidence/2026-09-24-codex-expansion (437 files): PATH cut from 323 files (712 values keep only codex-arg0 alias entries), the real ~\.codex listing cut from real-codex-before; originals.sha256; probe-stub.exe and the which 8.0.0 / env_home 0.1.0 crates left out with digests; 10 rig/stub files gained SPDX. kb/mcp/protocol.md section "Codex expands nothing in a server's command, and finds a bare name on the server's PATH": 0/48 vs 45/45 controls, args literal 24/24, mcp add literal 78/78, bare name 3/3 WITH THE ENTRY'S OWN env.PATH; source at rust-v0.155.0-alpha.9.2 (4607249e): stdio_server_launcher.rs:263-285, program_resolver.rs:41-65, utils.rs:16-57; main 282cd7b0 re-read by me (resolver and env builder byte-identical ignoring CR); openai/codex#2680 OPEN since 2025-08-25 (gh, read today). Row 161; counts 275/161/170; fragments 1265.
- (i) LANDED: `7fc478e`, pushed. CodexRegistryView.Classify: not fully qualified -> Foreign, else McpRegistryView.ClassifyPath (split out of Classify). PLANTED RED, WATCHED: the new arm with Classify still delegating to the expanding reader -> "Expected to be equal to Foreign but received OursAndPresent" (Claude control passed first). Green 147/147.
- N113: THE BARE-NAME MEASUREMENT IS NOT THE ROUTE Q294 b USES. `barepath` (3/3) put the stub's directory in the ENTRY's own env.PATH; with no env, Codex hands the server its OWN inherited PATH (utils.rs reads each allowlisted variable from Codex's environment) and `which_in` searches that. So "a bare name resolves through the user's PATH" is READ FROM SOURCE, not measured, and so is "a Codex started before the install cannot find it until restarted". Written that way in the kb and the evidence README. Alternative: repeat "3 of 3" unqualified. Cost: a measurement (a scratch app-server started with a PATH holding a stub dir, no env in the entry) would close it.
- N114: a Codex command that is not fully qualified is FOREIGN, not OursAndStale. Alternatives: (a) expand for the ownership half and call it stale (the window would offer to repair an entry BrowserAI never wrote); (b) Foreign, per the charter "never adopts ... an entry it did not write" -- BrowserAI never writes a variable into a Codex entry. TAKEN: (b). Cost: one branch.
- N115: PRIVACY CUTS in the codex-expansion batch: every PATH value (the installed-software inventory) and the ~\.codex directory listing; PROFILE PATHS under C:\Users\jori KEPT, as in 20 other committed batches. Alternative: rewrite C:\Users\jori everywhere (every file, the `${LOCALAPPDATA}` resolution evidence becomes indirect). Cost: rerun cut_codex.py with one more substitution.
- N116: the evidence batch and its kb fact went in BEFORE (i), and (i) then added its citations (row 161's automated column, the README's cited-by), so no commit cites a test or a batch that does not exist yet (N77's precedent). Cost: none.
- N117: the Codex source was re-cloned (sparse, blob-less) into .work/p1b/codex-src at the tag and main's three files fetched through `gh api`, because the researcher's clones had been removed and the brief asked for "the files the researcher named", which are not on disk. The three files cited are my reading of the launch path, which may differ from the researcher's list. Cost: none.
- (iii) LANDED: `5770651`, pushed. kb/mcp/protocol.md corrected by addition: tmp\arg0 produced in 18 of 18 project setups (the rig's `files.projectDotCodexListing`), and it is the PATH-alias dir Codex puts FIRST on every server's PATH unless the entry sets one; the earlier empty reading kept, unexplained. Doc scans 51/51.
- (iv) LANDED: `f850628`, pushed. kb section "Every status list starts one more copy of each Codex server" (3 launches per started server in 31/31 runs; list pids unlike the other list's 84/84 and unlike the tool-call pid 86/86; copies gone by the next census 31/31); HAZARDS row OPEN (60 open / 158 closed); re-verification row 162 (276 markers / 162 rows / 171 lines). What a BrowserAI copy starts is READ from Program.Main (census, pipe, sweep, Playwright child, feed check once serving), not measured under Codex. Doc and hazard scans 81/81.
- N118: the (iv) hazard row names a possible narrowing (start the feed check at the first tool call, not at serving) and says it is a decision about the update lane and NOT built. Alternative: build it now (a behaviour change nobody asked for). Cost: none now.
- Q292 a LANDED: `70bbe29`, pushed. Get-ClearanceSnapshot.ps1 reading 6 is now the [mcp_servers.browserai] entry of ~/.codex/config.toml, line for line (tables, sub-tables, a browserai key under [mcp_servers], a top-level dotted key; multi-line values followed by bracket count); reading 3 (Claude Code) already the entry. PLANTED RED, WATCHED: SuiteCoverageTests.TheClearanceComparesOnlyEachClientsBrowserAiEntry (drives the script with USERPROFILE/APPDATA moved for the child only; rewrites around the entry -> equal; the entry -> different; removed -> ABSENT twice) against the old script -> "Expected to be 2 but found 1". Green 89/89. Real-machine trial: Claude entry read, "browserai ABSENT from config.toml".
- N119: the Q292 arm DRIVES the script (a pwsh child with USERPROFILE and APPDATA moved in its own environment block only), and compares only the two registration readings. Alternatives: (a) a text scan of the script for Get-FileHash on the Codex path (holds a spelling, not the property); (b) compare whole snapshots (the real ARP key moving under a run would redden it). TAKEN: the driven arm over the two readings. Cost: none.
- N120: the TOML reader in the clearance script follows brackets for multi-line values and does not model TOML multi-line strings (''' or """). Alternative: a TOML parser module in the gate (a dependency the gate does not have). Cost: an entry written with a multi-line string would be compared by its first line only.
- Q291 a LANDED: `559efd1`, pushed. From here the suite takes .work/installer.lock itself (session hook), so filtered runs need no hand-taken lock. InstallerLock harness (CreateNew + "holder=... pid=N created=FILETIME at=..."; alive = pid + creation time, pid alone for the rigs' legacy "pid N" lines; bounded wait TestDefaults.InstallerLockWait = 30 min; dead holder -> stale takeover; unreadable file stale only after 10 s grace; released in the After hook before the refusal; declares BROWSERAI_INSTALLER_LOCK_HELD for child hosts). Drivers take it through build/InstallerLock.ps1 before their first clearance snapshot (bash: /proc/$$/winpid; trap EXIT releases). PLANTED REDS, WATCHED: ThisRunHoldsTheInstallerLock -> "Expected to be true, because Unread: not asked"; EveryGateDriverHoldsTheInstallerLockForItsWholeRun -> 3 offences x 4 drivers. Green 102/102; a declared stand-in holder covered a run (row HELD), lock untouched by the run.
- N121: after the wait a run that cannot take the lock SKIPS the installer arms (capability absent, a failure in a release run). Alternatives: (a) throw from the Before hook and fail the whole run; (b) run the arms anyway. TAKEN: the capability route, which reuses the suite's own loud-skip / release-fail mechanism. Cost: one switch in SuiteEnvironment.
- N122: the wait is 30 minutes (TestDefaults.InstallerLockWait = BrowserHang), and InstallerLock.ps1's -WaitSeconds defaults to 1800 to match; nothing holds the two equal. Alternative: a scan tying them. Cost: one number in two places.
- N123: ONE LOCK FOR EVERY CHECKOUT: a linked worktree resolves the main checkout's .work through its .git file's gitdir/commondir, because the Add/Remove key, Start Menu title and user PATH it guards are the machine's. Alternative: each worktree's own .work (two locks over one machine). Cost: one function in each half.
- N124: the gate drivers' lock logic is ONE script, build/InstallerLock.ps1, shared by the four drivers; the C# half re-implements the same protocol and InstallerLockTests drives the script so the two cannot drift silently. This is not the shared wrapper CLAUDE.md forbids (that is about the test invocation and its drive-letter spelling, which stay per driver). Cost: none.
- (vi) LANDED: `f5a2cf6`, pushed. ServerPipeTests stop arm: WaitUntilGone per browser, bounded by TestDefaults.ProcessHang. WATCHED RED: the old single IsAlive read planted right after the stop answered (before the server's exit) -> "Expected to be false but found True" (line 489); the waited read at that same planted position -> green; plant removed, the post-exit read made the waited one. 30/30 with HouseRule. Both slices republished first at 1.1.1-alpha.0.115: 0 IL warnings, 0 "will always throw", ILC full pass each (Test-IlcFullPass).
- 19:58Z RESUMED after a session limit (coordinator: next = f5a2cf6, New-Release.ps1 and ReleaseLayout.cs uncommitted). The second publish (19:53 local) had completed: both slices 1.1.1-alpha.0.116, 0 IL diagnostics, 0 "will always throw", ILC full pass each. ReleaseLayout.cs had been TRUNCATED by my own over-wide replacement (FullPackage, the uninstall key, HasCurrentInstaller gone); rebuilt from HEAD plus my block before any build. Nothing wrong was committed.
- Q287 a LANDED: `eb8ddba`, pushed. New-Release.ps1 -TestPackOnly (reads src\*\bin\Release\...\publish, refuses a baked version that is not the tree's, packs BrowserAI.app.test into Releases\test-pack and a shipping-id twin into test-pack\twin with its Setup and Portable deleted; the shipping feed neither read nor written); drivers run it under the lock before the first run; capability requires the pack byte-identical to the published slices; RequireReleaseInstaller adds EnsureFresh; N17 lifted (record schema 2, both clients, each asked-for, each naming the install's server); N74 lifted (general exit typed); twin comparison. PLANTED REDS, WATCHED: a 1.1.0 test pack packed for the purpose (.work/p1b/old-testpack.ps1, byte sizes identical to the old pack: nupkg 55,022,766, installer 62,585,518) in a scratch BROWSERAI_RELEASE_FEED with the capability's byte check planted out -> 4 red: "Expected to be 2 but found 1" / "received started by the installer" / "no shipping-id package ..." / "...BrowserAI.Server.exe is not ...publish\BrowserAI.Server.exe"; driven mode arm vs the old script -> "A parameter cannot be found that matches parameter name 'TestPackOnly'"; driver scan -> 4 x "never packs". Green 136 with doc scans; clearance after the red run clean. Timing: -TestPackOnly 40.2 s end to end, vpk 15.8 s (test) and 18.0 s (twin), one run, in kb/packaging/velopack.md.
- N125: THE TWIN. The arm "the suite's pack and the shipping pack differ only where the id appears" needs two packs of ONE publish; a tree-built test pack beside the last release's shipping pack is two publishes. Alternatives: (a) drop or weaken the arm; (b) compare only when versions match (silently stops covering); (c) pack a shipping-id twin from the same directory in -TestPackOnly, installer and archive deleted, package and manifests kept under Releases\test-pack\twin. TAKEN: (c). Cost: about 18 s per gate half and a shipping-id .nupkg on disk under test-pack\twin.
- N126: THE TEST PACK IS PACKED FROM THE DEV PUBLISHES (src\<project>\bin\...\publish), not from a publish of its own. Alternatives: (a) -TestPackOnly publishes itself (a third ILC run, different bytes from what the slice arms test, +1-2 min); (b) reuse the dev publishes the slice arms test. TAKEN: (b). CONSEQUENCE AT A RELEASE CUT: the release gate replaces the test pack item 7 made from the release publish, so the real-installer arms install the dev publish, not the release publish's bytes (same tree; ILC gives different bytes). Written into RELEASING item 8 and DECISIONS. Alternative for the maintainer: have the capability compare against artifacts\publish-release at a cut. Cost: one path.
- N127: a stale or mismatched test pack makes the release installer capability ABSENT (loud skip, release-run failure) and a stale publish makes the arms FAIL (EnsureFresh), the same split the slice arms already have. Alternative: fail on both. Cost: one switch.
- N128: the release gate packs ONCE before its three runs, not before each; "before each run" read as "before the runs of a gate half", since the three runs test one publish. Cost: moving one block into the loop.
- Q294 b LANDED: `6cb9acd` + `518d584`, pushed. A Codex project entry names `BrowserAI.Server.exe` alone; the install and update hooks append the install's own `current\` folder to HKCU\Environment\Path (kind kept, created REG_EXPAND_SZ when absent) and broadcast WM_SETTINGCHANGE "Environment" (SMTO_ABORTIFHUNG, 1,000 ms); the uninstall hook removes exactly that entry; each root only its own. CodexRegistryView.Classify: a bare name is resolved over HKLM then HKCU Path from the registry and the found file judged by ClassifyPath. The window's sentence after a Codex project registration names what the bare name finds today. Clearance reading 7: `--- HKCU\Environment Path, read raw ---` kind, chars, SHA-256 of the UTF-16 text, entries naming BrowserAI. (v) folded in. PLANTED REDS, WATCHED (.work/p1b/q294_plant.py: no PATH step, String kind, naive append, no bare-name branch, absolute project command): all 5 UserPathTests arms red ("Expected to be equal to OursAndPresent but received Foreign" / "...found C:\Tools" / "...expected ExpandString but found String" / "Expected ... BrowserAI.Server.exe but received C:\Users\someone\..." / "Expected to be equal to Added but received" null); RealInstallerTests InstallingTwice and TwoRoots red "Expected to be 1 but found 0" against a test pack of the planted publish (packs 9.4 s / 9.3 s); SuiteCoverageTests.TheClearanceReadsTheUserPathByteForByte red "Expected to be greater than -1" against the old script. Plants restored, both slices republished at 1.1.1-alpha.0.117 (0 warnings), test pack re-packed under the lock (10 s / 9.2 s). GREEN: 213/215 then the two doc reds fixed (changelog headline 123 > 100 chars; fragment count), 100/100 scans; clearance before/after the green run identical in all 26 lines, PATH `kind=ExpandString chars=1465 sha256=7A3C7896...`, no BrowserAI entries. MEASURED for the kb (kb/windows/processes.md, new section): CreateEnvironmentBlock's Path = the machine's 31 non-empty entries then the user's 20 of 22 the machine value does not hold, 3 of 3 (a first read taken while a real-installer arm had its entry on the PATH is not counted); the broadcast 261-436 ms, 5 of 5, 669 top-level windows. HAZARDS row OPEN (61 open / 158 closed): a Codex started before an install cannot find the bare name until restarted, READ not measured. Fragments 1277.
- N129: THE SUITE AND THE REAL PATH. In-process arms take the PATH through an IUserPathStore seam, a required parameter of HookRegistration.Run's test overload (ScratchUserPath in memory; the registry round trip on a scratch HKCU key `Software\BrowserAI.Tests\UserPath-<guid>`, broadcast off). The real-installer arms write the REAL HKCU\Environment\Path through the installed hooks, each adding and removing only its own scratch root's entry, held byte-identical (text and kind) by the arms and by clearance reading 7; ReclaimAsync takes a scratch root's own entry off exactly. Alternatives: (a) an environment variable redirecting the installed hooks to a scratch key (a third product-side test hook, which Q293's row calls a new decision); (b) the real PATH in-process too (every run writes the person's PATH, and a crash mid-arm leaves an entry); (c) the split. TAKEN: (c). Retroactive cost of (a): one variable, one branch, a DECISIONS row.
- N130: an unresolvable bare `BrowserAI.Server.exe` in a Codex entry is OursAndStale (the window offers repair and removal); any other bare name that finds nothing is Foreign; a bare name that finds a file is judged by that file. Alternative: Foreign (after an uninstall the window could never remove BrowserAI's own committed entry). Cost: one branch.
- N131: resolution reads the machine and user Path from the REGISTRY, not this process's PATH, because a window Setup starts, or any program started before the hook, holds the old one; the machine-then-user order is measured (N-entry above). Cost: none.
- N132: the PATH outcome goes to HookOutcome.PathEntry and the installer log (RegistrationHookLog.PathChanged, event 2, "User PATH: {Change}. {Detail}"), NOT into the registration record. Alternative: a record field (schema 2 -> 3). Cost: a schema bump.
- N133: append after a separator even when the value already ends in one, so removal restores the value byte for byte; a value created by the add is deleted when the entry was its only content; an empty-text value stays. Alternative: append without a doubled separator (removal then changes bytes the person wrote). Cost: none.
- N134: the PATH step runs after the record for every intent and never fails a hook (a Failed report is logged). Alternative: fail the install when the PATH cannot be written. Cost: one branch.
- N135: the broadcast is bounded at 1,000 ms per window with SMTO_ABORTIFHUNG; measured 261-436 ms; the documented ceiling is the timeout once per slow window, under Velopack's 15 s update-hook timeout. Alternatives: a longer timeout, or no broadcast (the shell would not see the entry until sign-out). Cost: one constant.
- N136: a hazard row and a kb section were added though the brief asked only for README's sentence, so the sentence rests on a record; README and CHANGELOG say both Codex claims (a bare name resolves through the user PATH; a running Codex needs a restart) are read in source, not measured end to end (N113). Cost: none; a scratch app-server run with a stub folder on its PATH and no env in the entry would measure both.
- N137: (v) folded into the Q294 commit: its two remarks sat on PortableCommandFor, which Q294 replaced with ProjectCommandFor; the remarks now say Codex expands nothing (0/48) and that Q294 b is the answer. Alternative: a separate commit on code about to be replaced. Cost: none.
- Q289 b LANDED: `417409a`, pushed. "Remove from a project for <client>..." per client: the dialog's picker (owned by the window, opened only on that click) -> McpRegistrar.ApplyToProject(register: false, the folder picked exactly), which removes only an entry this install wrote (another install's refused and reported; none told so). The found-folder link stays. ConfigurationSession is handed imagePath, pickFolder and read; Attach() builds the host Show() opens; Note is an internal auto property. PLANTED REDS, WATCHED (the verb left out of OnCommand): RemovingFromAProjectYouPickAsksOnlyOnItsClickAndRemovesOnlyOurEntry "Expected to be 1 but found 0"; RemovingFromAPickedCodexProjectMovesTheHomeAndLeavesTheUsersEntry "Expected to be false but found True"; and HouseRuleTests.EveryFolderPickerIsOwnedByTheDialogThatOpenedIt with its first needle alone "Expected to be greater than 0 but received 0" (the call sites moved behind the delegate). Green 46/46, then 100/100 with the doc scans. README, CHANGELOG, DECISIONS row "Removing BrowserAI from a project you pick", ARCHITECTURE row; remarks superseded by addition in Program.cs, ClientState.cs, ConfigurationAppTests.cs.
- N138: THE FOLDER PICKED, EXACTLY: no upward walk, the way Register in a project writes into the folder picked. Alternative: walk upward like NearestProject (a subfolder pick would reach the repository's file, and could also reach a parent repository's the person did not mean). Cost: one call.
- N139: the found-folder link STAYS beside the picker link, so a window started inside a registered project offers two removal links per client. Alternative: replace it with the picker. Cost: one block.
- N140: the picker link is offered whenever the client is found and this install composed a server (MayRegisterInProject's condition), whether or not any project registration exists. Alternative: offer it only when one is known (the app keeps no list of projects). Cost: one predicate.
- N141: the session's seams are constructor parameters (imagePath, pickFolder, read), Attach() is internal and Note an internal auto property (IDE0032 refused a field-backed one). The owner half dispatches TDN_DIALOG_CREATED with a made-up handle 4321 only before a click whose picker cancels, so no message is ever sent to that handle. Alternative: a test-only subclass. Cost: none.
- N142: the picker scan reads a second spelling, "pickFolder(", because the product now passes ShellInterop.PickFolder by name and no direct call site is left in src; the scan's own not-vacuous check is what caught it. Cost: none.
- N143: FOR HIM: the window grows by one link per client (up to twelve links with both clients found inside a registered project). No rendering was looked at, because nothing may show a window; Q289's recommendation (a) had included "show a rendering of the whole window before phase 4", which b did not carry. Cost: a private-desktop screenshot rig, which the suite has (Q279's desktop).
- 22:55-23:07 local GATE 1 at `417409a` (both slices 1.1.1-alpha.0.120, ILC full pass, 0 diagnostics): PowerShell p1b-ps-1 870 total / 869 / 1 failed / 0 skipped, 3m 54.8s, UPPER, FULL RUN, FRESH, windows CLEAN (7234 events), lock HELD, clearance clean, driver 22:55:17-22:59:56; the red was ServerPipeTests.APublishedServerDescribesItselfWithWhatTheHarnessGaveIt "Expected to be 0 but found 1" at after.CallsInFlight. Git Bash p1b-bash-1 870 / 868 / 2 failed / 0 skipped, 5m 37.1s, LOWER, windows CLEAN, clearance clean, driver 23:00:13-23:06:48, commit charge TIGHT (72.4% -> 75.0%); reds: SuiteCoverageTests.TheCoverageBlockStatesWhatWasExercised "Expected to contain HEALTHY" (the row said TIGHT) and PasswordPromptTests.TheFirefoxRememberSignonsPreferenceReachesTheChild, Firefox 1549's `initializeServer` timing out at Playwright's 180 s (the stuck-launch shape kb/playwright/provisioning-and-timings.md records at 2 in ~20 launches, and the six-run-gate HAZARDS row's; that row's re-opener requires healthy memory and this run's was TIGHT).
- FIX `3f93ad6`: CommitCharge.CoverageRow keeps the reading it printed ([field: ThreadStatic] PrintedOnThisThread) and the arm classifies that one. PLANTED RED, WATCHED: the row planted to read 76/100 with the machine at 72.3% -> "Expected to contain HEALTHY"; green with the fix and the plant in; green clean (67/67 with the scans).
- FIX `883e347`: ServerPipeRig.DescribeWhenSettledAsync (describe until no call in flight, bounded by TestDefaults.ProcessHang) used by the describe arm. PLANTED RED, WATCHED: a server planted to hold each call's count 2 s after answering, republished, the unchanged arm -> "Expected to be 0 but found 2"; green there with the waited read; product restored and republished, 78/78 with the pipe, update, catalogue and scan arms.
- N144: THE FIREFOX RED WAS NOT FIXED, because nothing in this tree changed the path and the shape is recorded upstream behaviour: a Firefox that starts and never finishes the juggler handshake. Alternatives: (a) retry the launch in the test (relaxes an assertion, forbidden); (b) a product-side relaunch on a stuck initializeServer (a behaviour change nobody asked for); (c) re-run the gate and report it. TAKEN: (c). The six-run-gate hazard row was NOT amended, because its re-opener names healthy memory and the block said TIGHT. Cost: a row amendment if he wants every recurrence recorded.
- N145: both race fixes are in the suite, not the product: the proxy counting a call until its handler returns, and the coverage row taking its own reading, are both correct behaviour; what was wrong was an arm asserting a stronger ordering than either promises. Cost: none.
- 23:16 local GATE 2 started at `883e347`, both slices republished at 1.1.1-alpha.0.122 (ILC full pass, 0 diagnostics).
- 23:16-23:21 local GATE 2 at `883e347` (slices 0.122): PowerShell p1b-ps-2 870 / 869 / 1 failed / 0 skipped, 3m 56.2s, UPPER, windows CLEAN (3791 events), commit charge HEALTHY 73.7%, clearance clean, driver 23:16:26-23:21:09; the red was InstallerHandoffTests.ThePublishedBinaryTreatsALauncherThatExitedButStillOpensAsNobodyToServe "Expected to be equal to has already exited but received Watching the MCP client". The Git Bash half was not run on that commit.
- FIX `5f7493a`: the orphan-console rig's launcher is BrowserAI.TestProbe.exe launch-suspended (CreateProcessW CREATE_SUSPENDED | CREATE_NO_WINDOW, reports pid and main thread, exits); the rig resumes the thread only after the launcher is gone, keeping its handle for Openable and disposing it first for Freed. The product (ClientLiveness: WaitForSingleObject(parent, 0) == WAIT_TIMEOUT -> watch) was right for what it saw. PLANTED REDS, WATCHED: a 2 s linger after cmd's start (`& ping -n 3 127.0.0.1 >nul`) -> the corpse arm "received Watching the MCP client"; the same after the Freed shape's inner start (`^&`) -> both ThePublishedBinaryExitsWhenItsLauncherIsGoneAndStdinIsAConsole arms "received Watching the MCP client". The same linger planted in the probe: all 9 handoff arms green; clean: 86/86 with RealInstallerTests and the scans; clearance identical to the baseline. `fcdb1c1`: the 2026-09-15 racing-rig HAZARDS row amended by addition (stays closed).
- N146: BOTH SHAPES MOVED TO THE SUSPENDED LAUNCHER, though only the Openable arm was seen red: the planted linger showed the Freed arms carry the same race. Alternative: fix Openable only. Cost: none now; the Freed shape's "nothing holds the pid" is now "the rig let go before the product ran", which the arms do not tell apart (both routes end in the general exit).
- N147: the probe gives the server a console of its own (CREATE_NO_WINDOW) instead of sharing the launcher's, which cmd's start /b did. Both are windowless and both are consoles to GetConsoleMode, which is what the product reads. Cost: one flag.
- 23:33-23:39 local GATE 3 PowerShell half at `fcdb1c1` (slices 0.124, ILC full pass, 0 diagnostics): p1b-ps-3 870 / 869 / 1 failed / 0 skipped, 5m 29.1s, UPPER, windows CLEAN (5570 events), commit charge TIGHT 74.3% -> 75.0%, clearance clean, driver 23:33:06-23:39:20. The red: FirefoxSessionTests.AFirefoxSessionRunsFromInitThroughAnArtifactToDestroy, navigate isError after 3m 01.8s -- the stuck Firefox launch again (second in four runs, both in TIGHT runs).
- EXPERIMENT (Q294 b's broadcast as a suspect for the stuck Firefox launches): .work/p1b/broadcaster.ps1 sent WM_SETTINGCHANGE "Environment" every 500 ms (293 in 300 s) while the four Firefox-launching classes ran 8 times (14 arms each, 12-20 s a run): 112 of 112 arms green, 0 stuck launches. The broadcast is NOT implicated by this; one experiment, not a proof of absence.
- N148: THE FIREFOX STUCK LAUNCH IS NOT FIXED AND THE GATE IS RE-RUN, with every red reported. Alternatives: (a) a product-side relaunch when Firefox's initializeServer exceeds a bound (a behaviour change for him to decide); (b) bounding suite parallelism during Firefox arms (costs the race detection the unbounded suite exists for, per the six-run-gate row); (c) re-run. TAKEN: (c). Cost: gate wall clock.
- 23:45-23:55 local GATE 3 completed at `fcdb1c1` (slices 0.124): Git Bash p1b-bash-3 870 / 870 / 0 failed / 0 skipped, 3m 22.3s, LOWER, FULL RUN, FRESH, every capability PRESENT, windows CLEAN (4878 events), commit charge HEALTHY 74.8% -> 74.3%, lock HELD, clearance clean, driver 23:45:36-23:49:52; PowerShell re-run p1b-ps-4 870 / 870 / 0 / 0, 3m 44.0s, UPPER, FULL RUN, FRESH, windows CLEAN (5109 events), HEALTHY 74.4% -> 74.3%, lock HELD, clearance clean, driver 23:50:02-23:54:36. Test packs in the six gate halves: 9.7-10.4 s (suite installer), 9.2-9.7 s (twin).
- `3c9597b` README 870 (+17, every red of the three attempts named); `234f8ff` kb: twelve more pack timings. MASTER FAST-FORWARDED c97188a -> 234f8ff (22 commits), pushed with `git push origin next:master` and the local ref moved with `git fetch . next:master`, so the working tree was never switched. Both slices republished at the final commit, 1.1.1-alpha.0.126, ILC full pass, 0 diagnostics. No lock file left.
- N149: master was moved without checking it out (push next:master, then a fast-forward-only local fetch), so no file's mtime moved and the published slices stayed fresh. Alternative: checkout master, merge --ff-only, push, checkout next, as briefed in words. Cost: none; both refuse a non-fast-forward.

## 2026-09-24T22:10Z -- PHASE 1b LANDED, master = next = 234f8ff (22 commits over c97188a: (ii) 65ecc8d, Q293 a906f0e, (vii) 79323b6, Q290 f8f2e85, Q288 evidence 82744df, (i) 7fc478e, (iii) 5770651, (iv) f850628, Q292 70bbe29, Q291 559efd1, (vi) f5a2cf6, Q287 eb8ddba, Q294 b 6cb9acd + 518d584 with (v), Q289 b 417409a, gate fixes 3f93ad6 / 883e347 / 5f7493a / fcdb1c1, README 3c9597b 870, kb 234f8ff). Gate clean on the third attempt at fcdb1c1: Git Bash 870/870, PowerShell re-run 870/870, windows CLEAN; earlier runs saw three races (fixed with planted reds) and a stuck Firefox launch twice in six full runs (PasswordPromptTests hit Playwright's own 180 s initializeServer timeout; FirefoxSessionTests navigate failed at 3m01.8s), both while commit charge read TIGHT (~75%), NOT fixed; 8 runs of the Firefox tests under a 500 ms PATH broadcast were 112/112 green. N111-N149 logged by the writer. Highlighted for him: N113/N136 (Codex resolving the bare name through PATH read from source, not measured end to end; a running Codex's restart need likewise), N126 (the test pack is packed from the dev publishes, so a release gate's installer arms would install dev bytes), N143 (the configuration window can show up to twelve links, never rendered), N144/N148 (the stuck Firefox launch), N129 (real-installer tests write the real HKCU PATH and restore it byte for byte, clearance-checked).
QUESTIONS POSED: Q302 the stuck Firefox launch; Q303 rendering the grown window; Q304 measuring Codex's bare-name resolution end to end; Q305 the release gate's test pack; Q306 real PATH writes during tests. Q296-Q299 still open. PHASE 2 (coordinator core) DISPATCHED.

## 2026-09-25T00:10Z -- PHASE 2 WRITER (coordinator core) STARTED on `next` = 234f8ff = master
- Brief from the root: seven steps (coordinator pipe + single instance; per-user logon task; the sign-in step; a blocked server starts the coordinator; the minimal apply loop; records; gate + merge). Phase 3 (toast) and phase 4 (sessions page) out of scope. Q296-Q299, Q302-Q306 open, build nothing for them.
- `0c2d65e` drift-check 2026-09-25: no drift on the five or the vendored row (lastChecked was 2026-09-24).
- Experiment (.work/phase2/tsprobe, results-2026-09-25.txt): Task Scheduler through [GeneratedComInterface] under NativeAOT, non-elevated: RegisterTask from XML with a SID-scoped LogonTrigger 19.8-21.9 ms; IRegisteredTask.Run(BSTR "--coordinate") gives the action `--sign-in --coordinate`, Run(VT_EMPTY) leaves `--sign-in $(Arg0)` literal; an unquoted <Command> with a space is quoted by the scheduler; DeleteTask/GetTask of a missing task is 0x80070002; works from MTA and from STA. ComVariant by value needs DisableRuntimeMarshalling (SYSLIB1051), so VARIANT is a hand-written 24-byte blittable struct.

## 2026-09-24T22:45Z -- MAINTAINER (verbatim): "Q296 a / Q297 You recommend a. Why not b? / Q298 b / Q299 d / Q302 your recommendation / Q303 a / Q304 a / Q305 a / Q306 a"
=> DECIDED: Q296 a (measure what Claude Code and Codex do with a tools/list error at a first connection, then decide); Q298 b (cut every anonymous ID from the sandbox client config in the evidence, record their fingerprints); Q299 d (no identity-override hook, no suite check; the standing instruction to writers is the only guard); Q302 a (research the stuck Firefox launch; serialize Firefox tests only if contention is confirmed); Q303 a (render the configuration window on a hidden desktop, capture every state, show him the images); Q304 a (measure Codex's bare-name resolution end to end with the test pack, fresh and already-running Codex); Q305 a (the release gate packs its test installer from the release publish, ordinary gates from the dev publishes); Q306 a (tests keep writing the real user PATH briefly, restored byte for byte, clearance-checked). Q297 answered with the reasoning, awaiting his choice.
DISPATCHED: Q298 b + Q305 a queued to the phase 2 writer; a client-behaviour researcher (Q296 + Q304, holding the installer lock for its test-pack install); a Firefox researcher (Q302, a scratch Firefox copy outside the browsers root so no sweep touches it, never pressuring the whole machine); a rendering researcher (Q303, private desktop, scratch client configs).

QUESTION LOG, continued (phase 2 writer):
- STEP 1 BUILT (coordinator pipe + single instance + hand-over). PLANTED REDS, WATCHED (.work/phase2/plants.txt): p1 key with one character dropped -> "Expected ...FFEF04C2... but received ...FEF04C2..."; p2 CreateServer without FIRST_PIPE_INSTANCE and two instances -> twenty-starts arm "Expected to be 1 but found 2", the pipe arm "Expected IOException but no exception was thrown", the show arm "Expected HandedOver but received Coordinator"; p3 no grant -> "collection has 0 items but expected 1"; p4 raise handed window 0 -> "does not contain expected item: 4321"; p5 published app hands over recheck for every start (republished) -> "Expected to be equal to Show but received Recheck". All restored byte-identical (SHA-256), rebuilt, both slices republished (0 warning lines each). CoordinatorTests 6/6; with ServerPipe/ConfigurationApp/HouseRule/DocumentationLink/RecordedCount/InteropLayout/ProcessLiveness/NeverByImageName/UpdateInProgress 95/95 after the fragment restamp 1277 -> 1280.
- N150: THE COORDINATOR'S PIPE IS SERVED BY THE SERVER PIPE'S OWN LOOP, generalised to a verb table (IPipeAnswers; ServerPipe.OpenNamed gains an overload), and ServerPipeClient's connect/ask/read is extracted as ExchangeAsync for both. Consequence: ServerPipeLog event 1's text becomes "Listening on {Name} for {Verbs}." (a server's still renders "describe and stop"). Alternatives: (a) a second copy of the ~150-line serving loop for the coordinator; (b) the framework's pipe (refused by Q284 a on size and the remote flag). TAKEN: the table. Cost of (a): a second loop to keep in step.
- N151: A SECOND START DOES NOT CHECK WHO SERVES THE COORDINATOR'S PIPE. The pipe's DACL admits only the current user, FIRST_PIPE_INSTANCE refuses a squatted name, and the only things sent are "show"/"recheck" and a foreground grant; a same-user process could impersonate anything a check could ask for. Alternative: require the serving pid's image to be this process's own image (then the published-app arm could not hand over to a coordinator held by the test host). Cost: one image read per hand-over plus a rig change.
- N152: Settle tries both halves THREE times (a coordinator can exit between a failed create and the connect, which then finds no pipe); a hand-over that meets a pipe that does not answer is Neither at once. Cost: one constant.
- N153: Settle takes an optional hand-over bound the product never passes (ServerPipeProtocol.CallBound, 500 ms, in the product); the suite passes its hang detector, on N84's precedent for ServerPipeClient. It is not counted as a third Q293 exception because N84's bound is not one of the two either. Alternative: no parameter, and the twenty-starts arm at 500 ms per hand-over under a loaded host (flake risk). Cost: remove one parameter.
- N154: IN STEP 1 BOTH HIDDEN STARTS RUN THE SAME MINIMAL LOOP (act on verbs while a newer package is staged; stop when none is). `--sign-in` gets its own one-pass behaviour in step 3 and the loop gets the scan and the apply in step 5. Alternative: leave `--sign-in` unparsed until step 3 (then step 2's commit would register a logon task whose start opens the window at sign-in). Cost: none.
- N155: THE APP READS ITS STATE ONLY WHERE IT IS SHOWN. AppState.Read (which starts each client's CLI to ask for its registration) moved out of Main's head into the report branch and ConfigurationWindow.Show, so a start that hands over, or a hidden one, starts nobody's CLI. Not in the brief. Cost: move one line back.
- N156: A PERSON'S START THAT CAN NEITHER BECOME THE COORDINATOR NOR HAND OVER STILL OPENS ITS WINDOW (uncoordinated, logged as CoordinatorLog.Neither); a hidden start in that state exits 1. Alternative: refuse the window too. Cost: one branch.
- N157: AFTER A PERSON'S WINDOW CLOSES THE PROCESS STAYS, AS THE COORDINATOR, WHILE A NEWER PACKAGE IS STAGED, and exits at once otherwise (previously it always exited on close). Reason: a recheck a blocked server sent while the window was open would otherwise be lost. Alternative: exit on close and rely on the next blocked server's start of the task. Cost: one call.
- N158: A `show` WHILE THE WINDOW IS OPEN IS ANSWERED FROM THE DIALOG'S OWN ~200 MS TIMER (inbox.TakeShow -> Foreground.Raise), so the raise can lag by up to one tick. Alternative: subclass the task dialog's window and post a registered message. Cost: a subclass procedure.
- N159: THE FOREGROUND GRANT TAKES THE BARE PID the pipe reports during the live connection (GetNamedPipeServerProcessId), with no creation-time pair, against the Interop directory's pid rule; argued in Foreground's remarks: the pid names the process serving that open connection and only a grant is made with it. Alternative: open the pid and pair it with a creation time before granting (it cannot change what the grant does). Cost: one OpenProcess.
- N160: THE COORDINATOR IS KEYED TO THE CENSUS'S OWN ROOT: the install root, or the data root when not installed (BROWSERAI_ROOT in the suite), and the key is LiveInstances.RootKeyFor, now one derivation for three names (gate, pipe, task). Cost: none.
- N161: PublishedSlice.EnsureAppFresh added (the app's publish against the same inputs), since EnsureFresh reads the server's binary only; the one app arm that drives it calls it. Cost: none.
- Q298 = b (maintainer, relayed 2026-09-25): the coordinator-lifecycle batch's sandbox client file loses its two installation identifiers.
- N162: the cut file is renamed `sandbox/client/.claude.trimmed.json`, the batch's own convention for a file whose content was cut, and each value is replaced by a sentence (JSON stays valid); `"mcpServers": {}` is the registration record the file exists for (the entry after the uninstall) and stays with every other member. The file held `machineID` and `userID` (64 hex each) and NO account or organisation UUID. The README records each value's SHA-256 and the original's, and says the history before the cut still carries both values. Alternative: keep the name and cut in place. Cost: one rename.
- Q305 = a (maintainer, relayed 2026-09-25). PLANTED RED, WATCHED: SuiteCoverageTests.EveryGateDriverPacksTheSuitesInstallerFromTheTreeBeforeItsRun extended with the release/ordinary rule, run against the four drivers as they stood -> 2 offences, "Invoke-ReleaseGate.ps1: is a release half and packs the suite's installer from the dev publishes ... pass -FromReleasePublish (Q305 a)" and the same for invoke-release-gate.sh; green after the two release drivers pass it. Scans 92/92.
- N163: the switch is `New-Release.ps1 -TestPackOnly -FromReleasePublish` (refused without -TestPackOnly), reading artifacts\publish-release with the same baked-version refusal. The capability's byte comparison follows it: ReleaseLayout.PackedBinaries is the release publish in a release run (BROWSERAI_RELEASE_RUN) and the dev publishes otherwise, and ReleaseScriptTests' real -TestPackOnly arm now names the dev table explicitly, since it packs from the dev publishes whatever the run. The slice arms still drive the dev publishes, so a release gate needs both publishes (written in TESTING and RELEASING). Alternative: have the release publish also write the dev publish directories. Cost: one switch and one table.

## 2026-09-24T23:45Z -- Q303 RENDERED (.work/window-render/images/, 24 states + 2 pickers, private desktops only, nothing on screen; the root viewed 05 and 11 and they match the report). The window works and its per-client logic is right, but it is a column of 6 to 12 equal-weight command links, 707 to 1199 px tall at 100%; Microsoft's guidance is 2 to 5 command links; at 1920x1080 and 150% only two states would fit; an overflowing task dialog scrolls its Close button out of view. 150%/200% not rendered (a private desktop takes the session's DPI; no system setting was changed). Directions: 1 trim the page; 2 text links; 3 hub with pages; 4 status home + one page per client + Sessions as its own page (REC, with a "Show details" area and plain text links for rare actions); 5 a property sheet with tabs and a list view (reserve for Sessions if it needs columns and multi-select). Sub-decisions posed: older version offered as if an upgrade (rec: say it is older, the flag exists); raw error text (rec: one plain sentence, raw text under Show details); staged update invisible (rec: show it with an Install now link). Defects found (fold into phase 4): client name repeated as a prefix; the heading ignores a project registration; "Register again to repair" vs "Repair" link; the install warning names only Claude Code; first run always claims success; long paths clipped in link notes, paths wrapping at backslashes; the folder picker is the old tree dialog; "my"/"your" switch; internal wording; a feed folder without a manifest reads "up to date"; a hung check shows "Checking" up to 45 minutes with no cancel; the staged-update coordinator stays running after the window closes, as designed. Privacy: 19a/19b show his Desktop folder names; never commit them. QUESTIONS POSED: Q307 layout, Q308 older version, Q309 error text, Q310 staged update. Q297 still open.
- Q299 = d (relayed): no identity hook, no suite check; never passing an identity to git stands. Nothing to build.
- STEP 2 BUILT (per-user logon task). PLANTED REDS, WATCHED: s2p1 action without the placeholder -> "Expected --sign-in $(Arg0) but received --sign-in"; s2p2 a 30 s <Delay> in the trigger -> definition arm and real-scheduler arm "Expected to be false but found True"; s2p3 install removes / uninstall registers -> "Expected Registered but received Absent"; s2p4 a failed task logged as Information -> "Expected Warning but received Information"; s2p5 a missing task reported Removed -> "Expected Absent but received Removed"; s2p6 (real installer, both slices published, test pack packed under the lock) the real hook names the pack id "<id>.planted" -> TwoRoots "Expected ...two-roots-1a...\current\BrowserAI.exe but received" nothing (the planted uninstall removed its own task; scheduler checked empty after); s2p7 ITaskFolder's GetTasks and DeleteTask swapped -> the slot oracle "differs at index 114". All restored byte-identical, rebuilt, both slices republished. Green: TwoRoots 1/1 under the lock with the test pack packed from this tree (vpk 12.1 s / 8.4 s); step 2 set 149/149 after the fragment restamp 1282 -> 1283.
- N164: THE SCHEDULER IS DRIVEN THROUGH COM ([GeneratedComInterface]), NOT schtasks.exe. Deciding fact: the on-demand start must pass `--coordinate` through $(Arg0), and only IRegisteredTask::Run takes a parameter (schtasks /run and Start-ScheduledTask take none); then the same interface registers, reads back and removes with no temp XML, no child process from a server whose std handles belong to its client, and an HRESULT per failure. Measured first in .work/phase2/tsprobe (NativeAOT, MTA and STA, non-elevated). Cost measured at this commit: the app 10,674,176 -> 10,834,944 B (+160,768) with the COM code; the server does not link it until step 4. Alternative: schtasks with an XML file for register/remove plus two tasks (logon and on-demand) since /run takes no argument. Retroactive cost: two tasks, a temp file and a child process per call.
- N165: THE TASK IS `<pack id> sign-in <root key>` IN THE ROOT FOLDER `\`. The pack id comes from the Velopack locator inside the hook (new InstallLocation.AppId; Run() creates the locator before it invokes the callback, read at 1.2.158), so the test pack registers `BrowserAI.app.test sign-in ...` and never the shipping name; the key is LiveInstances.RootKeyFor (N160). A BrowserAI subfolder was not used because a non-elevated folder creation was never measured. Alternatives: name by root key only (then "named for the test pack's id" fails); parse current\sq.version for the id (a second reader of Velopack's manifest). Cost: one property.
- N166: SETTINGS THAT DIFFER FROM THE SCHEDULER'S DEFAULTS, each argued in SignInTask.DefinitionFor's remarks: MultipleInstancesPolicy Parallel (default IgnoreNew would drop a blocked server's start while a sign-in instance runs; the pipe decides who coordinates), DisallowStartIfOnBatteries false, ExecutionTimeLimit PT0S (default 72 h would end a coordinator waiting on long sessions), Priority 5 (normal; default 7 is below normal and the coordinator opens a window). Alternatives: Queue policy; defaults. Cost: one element each.
- N167: THE USER IS GIVEN BY SID in the trigger and the principal (NamedPipes.CurrentUserSid, now internal); the scheduler stores the trigger's as DOMAIN\user and drops default-valued elements, measured. Cost: none.
- N168: EVERY SCHEDULER CALL RUNS ON A THREAD OF ITS OWN in the MTA, joined within ScheduledTasks.CallBound = 5 s (register measured 19.8-21.9 ms), and never throws: a hook on the app's STA main thread and the server's thread-pool caller both get the same apartment, and a hung scheduler costs 5 s. Alternative: call on the caller's thread (STA for hooks: works, measured, but mixes apartments with the dialog's). Cost: none.
- N169: VelopackStartup.Register's body is split into Mirror(outcome, intent, version, log) so the suite can hand it a failed task outcome; a failed registration is a Warning line in the installer log and the hook returns normally (HookOutcome.IsWhatWasAskedFor is about the clients and is unchanged by it). Cost: none.
- N170: VARIANT IS HAND-WRITTEN (24-byte blittable struct): ComVariant by value needs [assembly: DisableRuntimeMarshalling] (SYSLIB1051, measured), which would change marshalling for every other declaration in Core. Its oracle is sizeof(ComVariant), the OVERLAPPED/NativeOverlapped precedent, because CsWin32 refuses VARIANT in COM interface mode (PInvoke003, measured). The four COM interfaces were added to NativeMethods.txt as ORACLE TYPES (its "only structs, no functions" comment amended by addition) and InteropLayoutTests compares GUID, IDispatch base, slot order and native arity. Cost: none.
- N171: CLEARANCE READING 8 reads the scheduler through PowerShell's Schedule.Service COM object (read-only): each `BrowserAI.app sign-in *` task by name with the SHA-256 of its stored XML, and every `BrowserAI.app.test *` task marked MUST BE ABSENT (so a leftover under any test-derived id is caught, the planted `.planted` included). SuiteCoverageTests.TheClearanceNamesATestPackTaskLeftBehind drives it. Cost: none.
- N172: RealInstallerTests computes the REAL install's task name for `%LocalAppData%\BrowserAI.app` (Velopack's default root) and holds its definition byte-identical across the two-roots arm; an install moved with --installto would carry another name and the comparison would read absent both sides. Cost: none.
- STEP 3 BUILT (the sign-in step). PLANTED REDS, WATCHED: s3p1 apply whatever the scan found -> "Expected NotAlone ... but received Applied"; s3p2 scan when nothing is staged -> "Expected to be 0 but found 1"; s3p3 match the file name and not the path -> "Expected to be equivalent to [85220]" (the copy outside the root counted); s3p4 leave out nobody -> "Expected 0 but found 1"; s3p6 (published app republished) --sign-in never reaching the step -> log lacks "Sign-in step: NothingStaged". s3p5 (count an exited process) STAYED GREEN, which prompted a measurement (.work/phase2/zombie): an exited process whose handle is still held is NOT in EnumProcesses' list and QueryFullProcessImageNameW refuses it; the filter it tested was removed (N175). All restored byte-identical, rebuilt, both slices republished (0 warning lines). Step 3 set 100/100 after three moved-file links were repointed and the fragment count restamped 1283 -> 1285.
- N173: BrowserProcesses.cs MOVED WHOLE from src/BrowserAI/Interop to src/BrowserAI.Core/Interop (namespace unchanged, no call site changed) so the app's coordinator uses the same full-path identification; the app never calls ScanFor, so ILC leaves the terminating half out of BrowserAI.exe. Alternatives: split the file and move only the non-terminating half; a second scan in the app. Cost: a move; ARCHITECTURE, QUESTIONS and MessageWindows' links repointed; interop counts restamped (91 in 18; server 32 in 7, Core 42 in 8).
- N174: THE SCAN HOLDS EACH PROCESS AS A WaitHandle (HeldProcess : WaitHandle, owning a SafeWaitHandle from an OpenProcess overload), with three new overloads over SafeWaitHandle, so the loop can WaitAny on the STA thread and no raw handle value ever leaves a SafeHandle. Alternative: SafeProcessHandle plus raw values for WaitForMultipleObjects under DangerousAddRef. Cost: three declarations.
- N175: THE EXITED-PROCESS FILTER WAS REMOVED AFTER MEASURING THAT IT CANNOT FIRE (see s3p5): the arm's "ended, gone from the next scan" holds by Windows' own behaviour, which HeldUnder's remarks now record. Alternative: keep it as defence in depth (an untested branch). Cost: four lines.
- N176: a scan with no process found but an unresolved spelling of the root is NotAlone, never Applied: it cannot say nothing runs. Cost: one condition.
- N177: the sign-in step does not join the live census for its pass. Alternative: join, so a server starting in the same instant defers to it (the loop joins in step 5). Cost: one line.
- N178: A VERB THAT REACHES THE PIPE DURING THE SIGN-IN PASS IS ANSWERED AFTERWARDS by the loop (a show opens the window, a recheck keeps the coordinator), because the start that sent it has exited; otherwise the step exits as briefed, logging why. Cost: one condition.
- N179: the not-alone sentence names at most five processes (pid and image) and counts the rest. Cost: one constant.
- STEP 4 BUILT (a blocked server starts the coordinator). PLANTED REDS, WATCHED: s4p1 the lane wakes nobody -> "Expected to be 1 but found 0"; s4p2 the task started even with a coordinator serving -> "Expected Rechecked ... but received Failed"; s4p3 the task started with --sign-in -> the scripted arm "Expected ...--coordinate but received ...--sign-in", and s4p3b with the app republished the REAL arm "Expected to end with --sign-in --coordinate but received ...BrowserAI.exe --sign-in --sign-in". Restored byte-identical, rebuilt, both slices republished (0 warning lines). Step 4 set 117/117 after the fragment restamp 1285 -> 1286. MEASURED by the real arm: a task started on demand through IRegisteredTask::Run starts the app as `"<publish>\BrowserAI.exe" --sign-in --coordinate`, and its parent is the pid the service control manager gives for the Schedule service (a svchost this user cannot open: OpenProcess error 5, measured).
- N180: UpdateService takes the wake as a REQUIRED parameter (ICoordinatorWake), placed before the optional budgets; every test site passes a RecordedWake. The server's one construction site passes CoordinatorWake(installRoot, InstallLocation.AppId, ScheduledTasks.Instance): that wiring is held by the compiler and by nothing that runs, because the update lane runs only in an installed release build. Alternative: an optional parameter defaulting to no wake. Cost: one argument per site.
- N181: A COORDINATOR PIPE THAT IS THERE AND DOES NOT TAKE THE RECHECK STARTS NO TASK (a new start would meet the same holder); reported, and the next blocked pass tries again. Alternative: start the task anyway. Cost: one branch.
- N182: THE WAKE NEVER THROWS (catch-all at its boundary, one log line CoordinatorLog.Woke either way), so StagedButNotAlone is still what the pass returns. Cost: none.
- N183: THE SERVER NOW LINKS THE COM TASK SCHEDULER CODE: BrowserAI.Server.exe 19,402,752 B (step 2 publish) -> 19,592,192 B (+189,440) at this commit, measured on this tree's publishes; the app is 10,902,528 B. Alternative: have the server ask a separate process to start the task (a child it would have to start outside its client's job anyway). Cost: the bytes.
- N184: the real arm identifies the scheduler's host by the Schedule service's pid through the service control manager (a harness ServiceHost with OpenSCManagerW/OpenServiceW/QueryServiceStatusEx, read-only), because the host cannot be opened to read its image. Alternative: WMI's Win32_Service (a query the image-name scan could mistake). Cost: one harness file.

## 2026-09-25T00:05Z -- ZOOM-OUT REQUESTED. MAINTAINER (verbatim): "Q307 Give me the tradeoffs for all of these with more details on your recommendations. Also, I am surprised to find entries in eg "C:\Source\SixFive7\BrowserAI\.work\window-render\images\05-everything-registered.png" that refer to specific repo installs. I expected a single uninstall button that launches a folder picker where I can select what dir I want toi uninstall from with a nice prompt if I select a dir where it was not installed or if it was successfull. This looks like we are keeping an index of sorts. That sounds like complexity. Explain this. / Q308 Why would the feed ever offer an older version? How does that work? / Q309 b / I'm starting to get a bad feeling about this direction. Some thoughts: - We are trying to figure out how to subscribe mcp servers to different harnasses system wide and repo wide with all the quirks involved. This is something many people have done already. Maybe we first need to find if there is a good library we can lean on and reduce our complexity and maintenance burden here. - The interface requirements are blowing up. Maybe it is time to move to a web interface for management? - I would like to consider exposing the playwright dashboard. That would fit nice in our hosted web interface. But before we do that we need to research if providing that interface results in downsides given our dir session architecture. - I just discovered this: https://news.ycombinator.com/item?id=49756671 Would that be a good fit to replace playwright? Would that reduce our complexity and gain features and performance? What would we loose? Do deep research into the downsides. Give me a detailed pro/cons comparison. / Given this all I want you to finish any small outstanding work. Save your state durably. Then I want to compact you and have you run this zoom out moment before we decide on the feature approach. Plan? Tell me when to compact you. Before you do give me the durably saved todo tree."
=> Q309 b DECIDED. Q307 answered with tradeoffs; the per-project rows explained (no index: the window searches the folder it was started from and its parents; from the Start Menu it never finds one; the picker is the flow he expects); a simplification offered. Q308: in the real product BrowserAI never sets AllowVersionDowngrade; the render researcher was asked which Velopack 1.2.158 rule produced the offer in its scratch setup. FEATURE WORK PAUSED: the phase 2 writer told to stop at a clean boundary (no gate, master stays 234f8ff); the client and Firefox researchers told to wrap up within ~20 minutes with REPORT.txt files. NEXT: a state-save writer snapshots this ledger into docs/ledger (interim, characters swept per the ledger README) and writes the todo tree into TODO.md, filtered doc tests, commit, push next; then the maintainer compacts; after compaction the ZOOM-OUT runs as four research tracks: (A) libraries or tools that register MCP servers across harnesses at user and project scope; (B) a local web management interface in place of the task dialog; (C) exposing the Playwright dashboard inside it, and its downsides under the directory-session architecture; (D) the HN item 49756671 as a Playwright replacement, deep pros and cons.

## 2026-09-25T00:20Z -- Q296/Q304 MEASURED (.work/client-behaviour/REPORT.txt, runs-index.json, texts.json; Claude Code 2.1.282, codex-cli 0.155.0-alpha.9.2, BrowserAI 1.1.1-alpha.0.130). Q296: a tools/list ERROR at a first connection leaves Claude Code "connected" with ZERO tools for the whole session (3 retries, then only a debug-log line; no relaunch; the model sees "No such tool available" while the instructions still say "Call browserai_init first"); codex exec terminates the server and the model gets no tools; the app-server reports failed and recovers on config/mcpServer/reload or a new thread; the real server in real updating mode behaves the same. Stub arms: b as posed (real list, calls refused, exit when the updater exits) lets Claude Code recover on its next call and tells Codex why; c (b + keep serving after the updater exits) recovers both in the same process; holding tools/list until the updater exits (1.3-2.8 s) worked for both within Codex's 10 s; list_changed only helps if BrowserAI declares tools.listChanged (it declares "tools":{} today). Researcher recommends c, b minimum. Q304: a Codex started AFTER the install finds `BrowserAI.Server.exe` by bare name through PATH (3/3); one started BEFORE never does, even with a new thread or reload (3/3); the real HKCU Path byte-identical after uninstall. Researcher recommends keeping Q294 b plus having the window tell the person to restart a Codex older than the install. INCIDENT: one `codex --version` without CODEX_HOME created ~/.codex/tmp/arg0/codex-arg0O3cd8S in the real ~/.codex; removed by the researcher. Other findings: Releases\test-pack held a planted build whose hook registered "BrowserAI.app.test.planted sign-in ..." and the clearance only matches 'BrowserAI.app sign-in *' and 'BrowserAI.app.test *' (a leftover planted task would pass unseen); the PATH clearance line misses roots without "BrowserAI" in the path (hash still catches); scratch Codex runs clone the plugin catalogue and call chatgpt.com (401).

## 2026-09-25T00:30Z -- Q308 ANSWERED, AND THE ROOT'S EARLIER CLAIM WAS WRONG. The root told the maintainer "BrowserAI never turns on Velopack's downgrade option"; that came from a grep piped through `head -12`, which cut the list off before VelopackUpdateClient.cs. FACT: src/BrowserAI.Core/Updates/VelopackUpdateClient.cs:59 sets AllowVersionDowngrade = true, since 928843a (2026-08-16, "package, update and roll back"), deliberately, as the rollback mechanism. Velopack 1.2.158 UpdateManager.cs:152-154 offers the manifest's highest full release whenever it is lower than the installed one and the option is on (8 single-variable runs: only the option flips it). CONSEQUENCE: the server's background lane has no IsDowngrade check; it logs rollback=True (UpdateService.cs:340), downloads and applies when alone (:384) with nobody clicking. It happens whenever GitHub's "Latest" release carries a lower version than installed: a deliberate New-Release.ps1 -RollbackRepublish; Latest moved back by hand (a newer release deleted, drafted or marked prerelease, or a new release for an older line made Latest, which GitHub does by default); or an install above the latest stable (a prerelease build under the production id). The window shows it exactly like an upgrade. Q308 RE-POSED with directions: keep rollback, name it in the window; guard the automatic apply of a downgrade; or turn the option off and roll back by publishing a higher version (roll forward).

## 2026-09-25T00:45Z -- Q302 SOLVED (.work/firefox-launch/REPORT.txt). The stuck Firefox launch is SAFE MODE: on Windows a new Firefox browser process checks the Shift key at start (toolkit/xre/SafeMode.h, nsAppRunner.cpp) and, with Shift held, enters safe mode; BrowserGlue (1549 omni.ja, BrowserGlue.sys.mjs:395-407) opens the modal safeMode.xhtml before any browser window, headless or not; delayed startup never runs, juggler's Browser.enable never answers, Playwright stops at its own 180 s DEFAULT_PLAYWRIGHT_LAUNCH_TIMEOUT. Reproduced serially on an idle machine 4/133; every stuck browser had a -safeMode content process (3/3 organic, 5/5 forced), 0/287 healthy; MOZ_DISABLE_SAFE_MODE_KEY=1 gave 0/155 (it covers the Shift trigger only; MOZ_SAFE_MODE_RESTART still forces it). Contention, memory pressure, CPU, the sweep, profile lock, Defender, the content-process cap: ruled out. Real users are affected too (anyone holding Shift while an agent starts Firefox). Fix: one line in ChildEnvironment.Forced (src/BrowserAI/Protocol/ChildEnvironment.cs:217-221), which reaches Firefox through node. The real trigger (a held Shift key) cannot be planted red without touching the keyboard, so a mechanism-only test would be a SECOND named exception to the plant-it-red rule, which the rule says only its owner may make. Q312 POSED. Also: an upstream Playwright report is possible (microsoft/playwright#40882, closed for lack of a repro); posting under his name needs his explicit word. Side findings: firefoxUserPrefs go over Browser.enable at runtime, not into user.js as BrowserConfiguration.cs:725-731 and FirefoxProfile.cs:87-90 say; the TIGHT commit band is not memory pressure on this machine; kb row 34, provisioning-and-timings, README:354 and the HAZARDS six-run-gate row need correction by addition.

## 2026-09-25T02:10 local -- WRITER: STEP 5 (the coordinator's apply loop) BUILT; PAUSE ORDER RECEIVED. The coordinator relayed the maintainer's zoom-out pause mid-step: finish the step in hand, commit, push next, start nothing else, run no gate, master stays at 234f8ff. Step 5 is the step in hand; steps 6 (records) and 7 (gate and merge) are NOT STARTED.
- N185: THE LOOP TAKES THE SCAN AS A SEAM (Func<RootScan>), the sign-in step's own shape; Program passes one local function, BrowserProcesses.HeldUnder(root, own pid), to both. Alternative: the loop calls the scan itself, and the 63-per-wait arm starts 70 real processes (cmd.exe copies plus their console hosts, on a machine whose commit band reads TIGHT). Cost: one parameter.
- N186: THE COORDINATOR JOINS THE LIVE CENSUS FOR AS LONG AS THE LOOP RUNS (the window already joins it while open): a server finishing its update pass then counts it and sends a recheck (step 4) instead of applying on its own exit, whose kill pass would end the coordinator. Alternative: stay out of the census and let the last server apply. Cost: one line, one marker per coordinator.
- N187: AT MOST 63 PROCESSES PER WAIT beside the pipe's handle (one WaitAny holds 64). More are still held and counted; the wait takes the first 63 in scan order and the next pass the rest. Alternative: a wait thread per 63, or RegisterWaitForSingleObject per handle. Cost: extra passes only when more than 63 run; the apply still waits for all, since it happens only on an empty scan.
- N188: THE WAITING LINE (CoordinatorLog.Waiting, event 8) IS WRITTEN WHEN THE HELD COUNT CHANGES, not on every pass, so a recheck that changes nothing adds no line. Alternative: a line per pass. Cost: one local.
- N189: A PASS THAT FINDS NOTHING BUT COULD NOT RESOLVE A SPELLING OF THE ROOT WAITS ON THE PIPE ALONE and applies nothing (N176's rule); only a verb wakes it, since the brief allows no timer. Alternative: a timer. Cost: none.
- N190: THE LOOP RUNS ON THE MAIN STA THREAD after the window (for a person's start) closes, through WaitHandle.WaitAny, which pumps on an STA; a show that arrives while it waits opens the window from the loop and the loop carries on after it closes. Alternative: a separate MTA wait thread. Cost: none.
- N191: THE 63-PER-WAIT ARM SCRIPTS ITS STAND-INS AS NAMED EVENTS: each pass opens a fresh handle to each, moved into the HeldProcess by emptying the EventWaitHandle wrapper, because one wait refuses the same handle twice and the arm has to be able to set what a pass holds. Alternative: DuplicateHandle through CsWin32 in the test project. Cost: one test helper.
- N186 CORRECTED 02:30 local (previously "THE COORDINATOR JOINS THE LIVE CENSUS FOR AS LONG AS THE LOOP RUNS"): THE LOOP JOINS THE CENSUS ON THE FIRST PASS THAT FINDS A PACKAGE STAGED, BEFORE IT SCANS, AND LEAVES IT WHEN IT STOPS. With nothing staged it stops before joining, so a build that is not installed (NothingStaged, no feed) never adds a marker to the census of the root it was keyed to, which without BROWSERAI_ROOT is the default data root. Joining before the scan means a server that starts after the scan already counts it. Alternative: join at Run's start (the first form; planted as s5p7 and red). Cost: a try/finally.
- N192: THE REAL ARM STOPS ITS FIRST PASS INSIDE THE SCAN WHILE IT READS THE CENSUS (a ManualResetEventSlim the arm sets in a finally), so the census is read between the loop's join and its verdict; the first form read it after the pass, and a loop planted to apply at once went red there as "census Alone", naming the wrong defect. The arm now also counts the scans (two: one before the exit, one after). Cost: one event in the arm.
- N193: THE SCRIPTED SCAN'S WAIT ENDS WHEN THE LOOP ENDS AND RETHROWS WHAT IT THREW (PassesAsync(count, run)): in the first form the 64-handle plant went red only at the five-minute hang detector, with the NotSupportedException from WaitAny never shown. Cost: one parameter.
- STEP 5 FIRST-FORM PLANTS (the code before N186's correction, all red, all restored byte-identical): s5p1 apply whatever the scan found -> "Expected NotAlone ... but received Alone" (the misleading red N192 fixes); s5p3 never join -> same; s5p4 64 processes beside the pipe -> "PassesAsync(2) ... Expected to be true but found False" at 5m 00s (N193); s5p2 wait on the pipe alone -> TimeoutException at 5m 00s; s5p5 read what is staged once -> TimeoutException at 5m 00s; s5p6 a verb wakes without a pass -> "PassesAsync(2) ... found False" at 5m 00s. Re-run against the final code below.
- STEP 5 BUILT (the coordinator's apply loop, final form). PLANTED REDS, WATCHED against the final code (.work/phase2/plants.txt, all restored byte-identical by SHA-256): s5p1 apply whatever the scan found -> "Expected to be 1 but found 0" at the waiting-line count; s5p3 never join the census -> "Expected NotAlone ... but received Alone"; s5p4 64 processes beside the pipe -> "NotSupportedException: The number of WaitHandles must be less than or equal to 64." at once; s5p7 join before anything is staged -> "Expected to be false but found True" (the live directory exists); s5p2 wait on the pipe alone -> TimeoutException at the 5 min hang detector; s5p5 read what is staged once -> TimeoutException at 5 min; s5p6 a verb wakes without a pass -> "PassesAsync(2, run) ... Expected to be true but found False" at 5 min. Rebuilt; app slice republished 10,914,304 B, server slice unchanged 19,592,704 B, 0 warning lines. Filtered set (Coordinator, SignInStep, CoordinatorWake, HouseRule, DocumentationLink, RecordedCount, BuildConfiguration, ConfigurationApp, Update) 126/126. No gate run, by order.
- `21de415` STEP 5 COMMITTED AND PUSHED (next = origin/next = 21de415; master = origin/master = 234f8ff, untouched). WRITER STOPPED HERE ON THE PAUSE ORDER: steps 6 (records) and 7 (gate + merge) not started; no gate has run on anything in phase 2 (0c2d65e..21de415). Tree clean; .work/installer.lock absent; no process of this writer's running (python, test hosts, stand-ins all gone; the BrowserAI.Server.exe/node.exe processes under %LOCALAPPDATA%\BrowserAI.app are the installed product's, not this writer's); no BrowserAI task in the scheduler's root folder.

## 2026-09-25T01:10Z -- PHASE 2 STOPPED CLEANLY at next = 21de415, pushed, UNGATED (0c2d65e drift 2026-09-25 no drift; 911d03e step 1 coordinator mode and single instance; 73d78d0 Q298 b; 6169531 Q305 a; e34d8c4 step 2 per-user logon task via COM; 5883673 step 3 sign-in step; dec60a9 step 4 a blocked server starts the coordinator; 21de415 step 5 the apply loop). Owed: step 6 records (DECISIONS, ARCHITECTURE, HAZARDS, CHANGELOG, fragment count now 1286) and the .work-only measurements to kb (.work/phase2/, tsprobe results, the zombie-process reading); step 7 gate and merge. master stays 234f8ff. N150-N193 logged by the writer. Gaps: clearance reading 8 matches only 'BrowserAI.app sign-in *' and 'BrowserAI.app.test *'; SignInStepTests' stand-ins lack PlantedProcess's scan-visible wait.
STATE SAVE DISPATCHED: interim ledger snapshot into docs/ledger per its README, the todo tree into TODO.md, filtered documentation tests, commit, push next; no gate. Then the maintainer compacts; the zoom-out runs after.
