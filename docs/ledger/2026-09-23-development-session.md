<!-- SPDX-FileCopyrightText: 2026 Jori Huisman -->
<!-- SPDX-License-Identifier: LicenseRef-BrowserAI-FSL-1.1-MIT-5yr -->

<!-- The decision ledger of the session opened 2026-09-23, appended to live by the architect and snapshotted here at commit cbbebbe; nothing below this comment has been edited. This is the FIRST and FINAL snapshot of that session: it covers the no-trace-of-AI directive and both halves of the sweep it ordered, the triage of the outstanding work items T1 to T11, the research behind Q254 and Q261 (what each client does with a server that exited), Q255 and Q259 (the password prompt and what the automation switch costs), Q258 (registering with a second client), T7 (Playwright's descriptor registry), and the decisions the maintainer took on all of them. -->
<!-- There is no live copy any more. .work/STATE.md was deleted with the rest of the scratch folder in this same commit, so from here the snapshot IS the record; a new session opens a new ledger beside this one instead of appending to this file. -->
<!-- The body below is byte-identical to the live file as it stood at the snapshot, sha256 c20d63c52b9843429328e1749f306a37eb9e29710a64d08c666deaf2d081fe28, with ONE exception taken before the copy and applied to the live file first, under the 2026-09-23 grant: 66 em dashes and 10 en dashes were swept to `--` and `-`, six sentences carrying the comparison frame were rewritten as plain statements, three mentions of the bracketed ASSUMED marker lost their brackets so a scan reads them as prose and not as a claim, ten BEFORE/AFTER pairs quoting a swept word or phrase moved from italics into straight quotes, so a scan reads them as the quotations they are and not as uses, and one relative link was re-pointed at the repository root it now sits two directories below. No fact, number, date, name, claim or maintainer-verbatim line moved. -->
<!-- Edited once after the snapshot, on 2026-09-24, under the maintainer's decision Q295 a. That decision rewrote this repository's history to correct the author and committer address on 22 commits, which gave them and every commit descending from them a new hash, so the 60 citations in the body below of a commit whose hash changed now carry its new hash, each abbreviated to its old length, and so does the snapshot commit named in the first comment above. No other byte of the body moved. The body's sha256 is now b7a1222bb234fe3ae245a88e1bae2de46beb9c3561f5013e27eaba0f39234a0e, previously c20d63c52b9843429328e1749f306a37eb9e29710a64d08c666deaf2d081fe28. -->

# BrowserAI decision ledger -- session opened 2026-09-23 ~18:30, after v1.1.0 (the 2026-09-22 release session's ledger is closed and snapshotted at docs/ledger/2026-09-22-release-session.md, commit 08e1ac1)

STANDING RULES: unchanged, near-verbatim in the snapshot above under "RESUME NOTE FOR THE POST-COMPACTION SESSION" (model discipline: Fable plans and talks, Opus sub-agents do the work, one writer at a time; HALT-A/HALT-B; hostile-caller defence a non-goal; Q175 posts wait for his word; Q185 tag rules; release rules incl. "no trace of AI"; repo rules; clearance conditions; harness path guard; instructions budget 2,026/2,048; a ledger is not a decision of record).

## 2026-09-23 ~18:30 -- MAINTAINER ANSWERED Q239-Q244. NEW WORK STARTS NOW; NO FULL TEST RUNS UNTIL THE NEXT VERSION'S EDITING IS DONE.

MAINTAINER (verbatim): "Q239 b / Q240 do not touch it. I want to see if it works as expected. But do figure out which instances are running and by who and tell me. Testing the index with it :-) / Q241 Sweep everything now. This makes everything dirty if I am interpreting the seal as document that if changed require all the test to run again? If so, sweep everything but do not run the tests. I want to start dev on the next version and only run all the tests after we are done editing instead of in between all the steps because iterating is going to slow right now. / Q242 a / Q243 b. The start of new work moment is now. / Q244 a - Are they still active. Who is using them for what? / But before I triage I want to discuss the new work. But before that process these answers first."

DECISIONS OF THIS ENTRY: (1) The two-shell gate is NOT run between steps of the next version's editing; it runs once when the editing is done. Filtered single-test runs to watch a planted red stay allowed as iteration (seconds), never as verification. (2) Ungated work goes on a branch `next` pushed to origin, so nothing is lost and master stays at the last gated commit 08e1ac1; merge (fast-forward) after the gate. (3) Q239b: the five gate drivers move into build/, one per shell, each forcing and declaring its spelling; a test holds the declaration. (4) Q241: em and en dashes swept to ASCII in every UNSEALED prose file; sealed records are excluded (docs/reviews bodies, released CHANGELOG sections, docs/ledger snapshots, docs/evidence batches, licence and notice texts, vendored third-party, positive-control strings in tests); heading anchors recomputed and every link updated; a house-rule scan keeps it. The seal is immutability of a dated record, not a test-run trigger. Curly quotes, ellipses and NBSP are counted and reported, not swept (his question was dash style). (5) Q240 + Q244: read-only. (6) Q242a: rows 123/124/126/130 stay owed. (7) Q243b: primers per TODO item prepared now; triage after the new-work discussion.

## 2026-09-23 ~18:50 -- Q240/Q244 INVESTIGATED (read-only; bulk in .work/instances-2026-09-23/, to be persisted to kb before the wipe). TODO PRIMERS READY (reader returned; held for the triage).

22 servers, all owned by live claude.exe: 20 in ONE VS Code window (pid 51128, PortainerCompose folder, opened 2026-09-21 00:08, incl. this session 278efa76 = server 112980), 2 CLI (StationeersPlus busy = 46760; Downloads mvno = 134344). Working 2, just quiet 1, idle 22-46 h 5, NEVER USED 14 = the 15:30 burst of 2026-09-22 is a VS Code terminal restore of 14 conversations last written May-July 2026. Only 4 of 22 ever opened a BrowserAI session; no browser alive for any. The 8 Chromium processes = one tree (1 attributed + 7 helpers the sweep spares by design) of the Downloads mvno session (Dutch MVNO numbering research), died 18:14:42 local when that session ran browserai_destroy; catch_up refuses the directory now (no browserai.data). INDEX TEST: 6 entries, 3 pointing at deleted suite scratch dirs; browserai_list reported exactly the 3 live ones with purposes and in-use state; the read pruned the stale entries (self-healing read path); find/lock cross-checks agree. LOOSE ENDS: 8 orphan instance dirs under BrowserAI\instances from the 17:36 suite run (profiles empty); config app pid 95472 open since 09-17; kb-worthy trap: the process log's pid= is reused, identity is pid@processCreatedFileTime (a pid-only grep misattributed four servers); live markers are under BrowserAI.app\live, not BrowserAI\live. What blocks the staged 1.1.0 apply = these 22 (+ the app); it applies when the last exits.

## 2026-09-23 ~19:30 -- BRANCH `next` OPENED FROM `08e1ac1`. FOUR COMMITS, ALL PUSHED. NO FULL GATE, AS INSTRUCTED.

`master` stays at the last gated commit `08e1ac1`; everything below is on `next` and is UNGATED by design.

**A -- Q239b, `e3973d4`. The five gate drivers move into `build/`.** `Invoke-OrdinaryGate.ps1` + `invoke-ordinary-gate.sh` (one run each), `Invoke-ReleaseGate.ps1` + `invoke-release-gate.sh` (three each, `BROWSERAI_RELEASE_RUN`), and `Get-ClearanceSnapshot.ps1`, which all four share and which reads and never repairs. Each derives the repository root from its own location, so none is machine-specific the way the wiped copies were. TESTING and RELEASING say by addition that this is **not** the shared wrapper CLAUDE.md forbids, and name the 19:26 loss as the reason.

⚠️ **THE TEST FOUND A DEFECT OF MINE ON ITS FIRST RUN.** `SuiteCoverageTests.EveryGateDriverDeclaresTheDriveLetterSpellingItForces`, planted red with the bash ordinary half declaring `upper` while forcing lower, reported exactly that -- and in the same breath reported `Invoke-OrdinaryGate.ps1: is the ordinary half and sets BROWSERAI_RELEASE_RUN`, which was the SCAN being wrong: every driver EXPLAINS in prose whether it is a release half, and a `Contains` reads an explanation as a setting. It keys on an assignment now. ⚠️ `.sh` joined `RepositoryLayout`'s source list and link predicate; two new shell scripts would otherwise have been the first files in this tree outside the SPDX rule, the link scan and the fragment count.

**B -- Q241, `02ef391`. The dash sweep: 8,979 characters in 291 files.** em -> `--` (8,696), en -> `-` (156), whole-cell token -> `-` (111), quoted token -> `-` (16). ⭐ **105 ANCHORS MOVED and 223 LINKS followed them**, computed by porting `MarkdownAnchor`'s slug rule and not by pattern: it DROPS an em dash and KEEPS a hyphen, so `a--b` becomes `a---b`.

⚠️ **THE SEAL CAUGHT AN OVERREACH, WHICH IS THE MECHANISM WORKING.** The link rewrite reached inside CHANGELOG's sealed `1.0.0` section -- 14 fragments, 28 characters, reported as REWRITTEN to the byte. Reverted, and the 14 headings they name now carry an explicit `<a id="old-slug"></a>` so a sealed link still resolves.

⚠️ **AND THE SCAN WENT RED ON ITSELF, TWICE.** Its remarks explaining the heading rule contained the character they forbid, and so did its own controls -- **the Write path turns a `\uXXXX` escape into the real character**, so a test about a character cannot spell it that way. Built from code points now. Same trap as the floating-fact marker sentence earlier today.

**FIVE OCCURRENCES SURVIVE ON PURPOSE** and the test names each: two char literals in `ChangelogTests.NotTyped`, one in `ModelSurfaceTests` (a quoted previously clause about byte width), one `[Arguments]` worked example in `DocumentationLinkTests` that pins the em-dash branch of the slug rule, and one quotation -- Chrome titles its windows with an en dash and `kb/windows/detection.md` is reporting two windows it saw.

**NOT SWEPT, COUNTED FOR HIM:** **680** ellipsis, **22** curly double quotes, **19** curly single quotes, **1** non-breaking space, across 127 files. Per-file table at `.work/unswept-counts.txt`.

**D -- `cd7d3e8`, the reader's two corrections.** README's Status and CLAUDE.md's opening said *"Tagged `v1.0.0` and published"* on the day `v1.1.0` was published, and `1.1.0` appeared nowhere in README. Both say `v1.1.0` now; the *no install is known* half is untouched. And `kb/playwright/tools-and-artifacts.md`'s *"Every one of them is the SUITE's"* is narrowed: **re-measured myself, 3,749 files / 6,619,667 b, 2026-09-16T07:04 to 2026-09-23T18:13, and 26 of them name the INSTALLED product.** ⚠️ The re-establishment procedure now names the shape that defeated my first pass: the descriptors are JSON, so a grep for the single-backslash Windows path returns zero on a directory holding 26.

**FILTERED RUNS ONLY**, per the maintainer's instruction: the last one was 103/103 green across `HouseRuleTests`, `DocumentationLinkTests`, `ChangelogTests`, `RecordedCountTests`, `AppendOnlyRecordTests`, `ReVerificationIndexTests`, `HazardIndexTests`, `SuiteCoverageTests` and `BuildConfigurationTests`. **No full run and no gate**: they wait until the next version's editing is done. Fragment count 1130 -> 1131 (item A); unchanged by the sweep.

## 2026-09-23 ~19:30 -- WRITER RETURNED: BRANCH next AT cd7d3e8, PUSHED (e3973d4 drivers into build/ + test; 02ef391 the sweep: 310 files, 8,979 replacements, 105 anchors, 209 links, 14 explicit old-slug anchors so sealed links resolve; cd7d3e8 README/CLAUDE.md say v1.1.0, descriptor-cache sentence narrowed with today's count 3,749 / 26 product). master untouched at 08e1ac1. No gate, as decided; filtered closing pass 103/103.

OPEN: Q245 the three unswept classes (680 ellipses, 22+19 curly quotes, 1 NBSP across 127 files; REC sweep all and extend the scan, or leave) / Q246 the table "no evidence" token became "-" (already an accepted spelling; tally unmoved; REC keep) / scope call: QUESTIONS.md was swept (284) though not listed; reverts on his word. Verified by the root session: next == origin/next == cd7d3e8; drivers present under build/; em dashes remain only in sealed places.

## 2026-09-23 ~20:00 -- MAINTAINER: THE "NO TRACE OF AI" RULE BECOMES A REPOSITORY DIRECTIVE AND THE SWEEP COVERS BOTH HALVES; SEALED DOCUMENTS MAY BE EDITED FOR IT. Q246 = a.

MAINTAINER (verbatim): "How come the index only mentions 3 sessions but you reported 22? What is happening here? / Q245 Ensure there is no trace of AI both in wording and character use. Both as a directive and as part of the sweep. / Q246 a / When it comes to no semantic differences and only removing traces of AI (both in wording and character use) then I hereby grant and instruct you the right and instruction to edit sealed documents."

=> (1) Directive: "Ensure there is no trace of AI both in wording and character use." goes into CLAUDE.md as a repository rule: the character half is the scan (em dash, en dash, ellipsis, curly quotes, NBSP) over every tracked text file with only verbatim captures excluded (docs/evidence, third-party, LICENSE and notices, quoted error/log/window text, positive-control strings); the wording half needs a reader and says so. (2) The grant: sealed records (docs/reviews bodies, released CHANGELOG sections, docs/ledger snapshots) are edited for this purpose only, with no semantic difference, seals re-recorded once, the grant quoted where the seal rule lives. Verbatim maintainer quotes, quoted error text, code, commands, names, numbers, dates and claims never change. (3) Wording pass: four Opus readers produce exact before/after edit lists over the 94 markdown files (38,318 lines) in .work/wording/; one writer applies them. Code comments (~57,572 comment lines in .cs/.ps1/.sh) get the character half now; the wording half over comments is posed as Q247. (4) Q246a: the table token stays "-". QUESTIONS.md stays swept (no revert word). docs/reviews/README.md joins the sweep.

## 2026-09-23 ~21:15 -- THE CHARACTER HALF IS COMPLETE AND THE DIRECTIVE IS A REPOSITORY RULE. `60d341c` + `b4cb6fe`, pushed to `next`.

**PASS TWO: 3,539 characters in 144 files** (pass one was 8,979 in 291): em -> `--` 2,572 · ellipsis -> `...` 897 · en -> `-` 24 · curly double 20 · curly single 17 · cell token 5 · quoted token 4. **By area: sealed records 2,814 · unsealed prose 474 · code and config 251.** NBSP: the only one in the tree was a positive control and stayed.

**THE ELEVEN SEALS RE-RECORDED** in the same commit as the edit that moved them (three CHANGELOG sections, eight review bodies). **THE GRANT IS QUOTED WHERE THE RULE LIVES** -- `AppendOnlyRecordTests`' remarks, `docs/reviews/README.md`, `docs/ledger/README.md` -- dated, with what moved and what did not, and with the sentence that it is narrow.

**293 more anchors moved, all inside ledgers and reviews, and NOTHING links to them** -- checked by resolving every fragment link in the tree against the map, never assumed. The 14 explicit old-slug anchors from pass one are gone; the sealed sections' links point at the new anchors directly.

⚠️ **THREE THINGS THE SWEEP BROKE, ALL CAUGHT BY MECHANISMS, ALL WORTH KEEPING:**
1. **An XML comment may not contain `--`.** `BrowserAI.TestProbe.csproj` stopped loading and MSBuild refused the project -- **a red no test reaches, because nothing compiles.** It is a comma now.
2. **Two `ChangelogTests` controls went quiet**: the pair asserting the body scan reads around a code span held a literal ellipsis, which the sweep turned into three full stops, so the `IsNotEmpty` half started asserting nothing. Built from its code point now.
3. The scan's own file and controls carried the characters, again -- the same trap as pass one.

**THE SCAN IS WIDENED** to five classes over every non-binary file the walk reaches. Exclusions are exactly the verbatim captures (`docs/evidence`, `third-party`, `LICENSE`, `THIRD-PARTY-NOTICES`) plus `TestResults`, which the layout prunes at the root and not below it. **A dated record is no longer excluded and the arm asserts that in both directions.** Planted red on a doctored `TESTING.md` and watched.

**THE DIRECTIVE, `b4cb6fe`**: the character half is a mechanism row pointing at the scan; the wording half is in the needs-a-person list, with what a reader looks for in his terms and the rule that reading and writing are separate jobs. RELEASING points at it and keeps its own paragraph as the narrower case.

**FILTERED RUNS ONLY**: 115/115, then 67/67. No full run and no gate.

⚠️ **WORDING LIST 1 IS IN HAND AND DELIBERATELY NOT APPLIED** -- `.work/wording/1-changelog-reviews.md`, two edits (`CHANGELOG.md:6264` "brings" -> "carries"; `docs/reviews/2026-08-24-adversarial-since-the-mode-drop.md:399` "essentially every" -> "almost every"). The coordinator's instruction is that it lands in the same commit as the other three lists, re-sealing as needed. **The second edit is inside a SEALED body**, so that commit owes a reseal.

## 2026-09-23 ~21:30 -- WORDING LIST 2 RECEIVED AND HELD. NOTHING APPLIED. Branch `next` still at `b4cb6fe`.

**`.work/wording/2-root-docs.md`, four edits, held with list 1 until lists 3 and 4 are named:**
- `DECISIONS.md:427` "covering essentially every option" -> "covering nearly every option"
- `HAZARDS.md:6` *The overwhelming majority are silent* -> *The great majority are silent*
- `HAZARDS.md:202` "the scheduler option that sounds more robust" -> "...more reliable"
- `README.md:8` the tagline split into two sentences at *and*, no word removed

⚠️ **`.work/wording/` ALREADY HOLDS `4-questions-ledgers-docs.md` TOO**, which the coordinator has not named yet. Only **list 3** is outstanding. Nothing from any list is applied: the instruction is one commit for all four, and list 1's second edit is inside a SEALED review body, so that commit owes a reseal.

⚠️ **A CLAIM THE READER FOUND AND CORRECTLY DID NOT TOUCH -- Q248 below, and it is the maintainer's.** `README.md:8` calls the runtime **pinned**; `DECISIONS.md:126` is headed *"The pin is an output, not an input"* and `HAZARDS.md:176` calls `@latest` *the float*. Two senses of one word in the three documents a reader meets first. Not wording, not a sweep, and not mine to settle.

## 2026-09-23 ~21:40 -- WORDING LIST 4 RECEIVED AND HELD. THREE OF FOUR IN HAND; ONLY LIST 3 (kb) OUTSTANDING. Nothing applied; `next` still at `b4cb6fe`.

**`.work/wording/4-questions-ledgers-docs.md`, ten edits**: eight remove a decorative star before an already-bold sentence in the two ledger snapshots; `docs/ledger/2026-09-15-release-session.md:1562` "TWO GAME-CHANGERS" -> "TWO CORRECTIONS" (the body labels them Correction 1 and 2, so the heading follows the body and not the other way round); `docs/design/icon-candidates/NOTES.md:227` *## Shortlist, if it helps* -> *## Shortlist*. **No maintainer-verbatim line is touched.** Both heading sites verified as they stand.

⚠️ **TWO OF THE TEN MOVE A HEADING SLUG** -- `NOTES.md:227` and the ledger's `:1562`. Nothing tracked links to either, but **the anchor map is re-derived AFTER applying and is never merged with the character pass's map**: the character pass already moved 398 anchors, and a stale map is how a link check passes over a fragment that no longer exists.

⚠️ **EIGHT OF THE TEN ARE INSIDE LEDGER SNAPSHOTS AND ONE IS INSIDE A SEALED REVIEW (list 1)**, so the single commit that lands all four lists owes: the reseal of `docs/reviews/2026-08-24-adversarial-since-the-mode-drop.md`, a fresh anchor map, and a note that the ledger snapshots moved again under the same 2026-09-23 grant. Ledger snapshots are not sealed by a test; the rule that holds them is the prose in `docs/ledger/README.md`, which already carries the grant.

The reader confirms the exclusion this writer already applies: **`docs/evidence/` is untouched by both halves**, because its batch READMEs record byte counts and digests *as written*.

## 2026-09-23 ~21:00 -- CHARACTER HALF DONE ON next (60d341c sweep+seals+grant+scan, b4cb6fe directive; both passes 12,518 characters, 398 anchors, 223 links; seals re-recorded 11×; XML "--" trap in a csproj comment caught). WORDING LISTS ALL IN: 1 changelog+reviews = 2 edits / 2 root docs = 4 / 3 kb = 56 (37 "worth <verb>ing", 18 "Note that", 1 intensifier) / 4 questions+ledgers+docs = 10 (8 decorative stars, 1 heading, 1 ", if it helps"). TOTAL 72 over 38,272 lines; every named vocabulary measured absent under positive controls in all four shards. LEFT ALONE BY THE READERS (his to overrule): "X rather than Y" (456 changelog/reviews, 267 kb) = the author's signature; 89 changelog bodies opening by repeating their headline (recorded 2026-09-16 as deliberate); the per-finding rubric labels in reviews; the kb marker vocabulary (check marks, stars, no-entry). WRITER RESUMED TO APPLY ALL 72 + reseal + fresh anchor map + push.

OPEN: Q247 code-comment wording (57,572 comment lines; REC documents only now) / Q248 README tagline "pinned" vs the charter's "frozen at build time" (REC b, one word).

## 2026-09-23 ~22:00 -- THE WORDING HALF IS APPLIED. `84fe851`, pushed to `next`. 72 of 72 rows, none refused.

| List | Applied | What |
|---|--:|---|
| 1 changelog + reviews | 2 | one intensifier, one stock filler |
| 2 root docs | 4 | "essentially", "overwhelming majority", "sounds more robust", README's tagline split at "and" |
| 3 kb | 56 | 37 *worth <verb>ing* frames, 18 imperative "Note that" lead-ins, one intensifier |
| 4 questions + ledgers | 10 | 8 decorative stars, "TWO GAME-CHANGERS" -> "TWO CORRECTIONS", "## Shortlist, if it helps" -> "## Shortlist" |

**Every BEFORE verified to occur exactly once before replacement; every line number still correct as a cross-check -- ZERO drift**, although the character pass had moved seven kb articles. The four wrapped clauses went in as pairs.

⚠️ **TWO SEALS RE-RECORDED, AND THE SECOND IS A TRAP WORTH CARRYING.** `CHANGELOG.md#0.1.0` grew by one character and printed its own replacement line. `docs/reviews/2026-08-24-adversarial-since-the-mode-drop.md` got **shorter** ("essentially" -> "almost", five characters), and **a record that shrinks takes `Explain`'s TRUNCATED branch, which prints NO replacement line** -- so collecting reseal lines by pattern silently misses it. The seal was obtained by temporarily shortening the recorded length so the reseal branch fired. Written into `docs/reviews/README.md`.

**TWO ANCHORS MOVED**, both predicted: `NOTES.md` *Shortlist* and the 2026-09-15 ledger's heading. **The map was derived FRESH against HEAD and not merged with the character pass's** -- a stale map is how a link check passes over a fragment that no longer exists. Nothing links to either; 0 fragments rewritten, checked and never assumed.

`docs/ledger/README.md` records the second move of both snapshots under the same grant, with the count corrected to nine ledger edits (eight stars plus the heading; the tenth row is `NOTES.md`).

**LEFT ALONE, on the readers' findings:** `README.md:8` keeps **pinned** -- a claim, not wording, and **Q248** for the maintainer; the sparkle in `kb/toolchain.md` is inside a fenced block quoting GitHub's rendered HTML; the check marks, stars and no-entry marks across the kb are a marker vocabulary; `docs/evidence` untouched by both halves.

**Filtered runs: 115/115 green.** No full run; the gate still waits. **`next` = `84fe851`; `master` = `08e1ac1`.**

## 2026-09-23 ~21:30 -- WORDING HALF LANDED: 84fe851 on next, pushed; 72 of 72 edits applied, zero refused, zero BEFORE mismatches; two reseals (CHANGELOG#0.1.0 3,873->3,874; the 2026-08-24 mode-drop review 32,486->32,481) with the trap that a shortening record takes the TRUNCATED branch and prints no reseal line, now noted in docs/reviews/README.md; two anchors moved, zero links affected. Branch next = 6 ungated commits over master 08e1ac1; the gate owed before merge = one run per shell through build/Invoke-OrdinaryGate.ps1 and build/invoke-ordinary-gate.sh. Verified by the root session: next == origin/next == 84fe851; tree clean; em dashes remain only under docs/evidence (excluded record captures).

## 2026-09-23 ~22:15 -- MAINTAINER: Q247 b (+ rules so it never recurs), Q248 = new tagline on the main features, Q249 c (parallel Opus writers), Q250 b, Q251 b. FIVE WORKTREE WRITERS DISPATCHED; THE INTEGRATOR FOLLOWS.

MAINTAINER (verbatim): "Q247 b - I want to be done with it and I want rules so we don't have to do this ever again. / Q248 Not what I want to focus on in a tagline. The tagline should contain the browserai's main features such as autoamtic updates and session management etc. The pinning feature is a minor feature that can easily be misunderstood. / Q249 c - spawn opus subagents to do this work. / Q250 b / Q251 b"

PLAN: five Opus writers, each in its own git worktree OUTSIDE the repository (C:\Source\SixFive7\BrowserAI-wt-<A..E>) on a branch next-<A..E> off next, disjoint file sets, comments only in code (never string literals), no heading text changed (headings carrying the frame are listed for the integrator), quoted text of any kind untouched, docs/evidence + third-party + licences untouched, no test runs in the worktrees. A = root markdown (incl. CHANGELOG: the 89 headline repeats removed with the stitch; README tagline rewritten around the main features; "rather than" rewritten) / B = kb + docs markdown (kb/README legend for the check mark, star and no-entry marks; "rather than") / C = src comments / D = tests comments (never AppendOnlyRecordTests or the seal record) / E = build, .claude, docs/probes scripts. Each pushes its branch and removes its worktree. THEN the integrator (the long-lived writer) merges the five into next, changes the listed headings with a fresh anchor map, re-records every seal once, adds the never-again mechanisms (a house-rule scan refusing the tell vocabulary incl. the "rather than" frame in prose and comments, with quoted/captured text excluded, planted red; a changelog test that a detail does not restate its headline, planted red), writes the rules into CLAUDE.md with his words, runs the filtered checks, pushes next. No gate until the editing is done.

## 2026-09-23 ~23:35 -- ALL FIVE SHARDS BACK, BRANCHES PUSHED, WORKTREES REMOVED. INTEGRATOR RESUMED.

A next-A 0dba71e: 1,368 rather-than rewrites in root docs; 103 changelog headline repeats removed (not 89); new README tagline (Q252 posed to him); 10 other tells; headings left: README.md:40, RELEASING.md:1550, TESTING.md:957; 39 left in quotes/fenced; fragment count falls by 2 (two duplicate links left with the repeats). B next-B 820ce51: 784 of 829 in kb+docs; legend of the four marks in kb/README.md; headings left: 9 rather-than + ledger 2026-09-15:497 "Near-miss worth remembering"; tied cells kb/re-verification.md:436 (row 120 title = detection.md:476 heading); RECIPE.md:335 and the truncation-prompt review :335 share a heading and move together; refusals kb/mcp/sdk.md:118 (quoted at :399); 5 review bodies changed length. C next-C c1eb509: 937 in src comments + 13 tells; non-comment bytes identical; no headings; run RecordedCountTests.TheUpstreamVariableCountInTheDocCommentIsWhatRowSeventeenSays early. D next-D 4b1b530: 1,626 in tests comments + 14 tells; AppendOnlyRecordTests.cs untouched (it holds the seal array; 6 rather-than remain there for the integrator); refusal BrowserIdleTimerTests.cs:437 (live sentence quoted by a dated correction); 8 were in .csproj/.txt so the scan must read every file type. E next-E 08b20f2: 165 in build/.claude/probes scripts + 8 tells; 11 left in string literals/quotes; XML "--" trap respected.

TOTAL rather-than rewritten by the shards: 4,880. Integrator: merge, headings + fresh anchor map, reseal (3 changelog sections + the review bodies), build, filtered checks, the never-again mechanisms (tell scan over every tracked text file with quoted/captured text excluded; changelog no-restate test), CLAUDE.md rules in his words, delete the five branches, push next.

## 2026-09-24 ~00:40 -- INTEGRATION DONE. Five shards merged, headings changed, seals re-recorded, two never-again rules in. `next` = `34cd5e8`, pushed; the five shard branches are deleted here and on origin.

**THE MERGE**: A, B, C, D, E in order, **no conflict in any of them** -- the disjointness was checked before the first merge and the overlap set was empty. 354 files.

**FOURTEEN HEADINGS** rewritten as plain statements, including the tied pair (`kb/windows/detection.md` and row 120's title in `kb/re-verification.md`) and the two near-duplicate `Caveats` files moved identically. **13 anchors moved, 9 links followed**, map derived **fresh against the merged HEAD**.

**ELEVEN SEALS RE-RECORDED, ALL ELEVEN MOVED.** ⚠️ **Harvested by parking every recorded length at 1** so each took the reseal branch: five records got SHORTER, and a short record takes the TRUNCATED branch and prints **no** replacement line. Collecting the complaints would have re-recorded six and left five silently broken. Written into the class's remarks.

**THE SCAN**: `HouseRuleTests.NoMaintainedProseCarriesATell` over every non-binary tracked file, reading **commentary and never code** via the new `Harness.Commentary` lexer. ⚠️ It had to be a lexer: a URL in a string literal starts with two slashes, and reading those as comments reported **114** offences where the real number was **2**.

**47 residual occurrences rewritten** so the scan starts green -- **27 in CAPITALS**, which the shards' lowercase sweep could not see, plus `assets/README.md` (no shard owned it), a JSON value, an XML comment and a pragma comment. **Two survive, named**: `kb/mcp/sdk.md` and `BrowserIdleTimerTests`, each quoted verbatim by a dated correction.

**TWO PHRASES DROPPED FROM THE MAINTAINER'S LIST, WITH THE REASON**: *unlock* (16 plain uses in a product built on locking) and *underscores* (9 uses of the character's name in a repository whose slug rule turns on it). **Two narrowed** to the comma form: "that said," and "additionally,".

⚠️ **A GAP STATED IN THE TEST**: the product's own refusal text is a string literal, so it is outside the scan. `SessionErrors` and `SessionManager` still carry the frame in words a caller reads.

**CHANGELOG ARM**: `NoEntrysDetailOpensByRestatingItsHeadline`, budget four shared opening words; shard A had removed 103 of these. **Both arms planted red and watched.**

**Build** `-warnaserror` **succeeded**; **filtered runs 135/135**; fragment count **1131 -> 1129**, the fall of two shard A predicted. Q252: the tagline stays. **Q248 (`pinned`) is still open and is a claim, not wording.**

## 2026-09-24 ~00:55 -- INTEGRATION DONE: next = 34cd5e8, pushed; master untouched at 08e1ac1; shard branches deleted. Five merges, no conflict; 14 headings, 13 anchors, 9 links; 11 seals re-recorded (all eight review bodies had moved, not five); build -warnaserror green; 135/135 filtered; fragment count 1131 -> 1129. NEW MECHANISMS: HouseRuleTests.NoMaintainedProseCarriesATell (lexes commentary out of .cs/.ps1/.sh/.js and project files; the frame + imperative Note lead-ins + "that said," + "additionally," + 31 stock phrases; "unlock" and "underscores" dropped as undecidable by rule; exclusions: straight-double-quoted text incl. previously clauses, code spans, <c>, fenced blocks, blockquotes, docs/evidence, third-party, licences, TestResults, code outside comments, two named allowances kb/mcp/sdk.md:118 and BrowserIdleTimerTests.cs:437; paragraphs joined before exclusions); ChangelogTests.NoEntrysDetailOpensByRestatingItsHeadline (budget four shared opening words). Both planted red and watched. 47 residual hits + 6 lead-ins fixed by the integrator (27 in capitals). CLAUDE.md carries the wording row + the narrowed needs-a-person entry.

Q248 CLOSED by the tagline rewrite (README:8 no longer says "pinned"; the word remains only in the Install and licensing claims at 224/409/625/646). Q252: tagline unchanged unless he says otherwise. Q253 POSED: the product's own words to a caller (string literals: SessionToolSurface.cs 14, SessionErrors.cs 12, ServerInstructions, ProxyLog messages) still carry the frame; string literals are behaviour and outside the scan by design. Remaining "rather than" in the tree: 418 in 140 files = third-party + docs/evidence + product strings.

## 2026-09-24 ~01:20 -- Q252 = b, tagline approved (his words: "Look good use that."). Q253 still open.

TAGLINE (verbatim, approved): A Windows MCP server that gives an AI agent a real browser through Playwright's own MCP server, keeps each session in a directory that outlives the agent and can be resumed or handed to another, and ships as one installer that updates itself.

## 2026-09-24 ~01:30 -- CORRECTION FROM THE MAINTAINER: "Sorry I meant Q253 b not Q252 b." => Q253 = b (every person-facing string swept, tests updated, scan extended to those literals by file). The tagline approval stands ("Look good use that."). Writer continues from the tagline commit into Q253.

## 2026-09-23 ~22:15 local (20:15Z) -- CORRECTION OF THREE STAMPS ABOVE, by addition: the entries headed "2026-09-24 ~00:55", "2026-09-24 ~01:20" and "2026-09-24 ~01:30" were written on 2026-09-23 at about 20:55, 21:20 and 21:30 local; the root session derived local time from the heartbeat's UTC ticks with the wrong offset (this machine is UTC+2). Nothing in those entries changes except the date and hour. The daily drift check for 2026-09-23 was already stamped in drift-check.json; a read-only re-check taken 20:08Z found NOTHING MOVED on all five upstreams and the vendored SQLite row (@playwright/mcp 0.0.82; playwright-core 1.64.0-alpha-1789764292000 as 0.0.82's exact dependency, npm latest 1.63.0 and next 1.64.0-alpha-2026-09-23 untracked by design; ModelContextProtocol 2.2.0; Velopack 1.2.158; node v24.21.0 Krypton, with v22.23.3 Jod published 2026-09-23 on the older LTS line as a date-order near-trap the positional predicate avoids; sqlite 3.53.4 / 2,946,650 B / SHA3-256 628a44cf...27934e). No write-back owed today; the 2026-09-24 check is owed after local midnight if work continues.

## 2026-09-23 ~22:45 local -- MAINTAINER CHALLENGES THE UPDATE-BLOCK EXPLANATION AND ANSWERS THE TRIAGE IN PART.

MAINTAINER (verbatim): "Wait, the existence of sessions holds an update? That makes no sense. I'd expect many agents to forget closing sessions meaning any old stale session on disk will block updates forever. Are you sure this is right? / t1 leave it on the todo for now / t2 I do not understand this issue. Also, what even is an arm? Explain this all to me. / t3 your recommendation / t4 I do not understand what this all is. Explain it. / t5 I do not understand what this all is. Explain it. / t6 a - Only I drive when there is a release. You can only suggest to me when it is a good moment. / t7 I do not understand what this all is. Explain it. / t8 a / t9 a + d + check upstream if it is fixed already. Also check if there are newer upstream versions. / t10 what do you mean fold it into row 121. What row? How is that a solution? / t11 leave it on the todo for now."

=> Clarified to him: nothing on disk blocks the update; only RUNNING BrowserAI.Server.exe processes do (live markers the kernel holds for a live process), and each Claude Code session keeps one alive for its whole life; 27 today. Q254 posed: a staged update can wait indefinitely behind long-lived idle sessions; product directions offered. T1 stays. T3 = b + c (standing rule "no generated code ships" into DECISIONS; a test keeps CsWin32 out of src; item dropped). T6 = a, DECIDED with his words: the no-automated-checks arrangement stands; only the maintainer drives a release; agents may suggest a moment; item leaves TODO into DECISIONS/RELEASING. T8 = a (drop). T9 = a + d + a reader checks upstream (issue 42717 status, whether a released or alpha build carries the fix, newer upstream versions). T11 stays. T2, T4, T5, T7, T10 explained again in plain words; answers pending.

## 2026-09-23 ~23:05 local -- Q254 RESEARCH ORDERED; FOUR NEW ASKS BROUGHT IN SCOPE; TRIAGE ANSWERS T2/T4/T5/T7/T10.

MAINTAINER (verbatim): "Q254 I'm leaning towards keeping the servers online during normal usage. But only stopping the servers if there are no browsers and sub processes running and only if there is an update available. But before we decide on that I need to know how claude code deals with that. Best scenario would be if it just relaunced the mcp server if it dies when and if it needs it again. But I am afraid it will not. maybe we can nudge it by closing with a friendly message explaining the in flight update and asking it to restart? Can we research that first before I make a decission? Research this in depth now. / I also have some extra asks that touch this behaviour so I will brind these in scope now: - I want the system level registration and unregistration to be an option in the gui. - I want the system level and repo level registration to also work for codex and not only for claude code. - I easy I want separate control over system level registration between codex and claude. - I want the same update effects to be tested on coded. / t2 tag everything ASSUMED now and then start measuring and researching to get the number to 0. I want the rule to be that this number needs to remain zero. Add a test to check if it is zeo. / t4 b / t5 a / t7 answer the three questions. research the answer to the question of safe removal. identify the risks and then tell me the conclusion and ask me again. / t10 c"

=> Researcher R1 (Opus, read-only, probes in scratch only): Claude Code's and Codex's behaviour when a stdio MCP server exits idle (docs, changelog, issues, and MEASURED with a dummy server under a scratch config, never the real one); whether a pre-exit notification reaches the model; Codex's registration model (global config.toml, project-level), how Codex spawns and stops servers; the config app's registration features today vs the four asks. Researcher R2 (Opus): T7 descriptors: writer and readers in playwright-core at the pinned alpha, the item's three questions, safe-removal measurement in scratch, risks, conclusion. TRIAGE OUTCOMES for the next writer batch: T2 tag all 27 ASSUMED + a test that the count is zero (red until settled) + the measurement work to reach zero; T3 b+c; T4 b; T5 a (ordered relay on the ChildLink seam, planted red); T6 a decided with his words; T8 drop; T9 a+d (re-verification row keyed on the browser revision; item re-stamped with the reader's findings); T10 c (item folded into row 121). T1, T11 stay. T7 awaits R2.

## 2026-09-23, Q253 = b -- the product's own strings, and the scan's second corpus

FOUR COMMITS ON `next`, pushed. Head `6233fc3`; `origin/next` agrees.

- `b4e7dce` src/** -- 70 literal lines in 21 files.
- `7f58f29` build/** + .claude/hooks/** -- 14 literal lines in 8 files.
- `6817165` tests/** -- the second corpus, the three planted reds updated, three elisions swept.
- `6233fc3` CHANGELOG.md + CLAUDE.md -- two [Unreleased] entries, the row corrected.

84 changed lines over 30 files in total, 74 of them carrying the frame.

THE SECOND CORPUS: `Harness.Commentary.LiteralsOf` + `HouseRuleTests.IsProductVoice` +
`TellsIn` (split out of `TellOffences`). Planted red once per shape, 7 of 7; an eighth plant
in `build/BannedSymbols.txt` was caught by the COMMENT pass, which is the proof the two
corpora do not overlap. The `.mjs` shape stayed green until the lexer learned that a script's
single quotes and backticks are strings.

VERIFICATION: `dotnet build` warnings-as-errors clean; both slices republished; filtered set
of eleven classes -- HouseRule, ErrorCatalogue, ModelSurface, SessionList, SessionTool,
ToolVerdict, Changelog, RecordedCount, DocumentationLink, AppendOnlyRecord, HazardIndex --
139 of 139 green, log at `.work/suite/q253-set.log`. Filtered runs are iteration, never
verification; NO FULL TWO-SHELL GATE HAS BEEN RUN on this work.

REFUSED: `upstream-review.json`, `drift-check.json`, `tool-verdicts.json` (a reviewer's
reasoning and dated checks), `build/payload/package.json` (a dependency manifest), and the
suite's own harness messages (about 60 lines; `SuiteEnvironment` 6, `DesktopHeapProbe` 4 and
so on). All named in `IsProductVoice`'s remarks.

LEFT AS DATED RECORDS, quoting what the product said on the day: `CHANGELOG.md:3085` (a
released section), `docs/ledger/2026-09-15-release-session.md:400`,
`docs/reviews/2026-08-26-post-course-correction.md:481`.

FINDING FOR THE MAINTAINER: `NoTextFileCarriesACharacterAPersonDoesNotType`'s `IsAControl`
reads the two neighbouring characters to recognise a C# character literal, so the same shape
inside a COMMENT is admitted too. It hid three ellipsis characters (now swept). One admitted
occurrence remains and is legitimate: `ModelSurfaceTests.cs:408`, an em dash inside a
previously clause quoting an old comment about byte costs.

STILL OPEN WITH THE MAINTAINER: Q248 -- `README.md:8` says "pinned" while `DECISIONS.md`
heads "The pin is an output, not an input".

## 2026-09-23 ~23:40 local -- NEW ITEM (his words): "Running interactive browsers and login in prompts me with a 'do you want to save this password' browser popup. Can we disable that behavior on both browsers and add a test for that?" => Q255. Researcher dispatched to reproduce the popup in headed sessions on both provisioned browsers (chromium 1246, firefox 1549/156.0) through the payload, find the switch or profile preference that suppresses it, verify, and propose the test (detection keyed by the browser's pid, never by name). The writer implements after the triage batch.

## 2026-09-24 ~00:15 local (22:15Z) -- Q254 RESEARCH IN (.work/q254-2026-09-23/, FINDINGS-INDEX.txt). T7 RESEARCH IN (.work/t7-2026-09-23/). ASSUMED docs+src REPORT.md on disk; kb reader still out.

Q254 MEASURED @ Claude Code 2.1.281 with a local API stub (byte-exact tool_result capture): a stdio server that exits cleanly is transparently relaunched on the next tool call, 3/3 in every scenario (same session via --mcp-config, via `claude mcp add --scope user`, idle exit before the first call, --continue, fresh -p). The relaunched server gets initialize + tools/call and NO tools/list; its new instructions are ignored (old tool list and instructions persist). notifications/message is dropped (Claude Code declares no logging capability); stderr reaches only --debug-file. A relaunch that fails to start yields `MCP server "probe" is not connected` (is_error) and NO second attempt for the rest of the session. At exit Claude Code taskkills the server tree /T /F. Docs say stdio is not reconnected automatically; the onclose handler clears the cache so the next call re-dials (undocumented, not established when it began). Issue #82746 (open) is about a wedged-alive server, not a clean exit; #75674 closed not-planned = the same file-handle update problem in Anthropic's plugin updater. CODEX 0.155.0-alpha.9.2: NEVER relaunches (3/3 both scenarios; "Transport closed" to the model; no recovery until a new codex process, incl. `codex exec resume --last`); logs the notification and stderr to its own log only; hard-kills at exit. Codex config: $CODEX_HOME/config.toml [mcp_servers.NAME]; project .codex/config.toml only in TRUSTED projects; `codex mcp add` has no --scope. PRODUCT TODAY: user-scope register/unregister buttons EXIST in the GUI (hidden unless Absent/OursAndStale), Register-in-project exists, NO project unregister; Codex appears nowhere; UpdateService already exits+applies when Alone (StagedButNotAlone otherwise). DIRECTIONS: D1 client-gated idle exit (clientInfo.name == claude-code) [REC, with D4's ordering: idle servers exit without applying; only the last applies]; D2 unconditional; D3 relay outside current\ so nothing blocks the swap; D4 tie to stdin EOF (dormant restores never close stdin); D5 nudge only (closed by measurement: no channel reaches the model except a tool result, and the model cannot restart a server). OPEN Q for him: breaking Codex sessions acceptable?; old tool list after relaunch across an update (never remove/rename a tool in a patch? accept old names? HAZARDS row); the unstartable window during the swap; Codex repo-level = owning a TOML merge vs DECISIONS:752.

## 2026-09-24 ~00:40 local -- Q255 RESEARCH IN (.work/password-2026-09-23/). ASSUMED kb REPORT.md being written; docs 43,011 B and src 28,593 B on disk.

Q255 MEASURED: Chromium 154.0.8037.0 shows "Save password?" (Chrome_WidgetWin_1) 3/3 headed AND creates the same window invisibly headless; upstream's --password-store=basic/--use-mock-keychain do not suppress it; `credentials_enable_service:false` seeded in Default/Preferences suppresses (profile.password_manager_enabled is a dead key at 154, absent from chrome.dll); `--enable-automation` suppresses 3/3, but upstream APPENDS --disable-blink-features=AutomationControlled to config args unless present, so the round-trip test needs both entries written. Firefox 156.0 is already quiet because the provisioned build's playwright.cfg:342 sets signon.rememberSignons=false (a browser-build property that rolls with browsers.json); positive control with rememberSignons:true shows the doorhanger (MozillaDropShadowWindowClass, empty title); headless Firefox creates no HWND for it. DIRECTIONS: Chromium A args (REC) / B seed Preferences (outside the round-trip doctrine) / C / D / E; Firefox F write the pref via firefoxUserPrefs (REC) / G row / H hazard; test I Chromium headless arm + Firefox round-trip arm (REC) / J / K / L. Test shape: TopLevelWindows before/after keyed by handle, pid set from the job, control child as the clock, POST precondition, title never asserted (display language). Tangential: navigator.webdriver is TRUE at chromium 1246 (row 109 stale); any BrowserAI arg containing "--disable-blink-features" silently drops upstream's AutomationControlled switch.

## 2026-09-24 ~00:55 local -- MAINTAINER (verbatim): "Q254 I feel we are missing something. No way codex has no way to reconnect. Research more and get back to me." => the Q254 researcher resumed: codex source at the installed tag and main (mcp_connection_manager, rmcp client, TUI, app-server) for every reconnect path; turn-boundary measurement in ONE Codex process via app-server or an interactive session (the earlier runs only covered an exit inside one turn); /mcp and `codex mcp` at runtime; config reload; whether the notification or stderr reaches the model or the TUI. Q254 decision waits.

## 2026-09-24 ~01:05 local -- MAINTAINER (verbatim): "About the system level codex registration. I have a feeling that editing the codex sessions is not recommended. Research what the recommended path is. There must be something for this. Don't they have a store or such?" => a second Codex researcher dispatched: the official registration paths (`codex mcp add` and what it writes, user vs project, trust), the plugin/marketplace system (the plugins repository Codex clones, manifest, whether a local exe can be declared, how third parties publish), the MCP Registry, the app-server protocol's MCP methods, and directions for BrowserAI with the charter consequence of each.

## 2026-09-23, the triage batch: T8 T6 T3 T10 T9 T4 T5 T2 + the control fix

NINE COMMITS ON `next`, all after Q253's four.

- `4f59477` T8 -- the declined no-clobber ask leaves TODO.md; both hazard rows hold the record.
- `bfc8ce9` T6 -- only the maintainer drives a release; RELEASING item 14 + DECISIONS by addition.
- `6810311` T3 -- no generated code ships becomes a DECISIONS row; ForbiddenDependencyTests arm.
- `3eacad1` T10 -- the network-service watch folds into re-verification row 121.
- `2f5b3d8` T9 -- the webp watch re-stamped; row 138 keyed on the browser revision.
- `ca2b28c` T4 -- every review entry adjudicates every golden snapshot; reverification unbuilt.
- `6b6ee85` T5 -- relayed notifications leave in arrival order, on the ChildLink seam.
- `a12d5e4` the control fix -- IsAControl asks the lexer whether it is in code.
- `3e4998d` T2 -- 27 ASSUMED markers + the zero arm, RED ON PURPOSE.

PLANTED REDS WATCHED: T3 (a CsWin32 reference under src/, named by file and line); T4 (one
snapshot line deleted, named by entry and file); T5 (relay unsequenced -- 11 of 12 rounds out
of order, first was 1,8,4,14,3,16,6,5,10,11,9,12,7,2,13,15); the control fix (a doctored
comment in ProtocolSplitTests.cs); T2 (the 27 themselves).

VERIFICATION: 235 arms over 19 classes, 234 green, 1 red -- and the red is
RecordedCountTests.NoClaimInTheTreeIsStillMarkedAssumed, which is red by design until the 27
are settled. Both slices republished. Filtered runs are iteration, never verification; NO FULL
TWO-SHELL GATE has been run on this work, and one cannot pass while the zero arm stands.

FRAGMENT COUNT re-measured twice through the scan: 1129 -> 1126 -> 1123.
RE-VERIFICATION TALLY re-counted: 138 rows, 147 lines.

ONE DISCREPANCY TO SETTLE: the ledger records the maintainer's instruction ending "check if it
is zeo" and the coordinator's relay ends "check if it is zero". CLAUDE.md and the test quote
"zero".

## 2026-09-24 ~01:15 local -- TRIAGE BATCH LANDED: next = 3e4998d (T8 4f59477, T6 bfc8ce9, T3 6810311, T10 3eacad1, T9 2f5b3d8 + row 138, T4 ca2b28c, T5 6b6ee85 ordered relay planted red 11/12 wrong, IsAControl lexer a12d5e4, T2 3e4998d: 27 ASSUMED markers, NoClaimInTheTreeIsStillMarkedAssumed RED BY DESIGN until settled). 234/235 filtered green. Writer resumed to settle the 27 from the three REPORT.md files and implement Q255 (A+F+I).

## 2026-09-24 ~01:35 local -- CODEX REGISTRATION RESEARCH IN (.work/codex-registration-2026-09-24/). Q258 POSED.

Premise corrected: ~/.codex/sessions/ holds transcripts; registration touches config.toml, which OpenAI documents as editable. Installed codex-cli 0.155.0-alpha.9.2 is NOT on PATH (desktop-app MSIX); discoverable via %LOCALAPPDATA%\OpenAI\Codex\chrome-native-hosts-v2.json -> ~\.codex\plugins\.plugin-appserver\codex.exe; npm latest 0.156.1. PATHS: `codex mcp add` writes [mcp_servers.<n>] user-only ("Added global MCP server"), idempotent (3 adds exit 0; remove of absent exit 0), lossy only for comments inside the mcp_servers region; hand-edit user config (documented); project .codex/config.toml hand-written, trusted projects only (measured: absent when untrusted); undocumented: CODEX_HOME=<repo>\.codex codex mcp add writes the project file by Codex itself; desktop app and IDE settings UIs write the same file; plugins: `codex plugin add/list/marketplace {add,list,upgrade,remove}/remove` exist; the store clones github.com/openai/plugins; a self-hosted marketplace + `.codex-plugin/plugin.json` + `.mcp.json` registers a local Windows stdio exe with an absolute path (measured; the portable root plugin.json + mcp.json pair does NOT work in this build though the docs prefer it; 62/62 curated plugins use the .codex-plugin form); the public directory requires a public HTTPS endpoint (closed to local stdio); MCP Registry unsupported (hostname absent from the binary); app-server exposes config/value/write (+filePath, expectedVersion), config/batchWrite, config/mcpServer/reload, marketplace/* and plugin/* methods ([experimental]). Trust: [projects.'<path>'] trust_level in the user config, drive-case-insensitive, prompt "Trust this folder?"; requirements.toml allowlists by name+identity. Plugin install is always user-global. DIRECTIONS: A drive `codex mcp add` user scope (REC now) / B project via CODEX_HOME (REC now; undocumented lever) / C self-hosted marketplace plugin (REC alongside; keeps the exe path out of user config) / D write TOML (stays rejected; closer call, record why) / E app-server protocol (migration target). Startup timeout default 10 s vs first-run provisioning noted.

## 2026-09-24 ~01:50 local -- Q254 CODEX HALF CORRECTED (round 2; the maintainer was right). Codex CAN re-launch a dead stdio server: reusable_client rejects a client whose transport is closed on ANY refresh (connection_manager.rs:93-107 @ rust-v0.155.0-alpha.9.2, same on main). Nothing fires a refresh on the failure: a new tool call 3/3 no, a new turn in the same thread 3/3 no, /mcp 3/3 no (throwaway probe; shows "failed"), a config.toml edit 3/3 no (no watcher). Recovers 3/3 on: config/mcpServer/reload from an app-server host (IDE/Desktop), a settings change (cwd/permissions: TUI /cd, /permissions), a new thread (/new), and a crash exit behaves like a clean exit. Op::RefreshMcpServers has no production caller. The log notification and stderr go only to Codex's tracing log. Issues: openai/codex #16899 (open, exactly this) and #4955 (open, restart request). Withdrawn: "Codex never re-launches it"; accurate: "never on the failure; on the next refresh, and only a host can ask for one." The D1 recommendation (gate the idle exit on clientInfo.name == claude-code) stands on a milder premise.

## 2026-09-24 ~02:20 local -- MAINTAINER: "Q254 Does option c actually work? I need codex to recover without user intervention. Report back after research and testing. / Q258 Your recommendation but skip c. / T7 [questions: the Playwright dashboard, can I see it, attached tools, why our own index, 'judges other tools'] / Q256 I do not understand. / Q257 I'm leaning towards your recommendation. But I do not understand how a differs from i. Explain."

=> Q258 DECIDED: a (drive `codex mcp add` at user scope from hooks + config app, with binary discovery and a named refusal) + b (project scope via the CODEX_HOME lever) now; e (app-server protocol) recorded as the migration target; no plugin (c skipped); d stays rejected. Implementation queued after the writer's current batch, together with separate per-client control (ask 3) and the Codex update-effect tests (ask 4). Q254 c: the Q254 researcher resumed to research AND PROTOTYPE a relay/handover so the client's transport never closes across an update (how each client decides a server is closed: process exit vs pipe EOF; a helper outside current\ holding the pipe during the apply; the new server resuming the negotiated MCP state; tools/list_changed handling; cost), measured in Codex (exec + app-server same thread/next turn) and Claude Code. T7 researcher resumed to explain and open the Playwright dashboard and the attach feature, and to state why BrowserAI's session index and Playwright's server registry are different things. Q256 and Q257 explained in plain words; T7 direction still pending.

## 2026-09-24, the settlement batch + Q255

SIX COMMITS ON `next`, pushed. Head `92733e9`.

- `4e71bbb` kb -- the eight kb claims settled.
- `1dc270a` docs -- the ten top-level claims settled.
- `339d0a9` src -- the nine src claims settled, four measurements moved to kb/.
- `5c3e55e` rows -- eight re-verification rows + the GC hazard row.
- `ce9f80d` Q255 -- the password-save prompt.
- `92733e9` the Firefox arm joins stray-sweep.

THE ZERO ARM IS GREEN. TODO's count re-derived 27 -> 19 -> 9 -> 0 through the scan.

PLANTED REDS WATCHED: Q255's control arm (3 windows, then 1 with the args restored) and the
product arm with --enable-automation removed, which named Chrome_WidgetWin_1 / "Save password?".

VERIFICATION: 304 arms over 24 classes, then 53 over the five contending ones. The only red in
the whole batch was FirefoxTests' attribution arm timing out beside the new Firefox arm, which
is fixed by the stray-sweep key and green on re-run. Both slices republished twice.

COUNTS: [FLOATS] 247 -> 258; re-verification 137 -> 147 rows, 146 -> 156 lines; hazard tally
55 -> 56; CLAUDE.md fragments 1129 -> 1136.

STILL OPEN: Q256 (the crash tripwire's code fix, three directions in the src report's entry 2 --
the comment is corrected and no code changed).

## 2026-09-24 ~02:35 local -- WRITER LANDED next = 92733e9: all 27 ASSUMED settled (4e71bbb kb, 1dc270a docs, 339d0a9 src), zero arm GREEN; rows 139-146 + the GC uninstall hazard (5c3e55e); Q255 implemented with PasswordPromptTests (ce9f80d; planted red named Chrome_WidgetWin_1 "Save password?"; Firefox behaviour unestablished and said so; row 109 marked stale, row 147 added); one real red fixed (the new Firefox arm needed [NotInParallel("stray-sweep")]). Q256 untouched, open. WRITER RESUMED FOR Q258 (Codex registration a+b, per-client control, Codex update-effect coverage).

## 2026-09-24 ~02:45 local -- MAINTAINER (verbatim): "Q256 Wrap the check in its own timer. So all three timers sit in the tripwire's time. / Q257 Sorry I meant I want i. I dont understand how a differs from f in implementation and how it works other than being another browser. / T7 So let's leave these files alone. But do research if they grow unbounded. If playwright at some point cleans it up all is fine. If it does not we have to do something about it. Research this and then ask t7 again."

=> Q256 DECIDED: a check budget (<= 15 min so check + AbsoluteBudget fit inside the 45-min tripwire), its own timeout event id, planted red with a never-returning probe check; queued to the writer BEFORE the Codex batch. Q257: a + f + i already landed in ce9f80d; a = Chromium command-line switches in launchOptions.args, f = a Firefox preference through firefoxUserPrefs, both in the generated config and both round-tripped; explained. T7: files left alone; the researcher, after the dashboard, establishes whether upstream ever prunes the registry (pinned alpha and main, changelog, issues, PRs), the growth arithmetic suite vs product vs a real user, and the reader cost at 10k and 100k entries, then asks T7 again.

## 2026-09-24 ~03:05 local -- MAINTAINER (verbatim): "Q257 Ok lets go with a and f and i. But we do have the outstanding issue of the browser identifying as automated. Testable by a google search askign for a captcha. Is option a something that triggers that?" => Q259 posed to the password researcher: everything --enable-automation changes that a page or server can observe at Chromium 154 (source + measured JS-visible diff with and without the switch through the product funnel); WHY navigator.webdriver reads true at chromium 1246 in both arms (kb recorded false at 1237; row 109 stale) with the cause named; the Google search test headed from fresh scratch profiles, at most three searches per arm from his IP, plus a Playwright-defaults control; and, if a triggers detection while b does not, b against the same test. Q257 = a + f + i stands (landed in ce9f80d) pending this answer.

## 2026-09-24 ~03:25 local -- The root session relaunched the T7 dashboard demo for the maintainer (nohup, payload node, PWTEST_SERVER_REGISTRY = .work/t7-2026-09-23/reg-scratch-dash; log dashboard-demo-relaunch.log): http://127.0.0.1:61165, one demo browser with two tabs, real registry untouched (3,815). It stops on .work/t7-2026-09-23/STOP-DASHBOARD or after 3 h. BEFORE THE SESSION-CLOSE WIPE: create the STOP file first, then wipe. A page reload hangs the session list (probe without timeout); open the bare URL in a fresh tab instead.

## 2026-09-24 ~03:40 local -- Dashboard demo STOPPED at the maintainer's word (STOP file created; no dashboardApp/dashboard-demo/profile-dash process alive; real registry untouched at 3,815). The "stop before wipe" step above is no longer owed.

## 2026-09-24 ~03:55 local -- T7 GROWTH RESEARCH IN. UNBOUNDED: serverRegistry.ts at main (91eeb0c) is functionally identical to the pin; browser.ts stop() still skips delete for a persistent profile; the only unlink is inside list(); upstream's own comment "// List early to GC."; no issue/PR about growth; `playwright uninstall --all` never touches the b directory. Rates here over 7.73 days: suite ≈ 499/day (3,723), installed product ≈ 3.5/day (26); one descriptor per browser bind (+1 per idle-close-then-resume). A real heavy user: ~1,280/year, 10k in ~7.8 years; the dev machine: 10k in 20 days. Reader cost is QUADRATIC (list ≈ n²/105 ms; 2.4 min today, ~16 min at 10k, ~26 h at 100k; ready is linear ~5-7 ms/entry); the cost is Playwright's dashboard/list/attach becoming unusable, not disk. Dashboard reload hang cause: one shared SessionProvider per server; a closing connection's dispose() removes ALL listeners incl. the new connection's, so the reloaded tab never receives SessionsChanged; a fresh connection against the same server listed normally; reload/second tab effectively unsupported upstream. Demo stopped, real registry 3,815, no t7 descriptors left. REVISED REC: 5 (PWTEST_SERVER_REGISTRY for the suite, removes 99.3% of growth here) first; 1 (own descriptors at session close) as housekeeping; 4 defensible for the product; 3 ruled out; an upstream ask for a supported registry-path or bound setting (not "delete on close", retention is deliberate). T7 ASKED AGAIN.

## 2026-09-24 ~04:05 local -- Q254 OPTION C MEASURED: YES, IN THE RELAY SHAPE. Both clients decide a stdio server is gone from the PIPE (Claude Code: Node 'close' after streams shut; Codex: the rmcp transport; nothing watches the child exit), so a relay R outside the install root that never exits and re-spawns the server S across the apply keeps the transport alive: 3/3 Claude Code same session, 3/3 codex exec same turn, 3/3 Codex app-server SECOND TURN SAME THREAD with no reload, no /new, no user action; swap 308-365 ms; in-flight request recovered 3/3 by RESENDING the frame (at-least-once: unacceptable for side-effecting tools; resend only read-only tools per tool-verdicts.json, else fail retryably). The handover shape (A spawns helper H with inherited handles, exits) works 3/3 under Codex and COLLAPSES 3/3 under Claude Code (inherited stdin EOFs at A's exit; Claude Code silently re-dialled). list_changed: Claude Code re-fetches tools/list 3/3; Codex ignores it (a log line, same on main) => the tool list a Codex session sees is frozen at first connect: no tool rename/removal in a release a live session can cross (RELEASING rule). Costs: 2 processes per session; R outside the root needs its own update lane (replace on next session start); ClientLiveness.ForParentProcess -> ForProcess(clientPid) threaded through R (the type's doc already names the wrapper case); Velopack: Update.exe waits on the pid that called WaitExitThenApply (must be S, not R); force_stop_package kills everything under the root incl. browsers, so the no-browsers precondition stays; R retries spawning S until it starts (no apply-finished signal exists). Not measured: a NativeAOT relay's footprint; a real Velopack apply against a live relay. OPEN: in-flight policy; relay update lane; keep the idle precondition. DECISION HIS.

## 2026-09-24 ~04:20 local -- Q259 ANSWERED: option a does NOT make the browser identify as automated more than it already does. Headed through the product funnel, 43/43 JS-visible properties and 13/14 headers identical with and without the switch (the 14th is the ephemeral port); navigator.webdriver is FALSE in both. CORRECTION: the earlier "webdriver true at 1246" was the researcher's raw playwright-core path lacking --disable-blink-features=AutomationControlled (Playwright's own --remote-debugging-pipe sets EnableAutomationControlled; @playwright/mcp's appended blink switch is applied later and wins); kb row 109 is CORRECT, NOT stale -- the writer's stale mark in ce9f80d must be reverted. Google search test: unusual-traffic/reCAPTCHA in every arm incl. no-switch and raw defaults (8/8, interleaved, IP 188.90.181.40); the home page loads normally; the cause is not the switch (candidates: IP reputation; the Chrome for Testing build's sec-ch-ua brand "Chromium" not "Google Chrome"; Playwright's remote-debugging pipe) -- not established. Option b would be identical on the wire by construction. Q257 = a + f + i stands.

## 2026-09-24, Q256 + the Codex batch (partial)

THREE COMMITS ON `next`, pushed. Head `295425d`.

- `ec2a695` Q256 -- the check's own timer, UpdateBudgets, event 21, the webdriver correction.
- `c5fed86` Codex step 1 -- CodexRegistration, CodexRegistryView, RegistrationClient, 8 arms.
- `295425d` Codex step 5 -- the startup measurement and the mcp add option set.

Q256 PLANTED RED TWICE: with the fix removed and the probe ignoring its token the pass NEVER
ENDED (the run was killed); with the probe honouring its token, TripwireFired and no event 21.

CODEX STEP 1 PLANTED RED TWICE: the `--` separator dropped, and the project environment emptied.

MEASURED FIRST-HAND at codex-cli 0.155.0-alpha.9.2, scratch CODEX_HOME only:
initialize 332-342 ms, tools/list +7-8 ms, 79 tools, against a 10 s default. `mcp add` accepts
NO startup-timeout flag and `-c mcp_servers.<id>.startup_timeout_sec=30` FAILS with "invalid
transport" and writes nothing. The CLI is NOT on PATH here; it resolves through the desktop
manifest. Idempotent both ways. NO tmp\arg0 residue appeared.

WHAT IS NOT DONE, and it is most of the batch: step 2 (the hooks register both clients, each with
its own record entry), step 3 (the GUI: per-client rows, project unregister, the re-register
affordance), step 4 (the real-client arms under a scratch CODEX_HOME, the SuiteCapability row, the
update-effect arm, the HAZARDS row for no-relaunch-on-failure), step 6 (DECISIONS by addition,
README's Codex section, the CHANGELOG entries).

## 2026-09-24 ~05:00 local -- WRITER LANDED next = 295425d: Q256 done (ec2a695: CheckBudget 15 min derived from 45 - 30, awaited through the budget's token, event id 21, planted red twice incl. the never-ending pass with a token-ignoring probe); row 109 stale mark REVERTED with the cause named; Codex step 1 (c5fed86: CodexRegistration + CodexRegistryView + RegistrationClient record; discovery PATH/~/.local/bin -> desktop manifest codexCliPath -> %APPDATA%\npm; refusal names all four + the manual command; planted red twice) and step 5 (295425d: initialize 332-342 ms vs the 10 s default; `codex mcp add` accepts no startup timeout and `-c mcp_servers.x.startup_timeout_sec` fails, recorded, TOML stays rejected; no tmp\arg0 residue reproduced; row 148). NOT STARTED, deliberately: Codex steps 2 (hooks), 3 (GUI per-client rows, project unregister, re-register), 4 (real-client arms, capability row, update-effect arm, HAZARDS row), 6 (DECISIONS/README/CHANGELOG). Nothing half-built; no hook, GUI or installer path registers with Codex yet. The long-lived writer is at ~900k tokens; the remainder needs a fresh writer. HIS CALL: close the session now (gate, merge, snapshot, wipe) with Codex 2-6, the relay, T7's product half and the feature list as the next version's work, or continue Codex 2-6 first.

## 2026-09-24 ~05:35 local -- TOAST ITERATION (his ask; screenshots .work/toast-2026-09-24/v1..v3.png, rig show.ps1 + v*.xml; to persist under docs/design at close). LESSON: Windows lays all toast buttons in ONE row and never wraps, so five buttons truncate to their first word ("Revie", "Ask", "Ask", "Not", "Skip") with the emoji as a tiny icon above -- five labelled buttons cannot communicate. v2/v3: a selection input for the snooze choices (10 minutes / 6 hours / after the next reboot / not for this version) + two buttons: "Review 27 sessions" (green, opens the sessions window; the toast body click does the same) and "Ask me later" bound to the dropdown. Title "BrowserAI 1.1.0 is ready to install"; body "It installs itself once the last open session closes. 27 sessions are holding it, 1 of them from Codex. Nothing is closed until you choose."; dropdown title "If not now, ask me again". Renders cleanly at 3840x2160. Product notes: the identity line reads "Windows PowerShell" in the demo and would read BrowserAI with its icon under the app's own identity; real button icons need 16 px white PNGs for all buttons or none.

## 2026-09-24 ~06:05 local -- MAINTAINER: Q260 = a (CLOSE NOW). Q254 = NO RELAY; the toast linking to a GUI that lists the sessions, allows control and warns of the dangers. T7 leaning c (no throttle, no arm).

MAINTAINER (verbatim): "Q260 a / Q254 No relay. The toast linking to the gui that lists the sessions and allows control and informs you of the dangers feels like the right way to go. Some open questions: How will restarted codex or reconnected claude code sessions learn about the new version and that it needs to refresh the tool list? Can we save per session the last tool call's browserai version inside of browserai and refuse any call until a tool list has been called? Or is this over enginering and are there simpler ways. Or should we just accept it as tool mistmatches will most certainly trigger the llm to re-tool? what do you think? / t7 Why would it be so slow. A test suite only has a few hundred file and pipe not? Also, I want to avoid complexity. Does throtthling not mean we need to implement anything ourselves? I'm leaning towards c."

=> DECISIONS OF RECORD for the close writer to write by addition with his words: Q254: no relay (the relay measurements stay as the direction not taken, with the evidence); the update-lane design = a toast when an update has been staged and blocked (reminder scenario, dropdown default 6 h; the X means the dropdown's default; buttons "Review N sessions" green and "Ask me later"; screenshots and rig in .work/toast-2026-09-24 -> docs/design) linking to a configuration-window page that lists every open session with its client, purpose and recency, warns for Codex-hosted (no relaunch on failure) and recently active ones, and closes the selected ones. OPEN (root's recommendation recorded, his answer pending): the tool-list refresh after a relaunch -> the server sends notifications/tools/list_changed right after the handshake (Claude Code re-fetches, measured 3/3; Codex ignores it but lists fresh on every restart or new thread by construction); no per-session version stamp; the release rule about tool renames stays a hazard note. T7 = c: at session close BrowserAI starts Playwright's own list command from the payload, detached, never awaited; no throttle, no concurrency arm; a kb entry with the measurements (2.4 min once on the backlog, milliseconds afterwards; 8 concurrent callers lost no live browser; the pipe-busy false positive beyond that unmeasured, discoverability only) and a re-verification row on the Playwright version; the suite variable is not used; no upstream ask. Q258 a+b stands with Codex steps 2-6 as next-version work.

## 2026-09-24 ~06:40 local -- MAINTAINER: T7 = a (as recorded). The browser-close hint question = a: leave the model-facing text as it is (destroy is the agent's job; a browser left idle closes after 10 minutes and the agent is not told that). Q261 (stale tool list after a relaunch) still open; the root session's recommendation = b: a per-connection "listed since the handshake" flag with one informed refusal plus the list_changed notification, and the serving version stamped in the session record as a courtesy line on resume and catch-up.

## 2026-09-24 ~06:50 local -- MAINTAINER: "Q261 b". DECIDED: (1) a per-connection flag "tool list requested since the handshake"; a tools/call arriving before any tools/list on that connection is refused once with an informed message naming the running version and the remedy per client (Claude Code: the list-changed notification has been sent, retry; Codex: start a new thread), and notifications/tools/list_changed is sent with that refusal; a first connect lists before calling so new sessions pay nothing; planted red with a stub client that calls without listing. (2) The serving BrowserAI version is stamped into the session record on every call, and resume and catch-up carry a courtesy line when it differs from the running server, never a refusal. Next-version work; DECISIONS and TODO by the close writer.

## 2026-09-24 ~06:55 local -- MAINTAINER (verbatim): "MAke sure to test Q261 from a subagent once implemented and make sure to have test coverage." => Q261's acceptance, for the TODO item: (1) suite coverage: the stub-client arm (a call before any list on the connection is refused once with the informed message and the notification is sent; a first connect that lists first is never refused), a real-Claude-Code headless arm under a scratch config (server exits mid-session, relaunch, first call refused, list refreshed on the notification, second call succeeds), a Codex arm under a scratch CODEX_HOME (a new thread lists fresh, no refusal), and the resume/catch-up courtesy line when the stamped version differs; (2) at implementation time an Opus sub-agent drives the real clients against the published slice through the same rigs the 2026-09-23 research used, never the real install, and reports byte-exact results before the item is called done.

## 2026-09-24 ~07:25 local -- OVERNIGHT ORDERS (the maintainer sleeps; verbatim): "I'll be going to sleep. I want you to continue your work autonomously. Get as far as possible. If there are any questions along the way I want you to keep a record of these questions and follow your own recommendations. When I am back I will say so. Then you may present me with all the choices (if any) that have been made, the alternative answers, the reasoning for your recommendation and the consequences if I chose to chose differently retroactively. I expect that if there are good reasons I will not have to revert anything. Arm the watchdog. I'll be back after sleep. Use my system. But beware, other agents are also doing this in other projects. So do not restart my system or anythign crazy like that." and "Oh and for large bodies of work. Call opus subagents not fable. I do not want to burn all my tokens on work that was simple enough for opus. / 1 my machine does not go to sleep by itself. / 2 ok / 3 ok / 4 its ok"

=> NIGHT PLAN (after this close lands): a NEW ledger and a new branch for the next version; one Opus writer at a time; order: Q261 (flag + refusal + notification + version stamp + courtesy line; stub, real Claude Code and Codex arms; sub-agent verification against the published slice), T7 c (detached list call at session close + kb + row), Codex steps 2-6 (hooks, per-client GUI rows with project unregister and re-register, real-client arms and capability row, HAZARDS row, documents), then Q254 (toast research under the app's identity from NativeAOT without generated code, then the toast and the sessions page). After each item: two-shell gate; on green, fast-forward master and push. NO release (T6). Feature-list candidates untouched. Every question -> the ledger with alternatives, the choice, the reasoning, and the retroactive consequence; presented when he says he is back. Never touch the real install or client configs; never kill others' processes; never restart the machine; the heartbeat re-armed on every expiry.

## 2026-09-24 ~03:45 local -- THE CLOSE. SIX COMMITS, THE GATE GREEN BOTH SHELLS, MASTER FAST-FORWARDED TO cbbebbe, `next` DELETED.

**SIX COMMITS ON `next`, then the branch was merged and deleted.**

- `6973ed8` the decisions of record -- Q254 (no relay; the toast and the sessions page; the relay recorded as the direction not taken with its measurements), Q261 (one informed refusal plus the list-changed notification, and the version stamp as a courtesy line), Q258 (a+b now, e as the migration target, c skipped, d still rejected and recorded as a closer call for Codex), T7 (the detached list call with three accepted risks named), Q260 (the gate arrangement itself, into the Automated checks row). RELEASING item 4 gains the no-rename rule; HAZARDS gains the frozen-tool-list row, open, tally 56 -> 57.
- `36dfeed` the persistence pass -- seven evidence batches, one probe record, one design, six kb measurements, a new kb article for the Windows toast properties, two corrections by addition, four re-verification rows (149-152) and the index re-stamped 148 -> 152 rows, 157 -> 161 lines, 259 -> 265 markers.
- `2bf0f72` TODO gains the next version's work -- Codex steps 2-6, the toast and sessions page, Q261 with its acceptance, T7's implementation, five feature-catalogue candidates as candidates, and the four owed re-verification rows (123, 124, 126, 130 at Velopack 1.2.0 against a build resolving 1.2.158).
- `690fc0a` the changelog -- five [Unreleased] entries for the subjects that had none: Codex registration, the gate drivers, the no-trace-of-AI directive, the first documents a reader meets, and this session's own record-keeping.
- `cbbebbe` the one red the gate found, fixed with a planted red (below).
- and the close commit that carries this snapshot.

**THE GATE. PowerShell 807/0/0 `UPPER C:\` FULL RUN 3m25s (03:35:24 -> 03:38:49); Git Bash 807/0/0 `LOWER c:\` FULL RUN 3m21s (03:39:08 -> 03:42:29).** Both halves: publish freshness FRESH, every capability PRESENT, *no test took a degraded path*, clearance byte-identical either side of each run. Both slices were republished first and **neither produced an ILC, trim or AOT warning**, so HALT-A never fired.

⚠️ **IT WAS RED ONCE, AND THE CLAIM WAS NEVER WRONG.** The first PowerShell half came back 807/1/0: `UpdateTests.ACheckThatNeverAnswersEndsOnItsOwnBudgetAndSaysSo` at **1,054 ms against a 600 ms bound**, with every other assertion in it green -- the check budget fired, event 21 was logged, event 11 was not. The arm divided the product's four update budgets by **4,500**, which puts the outer bound below the scheduling noise of this machine running the whole suite, so it passed every filtered run it was written against and went red in the first full one it met. **The factor is 450 now** (2 s / 4 s / 133 ms / 6 s), one factor still dividing all four so the orderings stay the product's own. **Watched red at the new factor** with the check budget parked at 30 s: the pass ran 7.4 s past the 6 s tripwire and went red on the ATTRIBUTION assertion, with the elapsed bound behind it.

**THE MERGE.** `master` fast-forwarded 08e1ac1 -> **`cbbebbe`**, 86 commits, pushed. `next` deleted locally and on origin. There is no merge commit: the fast-forward makes the branch tip the merge hash.

**WHAT WAS PERSISTED, AND WHAT WAS DISCARDED.** Seven evidence batches (client-reconnect 715 files, password-prompt 341, assumed-justifications 91, codex-registration 87, server-registry 18, playwright-surface 12, instances 4), one probe record (the dashboard demo rig), one design (the three toast renderings, cropped to the toast because the captures carried an unrelated project's editor window). **Discarded and said so in the batch READMEs**: 234 MB of openai/codex checkouts (cited by tag and commit), a 1 MB slice of claude.exe, 1.4 GB of plugin-store clone, two large fetched registry indexes, every bin/ and obj/ under six rigs, the browser profile trees, and the 28 full-desktop screenshots. **Five departures from the bytes as taken**, each named where it applies: every request capture trimmed of a third-party client's system prompt, this repository's own CLAUDE.md and the tool schemas, with the SHA-256 of all 69 originals kept; a cookie jar deleted; one public address redacted in 33 files; ANSI escapes stripped from five terminal captures; eighteen fetched documents and rigs renamed to `.txt`.

**ONE CONTROL WAS RE-EXPRESSED AND PLANTED RED**, and it is named here because changing a test to make a run pass is the shape this repository distrusts. `HouseRuleTests.NoTextFileInTheTreeCarriesAControlByte` asserted that under **20** files read as binary, which is a proxy for *the classifier did not call everything binary* and which the image records outgrew the day they landed. It is a **proportion** now, a tenth of what was scanned, watched red with `IsBinary` forced true.

**DEFAULTS TAKEN WITHOUT ASKING, both recorded so they can be overruled.** (1) The three toast renderings were **cropped**; the originals are not kept, because each carried a large area of an unrelated project's session transcript and the crop contains the whole toast. (2) The **daily drift check for 2026-09-24 was not run**: `drift-check.json` still reads `lastChecked: 2026-09-23`, which is the truth, and stamping a date without a lookup is what that file's own honesty rule forbids. No release is being cut, and the 2026-09-23 re-check found nothing moved on all five upstreams and the vendored row.

**STATE AT THE CLOSE.** `master` = `origin/master` = `cbbebbe`, tree clean, 807/0/0 both shells. `[Unreleased]` accumulates for the next version. Q248, Q252, Q253 and every question this session opened are closed or decided; what is open is [open design decision 3](../../DECISIONS.md), the dash style for the repository, which the maintainer deferred on 2026-09-22 and which nothing in this session touched.
