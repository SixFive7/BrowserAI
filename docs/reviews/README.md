<!-- SPDX-FileCopyrightText: 2026 Jori Huisman -->
<!-- SPDX-License-Identifier: LicenseRef-BrowserAI-FSL-1.1-MIT-5yr -->

# Reviews

Findings that are **too long to inline and too valuable to lose**. Everything
here was produced in `.work/`, which is gitignored — these are the copies that
survive a clean.

**These are records, not work lists.** Anything actionable is lifted into
[`TODO.md`](../../TODO.md) or [`HAZARDS.md`](../../HAZARDS.md); a finding that
lives only here is a finding nobody will act on.

| File | What it is |
|---|---|
| `2026-08-18-adversarial-locking.md` | Adversarial review of session locking and provisioning. Four wrong-answer findings, thirteen `lock.json` readers enumerated, and the probe-before-gate redesign attacked before it was built |
| `2026-08-18-adversarial-processes.md` | Adversarial review of process supervision, containment and attribution. Fourteen findings, four Tier-1 |
| `2026-08-18-truncation-findings.md` | What Claude Code's *"2KB each"* actually means, measured off the client's own outbound API request |
| `2026-08-18-truncation-prompt-for-sibling-project.md` | The same, written self-contained for another MCP server's maintainer. Includes the full re-run recipe |
| `2026-08-19-auth-transfer-and-session-modes.md` | Can a human's interactive login be handed to an unattended headless run? Measured yes, on one route of three — and copying the profile directory is **not** it. Also three claims this repository makes about its own modes that the code does not make |
| `2026-08-24-adversarial-since-the-mode-drop.md` | Adversarial review of the seams where the 2026-08-20/24 changes — the mode removal, the injected `why`, the durable action log, `browserai_catch_up`, the reader/writer maintenance lock and the instance-directory marker — meet the code that predates them. Nine findings, and fourteen things attacked that held |
| `2026-08-26-post-course-correction.md` | Narrow adversarial re-review of `118776e`..`4d4b34e` — the P0–P6 course correction, which replaced `browserai.json` with a SQLite record and a write-once guard, deleted artifact routing and BrowserAI's own `filename` gate, turned the tool allowlist into a file, and made path canonicalisation one function — and every seam where those changes meet the code that predates them. Read-only; every session it opened was destroyed. **Seventeen findings, eight queued leads answered with verdicts, and a do-not-touch list of what was attacked and held.** It carries **no editorial change at registration**: it was written with zero Markdown links deliberately, so the shape `DocumentationLinkTests` reads was final before the move and the body is byte-identical to what was handed over |
| `2026-08-24-adversarial-narrow-since-the-six-fixes.md` | Narrow adversarial re-review of the two commits that answered the review above — the six fixes it landed, plus the run's new `filter` row and the release refusal built on it — and where they meet code that predates them. Read-only and source-level, every claim carrying `file:line`. Thirteen findings, four leads it could not close read-only, and thirty things attacked that held. **It carries one editorial change made at registration, named in its own opening note**: a pointer shape written as an inline Markdown link inside a code span, which `DocumentationLinkTests` reads as a relative link into this directory |

**Every adversarial review here states what it tried to break and could not**,
which is the half that makes the rest trustworthy. A review that reports only
findings is indistinguishable from a shallow one. *(Previously "Both adversarial
reviews state what they tried to break and could not" — written when there were
two, and there are five.)*

## What has been acted on, as of 2026-08-26

**The review files are not edited when a finding is fixed** — they are dated
records of what was true when they were written, and rewriting one would destroy
the only account of the reasoning. This table is where the status lives. `git log`
carries the fix; [`HAZARDS.md`](../../HAZARDS.md) carries what is bounded rather
than closed.

⚠️ **That was prose until 2026-08-20, and prose did not hold it.** A rename sweep
reached these files and rewrote them; it was caught by a human reading the diff.
`AppendOnlyRecordTests` now seals every file in this directory **except this
one** — by character count and SHA-256, on the prefix, so an addendum may be
appended and a body may not be rewritten. This index is deliberately *not*
sealed: the status table above is meant to move. See
[the release gate](../../TESTING.md#the-dated-records-are-append-only); a new
review is registered in the seal list in the same change that adds it.

| Finding | State |
|---|---|
| locking **A1** — `destroy` releases the lock before deleting the tree | **fixed**, `SessionDestroyTests` |
| locking **A2** — `AcquiredAbandoned` deletes a complete, in-use tree | **fixed**, `ProvisioningTests` |
| locking **A3** — `reinstall` deletes with no provisioning mutex | **fixed**, `ReinstallBrowserTests` |
| locking **A4** — path aliasing gives one directory two gates | **closed by CANONICALISATION 2026-08-26** *(previously "closed by refusal 2026-08-19 — `SessionDirectoryGuard` refuses an aliased spelling at `init` and `resume`")*. The refusal was never the whole close: it ran at two doors, so `destroy`, `set_purpose`, `catch_up` and every forwarded call still reached the identity chain with an unresolved spelling. `Sessions/CanonicalPath` resolves every alias this machine can build into the filesystem's own name at every door, and `CanonicalPathTests.EverySpellingOfOneDirectoryIsAdmittedUnderTheOneIdentity` asserts the whole claim — admitted **and** one identity — where the old arm could be satisfied by a refusal. A4's own recommended fix was this one. One of its four aliases was measured away in 2026-08-19: `Path.GetFullPath` **does** expand 8.3 short names on .NET 10 ([kb](../../kb/windows/detection.md#a-mapped-drive-letter-is-a-network-path-and-costs-the-same-22-seconds)). **The two gaps stay open as hazard rows** — the unopenable ancestor and the door-not-a-guard remap — and neither is closed by canonicalising |
| locking **A5** — `SessionIndex.Sweep` deletes a live session's entry | open, hazard row; the kb claim it falsified is corrected |
| locking **A6** — `init`'s `Existing` guard acts on a transient absence | open, hazard row |
| locking **B1** — the gate is outlasted by the waits inside it | **fixed**: the sum is asserted and the gate re-sized |
| locking **B2** — unbounded calls inside the gate, incl. UNC | **network half closed** 2026-08-19, the rest open. The caller-visible decision was taken: a network session directory is refused, **by semantics rather than by spelling** — a mapped drive letter costs the same measured 22 s and passes every string test |
| locking **B3** — `LiveInstances` shares the per-directory mutex namespace | **fixed** 2026-08-23, `UpdateTests.TheLiveSetsGateCannotCollideWithASessionOpenedOnTheInstallRoot`. Still live when triaged: nothing refuses a session on the install root, so the collision was exactly reachable. The live set has `Global\BrowserAI-Live-` of its own now, and [`LockScopes`](../../src/BrowserAI/Sessions/LockScopes.cs) documents four scopes where it documented three — the undocumented fourth is how the name came to be shared |
| locking **B4** — same-process `destroy` racing `set_purpose` leaks the lock forever | **fixed** 2026-08-24, `SessionLock._inProcess` — a per-session lock `Rewrite`, `Append`, `ReleaseAndDelete` and `Dispose` all hold for their whole body. The maintainer took direction 1 over 3, and *not* 2, which narrows the window without closing it. **The deterministic interleaving the triage doubted was possible turned out to be the product's own delegates:** `update` runs past the disposal check and under the gate, `delete` runs after the handle is closed and before it is released, so both threads are placed at named instants rather than raced. `SessionLockTests.ADisposalArrivingDuringARewriteWaitsForItInsteadOfLeakingTheHandle` and `.AMutationArrivingDuringAReleaseAndDeleteWaitsForItRatherThanReadingAFieldItIsWriting`, both watched red first |
| locking **B5** — instance-directory liveness rests on the surface child alone | **real, recorded, not started** — hazard row, **and it is the same finding as processes 11**, which is itself a triage outcome: two reviews found it independently and the index carried it as two items. `Claim` is still `Directory.Move`, `Sweep` still judges on the directory's own mtime, and the BrowserAI process still holds nothing in its own instance directory |
| locking **D** — the probe-before-gate redesign | not adopted; the verdict stands as written |
| processes **1** — the client watch acts on a bare pid | **fixed**, `ProcessLivenessTests`, plus a mechanism for the next one |
| processes **2** — `TreeDelete` follows directory junctions | **fixed**, `TreeDeleteTests`; the ban message now names both properties |
| processes **3** — `RevisionPrune` decides on a stale census | **narrowed**, hazard row; the held handle was declined with a reason |
| processes **4** — a junction above the install root empties the sweep's candidate set | **real, recorded, not started** — hazard row. `BrowserProcesses` still matches `QueryFullProcessImageNameW`'s reparse-resolved answer against `Path.Combine`-composed strings, and there is still no `candidates=0` tripwire. **It got cheaper without getting done:** `VolumeIdentity.FinalNameOf` arrived 2026-08-19 and is what would canonicalise the wanted set — but this decides what the sweep may *terminate*, which is the one area the charter is most conservative about |
| processes **5** — concurrent launches cross-inherit pipes | **fixed**, `JobContainmentTests` |
| processes **6** — the job handle crosses `CreateProcessW` with no ref-count | **taken 2026-08-23, and it is this tree's one named exception to the planted-red rule** *(previously "real, recorded, not started, and the reason is unusual … That trade is the maintainer's")*. The maintainer took the trade. `ProcessAttributeList` now calls `DangerousAddRef` **before** it reads the raw value, holds the `SafeJobHandle` so the object cannot be collected either, and releases in `Dispose`, which the `using` runs after `CreateProcessW`; the misplaced `GC.KeepAlive(job)` moved to the line after the launch. **The behaviour change has no failing test behind it and the exception is recorded in [`CLAUDE.md`](../../CLAUDE.md)** rather than left implicit — what is asserted is the source-level rule, `HouseRuleTests.EveryRawHandleThatOutlivesItsExpressionIsRefCounted`, watched red against both real escapes |
| processes **7** — the sweep's remediation is one `TerminateProcess` | **declined, with the reason** — the tree teardown it asks for would mean ending helpers this pass could not attribute, and *never terminate on a guess* is the rule the whole class is built on. Attribution failing while detection holds is the designed outcome, and `unattributable` is reported by full path on every pass. **What is recorded instead is the sentence**: `SweepLog.Terminated`'s *"Terminated a stray browser"* reads as a tree dealt with when at most one process was |
| processes **8** — the title guard rejects the UNC *spelling*, not UNC *semantics* | **fixed** 2026-08-23, `StraySweepTests.AMappedNetworkDriveTitleIsRefusedThoughItIsSpelledLocally`, against a real mapped drive. **It was expensive when written and cheap when triaged**: `VolumeIdentity` arrived 2026-08-19 for the session directory's own refusals and answers *is this letter a network volume* with no filesystem call at all, so the guard still cannot pay the 22 s it exists to prevent. Only the network is refused — a `subst`ed or absent letter is local-speed and refusing it would narrow what the sweep can see. ⚠️ **And it was still open one layer down until 2026-08-26**: a letter `subst`ed onto a mapped drive passed, because the question stopped at the first hop. `StraySweepTests.ATitleOnALetterSubstitutedOntoAMappedDriveIsRefusedToo`, watched red on `Expected to be false but found True`; `VolumeIdentity.Through` follows the chain, and `GetDriveTypeW` on such a letter was measured that day at **21,035 ms** before answering `DRIVE_NO_ROOT_DIR` ([kb](../../kb/windows/detection.md#getdrivetypew-through-a-subst-onto-a-mapped-drive-costs-the-full-21-seconds--measured-2026-08-26)) |
| processes **9** — `NativeFile.Append`'s completion loop breaks per-call atomicity | **fixed 2026-08-24**, and by none of the four remedies it was recorded as needing *(previously "real, recorded, not started — hazard row … It is a decision rather than a fix … Directions are in `TODO.md`")*. All four repaired a loop whose premise was that the design is lock-free; the maintainer changed the design. The shared log is now written under a cross-process byte-range claim (`NativeFile.TakeGate`), taken one byte past any possible end of file so that no concurrent reader is locked out, with the length read, the instant stamped and the bytes written inside it. **Under a real lock there is no per-call size bound and nothing can interleave**, so the whole `FILE_APPEND_DATA`-without-`FILE_WRITE_DATA` machinery was deleted rather than repaired — and no truncation and no new record-length limit were needed, which is what every recorded direction cost. `ProcessLogTests.EveryRecordInTheSharedLogSitsInTheOrderItWasWritten` |
| processes **10** — `FILE_SHARE_DELETE` lets anything unlink the live process log | **fixed 2026-08-24** *(previously "real, recorded, not started — hazard row. The share mode is unchanged")*. The flag is now an argument each caller states: withheld for the machine-wide log, granted for a session's own `browserai.log`, which `browserai_destroy` must be able to remove under a live session. **The cost the decision named is real and was accepted rather than mitigated, and it is stated where it is decided**: the MACHINE's central log — not one process's own file — cannot be deleted or renamed while any BrowserAI runs, `SweepExpired` included, and that pass now tolerates the refusal. `ProcessLogTests.TheSharedLogCannotBeUnlinkedWhileAWriterHoldsIt` holds the refusal and the reader that must still get in |
| processes **11** — the instance-directory liveness test rests on the surface child's cwd | **the same finding as locking B5** — see that row. Recorded once, in one hazard row, rather than carried twice |
| processes **12** — a title over 32,768 characters is silently truncated | **declined, with the reason, written where it happens** — a truncated title still has to resolve to a directory whose `browserai.json` can be taken, so a prefix fails that test and lands in the *refuse* direction. The property is now stated in `MessageWindows.WindowText`'s own remarks, because the review's point was that the premise *everything here fails safe* deserved to be checkable by a reader rather than rediscovered |
| processes **13** — `WaitForExitAsync` waits over a handle another thread may close | **taken 2026-08-23, in the same decision as 6** *(previously "real, recorded, not started … the fix is small and cannot be planted red")*. The reference is taken before the non-owning `SafeWaitHandle` is built and released after the registration is unregistered, so `Dispose`'s release is no longer the one that closes the handle. `GC.KeepAlive(handle)` was retired rather than kept beside it: it covered collection, this covers disposal, and the finding was right that those are different problems. Under the same recorded exception; this file's escape is the one the new `HouseRuleTests` arm named when it was watched red |
| processes **14** — stdout: third-party code on a background thread | **closed at the referenced version, and half of it closed by accident.** The stray sweep now starts at `Program.cs:202`, *before* `StdioChannel.OpenStandardStreams()` — the review had it after. The half the review could not trace is now traced: `VelopackUpdateClient` builds its `UpdateManager` with no logger, which resolves to `VelopackLocator.Log` = *the logger this product set* or `NullVelopackLogger`, and **`ConsoleVelopackLogger` is constructed by nothing in the library** (read at Velopack 1.2.0). `[FLOATS]` — it is a fact about a dependency version |
| since-the-mode-drop **F1** — `browserai_list` reports `in use: no` for a session being driven right now | **fixed 2026-08-24**, `SessionListTests.ASessionWhoseGateIsHeldByAPeerIsReportedUnknownRatherThanFree`, planted red on `in use: no — nothing held …`. Every forwarded browser call rewrites `browserai.json`, which drops the ownership handle and takes it back with the per-directory gate held throughout — so a busy session's record is periodically *present and unheld*, and the bare probe read that as free. `SessionLock.ProbeLivenessUnderTheGate` asks the same question with that directory's gate held at a **zero** timeout, so a gate it could not take is `UNKNOWN` rather than `no`. **It gates `browserai_catch_up` as well as `browserai_list`** — one opener, two callers, and two tools printing different `in use:` lines about one session in the same second is exactly what `ProbeForHolder`'s own remarks exist to prevent. ⚠️ The zero-timeout half **could not be planted red**: a 120-second gate acquire would still return inside the rig's patience and the test above would still pass. `SessionLockTests.TheListingProbeTakesTheGateWithoutWaitingForIt` is the source-level guard for it and is **weaker than a red test**, and says so in its own remarks |
| since-the-mode-drop **F2** — page content can trigger the one answer-rewrite, which drops the artifact record and the note | **fixed 2026-08-24, both halves, and the second is the one that closed the bypass.** The rewrite branch returned before `Complete`, so a call whose answer tripped it pinned no name, recorded no artifact and carried no note — and upstream builds the `Error`, `Page` and `Events` sections into **one** result, so an error answer carries the page's own title beside the console and snapshot pointers. That branch now runs `Complete` like every other answered call: `ArtifactPointerTests.APointerSurvivesAnAnswerThatAlsoTrippedTheProvisioningRewrite`, planted red on the missing note. The scan is also gated on `isError`, which upstream sets from the presence of an `Error` section and nothing else (verified in the bundle 2026-08-24 @ `@playwright/mcp` 0.0.79): `ProvisioningRemediationTests.APageQuotingUpstreamsAdviceInASuccessfulAnswerIsForwardedUntouched`, planted red on BrowserAI's instruction text arriving inside ordinary page content. ⚠️ **The gate does not close the bypass on its own and is not claimed to** — the test for half 2 sets `isError` deliberately so half 1 cannot make it pass  ⚠️ **2026-08-26: half of the evidence no longer exists.** `ArtifactPointerTests.APointerSurvivesAnAnswerThatAlsoTrippedTheProvisioningRewrite` was deleted with artifact routing — there is no record, note or pin for a rewrite to drop. The `isError` gate stayed and is what still holds, plus a real-child canary over upstream's own wording (`ProvisioningRemediationTests.ARealChildWithNoBrowsersStillSaysTheSentenceTheRewriteIsAnchoredOn`).|
| since-the-mode-drop **F3** — a log entry's argument **name** is uncapped, un-flattened and replayed into another agent's context | **blocked on a maintainer decision, not started.** The fix is settled in shape — sanitise the name through `Flatten` before the withhold check, and bound how many arguments one entry records — but both halves need a **number**, and the numbers change a published bound rather than restore one. `LockRecord`'s own remarks and `QUESTIONS.md` say the record is *"250 × (400 + a handful of 200-character values) ≈ 400 KB"*; that was never a worst case, and with caps in place the real worst case is computable and is **not** 400 KB. The analysis put three options on the table — 64/32 ≈ 2.4 MB, 64/16 ≈ 1.3 MB, 32/8 ≈ 0.6 MB — and recommended 64/32, with the note that 32/8 puts `browser_cookie_set`'s eight properties *exactly on* the count cap. **The decision is which pair, and whether the published bound is restated as a measured worst case.** Nothing in `LockRecord.Of` was touched  ⚠️ **MOOT 2026-08-26 — the premise was removed.** A log row carries the tool name, the caller's `why`, an outcome, two instants and a failure payload; it carries **no arguments at all**, so there is no argument *name* to sanitise, cap or count. `LockRecord` itself is gone. **The published 400 KB bound went with it and was not replaced by a bigger one** — there is no cap anywhere in the record now, which was a separate decision taken with the store.|
| since-the-mode-drop **F4** — `browserai_destroy` and `browserai_set_purpose` take the per-directory gate without the boundary checks | **fixed 2026-08-26, and the split the row proposed was NOT taken** *(previously "blocked on a maintainer decision, not started … **The decision is whether the guard is split**, so that `destroy` and `set_purpose` get the cheap half without inheriting the expensive one")*. The maintainer's answer was **b — both halves, everywhere, uniformly**, and the row's own framing turned out to be the expensive one: the alias question is no longer a refusal at all, so there is no cheap half to separate from a dear one. `ErrorCatalogueTests.EveryDoorRefusesANetworkPathAndTheDeviceNamespaceWhileAnAliasIsTakenAsWhatItNames` drives all six doors and was watched red on `destroy`, which answered *that is not a session* — after **21 seconds** against `\10.255.255.1\share`, which is the cost the split was hiding. ⚠️ **This row and `DECISIONS.md` prescribed opposite splits and a future reader would have found both**; the disagreement is resolved on both records rather than on one |
| since-the-mode-drop **F5** — any I/O failure on `reinstall.lock` is reported as *"BrowserAI is replacing the browsers … right now"* | **fixed 2026-08-24**, `ErrorCatalogueTests.AnInitThatCannotOpenTheBrowsersClaimIsNotToldAReinstallIsRunning`, planted red against a real ACL denial that produced the reinstall sentence complete with a progress clause counting from zero. `MaintenanceLock` now carries the kernel's own answer out of both takes — a sharing violation is `Contended` and anything else is `Unreachable` — and `SessionErrors.TheBrowsersRootCouldNotBeClaimed` is a **new catalogue row** rather than a clause on the existing one, because **two recoveries are two rows**: waiting clears contention and will never clear an ACL, a full volume or an unwritable profile. The census in `ErrorCatalogueTests` moves 27 → 28 and is what makes the trigger arm mandatory. **It includes `TheRootIsBusy`'s half**: a census of zero over a file nothing could open was concluding *another reinstall has it*, so the unreachable arm is now answered **before** `LiveSessions()` is consulted. The contended sentence is deliberately left unhedged |
| since-the-mode-drop **F6** — a malformed `tools/call` argument answers `{"code":-32603,"message":"An error occurred."}` with no catalogue entry | **blocked on a maintainer decision, not started.** A kind-checker on `ToolArguments` is one edit; what it needs is a policy, because the answer is currently a generic SDK error for **three** caller-controlled sites rather than one, and the fix has to say what each refusal is *for a model to act on* — which is what this catalogue exists to decide. It also collides with F3 on the same three lines of `LockRecord.Of`, so the two are one edit or a sequenced pair. No kind-checker was added |
| since-the-mode-drop **F7** — a site-named download plus the monotone `_published` set defeats artifact sorting | **fixed 2026-08-24, and the review's own recommended fix was declined with the reason.** Requiring the pinned name to look generator-produced would move every real, site-named download out from under upstream's own pointer — upstream writes `- Downloaded file <name> to "./<name>"` into the answer, verified in the bundle 2026-08-24 and recorded in [kb](../../kb/playwright/tools-and-artifacts.md). What was actually defective was **the match** (an undelimited `Contains`, so `report.pdf` was pinned by an answer that only said `quarterly-report.pdf`) and **the set** (monotone across *files*, so a name pinned once pinned every later file sharing it, for the session's life, with no answer naming it). `ArtifactPointerTests.AFileWhoseNameOnlyOccursInsideALongerOneIsStillSorted` and `.APinnedNameIsNotInheritedByALaterFileThatHappensToShareIt`, both planted red. ⚠️ **The residue is stated rather than closed**: a short, word-shaped name a page renders in prose is still delimited and still pins, and the harm is bounded to classification inside the session tree  ⚠️ **MOOT 2026-08-26 — the mechanism was removed**, and the named tests were deleted with it. Nothing sorts, so nothing can sort a download out from under upstream's own pointer, and the stated residue is gone with the rest.|
| since-the-mode-drop **F8** — a cancelled `tools/call` leaks the in-flight filename reservation for the session's life | **fixed 2026-08-24**, `ArtifactRoutingTests.ACancelledCallGivesItsReservedNameBackSoTheRetryIsNotSuffixed`, planted red on the retry coming back as `login-2.png` with the answer reporting a rename that no file on disk justified. `AnswerToolsCallAsync` is wrapped in a `try`/`finally` from the plan onward and the three `Release` sites are folded into it. Releasing after a successful write is safe because `Taken` also tests `File.Exists`. ⚠️ **The test asserts cancellation only and says so** — the idle-timer scope and the remediation regex's 1,000 ms match timeout are covered by the same `finally`, are not deterministically reachable, and a test that provoked one by timing is the promptness assertion this suite forbids  ⚠️ **MOOT 2026-08-26 — the mechanism was removed.** There is no reservation set to leak; the named test was deleted with it.|
| since-the-mode-drop **F9** — every `browserai_list` fully parses every session's log on the machine, then filters | **fixed 2026-08-24**, `HouseRuleTests.NoIndexWalkFiltersBySubtreeAfterFollowingTheEntry`, planted red on the two real offenders — `SessionManager.List` and `SessionManager.Beneath`. The subtree filter ran after `SessionIndex.Follow` had opened and strictly parsed each record, and each of those opens inherits `RenameWindow`'s budget, so one denied record anywhere could add it to a call scoped elsewhere; **the roll-up did it on every `init` and every `resume`**, which the review does not name and is the sharper half. `SessionIndex.FollowUnder` applies the prefix above the open. ⚠️ **The behavioural half could not be planted red and that is a property of the fix**: the reported set is bit-identical before and after, so there is nothing to differ. The header-only-read fork *could* have been planted red, precisely because it changes what is reported, and it was declined for that reason — two readers of one file that disagree is the failure the tree already has a rule against. `SessionIndexTests.FollowingOneSubtreeReturnsExactlyWhatFollowingEverythingWouldHaveReturnedForIt` and `.FollowingOneSubtreeOpensNoRecordOutsideIt` ship with the change, name an API that did not exist, and say in their own remarks that they are weaker than a red test |
| narrow-since-the-six-fixes **W1** — the pointer scan compares a **JSON-escaped** answer against a **raw** file name, so an ordinary `AT&T.pdf` is never pinned | **blocked on a maintainer decision, not started.** The child's result is serialised with no `JsonSerializerOptions`, therefore `JavaScriptEncoder.Default`, which escapes `&`, `'`, `+`, `<`, `>`, backtick and every non-ASCII character as `\uXXXX`; the scan then looks for that string inside the raw name off disk. **The positive control is two hundred lines away in the same file** — the artifact index already sets `JavaScriptEncoder.UnsafeRelaxedJsonEscaping`, with a comment saying why. Pre-existing rather than a regression, since the old undelimited `Contains` had it too, but F7 rewrote the function and its new remarks reason explicitly about the serialised form without reaching the escaping of the name, so the comment now reads as though serialisation had been accounted for. **Held with W2**: both are in the artifact *pinning* machinery, and the decision they wait on is what the downloads folder should be  ⚠️ **MOOT 2026-08-26 — the mechanism was removed.** `ArtifactRouter.NoteWhatTheAnswerPublished`, `NamesTheFile`, `IsNameCharacter` and the `_published` set are deleted with the whole of artifact routing: BrowserAI does not read the child's answer, so there is no escaped-versus-raw comparison left to be wrong. **The decision this was held for is the one that dissolved it** — nothing moves a file, so nothing needs to know which files an answer names. Recorded rather than struck out: the finding was correct, and what closed it is a deletion rather than a fix.|
| narrow-since-the-six-fixes **W2** — the new eviction can drop a name that is still pointed at, under two concurrent `tools/call`s on one session | **blocked on the same decision as W1, not started.** F7 made the pinned set non-monotone, and the eviction is what introduces this: the sweep removes every name absent from the enumeration it took at its own top, and the publish and the sweep take the router's gate separately, so a sweep that snapshotted before a peer call published can drop that peer's name — and the next sweep then moves the file out from under a pointer the caller has already been given, which is verbatim the defect the mechanism exists to close. Before `dbf1346` the set was monotone and this could not happen. ⚠️ **One link is open and the review says so**: whether the MCP SDK dispatches two `tools/call` frames concurrently was not read, and if it serialises them this is unreachable  ⚠️ **MOOT 2026-08-26 — the mechanism was removed**, with W1 and for the same reason. There is no pinned set, no eviction and no sweep, so the two-concurrent-calls race has no subject. ⚠️ **The open link it named is still open and is now open somewhere else**: whether the MCP SDK dispatches two `tools/call` frames concurrently was never read, and it still governs anything in this proxy that keeps per-session state across a call.|
| narrow-since-the-six-fixes **W3** — `ArtifactRouter.SweepOutputRoot` added to the filename reservation set and gave nothing back | **fixed 2026-08-24**, `ArtifactRoutingTests.{ASweepWhoseMoveFailsGivesTheNameBackSoTheNextSweepIsNotSuffixed, ASortedFileTheCallerDeletesStopsHoldingItsName}`, both watched red on `Expected to be true but found False` against `downloads\report.pdf`, which was `report-2.pdf`. Both halves were defects. A `File.Move` that **threw** left a name held with no file behind it, so the next sweep of the same loose file landed it as `-2` and reported `renamed:` — *the answer reporting a rename that nothing on disk justifies*, the exact symptom the reservation set was extracted to remove, produced by the reservation set. A move that **succeeded** held the name for the session's life, so a sorted file the caller deleted went on suffixing every later download of that name, while `ArtifactRouter.Release`'s own new remark claimed the opposite for every reservation. The release is a `finally` around the move; `File.Exists` holds the name after it. **The failed move is provoked with a directory standing where the file must land**, which is deterministic rather than timed  ⚠️ **MOOT 2026-08-26 — the mechanism was removed.** `SweepOutputRoot` and the reservation set are deleted, and both named tests with them. ⚠️ **What the deletion re-opens is stated where it now lives**: nothing suffixes a colliding name any more, so a second file with a name already taken OVERWRITES the first — a hazard row of its own, and `FlatOutputTests.ASecondFileWithTheSameNameOverwritesTheFirstAndNothingSuffixesIt` is what keeps that stated rather than discovered.|
| narrow-since-the-six-fixes **W4** — the subtree filter moved above the **record** open and not above the **entry-file** open, which carries the same `RenameWindow` budget | **corrected 2026-08-24 as documentation; the exposure is left where it is.** `SessionIndex.FollowOne` still opens every index entry file on the machine through `RenameWindow.WaitOut`, once per entry, above the subtree test — so one denied or delete-pending entry file anywhere on the host still adds up to 30 s to a `browserai_list` scoped to an unrelated tree, and to the roll-up on every `init` and every `resume`. Only the *parse* moved. `SessionIndex` and [`ARCHITECTURE.md`](../../ARCHITECTURE.md) both presented the budget exposure as removed; both now say which open moved and which did not |
| narrow-since-the-six-fixes **W5** — `ERROR_LOCK_VIOLATION` is classified as a holder, and no BrowserAI holder can produce it | **the sentence corrected 2026-08-24, the behaviour deliberately unchanged.** `MaintenanceDenial`'s arithmetic remark read as a biconditional and was relied on as one: *not 32/33 ⇒ not a holder* is sound and is the direction the unreachable arm needs, and the converse is **false for 33** — `reinstall.lock` is claimed by share mode alone, and the tree's only byte-range lock is `NativeFile.TakeGate` on log files, so code 33 can only come from a foreign process and was routed to *"BrowserAI is replacing the browsers … right now"* with a progress clause counting from zero. Left as it is for the same reason code 32 was, and the reason is written where it is decided: the recovery a caller is given is right for a foreign holder too. **What was wrong was a sentence that made the misdiagnosis impossible** |
| narrow-since-the-six-fixes **W6** — `SessionIndex.Follow`'s remark claimed `HouseRuleTests` asserts three named callers keep using it, and it asserted no such thing | **fixed 2026-08-24 by making the claim true**, `HouseRuleTests.TheThreeWholeMachineIndexReadersStillTakeTheWholeMachineRead`, which names `SessionIndex.Sweep`, `SessionManager.LiveSessions` and `StraySweep.AttributeByProfileLock` and reads each member's brace-matched body. The older scan holds only that no `foreach` over the walk filters by subtree and that at least **two** such loops exist, and its own comment concedes `Sweep` is invisible to it — so two of the three were held by a `>= 2` lower bound on a loop shape and the third by nothing. Watched red twice against the real regression rather than against its control: with `LiveSessions` moved onto the subtree read both arms failed, the older one saying only `Expected to be greater than or equal to 2 but received 1`; with `Sweep` moved instead the new arm was the **only** red of the nine in that class |
| narrow-since-the-six-fixes **W7** — `SessionIndex.FollowUnder` is **not** `Follow` filtered, and the file said both things two paragraphs apart | **fixed 2026-08-24, sentence and test together.** It is `Follow()` filtered by `IsUnder` over the entries that resolved to a session, **plus** every entry that resolved to none, whatever subtree it does or does not name — an entry that is empty, relative or mis-hashed is *returned rather than dropped*, which the file also said, two paragraphs below the claim of equivalence. `SessionIndexTests.FollowingOneSubtreeReturnsExactlyWhatFollowingEverythingWouldHaveReturnedForIt` built its expectation with `entry.Session is { } session && …`, dropping exactly the divergent class, so the test the old sentence cited excluded the only case where the equivalence fails. It plants one now — mis-hashed, pointing under neither root — and was watched red at `Expected to be 2 but received 3`. Harmless in the product today, because both callers drop a null session on the next line |
| narrow-since-the-six-fixes **W8** — `FollowUnder`'s parameter contract names a producer that one of its two callers does not use | **corrected 2026-08-24 in both places rather than collapsed.** `SessionManager.Beneath` re-derives the prefix itself and never calls `SessionManager.Subtree`, skipping its `Path.GetFullPath`. Benign today, because its input is already canonical — but it is a second spelling of a derivation standing beside `SessionIndex.IsUnder`'s own new remark forbidding one, and **that remark's claim is true of the predicate and not of the prefix**. The duplication pre-dates `dbf1346`; the sentence that read as though it had been removed did not. **Collapsing it is a change to path handling and was declined on that ground** |
| narrow-since-the-six-fixes **W9** — the reservation-release record fired on every forwarded call, while its own remark said it fired only for calls that named a file | **fixed 2026-08-24**, `ArtifactRoutingTests.TheReleaseRecordNamesOnlyTheCallsThatReservedAName`, watched red on `Expected to be false but found True`. The `finally` covering `AnswerToolsCallAsync` logged *"released its in-flight filename reservation"* unconditionally, so `browser_click`, `browser_navigate` and `browser_snapshot` asserted an event that did not occur — **in a session log that is model-facing evidence**. **The record was made conditional rather than the remark corrected, because the remark was right about what the record is for.** Second-order and fixed with it: `ArtifactRoutingTests.ACancelledCallGivesItsReservedNameBackSoTheRetryIsNotSuffixed` waits on that exact string and was sound only because the screenshot happened to be its rig's first forwarded call; it names the tool and the session now |
| narrow-since-the-six-fixes **W10** — `BROWSERAI_RELEASE_RUN=1` made a filtered run a failing test only for filters that happened to select the refusing test | **fixed 2026-08-24**, `SuiteCoverageTests.AFilteredChildRunReadsAsFilteredAndIsRefusedAsARelease`, re-pointed at a method that is **not** the refusal and watched red: the child came back `total: 1 - failed: 0 - succeeded: 1 - Passed!` and exit 0 against `Expected to not be equal to 0` — a filtered release reporting success. **A refusal a filter can remove is not a refusal**, and this one could be: the guard shipped the day before as an ordinary `[Test]`, so the variable plus a `--treenode-filter` that did not happen to select that one method was a filtered run, a claimed release and **green**, while [`TESTING.md`](../../TESTING.md) stated the guarantee unqualified and nothing recorded the exception. It is raised from `SuiteCoverage.ReportWhatThisRunExercised` now — the `[After(TestSession)]` hook that writes the coverage block — after the block and the probe report are on disk, because a run that failed the way it was meant to still has to be readable. What TUnit guarantees was **measured** at TUnit 1.65.0 / Microsoft.Testing.Platform 2.3.3 rather than assumed: a session hook is registered against the session and not against a test node, so nothing can deselect it, and an exception out of one is reported as *Test adapter test session failure* and exits **10**. ⚠️ **The one limit is written down rather than implied** — a run that never starts a session is refused by nothing |
| narrow-since-the-six-fixes **W11** — the filtered child's console assertion was a tautology, under a comment calling it the decisive half | **fixed 2026-08-24, and demonstrated rather than argued.** The assertion required the child's console to contain `BROWSERAI_RELEASE_RUN`, which the `FILTERED` coverage row prints on every filtered run whether or not anything refused — and in the very console of the W10 red that string appeared **six** times while the refusal's own sentence appeared **none**. It names a sentence `SuiteFilter.Refusal` is the only producer of now. The weight was always carried by `exitCode != 0` and by `decision=Refuse` in the report file, and both of those were sound |
| narrow-since-the-six-fixes **W12** — the filtered child run clobbered the parent's `.work/suite-coverage.txt` mid-run | **fixed 2026-08-24.** `SuiteCoverage.ReportPath` was repository-rooted unconditionally, so the child wrote its own one-test `FILTERED` block over the parent's file while the parent was still running. The parent rewrote it at its own session end, so the copy a gate log appends was never wrong — but anyone reading the file during that window got the child's, and **a block whose whole job is to say what a run covered must not be readable as a statement about a different run.** `SuiteCoverage.ProbeReportFile` puts the child's block beside the child's own report now |
| narrow-since-the-six-fixes **W13** — a fifth *"takes no lock"* site the same day's sweep missed, and the missed one carried the mechanism claim that sweep corrected one file over | **corrected 2026-08-24**, in the test harness's `SessionRun` and in a sixth site in the unreleased `CHANGELOG` section. Both halves of it were false: `browserai_catch_up` **does** take the per-directory gate, through `SessionLock.ProbeLivenessUnderTheGate`, and that gate does not refuse a second writer — it waits 120 seconds, which is the reasoning the same commit corrected in `SessionToolSurface`. The arm still passes because `SessionManager.InUse` short-circuits for a session this process drives and never reaches the gate, **which is what the comment was hiding** |
| post-course-correction **1** — every BrowserAI startup opens the SQLite store of every session on the machine, leaving a `-wal` and a `-shm` beside each, and `SessionLock.TryHoldUnowned` states the opposite | **(a) the record: fixed 2026-08-26. (b) the mechanism: fixed 2026-08-26 (Q129 = a), later the same day** *(previously "pending a maintainer decision, not started")*. The record half is corrected in four places rather than one: `SessionLock.TryHoldUnowned`'s paragraph, `SessionStore`'s remarks, [`Storage/CLAUDE.md`](../../src/BrowserAI/Storage/CLAUDE.md) and [`ARCHITECTURE.md`](../../ARCHITECTURE.md), all three of which had recorded the side effect as a **caller-initiated read of the directory the caller named**. The mechanism half is **the sweep going probe-first**: `SessionIndex` walks at a stated depth — `Record` for the reporting callers, `Guard` for `Sweep`, where liveness is one `CreateFile` on `browserai.lock` and the fallback is two file checks — so **a server start opens no store at all**. `SessionIndexTests.ASweepOpensNoSessionsStoreAndLeavesACleanlyClosedOneAtTwoFiles`, watched red on `Expected "browserai.data, browserai.lock" but received "browserai.data, browserai.data-shm, browserai.data-wal, browserai.lock"` — the review's measured shape, at the mechanism rather than through a second process. **Nothing was given up:** the record was never part of the sweep's decision, and the removable set is bit-identical, which is what the guard-only reader's own remarks argue and what its handling of the legacy `browserai.json` keeps — skipping that check would have turned a legacy session into a *removable* `NotASession`. ⚠️ **One visible difference is named rather than glossed**: a `browserai.data` that exists and will not parse now reads `Session` rather than `LockUnreadable` at `Guard` depth. Both are kept, so the sweep behaves identically. The store is still opened on the entry a pass is about to act on — where there is nothing to open, because a removable entry has no `browserai.data` |
| post-course-correction **2** — `RecordText.Sanitise` keeps every supplementary-plane `Cf`, including the whole TAG block | **fixed 2026-08-26**, `SessionRecordTests.TheSanitiserDropsAFormatCharacterOutsideTheBasicPlaneAndAnUnpairedSurrogate`, watched red on `Expected to be equal to "payloadmore"` — the survivors the review measured through the published binary (U+E0001, U+E0048, U+E0049, U+1D173) put back in as the test's input. It iterated `char`, and `char.GetUnicodeCategory` answers `Surrogate` for either half of a supplementary-plane character and never `Format`, so **no supplementary-plane character was ever tested**. It enumerates runes now, through `Rune.DecodeFromUtf16` rather than `EnumerateRunes` — the enumerator answers U+FFFD for a lone surrogate and for a genuine U+FFFD alike, and only one of those is text. **The lone surrogate got its own decision and it is *drop*.** The positive control is in the same arm: a supplementary-plane character that is *not* `Cf` survives whole, so the fix cannot be "drop everything above the basic plane" |
| post-course-correction **3** — `browserai_catch_up`'s `page` is truncated to `int` unchecked, so an out-of-range page is served as a *different* page | **fixed 2026-08-26**, `CatchUpTests.APageNumberOutsideIntIsRefusedQuotingTheNumberTheCallerSent`, watched red on `Expected to be true but found False` for `isError` at the review's own wire input `page=4294967297`. `pages` is a `long` now, the bound is compared as a `long`, the refusal quotes what **arrived**, and the narrowing to `int` happens only after the bound holds. Both directions are in the arm, because a fix that only widened the comparison would still misquote: `2147483648` wrapped negative, so the refusal named a number the caller never sent |
| post-course-correction **4** — a duplicate tool name inside one half of `tool-verdicts.json` escapes the loader's named-refusal contract | **fixed 2026-08-26**, `ToolVerdictTests.OneNameWrittenTwiceInsideOneHalfIsRefusedByNameRatherThanThrownFromADictionary`, watched red on `Expected InvalidOperationException but got ArgumentException: An item with the same key has already been added. Key: browser_close` — which is what reached `Program`'s process boundary and exited 1 **naming neither the file nor the row**. `ToolVerdicts.Rows` collects names as it goes and raises the file's own `Unreadable` refusal on the second. ⚠️ **The fixture is planted in the TEXT and could not be planted anywhere else**: a `JsonObject` cannot hold two properties under one name, so the doctoring machinery every other malformed arm uses is structurally unable to express this shape, which is part of why it went unnoticed. The arm asserts that `JsonDocument` really does keep both properties, so a runtime that started collapsing them would not make it pass by asking nothing |
| post-course-correction **5** — `Remediate`'s remaining justification names `Complete`, a mechanism P3 deleted | **fixed 2026-08-26, documentation only, no red.** The acknowledged residual risk was defended by a method that went with the artifact machinery in `feec42b`; the rewrite branch returns immediately after sending. **What replaced it is the bound that was nowhere written**: `ProvisioningRemediation.Rewrite` fires only on a block containing `install-browser`, replaces only the anchored regex, and answers `null` when the replacement changed nothing — so a page that merely *mentions* the marker is forwarded byte-identical, and the worst page content can do is get BrowserAI's own two sentences substituted for upstream's exact install instruction, which is the substitution the method exists to make |
| post-course-correction **6** — `VolumeIdentity`'s "bounded call" guarantee is not what the code keeps: only the drive-letter root is judged, never a reparse point deeper in the path | **the claim is fixed and the behaviour is deliberately not — direction (a) of the three the review put up.** The three sentences claiming boundedness now say what `VolumeIdentity.Of` actually bounds, and a new [hazard row](../../HAZARDS.md#hazard-index) carries the residue, with the tally re-measured and the predicate quoted. **The cost class was re-measured on the day at 22,157 ms** for one `Directory.Exists` against a dead host. ⚠️ **The composition is NOT measured end to end and the row says so rather than implying otherwise**: this account has neither `SeCreateSymbolicLinkPrivilege` nor Developer Mode, so both `New-Item -ItemType SymbolicLink` and `cmd /c mklink /D` refused and the reparse point could not be planted at all — mechanism from the code, cost from the measurement beside it. Direction (b), one extra open per level with `FILE_FLAG_OPEN_REPARSE_POINT`, is named in the row and waits for somebody who has met the failure; (c), a watchdog, is refused outright as [a clock in a place that forbids one](../../TESTING.md#every-duration-is-a-hang-detector-or-it-is-a-defect) |
| post-course-correction **7** — `PathVerdict.Unestablished` is reachable **with a successful create**, on an ordinary machine, and nothing asserts it | **fixed 2026-08-26**, `CanonicalPathTests.TheAncestorWalkGivesUpExactlyPastItsLimitAndSaysSoWhileStillAnsweringAPath` — the control the third verdict has never had, at `AncestorWalkLimit - 2` and `AncestorWalkLimit + 2`, composed from the product's own constant so a moved bound moves the test. **No red, and the reason is stated rather than skipped**: the behaviour is correct and was simply unasserted, so there was nothing to plant. The P5 record that called it *"unreachable without `Create` failing too"* had already been corrected in place by the reviewer. **The note's shape was considered and kept**: it quotes both the ancestor the walk gave up on and the caller's own path, and the arm asserts the pairing — the ancestor alone means nothing to a caller, and the caller's path alone does not say how far the walk got |
| post-course-correction **8** — a caller-supplied control character is echoed raw into a model-facing refusal | **fixed 2026-08-26**, `ErrorCatalogueTests.ARefusalEchoesNoControlCharacterAndNamesNoCSharpParameter`, watched red on `Expected to be false but found True` against a real `browserai_init` on a path carrying U+0007. `RecordText.Escape` is the escaper and it lives beside `Sanitise` deliberately — same channel, opposite halves: one guards what goes **into** the record, the other what goes **out** in an answer. It **shows** rather than strips, because a caller has to see which character it typed was the problem, and it is applied at the four catalogue rows that quote a caller's raw spelling and at `CanonicalPath.UnkeepableName`'s four clauses. It is not applied to the `why` clause, which is BrowserAI's own prose |
| post-course-correction **9** — the idle timer closes the browser and writes no row | **fixed 2026-08-26 (Q130 = a)** *(previously "pending a maintainer decision, not started")*, `BrowserIdleTimerTests.AnIdleCloseWritesItsOwnRowSoTheLogSaysBrowserAiClosedTheBrowser`, watched red on `TimeoutException: the idle close never settled its row` — the row did not exist to settle. The maintainer took the row over the description clause, which is the option the review called the honest one. It is written the way every forwarded call's row is: `in-flight` before the call reaches the child, settled from the child's own answer — so a close that hangs or meets a dead child leaves it unsettled, which is exactly what `browserai_catch_up` renders as *"no answer was recorded"*. **The `why` is self-attributing** (`LiveSession.IdleCloseWhy`) because every other row in that log was written by a caller, and a reader meeting a `browser_close` with a caller-shaped sentence beside it would conclude an agent had closed the browser. ⚠️ **One rule is deliberately inverted here and it is written where it is decided**: a forwarded call whose row will not write is *refused*, and this close is not. There is no caller to refuse to, and declining would leave a browser tree up for the life of the session to protect a log line — so the failure goes to `SessionLog.IdleCloseNotRecorded` and the close proceeds |
| post-course-correction **10** — a C# parameter name leaks into a model-facing refusal | **fixed 2026-08-26**, `SessionPathTests.AVolumeRootIsNotASessionDirectory`, watched red on `Expected to not contain "(Parameter '"` against the real volume-root refusal, with `ErrorCatalogueTests` asserting the same absence through a `browserai_init` on `C:\`. The `paramName` is dropped at both `SessionPath.For` throw sites: `ArgumentException.Message` appends `(Parameter 'x')` whenever one is set, and `SessionManager.Resolve` interpolates that message verbatim into the catalogue sentence — so an internal identifier was arriving in the one sentence a model has to act on |
| post-course-correction **11** — the one environment variable that turns off the only containment there is is not in `ChildEnvironment.Refused` | **fixed 2026-08-26**, `ChildEnvironmentTests.TheRefusedNamesAreTheOnesTheDesignNames` (red on `collection has 12 items but expected 17`) and `.TheVariablesThatWouldOverrideAGeneratedConfigKeyAreRefusedByName`. Five names added: `PLAYWRIGHT_MCP_ALLOW_UNRESTRICTED_FILE_ACCESS`, `_CONFIG`, `_OUTPUT_DIR`, `_INIT_SCRIPT` and `_INIT_PAGE`, **verified against the shipped bundle's own `configFromEnv` rather than against the review's report of it**. ⚠️ **It was never a hole and the row says so**: the allowlist is the child's entire block by construction, so all five were already absent. The finding is against `Refused`'s own stated purpose — *"absent because nobody added it"* versus *"absent because it is refused"* — and the variable that switches off `allowUnrestrictedFileAccess: false` is exactly the one that should have been named on the day that key became load-bearing |
| post-course-correction **12** — `ReVerificationIndexTests.Exists` is narrower than its sibling on three axes, and nothing says so | **harmonised 2026-08-26.** It searched the test assembly alone, bound public methods only with no inherited members, and used `GetMethod`, which throws `AmbiguousMatchException` on an overload set. It searches both assemblies now, through `HazardIndexTests.OurAssemblies` — made internal so *"a symbol this repository can answer for"* has one definition — and asks `GetMember` with the sibling's flags. **No red: every live row resolved before and after, and it failed in the safe direction**, so this was a false-red risk rather than a hole. ⚠️ **One axis is deliberately NOT harmonised and the reason is in the method's own remarks**, which is what P6's rider L asked for: `HazardIndexTests` passes a type it cannot find, because a hazard row may cite `Directory.Move` or a phrase in prose that carries a dot; this gate's column names a test or the word *manual* and nothing else, so a type resolving nowhere is a row claiming coverage that does not exist — the eight-row defect the class was written for |
| post-course-correction **13** — "an override leaves a trace in the release artifact" is a claim the manifest cannot express | **fixed 2026-08-26 by making the claim true, with the wording moved in the same commit.** `build/Write-ReleaseManifest.ps1` takes five `-Override*` parameters and **always** emits the key: `"override": null` for an ordinary release, a five-part block for a held one. **An absent key is not a statement; `null` is** — which is what lets a reader tell *this release says it was not overridden* from *this manifest was written by a build that could not say*. A half-stated override refuses the manifest, for the same reason a missing file does. `ReleaseScriptTests.TheManifestStatesWhetherTheReleaseWasACrunchOverride`, red on the field being absent altogether, and `.AnOverrideStatedInPartRefusesTheManifestRatherThanWritingHalfAClaim`, red on *"A parameter cannot be found that matches parameter name 'OverriddenPackage'"*. [`DECISIONS.md`](../../DECISIONS.md) and [`RELEASING.md`](../../RELEASING.md) items 1 and 11 all say what the script now does |
| post-course-correction **14** — `CatchUp`'s remarks still describe the truncation P2 deleted | **fixed 2026-08-26, documentation only, no red.** Every clause was false of the code three lines below it: the log is printed **oldest** first, nothing is elided, and there is no cap anywhere. The replacement is a `Corrected` clause pointing at the paging that replaced it and saying why oldest-first is load-bearing — rows are only ever appended, so numbering from the oldest end means an append changes the last page and no other |
| post-course-correction **15** — `browserai_list` on a drive letter that does not exist answers "no sessions" rather than "no such drive" | **fixed 2026-08-26**, `SessionListTests.AListingOnADriveThatDoesNotExistSaysSoRatherThanSayingNoSessions`, watched red on the old sentence quoted back verbatim. A clause and **not** a refusal — the answer was *true*, and a listing that refused would charge a turn for a fact. One `Directory.Exists`, on the empty answer only, against a root the canonicaliser has already proven local, so the 22-second call this product is ordered around cannot be reached from it. The positive control is in the arm: a directory that **is** there and holds nothing still gets the plain empty sentence |
| post-course-correction **16** — a session directory deep enough to push `output\` past `MAX_PATH` is accepted, created and locked, and then fails at child launch with the cause unnamed | **fixed 2026-08-26**, `SessionPathTests.ADirectoryWithNoRoomLeftForItsOutputFolderIsNotASessionDirectory` and `ErrorCatalogueTests.ADirectoryWithNoRoomLeftForItsOutputFolderIsRefusedAtTheDoorRatherThanAtLaunch`, both watched red. ⚠️ **The red found a longer suffix than the review named, and the budget is set from the measurement rather than from the report.** The review named `\output`, which `CreateProcessW`'s `lpCurrentDirectory` bounds; the planted red at 254 characters showed the **store** failing first — `SQLITE_CANTOPEN`, *"unable to open database file (result 14)"* — because SQLite composes `browserai.data-shm` from the store's own path and its Win32 VFS is bounded in `MAX_PATH` characters too, while .NET is not and had already taken the guard. `SessionPath.LongestSessionDirectory` is `MAX_PATH − 1 −` the longer of the two, derived rather than written. **It lives on `SessionPath` and not in the canonicaliser, beside the volume-root predicate and for the same reason**: `browserai_list` is pointed at a directory of any depth on purpose and creates nothing. The refusal names the budget, both suffixes, and how many characters shorter to be |
| post-course-correction **17** — the `authored` half of `tool-verdicts.json` has no runtime effect, and an unjudged `browserai_*` name is refused without a row | **(a) the role: stated 2026-08-26. (b) the log row: fixed 2026-08-26 (Q131 = a), later the same day** *(previously "pending a maintainer decision, not started")*. `SessionToolSurface.IsAuthored` is an **exact match against `SessionToolSurface.Names`** now rather than a prefix test, so `browserai_zzz` falls through to the verdict door, is deny-by-defaulted, and is recorded against the session it named. `SessionPolicyTests.AnUnjudgedAuthoredNameIsRefusedAtTheVerdictDoorAndRecordedOnItsSession`, watched red on the session manager's *"'browserai_zzz' is not a BrowserAI session tool"* arriving where the verdict door's sentence belongs. **It also makes (a) partly obsolete in the good direction**: the `answer` rows are load-bearing at run time now — removing one stops that tool being answered and starts it being refused — so `ToolVerdicts`' remarks and the file's own `_what_the_answer_rows_actually_do` key carry a `Corrected` clause rather than the flat claim they were written with. ⚠️ **The residual stays and is asserted rather than described**: a call naming an unjudged `browserai_` name with **no** resolvable session still writes nothing and cannot — there is no session directory to write it into, which is the same reason the session-missing and unknown-session refusals go to the machine-wide log. The same arm drives that half — **and asserts what the answer IS**, because the exact match briefly made it wrong: `browserai_zzz` with no session was answered *"this needs a `session`"*, which sends a caller to supply one for a tool that does not exist. `SessionToolSurface.NotOneOfOurs` names the seven that do, and `IsInTheAuthoredNamespace` is the prefix question kept under a name that says which question it is. **Caught by `ReinstallBrowserTests.EveryAuthoredToolIsAnsweredAndAnUnknownOneIsRefused` on the first full run after the change, not by review** |
